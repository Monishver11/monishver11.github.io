<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://monishver11.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://monishver11.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-11T23:01:11+00:00</updated><id>https://monishver11.github.io/feed.xml</id><title type="html">blank</title><subtitle>A space for journaling my learnings, career, thoughts, and experiences in this amazing ride of life. </subtitle><entry><title type="html">Serving DeepSeek-V4.1-Flash on vLLM - Architecture, Config, and Where the Time Goes</title><link href="https://monishver11.github.io/blog/2026/deepseek-v41-flash-vllm/" rel="alternate" type="text/html" title="Serving DeepSeek-V4.1-Flash on vLLM - Architecture, Config, and Where the Time Goes"/><published>2026-09-11T18:00:00+00:00</published><updated>2026-09-11T18:00:00+00:00</updated><id>https://monishver11.github.io/blog/2026/deepseek-v41-flash-vllm</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/deepseek-v41-flash-vllm/"><![CDATA[<p>Hello all. This post is a working briefing on <a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash">DeepSeek-V4.1-Flash</a>, written for one purpose: to be able to tune it on vLLM without guessing. The model landed on 2026-09-10 with a stack of components that did not exist a year ago, a vLLM image that shipped the same day, and a recipe command with eight flags and two environment variables. Before profiling anything, I wanted three things in one place: what each new component is and what it costs to serve, what the launch command actually starts on the node, and what vLLM’s implementation has and has not landed as of today.</p> <p>The target is one 8x B300 node with the recipe’s “Data + Expert Parallel” command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">--gpus</span> all <span class="se">\</span>
  <span class="nt">--privileged</span> <span class="nt">--ipc</span><span class="o">=</span>host <span class="nt">-p</span> 8000:8000 <span class="se">\</span>
  <span class="nt">-v</span> ~/.cache/huggingface:/root/.cache/huggingface <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">VLLM_ENGINE_READY_TIMEOUT_S</span><span class="o">=</span>3600 <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">VLLM_USE_RUST_FRONTEND</span><span class="o">=</span>1 <span class="se">\</span>
  vllm/vllm-openai:deepseekv41-flash-0909 deepseek-ai/DeepSeek-V4.1-Flash <span class="se">\</span>
  <span class="nt">--tokenizer-mode</span> deepseek_v41 <span class="se">\</span>
  <span class="nt">--enable-expert-parallel</span> <span class="se">\</span>
  <span class="nt">--data-parallel-size</span> 8 <span class="se">\</span>
  <span class="nt">--tool-call-parser</span> deepseek_v41 <span class="se">\</span>
  <span class="nt">--enable-auto-tool-choice</span> <span class="se">\</span>
  <span class="nt">--reasoning-parser</span> deepseek_v41 <span class="se">\</span>
  <span class="nt">--mm-encoder-tp-mode</span> data
</code></pre></div></div> <p>The plan:</p> <ul> <li>The numbers that drive everything: the B300 node, the model’s sizes, and the arithmetic that follows</li> <li>The architecture, one briefing per component: CED, CSA2 and its three modes, the hierarchical indexer, the three caches, the MoE, single-pass mHC, Engram, DSpark, and the ViT</li> <li>The launch command, line by line, and what DP8 plus EP actually starts on the node</li> <li>The Engram placement problem, and why DP8 is not obviously the right layout</li> <li>vLLM day-0 status: what is merged, what is open, and what the image is</li> <li>Where to look for performance, as labeled hypotheses with a profiling plan</li> <li>Other vendors, briefly</li> <li>Takeaways, and a question bank</li> </ul> <p>I’m assuming the <a href="/blog/2026/llm-inference-systems/">LLM inference systems post</a> as background: the roofline, the decode floor, paged KV, DP attention with EP for MoE, speculative decoding, and CUDA graphs are all used here without re-derivation. Everything quoted from vLLM was checked against <code class="language-plaintext highlighter-rouge">main</code> at commit <code class="language-plaintext highlighter-rouge">2d75e586</code> as of 2026-09-11, and the recipe against the <a href="https://github.com/vllm-project/recipes">vllm-project/recipes</a> repo at commit <code class="language-plaintext highlighter-rouge">6e016941</code> the same day. Both move daily right now, so treat file paths and defaults as pointers with a date on them. Model facts come from the <a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/blob/main/DeepSeek_V41_Tech_Report.pdf">tech report</a> and <a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/raw/main/config.json">config.json</a>; where I derive a number from those, the computation is shown.</p> <p>Let’s get started.</p> <hr/> <h4 id="the-numbers-that-drive-everything"><strong>The Numbers That Drive Everything</strong></h4> <h5 id="the-machine"><strong>The machine</strong></h5> <p>The recipe’s verified hardware is H200, GB200 NVL4, GB300, and MI350X. B300 is offered by the command builder because it fits the model’s stated minimum, but it is not on the verified list (<a href="https://github.com/vllm-project/recipes/blob/main/models/deepseek-ai/DeepSeek-V4.1-Flash.yaml">recipe YAML</a>, <code class="language-plaintext highlighter-rouge">hardware:</code> block, as of 2026-09-11). So the numbers below are the datasheet, not a vendor’s measured run.</p> <table> <thead> <tr> <th> </th> <th>B300 (Blackwell Ultra)</th> <th>H200 SXM</th> </tr> </thead> <tbody> <tr> <td>HBM per GPU</td> <td>288 GB HBM3e</td> <td>141 GB HBM3e</td> </tr> <tr> <td>Bandwidth per GPU</td> <td>8 TB/s</td> <td>4.8 TB/s</td> </tr> <tr> <td>FP8 dense, per GPU</td> <td>4.5 PFLOPS</td> <td>1,979 TFLOPS</td> </tr> <tr> <td>FP4 dense, per GPU</td> <td>13.5 PFLOPS</td> <td>n/a</td> </tr> <tr> <td>NVLink per GPU</td> <td>1.8 TB/s</td> <td>900 GB/s</td> </tr> <tr> <td>8-GPU node HBM</td> <td>2,304 GB (NVIDIA rounds to 2.1 TB)</td> <td>1,128 GB</td> </tr> </tbody> </table> <p>Sources: the <a href="https://developer.nvidia.com/blog/inside-nvidia-blackwell-ultra-the-chip-powering-the-ai-factory-era/">Blackwell Ultra architecture post</a> for the 288 GB and 8 TB/s per GPU; the <a href="https://www.nvidia.com/en-us/data-center/hgx/">HGX page</a> for the 8-GPU HGX B300 figures (FP4 108 PFLOPS dense, FP8 72 PFLOPS with sparsity, “Dense is half sparse”, 2.1 TB total), which I divide by 8 for the per-GPU dense rates; the <a href="https://www.nvidia.com/en-us/data-center/h200/">H200 page</a> for H200. One trap to flag: the architecture post quotes 15 PFLOPS dense FP4 and 5 PFLOPS dense FP8 per chip, about 10% above the HGX product page. I use the HGX numbers because they describe an 8-GPU x86 node, which is what a B300 box is.</p> <p>The node-level figures that recur: \(8 \times 288 = 2{,}304\) GB of HBM and \(8 \times 8 = 64\) TB/s of aggregate bandwidth. The FP8 ridge point per GPU is \(4.5 \times 10^{15} / 8 \times 10^{12} \approx 562\) FLOP per byte, and roughly triple that at the FP4 rate. Decode on this machine is memory-bound until a step carries several hundred tokens, same story as Hopper with a higher ceiling.</p> <h5 id="the-model"><strong>The model</strong></h5> <table> <thead> <tr> <th>Quantity</th> <th>Value</th> <th>Source</th> </tr> </thead> <tbody> <tr> <td>Backbone parameters</td> <td>552B</td> <td><a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/blob/main/DeepSeek_V41_Tech_Report.pdf">tech report</a>, section 2.1</td> </tr> <tr> <td>Engram parameters (separate from the backbone)</td> <td>196B</td> <td>same</td> </tr> <tr> <td>Active per token, prefill</td> <td>8B</td> <td>same</td> </tr> <tr> <td>Active per token, decode</td> <td>16B</td> <td>same</td> </tr> <tr> <td>Layers</td> <td>40, as a 20-layer causal encoder plus a 20-layer decoder</td> <td>same, and <code class="language-plaintext highlighter-rouge">num_hidden_layers</code> in config.json</td> </tr> <tr> <td>Hidden size</td> <td>5120</td> <td>config.json</td> </tr> <tr> <td>Experts per MoE layer</td> <td>384 routed plus 1 shared, top-6</td> <td>config.json</td> </tr> <tr> <td>Expert intermediate size</td> <td>2304</td> <td>config.json</td> </tr> <tr> <td>Context</td> <td>1,048,576 tokens, YaRN factor 16 over a 65,536 training window</td> <td>config.json <code class="language-plaintext highlighter-rouge">rope_scaling</code></td> </tr> <tr> <td>Vocabulary</td> <td>129,280</td> <td>config.json</td> </tr> <tr> <td>Checkpoint on disk</td> <td>510.3 GB (475.2 GiB) across 48 safetensors shards</td> <td><a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/tree/main">HF file tree</a></td> </tr> <tr> <td>Weight formats</td> <td>FP8 dense with 32x32 blocks and UE8M0 scales; FP4 routed experts</td> <td>config.json <code class="language-plaintext highlighter-rouge">quantization_config</code></td> </tr> </tbody> </table> <p>Two of those rows need a note. The vLLM recipe says “522B total parameters” everywhere; the report, the model card, and the report’s Table 1 all say 552B, and the recipe’s own memory table sums to more than either. Use 552B. And “8B in prefill, 16B in decode” is not a typo: the encoder-decoder design in the next section means most prompt tokens run only half the network.</p> <p>The expert arithmetic is worth doing once because it explains the disk footprint. A SwiGLU expert is three matrices of \(5120 \times 2304\):</p> \[3 \times 5120 \times 2304 \approx 35.4\text{M parameters per expert}\] <p>Across 384 routed experts and 40 layers that is \(384 \times 40 \times 35.4\text{M} \approx 544\text{B}\), plus the shared experts and the DSpark draft blocks’ own experts, which is consistent with the recipe’s memory table putting routed plus DSpark experts at 557.2B parameters and 259.5 GiB in MXFP4 (<a href="https://recipes.vllm.ai/deepseek-ai/DeepSeek-V4.1-Flash">recipe guide</a>). The same table puts the Engram tables at 188.8 GiB in MXFP8, attention plus norms plus routers at 5.6 GiB, embedding plus LM head at 5.8 GiB, and quantization scales at 21.9 GiB. Those rows sum to about 481.6 GiB, slightly above the 476 GiB the guide states as the total, so treat the breakdown as approximate.</p> <p>Per decode token, 6 routed experts times 40 layers is \(6 \times 40 \times 35.4\text{M} \approx 8.5\text{B}\) routed-expert parameters, and the shared expert adds another 1.4B; with the 6.0B of attention and dense weights that reaches the report’s 16B.</p> <h5 id="the-caches"><strong>The caches</strong></h5> <p>The report’s title is “Pushing the Limits of KV Cache Compression”, and its headline number is that the <strong>global KV cache, the part that must stay in HBM, is 890 bytes per token</strong>, about a quarter of DeepSeek-V4-Flash, with the persistent prefix cache on SSD and host memory at about an eighth (tech report, abstract and section 3.2.1). Global KV means the compressed main KV plus the indexer keys; the sliding-window cache is bounded by the window and does not grow with context.</p> <p>How those 890 bytes arise, from config.json: only four layers write a main KV cache (<code class="language-plaintext highlighter-rouge">kv_source_layer_ids</code> is <code class="language-plaintext highlighter-rouge">[2, 8, 14, 20]</code>), three of them at compression ratio 2 (one entry per two tokens) and one at ratio 1, so a token adds</p> \[3 \times \tfrac{1}{2} + 1 = 2.5 \text{ main KV entries}\] <p>across the whole 40-layer network. Each entry is a 512-channel latent (<code class="language-plaintext highlighter-rouge">head_dim</code> is 512, <code class="language-plaintext highlighter-rouge">num_key_value_heads</code> is 1). In DeepSeek’s FP4 format that is \(512 \times 0.5\) bytes of E2M1 plus 32 E4M3 scales, one per 16 channels, or 288 bytes per entry, which lands at \(2.5 \times 288 = 720\) bytes of main KV per token. The report does not publish this split, so the remaining 170 bytes being indexer keys is my reconstruction, not their statement.</p> <p>vLLM does not store FP4 KV yet. Its layout for this model is <code class="language-plaintext highlighter-rouge">fp8_ds_mla</code>: 448 bytes of non-RoPE latent, 128 bytes of RoPE part, and an 8-byte scale, <strong>584 bytes per entry</strong> (<code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/attention.py</code>, as of 2026-09-11), so the same 2.5 entries cost \(2.5 \times 584 = 1{,}460\) bytes of main KV per token, roughly double DeepSeek’s number. The FP4 layout arrives with the open <a href="https://github.com/vllm-project/vllm/pull/56344">attention megakernel PR #56344</a>, whose description gives 288 bytes per compressed entry and 528 bytes per sliding-window entry. Until it lands, size your KV pool off the vLLM figure, not the paper’s.</p> <h5 id="what-the-arithmetic-says"><strong>What the arithmetic says</strong></h5> <p>Everything below is derived from the numbers above, with assumptions stated.</p> <p><strong>KV per sequence.</strong> At vLLM’s 1,460 bytes per token of main KV, a 128K-token sequence holds about 191 MB and a 1M-token sequence about 1.53 GB, before the indexer and sliding-window caches. At DeepSeek’s 890 bytes it is 117 MB and 933 MB. Either way this is a small model to cache: for comparison, Llama 3 70B at bf16 carries 320 KiB per token, over 200 times more.</p> <p><strong>What fits.</strong> Under DP8 with expert parallelism, each GPU holds its slice of the experts (259.5 GiB over 8 is about 32.4 GiB), a full copy of the attention and dense weights (5.6 GiB plus 5.8 GiB), and its share of the scales: roughly 47 GiB per GPU before Engram. Against 268 GiB per B300 and vLLM’s default 0.92 utilization (<code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code>), that leaves on the order of 190 GiB per GPU for KV, activations, and graphs, over a hundred 1M-token sequences per GPU at the vLLM byte count. KV capacity is not the constraint on this node, unless the Engram tables land in HBM; the launch command section returns to that.</p> <p><strong>The decode floor.</strong> A single decode token touches the 6 routed experts (8.5B parameters at MXFP4, about 0.53 bytes per parameter with the 1-byte-per-32 scale), the shared expert, and the 6.0B of dense weights at about 1.03 bytes per parameter in MXFP8:</p> \[8.5\text{B} \times 0.53 + 7.4\text{B} \times 1.03 \approx 4.5 + 7.6 \approx 12 \text{ GB per token}\] <p>Under DP8, attention is replicated, so every rank streams the 7.6 GB of dense weights for its own batch every step; at 8 TB/s that is about 0.95 ms. The experts are split, so at large batch a rank reads only its 32.4 GiB slice, about 4.3 ms when every expert is touched. So the per-step floor on this layout sits between roughly 1 ms at tiny batch and 4 to 5 ms at saturating batch. Hold onto the 1 ms: the only public vLLM measurement of this model at concurrency 1 (on MI355X, discussed in the performance section) reports an inter-token latency around 25 ms, twenty-plus times the byte floor. That gap is not bandwidth.</p> <p><strong>Batch at which experts saturate.</strong> With top-6 of 384 and independent routing, the expected fraction of experts touched by a batch of \(B\) tokens is \(1 - (1 - 6/384)^B\): about 63% at 64 tokens and 98% at 256. Past a few hundred tokens per step, every step reads the whole expert set, and DSpark’s extra draft compute stops paying for itself.</p> <p><strong>References</strong></p> <ul> <li><a href="https://developer.nvidia.com/blog/inside-nvidia-blackwell-ultra-the-chip-powering-the-ai-factory-era/">NVIDIA Blackwell Ultra architecture post</a>, <a href="https://www.nvidia.com/en-us/data-center/hgx/">HGX page</a>, <a href="https://www.nvidia.com/en-us/data-center/h200/">H200 page</a></li> <li><a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/blob/main/DeepSeek_V41_Tech_Report.pdf">DeepSeek-V4.1-Flash tech report</a>, <a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/raw/main/config.json">config.json</a>, <a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash">model card</a></li> <li><a href="https://recipes.vllm.ai/deepseek-ai/DeepSeek-V4.1-Flash">vLLM recipe for DeepSeek-V4.1-Flash</a>, <a href="https://github.com/vllm-project/recipes/blob/main/models/deepseek-ai/DeepSeek-V4.1-Flash.yaml">recipe YAML</a></li> <li>vLLM source, as of 2026-09-11: <code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/attention.py</code>, <code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code>; <a href="https://github.com/vllm-project/vllm/pull/56344">PR #56344</a></li> </ul> <hr/> <h4 id="the-architecture-one-briefing-at-a-time"><strong>The Architecture, One Briefing at a Time</strong></h4> <p>The report frames every design choice around one goal: make long-context serving cheap by shrinking what has to be cached and what has to be recomputed. Each component below is one attack on that goal, and each is described the same way: what it is, why it exists, and what it costs at serving time.</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/deepseek-v41-flash-vllm/layer-map.svg" sizes="95vw"/> <img src="/assets/img/deepseek-v41-flash-vllm/layer-map.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="The 40-layer map of DeepSeek-V4.1-Flash" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> All 40 backbone layers and what each one owns, from config.json. Layers 0 and 1 are sliding-window only. The encoder's CSA2 layers run at compression ratio 2 in three groups of six; the decoder's run at ratio 1 in five groups of four. Only the four Full-mode layers write a main KV cache, and only the eight Full and Reindex layers run an indexer; every other layer reuses. Engram modules sit at layers 1 and 14, and the three DSpark draft blocks hang off the end, reading the attention inputs of layers 37 to 39. Editable source: <a href="/assets/img/deepseek-v41-flash-vllm/layer-map.excalidraw">layer-map.excalidraw</a>. </div> <h5 id="causal-encoder-decoder"><strong>Causal encoder-decoder</strong></h5> <p><strong>What.</strong> The 40 layers are split in half. The bottom 20 are the causal encoder; the top 20 are the decoder. For global attention, the decoder layers do not compute keys and values from their own hidden states. They project them from the encoder’s final hidden state \(H_{L/2}\) with layer-specific weights (tech report, section 2.2, equation 1). Sliding-window attention is the exception: every layer computes its own local keys and values from its own hidden state.</p> <p><strong>Why.</strong> Agentic workloads are prefill-heavy: every tool call re-sends a growing context. If the decoder’s global KV can be produced from the encoder output, then a prompt token only needs to pass through the encoder to populate the cache that decoding will read. The report puts prefill cost at \(O(NL/2 + n_{win} L/2)\) for a prompt of \(N\) tokens, nearly halving it. That is where “8B active in prefill” comes from: half the layers, so half the experts.</p> <p><strong>Serving cost.</strong> The catch is the decoder’s sliding-window cache, which decoding needs and which the encoder-only prefill did not produce. Exactly rebuilding it means running the decoder over the last \(n_{win} \times L/2\) prompt tokens. The report’s answer is Decoder SWA Bounded Replay: replay only the last 128 tokens through the decoder, accept approximate window states, and it reports negligible quality impact (section 3.2.2). SGLang exposes this as an opt-in flag; in vLLM it is an open PR, so vLLM today runs the full decoder over the prompt and does not yet realize the prefill halving. The vLLM day-0 section has the pointers.</p> <h5 id="csa2-and-its-three-modes"><strong>CSA2 and its three modes</strong></h5> <p><strong>What.</strong> Compressed Sparse Attention 2 is the global-attention branch. Every CSA2 layer has a main KV cache of 512-channel latents, possibly compressed along the sequence (ratio 2 means one entry per two tokens), and a small <strong>indexer</strong> that scores those entries against an indexer query and keeps the top 512 per query (<code class="language-plaintext highlighter-rouge">index_topk</code>). Attention then runs over the selected entries plus the layer’s own 128-token sliding window. Compared with V4’s CSA, the compressor no longer overlaps source windows or adds absolute positions, and the indexer keys are projected from the main KV rather than from hidden states (section 2.3).</p> <p>The novelty is cross-layer reuse. Each layer is statically one of three modes (section 2.3.1):</p> <table> <thead> <tr> <th>Mode</th> <th>Main KV</th> <th>Indexer K</th> <th>Top-K indices</th> <th>What it computes itself</th> </tr> </thead> <tbody> <tr> <td>Full</td> <td>computes its own</td> <td>projects from its own KV</td> <td>runs the indexer</td> <td>everything</td> </tr> <tr> <td>Reindex</td> <td>reuses the most recent Full layer’s</td> <td>reuses</td> <td>rescores with its own indexer Q, fresh top-K</td> <td>query, SWA KV, indexer Q, scoring</td> </tr> <tr> <td>Reuse</td> <td>reuses</td> <td>reuses</td> <td>reuses the latest Full or Reindex layer’s</td> <td>query and SWA KV only</td> </tr> </tbody> </table> <p>All three compute their own query and sliding-window KV. The layer assignment, from the report’s section 4.2.1 and config.json:</p> <table> <thead> <tr> <th>Layers</th> <th>Ratio</th> <th>Modes</th> </tr> </thead> <tbody> <tr> <td>0, 1</td> <td>none</td> <td>sliding window only</td> </tr> <tr> <td>2 to 7, 8 to 13, 14 to 19</td> <td>2</td> <td>first of each group Full, next five Reuse</td> </tr> <tr> <td>20 to 23</td> <td>1</td> <td>first Full, next three Reuse</td> </tr> <tr> <td>24 to 27, 28 to 31, 32 to 35, 36 to 39</td> <td>1</td> <td>first Reindex, next three Reuse</td> </tr> </tbody> </table> <p>So four Full layers own a main KV cache (<code class="language-plaintext highlighter-rouge">kv_source_layer_ids</code>), eight Full or Reindex layers own an indexer (<code class="language-plaintext highlighter-rouge">index_source_layer_ids</code>), and the other 26 CSA2 layers own neither.</p> <p><strong>Why.</strong> Three multiplicative levers on cache size: entry size (MLA’s shared latent), sequence compression (ratio 2), and now the layer dimension. Sharing the main KV across a group of layers divides the cache by the group size; reusing top-K indices divides the indexer work. Decoupling the two is what Reindex mode is for: the selection can change across layers even while the cache does not.</p> <p><strong>Serving cost.</strong> For a Reuse-mode layer, decode is a sparse attention over 512 selected entries plus a 128-token window, with no indexer at all. The report says such a layer executes with 15 kernels in prefill and 11 in decode in DeepSeek’s stack (section 3.2). In vLLM the modes are not named; they are implied by which layers appear in the two source lists, and consumers “reuse the most recently published source below them” (<code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/attention.py</code>, as of 2026-09-11). Compression is why the decoder runs at ratio 1: it reads the encoder’s final state, and the report keeps that cache uncompressed.</p> <h5 id="the-hierarchical-sparse-indexer"><strong>The hierarchical sparse indexer</strong></h5> <p><strong>What.</strong> Decoder-only. The first decoder Full layer (layer 20) scores every visible main-KV position and picks its own top 512. It also scores 8-position blocks by their maximum entry score, keeps the top 2,048 blocks, and publishes those 16,384 positions as a shared <strong>candidate pool</strong> (<code class="language-plaintext highlighter-rouge">candidate_source_layer_id</code>, <code class="language-plaintext highlighter-rouge">candidate_topk_blocks</code>, <code class="language-plaintext highlighter-rouge">candidate_block_size</code> in config.json). The four Reindex layers above it score only the pool (section 2.3.2).</p> <p><strong>Why.</strong> Index reuse cuts how many layers run an indexer; it does not cut what each indexer scans. Bounding later indexers to a fixed pool makes their per-query cost constant in context length. The first scan is still full-range, so the per-token indexer cost at 1M context is one full scan plus four bounded ones instead of five full scans. It was introduced in post-training so the model is trained under the same restriction it serves with.</p> <p><strong>Serving cost.</strong> In vLLM the layer-20 indexer writes a <code class="language-plaintext highlighter-rouge">candidate_block_buffer</code> that layers 24 through 36 read (same file). The full-range scan at layer 20 is the piece that still scales with context, and it is where DeepGEMM’s sparse MQA-logits kernels and DeepSelect’s top-k are aimed; both are open PRs today.</p> <h5 id="three-caches-two-precisions-and-bounded-replay"><strong>Three caches, two precisions, and bounded replay</strong></h5> <p>Per token the model keeps three kinds of cache, and they are not stored alike (section 2.4.4):</p> <table> <thead> <tr> <th>Cache</th> <th>Grows with context?</th> <th>DeepSeek’s format</th> <th>vLLM’s format today</th> </tr> </thead> <tbody> <tr> <td>Main KV (4 source layers, 2.5 entries per token)</td> <td>yes</td> <td>FP4 E2M1, one E4M3 scale per 16 channels, quantized after RoPE</td> <td><code class="language-plaintext highlighter-rouge">fp8_ds_mla</code>, 584 bytes per entry</td> </tr> <tr> <td>Indexer K (projected from main KV)</td> <td>yes</td> <td>MXFP4</td> <td>FP8 by default; MXFP4 on Blackwell via <code class="language-plaintext highlighter-rouge">--attention-config '{"indexer_kv_dtype":"mxfp4"}'</code></td> </tr> <tr> <td>Sliding-window KV (every layer, 128 tokens)</td> <td>no, bounded by the window</td> <td>FP8, kept at FP8 “due to its sensitivity to quantization”</td> <td>FP8, 32-token blocks</td> </tr> </tbody> </table> <p><strong>Why FP4 for the main KV, and why not NVFP4 exactly.</strong> The format is NVFP4 minus its second-level global scale. The report’s argument is dynamic range: E4M3 scales times E2M1 values reach magnitudes of \(448 \times 6 = 2688\), while a 512-channel latent after RMSNorm has an L2 norm of at most about \(\sqrt{512} \approx 22.6\) and observed maxima near 10, so the global scale buys nothing. FP4 here is a storage format, not a matmul format: values are dequantized before attention, which keeps the cache portable across hardware. The indexer stays on OCP MXFP4 for the same portability reason, and it was already FP4 in V4. Quantization-aware training for the main KV was added in post-training.</p> <p><strong>Bounded replay.</strong> The sliding-window caches are the problem child of a persistent prefix cache: they are cheap to hold in HBM but expensive to persist, and reconstructing them exactly means replaying \(L \times n_{win}\) tokens. SWA Bounded Replay replays only the last 128 tokens and truncates each layer’s window to the replayed segment (section 3.2.2). Two uses: <strong>encoder replay</strong> lets DeepSeek drop SWA KV from the persistent cache entirely, keeping it in a host-memory pool with a minutes-long TTL, which is where the “1/8 persistent cache” comes from; <strong>decoder replay</strong> is the CED companion from above. Both are approximate by construction, and both are absent from vLLM main today.</p> <h5 id="the-moe"><strong>The MoE</strong></h5> <p>Standard DeepSeekMoE shapes with bigger numbers: 384 routed experts, 1 shared, 6 active, expert intermediate 2304, in every layer. Routing uses <code class="language-plaintext highlighter-rouge">sqrtsoftplus</code> scoring with the auxiliary-loss-free <code class="language-plaintext highlighter-rouge">noaux_tc</code> bias, <code class="language-plaintext highlighter-rouge">routed_scaling_factor</code> 1.5, and SwiGLU clamped at 10 (config.json). The multimodal twist is a second set of routing biases for image tokens, so vision and text balance separately (section 2.1.1); vLLM implements this by passing raw token ids through to the router.</p> <p>Serving-wise this is the familiar MoE picture from the inference post: all 259.5 GiB of experts must be resident somewhere on the node, routing turns into all-to-all traffic, and skew across experts is the throughput risk EPLB exists for. The vLLM-specific detail is that the MegaMoE kernel backends (DeepGEMM’s <code class="language-plaintext highlighter-rouge">fp8_fp4_mega_moe</code> and the FlashInfer variants) require expert parallelism and SM100 (<code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4/nvidia/model.py</code>, as of 2026-09-11), which is one reason the recipe pairs EP with every single-node strategy.</p> <h5 id="hyper-connections-single-pass-mhc-and-mega-mhc"><strong>Hyper-connections: single-pass mHC and Mega-mHC</strong></h5> <p><strong>What.</strong> mHC, introduced with V4, carries the residual stream as \(n = 4\) parallel copies (<code class="language-plaintext highlighter-rouge">hc_mult</code>). Between blocks, three token-wise coefficient sets mix them: \(A_l\) picks the block input, \(B_l\) updates the streams, \(C_l\) injects the block output. \(B_l\) is made doubly stochastic by 20 Sinkhorn iterations (<code class="language-plaintext highlighter-rouge">hc_sinkhorn_iters</code>). V4 implemented the update as three dependent kernels, because \(A_l\) depends on a reduction over all of \(X_l\) before the input mix can start, costing \((4n + 4)d\) of activation traffic against a lower bound of \((2n + 2)d\) (section 2.4.1).</p> <p><strong>Why single-pass.</strong> Shift the input-mixing coefficient by one block: block \(l\) mixes its input with \(A_{l-1}\) instead of \(A_l\). The dependency disappears, each tile of \(X_l\) can be used for input mixing and coefficient prediction at once, and the report says the shift costs negligible quality. Pre-training keeps the multi-kernel form; deployment fuses residual update, input mixing, coefficient prediction, pre-norm, and FP8 conversion into one <strong>Mega-mHC</strong> kernel that reads and writes the residual exactly once, halving activation traffic.</p> <p><strong>Serving cost.</strong> This is the component to watch. It sits at every sublayer seam, so it runs 80-plus times per token, on a tiny \(4 \times 4\) matrix per token. In vLLM the mHC pre and post ops are TileLang kernels with the 20 Sinkhorn iterations, warmed up at startup over token sizes up to 16,384 (<code class="language-plaintext highlighter-rouge">vllm/model_executor/warmup/deepseek_v4_mhc_warmup.py</code>, as of 2026-09-11). DeepGEMM’s Mega-mHC is on the kernel tracker, not integrated. On the AMD path, the one public trace of this model attributes 85% of per-layer kernel launches to the mHC block (more in the performance section).</p> <h5 id="engram"><strong>Engram</strong></h5> <p><strong>What.</strong> Engram is a conditional memory: a very large embedding table addressed by hashes of the last few tokens, added to the residual stream through a learned gate. It is DeepSeek’s way to “decouple memorization from computation”: store facts in a lookup instead of in matmul weights. V4.1-Flash carries two modules, at layers 1 and 14, with 196B parameters between them. Each module hashes n-grams of orders 2, 3, and 4 over a compressed vocabulary of 99,092 ids, through 8 hash heads per order, into tables of about 16M rows per head with distinct prime sizes; each row is 256 channels in FP8 (section 2.4.2; <code class="language-plaintext highlighter-rouge">engram_*</code> fields in config.json). The two tables have 384,006,168 and 384,016,682 rows respectively, which at \(384\text{M} \times 256\) is about 98B parameters each.</p> <p><strong>Why.</strong> Capacity without FLOPs: a lookup costs a hash and a gather, not a GEMM, so the model holds far more parameters than it activates.</p> <p><strong>Serving cost.</strong> Per token per module, 3 orders times 8 heads is 24 gathers of 256 FP8 values, about 6 KB; call it 12 KB per token for both modules. The bandwidth is trivial; the problem is latency and placement. The tables are 188.8 GiB, larger than everything except the experts, and the addresses are random. DeepSeek’s stack keeps them in host memory and prefetches over RDMA, overlapping the first module’s lookup with the first Transformer block, which works because the addresses depend only on the token sequence and are known before the forward pass starts (section 2.4.2). vLLM’s default is the same idea in simpler form: pinned host memory read over unified virtual addressing, with the table sharded by hash head across tensor-parallel ranks. What that means under DP8 is the subject of its own section below.</p> <h5 id="dspark"><strong>DSpark</strong></h5> <p><strong>What.</strong> DSpark is the speculative decoder, replacing the MTP head of V3 and V4. The drafter is three Transformer blocks with a 128-token sliding window, each an MoE of 128 routed experts with 3 active, reading the attention inputs of backbone layers 37 to 39 (config.json). One drafter pass produces base logits for <strong>five draft positions at once</strong>; a low-rank Markov head (rank 256) models dependencies among them; a confidence head predicts per-position acceptance, from which the engine computes prefix survival probabilities. A scheduler then combines survival probabilities with profiled throughput curves to pick, per request and per step, how many draft tokens to verify (section 2.4.3). The drafter was trained after backbone pre-training with the backbone frozen, and kept in sync through post-training without gradients flowing back.</p> <p><strong>Why.</strong> Semi-autoregressive drafting means one draft pass per five positions rather than five; confidence-scheduled verification means the engine can trim drafts at high load, when rejected tokens are compute wasted, and extend them at low load, when compute is idle anyway. vLLM’s <a href="https://docs.vllm.ai/en/latest/features/speculative_decoding/adaptive_verification/">adaptive verification doc</a> describes exactly this trade and notes that a fixed number of speculative tokens is never right across concurrencies.</p> <p><strong>Serving cost.</strong> The draft weights ship in the checkpoint, so there is no second model. The recipe’s toggle is <code class="language-plaintext highlighter-rouge">--speculative-config '{"method":"dspark","num_speculative_tokens":5,"draft_sample_method":"probabilistic","rejection_sample_method":"block","enable_adaptive_verification":true}'</code>. Adaptive verification requires full CUDA graphs, since step costs are profiled from captured graphs. Expect the gain to be workload-dependent and to vanish at large batch: SGLang’s cookbook turns DSpark off in its high-throughput cell because “at large batch the fixed step cost stops paying for itself” (<a href="https://docs.sglang.io/cookbook/autoregressive/DeepSeek/DeepSeek-V4_1">SGLang cookbook</a>). Acceptance and speedup numbers for V4.1 specifically are not published; the <a href="https://arxiv.org/abs/2607.05147">DSpark paper</a> reports a 51% throughput improvement for V4-Flash at an 80 tokens per second per user target, on DeepSeek’s own stack.</p> <h5 id="deepseek-vit"><strong>DeepSeek-ViT</strong></h5> <p>A 32-layer ViT at hidden size 1024, patch 14, trained from scratch with 2D-RoPE, RMSNorm, and SwiGLU. A 3x3 pixel-unshuffle cuts the token count nine-fold before an MLP projects into the backbone; images are capped at 1024 tokens, which at patch 14 puts the upper resolution near \(1344 \times 1344\) (section 2.1.1; config.json <code class="language-plaintext highlighter-rouge">vision_config</code>). At serving time it is a small dense model that runs once per image; the recipe’s note is that at this size, tensor-parallel communication costs more than it saves, hence the encoder-parallel flag below.</p> <h5 id="what-vllm-has-today"><strong>What vLLM has today</strong></h5> <table> <thead> <tr> <th>Component</th> <th>In vLLM main, 2026-09-11</th> <th>Pointer</th> </tr> </thead> <tbody> <tr> <td>CED, CSA2 modes, source-layer reuse</td> <td>yes</td> <td><code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/attention.py</code>, <code class="language-plaintext highlighter-rouge">sparse_mla.py</code></td> </tr> <tr> <td>Hierarchical indexer, candidate pool</td> <td>yes</td> <td>same</td> </tr> <tr> <td>FP4 main KV</td> <td>no, fp8 layout only</td> <td>open <a href="https://github.com/vllm-project/vllm/pull/56344">PR #56344</a></td> </tr> <tr> <td>SWA bounded replay (encoder or decoder)</td> <td>no</td> <td>open <a href="https://github.com/vllm-project/vllm/pull/56227">PR #56227</a></td> </tr> <tr> <td>MXFP4 indexer cache</td> <td>yes, opt-in, Blackwell datacenter only</td> <td><code class="language-plaintext highlighter-rouge">--attention-config '{"indexer_kv_dtype":"mxfp4"}'</code></td> </tr> <tr> <td>MoE with MegaMoE backends</td> <td>yes, SM100 with EP</td> <td><code class="language-plaintext highlighter-rouge">--kernel-config '{"moe_backend": ...}'</code></td> </tr> <tr> <td>Single-pass mHC</td> <td>yes, TileLang kernels</td> <td>Mega-mHC not integrated, <a href="https://github.com/vllm-project/vllm/issues/56217">tracker #56217</a></td> </tr> <tr> <td>Engram</td> <td>yes, host-offloaded by default</td> <td>async prefetch and DP sharding in open <a href="https://github.com/vllm-project/vllm/pull/56512">PR #56512</a></td> </tr> <tr> <td>DSpark with adaptive verification</td> <td>yes, opt-in</td> <td><code class="language-plaintext highlighter-rouge">--speculative-config</code>, method <code class="language-plaintext highlighter-rouge">dspark</code></td> </tr> <tr> <td>ViT with encoder data parallel</td> <td>yes</td> <td><code class="language-plaintext highlighter-rouge">--mm-encoder-tp-mode data</code></td> </tr> <tr> <td>DeepGEMM sparse indexer logits, DeepSelect top-k</td> <td>no</td> <td>open <a href="https://github.com/vllm-project/vllm/pull/56254">PR #56254</a>, <a href="https://github.com/vllm-project/vllm/pull/56464">PR #56464</a></td> </tr> <tr> <td>torch.compile</td> <td>no</td> <td>model is not decorated; breakable CUDA graphs instead</td> </tr> </tbody> </table> <p><strong>References</strong></p> <ul> <li><a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/blob/main/DeepSeek_V41_Tech_Report.pdf">DeepSeek-V4.1-Flash tech report</a>, sections 2.1 to 2.4 and 3.2; <a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/raw/main/config.json">config.json</a></li> <li><a href="https://arxiv.org/abs/2607.05147">DSpark paper</a>; <a href="https://docs.vllm.ai/en/latest/features/speculative_decoding/adaptive_verification/">vLLM adaptive verification doc</a></li> <li><a href="https://docs.sglang.io/cookbook/autoregressive/DeepSeek/DeepSeek-V4_1">SGLang DeepSeek-V4.1 cookbook</a></li> <li>vLLM source, as of 2026-09-11: <code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/</code>, <code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4/nvidia/model.py</code>, <code class="language-plaintext highlighter-rouge">vllm/model_executor/warmup/deepseek_v4_mhc_warmup.py</code></li> </ul> <hr/> <h4 id="the-launch-command-line-by-line"><strong>The Launch Command, Line by Line</strong></h4> <h5 id="the-docker-half"><strong>The docker half</strong></h5> <table> <thead> <tr> <th>Piece</th> <th>What it does</th> <th>Why the recipe emits it</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">--gpus all</code></td> <td>exposes all eight GPUs</td> <td>required</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--privileged</code></td> <td>full device access inside the container</td> <td>hardcoded by the recipes command builder for every non-NPU platform (<code class="language-plaintext highlighter-rouge">src/lib/command-synthesis.js</code>, recipes repo, as of 2026-09-11). vLLM’s own docs only call for it when InfiniBand or RDMA is in play (<a href="https://docs.vllm.ai/en/latest/serving/parallelism_scaling/">parallelism scaling doc</a>). On one NVLink node it is not functionally needed; the PD variant with NIXL is where it matters.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--ipc=host</code></td> <td>share the host’s IPC namespace, so <code class="language-plaintext highlighter-rouge">/dev/shm</code> is not the container’s tiny default</td> <td>vLLM’s worker and engine processes exchange tensors over shared memory; the <a href="https://docs.vllm.ai/en/latest/deployment/docker/">docker deployment doc</a> uses it in its canonical example</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">vllm/vllm-openai:deepseekv41-flash-0909</code></td> <td>the only build that carries this architecture</td> <td>no pip wheel does; see the day-0 section for what is known about this image</td> </tr> </tbody> </table> <h5 id="the-environment-variables"><strong>The environment variables</strong></h5> <table> <thead> <tr> <th>Variable</th> <th>Default</th> <th>What the recipe sets and why</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">VLLM_ENGINE_READY_TIMEOUT_S</code></td> <td>600 (<code class="language-plaintext highlighter-rouge">vllm/envs.py</code>)</td> <td>3600. Loading 510 GB plus kernel JIT and warmup can exceed ten minutes, and the front end would otherwise give up on the engine cores.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">VLLM_USE_RUST_FRONTEND</code></td> <td>0</td> <td>1. Replaces the Python API server processes with the Rust <code class="language-plaintext highlighter-rouge">vllm-rs</code> binary, which serves HTTP, renders the chat template, and parses reasoning and tool calls, talking to the same Python engine cores over ZMQ (<a href="https://github.com/vllm-project/vllm/blob/main/rust/README.md">rust/README.md</a>). The README calls it experimental and not feature-complete. It is multithreaded, so <code class="language-plaintext highlighter-rouge">--api-server-count</code> is ignored with it. The V4.1 chat renderer and DSML parser exist in both frontends; the recipe defaults to Rust since <a href="https://github.com/vllm-project/recipes/pull/957">recipes PR #957</a> and tells you to switch to Python on any compatibility issue.</td> </tr> </tbody> </table> <p>Two Rust-side gaps specific to this model as of 2026-09-11: DSML tool calls are buffered until the whole invoke block is complete rather than streamed incrementally (<a href="https://github.com/vllm-project/vllm/pull/56334">PR #56334</a>), and multimodal placeholder ordering has a pending fix (<a href="https://github.com/vllm-project/vllm/pull/56366">PR #56366</a>). Neither affects a text benchmark; both affect a client that streams tool calls.</p> <h5 id="the-vllm-half"><strong>The vLLM half</strong></h5> <table> <thead> <tr> <th>Flag</th> <th>What it does</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">--tokenizer-mode deepseek_v41</code></td> <td>Selects <code class="language-plaintext highlighter-rouge">DeepseekV41Tokenizer</code>, which wraps the standard Rust BPE from <code class="language-plaintext highlighter-rouge">tokenizer.json</code> and replaces Jinja chat templating with a Python port of DeepSeek’s reference encoder (<code class="language-plaintext highlighter-rouge">vllm/tokenizers/deepseek_v41_encoding.py</code>). Needed because the checkpoint ships no Jinja template; the model card points to <code class="language-plaintext highlighter-rouge">encoding/encoding.py</code> instead. vLLM would pick this mode automatically for the architecture (<code class="language-plaintext highlighter-rouge">vllm/config/model.py</code>), so the flag is explicit rather than required.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--enable-expert-parallel</code></td> <td>Shards the 384 experts of every layer across the EP group, with \(\text{EP} = \text{TP} \times \text{DP} = 8\), so each GPU holds 48 routed experts per layer. Routing becomes all-to-all; the default backend is <code class="language-plaintext highlighter-rouge">allgather_reducescatter</code> (<a href="https://docs.vllm.ai/en/latest/serving/expert_parallel_deployment/">EP deployment doc</a>). The MegaMoE kernel backends refuse to run without it.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--data-parallel-size 8</code></td> <td>Eight engine-core processes, one per GPU, each with a full copy of the attention and dense weights over its own batch. This is “internal load balancing”: one endpoint, and the front end routes each request to the least-loaded engine by queue length (<a href="https://docs.vllm.ai/en/latest/serving/data_parallel_deployment/">DP deployment doc</a>). The expert layers do collectives, so all eight forward passes stay in lockstep, and a DP coordinator makes idle ranks run dummy passes so their peers’ all-to-alls do not hang. <code class="language-plaintext highlighter-rouge">--max-num-seqs</code> applies per rank.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--tool-call-parser deepseek_v41</code> plus <code class="language-plaintext highlighter-rouge">--enable-auto-tool-choice</code></td> <td>Registers the V4.1 DSML parser, which recognizes the spaced tags this model emits (<code class="language-plaintext highlighter-rouge">&lt;｜DSML｜ calls&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;｜DSML｜ invoke name=...&gt;</code>), and lets the model decide when to call a tool (<a href="https://docs.vllm.ai/en/latest/features/tool_calling/">tool calling doc</a>). The doc page has no V4.1 section yet; the registration is in <code class="language-plaintext highlighter-rouge">vllm/tool_parsers/__init__.py</code>.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--reasoning-parser deepseek_v41</code></td> <td>Same parser engine, splitting <code class="language-plaintext highlighter-rouge">&lt;think&gt;</code> blocks into <code class="language-plaintext highlighter-rouge">reasoning_content</code>.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--mm-encoder-tp-mode data</code></td> <td>Runs the ViT with full weights on every TP rank and splits the image batch across ranks, instead of tensor-parallelizing a 32-layer, 1024-wide encoder (<a href="https://github.com/vllm-project/vllm/blob/main/vllm/config/multimodal.py">multimodal config</a>). <strong>In this command TP is 1, so the flag does nothing</strong>: each DP rank already runs the whole ViT on its own requests (<code class="language-plaintext highlighter-rouge">vl_model.py</code> only takes the sharded path when the TP world size exceeds 1). It matters for the TP8 and TEP8 renderings of the same recipe.</td> </tr> </tbody> </table> <h5 id="what-resolves-silently"><strong>What resolves silently</strong></h5> <p>Flags you did not pass still get values, and several of them are specific to this model or this GPU class (all from vLLM source as of 2026-09-11):</p> <table> <thead> <tr> <th>Setting</th> <th>Resolved value</th> <th>Where</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">--max-num-seqs</code></td> <td>1024 per DP rank</td> <td><code class="language-plaintext highlighter-rouge">vllm/engine/arg_utils.py</code>: GPUs with at least 160 GiB get the largest defaults</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--max-num-batched-tokens</code></td> <td>16384</td> <td>same</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--max-model-len</code></td> <td>1,048,576</td> <td>from <code class="language-plaintext highlighter-rouge">max_position_embeddings</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--gpu-memory-utilization</code></td> <td>0.92</td> <td><code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--kv-cache-dtype</code></td> <td><code class="language-plaintext highlighter-rouge">fp8_ds_mla</code></td> <td><code class="language-plaintext highlighter-rouge">auto</code> resolves to it; bf16 is rejected for this layout</td> </tr> <tr> <td>CUDA graphs</td> <td>breakable, full and piecewise</td> <td><code class="language-plaintext highlighter-rouge">DeepseekV41ForCausalLM</code> is in <code class="language-plaintext highlighter-rouge">DEFAULT_BREAKABLE_CUDAGRAPH_ARCHITECTURES</code> (<code class="language-plaintext highlighter-rouge">vllm/config/vllm.py</code>), which sets <code class="language-plaintext highlighter-rouge">VLLM_USE_BREAKABLE_CUDAGRAPH=1</code> and disables torch.compile</td> </tr> <tr> <td>Engram placement</td> <td>pinned host memory, one full copy per DP rank</td> <td><code class="language-plaintext highlighter-rouge">vllm/config/engram.py</code> and <code class="language-plaintext highlighter-rouge">common/engram.py</code>; next section</td> </tr> <tr> <td>DSpark</td> <td>off</td> <td>opt-in via <code class="language-plaintext highlighter-rouge">--speculative-config</code></td> </tr> <tr> <td>DeepGEMM warmup</td> <td><code class="language-plaintext highlighter-rouge">relax</code></td> <td><code class="language-plaintext highlighter-rouge">VLLM_DEEP_GEMM_WARMUP</code></td> </tr> <tr> <td>Attention backend</td> <td>FlashMLA sparse (<code class="language-plaintext highlighter-rouge">FLASHMLA_SPARSE_DSV41</code>) on SM90 and SM100</td> <td><code class="language-plaintext highlighter-rouge">nvidia/model.py</code></td> </tr> </tbody> </table> <p>Two to internalize: this model never goes through torch.compile, and instead gets CUDA graphs with runtime stream-capture breaks around the attention ops (<code class="language-plaintext highlighter-rouge">vllm/compilation/breakable_cudagraph.py</code>); and 1024 sequences per rank is a very large admission ceiling for a model whose per-step cost stops improving once every expert is touched.</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/deepseek-v41-flash-vllm/dp8-topology.svg" sizes="95vw"/> <img src="/assets/img/deepseek-v41-flash-vllm/dp8-topology.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="What the DP8 plus EP command starts on one 8x B300 node" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> One node, one command. The Rust front end owns the port and routes each request to the least-loaded engine core; the DP coordinator keeps the eight cores' forward passes in lockstep. Each core drives one GPU holding a full copy of the attention and dense weights (about 11 GiB) and a 48-expert slice of every layer (about 32 GiB), with the expert all-to-all crossing NVLink every layer. Under the default configuration, each core also holds its own full 189 GiB Engram table in pinned host RAM and reads it over UVA. Editable source: <a href="/assets/img/deepseek-v41-flash-vllm/dp8-topology.excalidraw">dp8-topology.excalidraw</a>. </div> <h5 id="the-engram-placement-problem"><strong>The Engram placement problem</strong></h5> <p>This is the part of the config that surprised me, and it is the first thing I would check on the node.</p> <p>The resolution chain in vLLM, as of 2026-09-11: <code class="language-plaintext highlighter-rouge">VllmConfig._resolve_and_verify_engram_config</code> leaves <code class="language-plaintext highlighter-rouge">engram_config</code> as <code class="language-plaintext highlighter-rouge">None</code> unless you pass <code class="language-plaintext highlighter-rouge">--engram-config</code> or set the legacy <code class="language-plaintext highlighter-rouge">VLLM_PLE_CPU_OFFLOAD</code> (<code class="language-plaintext highlighter-rouge">vllm/config/vllm.py</code>). The Engram module then builds its table with <code class="language-plaintext highlighter-rouge">cpu_offload=engram_config.cpu_offload if engram_config else True</code> (<code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/common/engram.py</code>). So <strong>with no flag, the tables go to pinned host memory</strong>, allocated with <code class="language-plaintext highlighter-rouge">device="cpu", pin_memory=True</code>, and are read from the GPU over unified virtual addressing. The table is sharded by hash column across tensor-parallel ranks (<code class="language-plaintext highlighter-rouge">ParallelEngramEmbedding</code>), and with <code class="language-plaintext highlighter-rouge">embedding_across_dp</code> at its default of <code class="language-plaintext highlighter-rouge">False</code>, “each DP rank has a separate TP-sharded embedding replica” (<code class="language-plaintext highlighter-rouge">vllm/config/engram.py</code>).</p> <p>Put the two together for this command. TP is 1, so the shard is the whole table. DP is 8, so there are eight replicas. That is eight copies of 188.8 GiB, about 1.5 TiB of pinned host memory, before anything else on the host. Whether that boots depends on the node’s RAM, which NVIDIA’s DGX B300 page does not state in the spec table I could find, so check it before launching:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>free <span class="nt">-g</span>
</code></pre></div></div> <p>The alternatives, and what each costs per GPU (weights from the recipe’s memory table; Engram at 188.8 GiB):</p> <table> <thead> <tr> <th>Layout</th> <th>Attention and dense per GPU</th> <th>Experts per GPU</th> <th>Engram, default (host)</th> <th>Engram if resident on GPU</th> </tr> </thead> <tbody> <tr> <td>DP8, EP8 (this command)</td> <td>11.4 GiB, replicated</td> <td>32.4 GiB</td> <td>8 host copies of 188.8 GiB</td> <td>188.8 GiB per GPU, leaving about 14 GiB for KV at 0.92 utilization</td> </tr> <tr> <td>DP8, EP8 with <code class="language-plaintext highlighter-rouge">--engram-config '{"embedding_across_dp": true}'</code></td> <td>11.4 GiB</td> <td>32.4 GiB</td> <td>one host copy, sharded 8 ways</td> <td>23.6 GiB per GPU</td> </tr> <tr> <td>TP8, EP8 (the recipe’s TEP strategy)</td> <td>about 1.4 GiB, sharded</td> <td>32.4 GiB</td> <td>one host copy, sharded 8 ways</td> <td>23.6 GiB per GPU</td> </tr> <tr> <td>2x TP4, EP4 (SGLang’s verified 8x B300 layout, run as two replicas)</td> <td>2.9 GiB</td> <td>64.9 GiB</td> <td>2 host copies, each sharded 4 ways</td> <td>47.2 GiB per GPU</td> </tr> </tbody> </table> <p>The knob to keep the tables in HBM is <code class="language-plaintext highlighter-rouge">--engram-config '{"cpu_offload": false}'</code>. Note the trap in the resolution chain: because <code class="language-plaintext highlighter-rouge">EngramConfig.cpu_offload</code> defaults to <code class="language-plaintext highlighter-rouge">False</code> while an absent config means <code class="language-plaintext highlighter-rouge">True</code>, passing any <code class="language-plaintext highlighter-rouge">--engram-config</code> at all without that key also moves the tables onto the GPU. Under DP8 that is 188.8 GiB of each B300 gone to a lookup table, which is why the DP-sharding flag exists.</p> <p>What this means for the tuning plan: the recipe’s DP8 command is the throughput layout for MoE models in general, but for this model it multiplies the one component that does not shard for free. The first experiment is not a kernel; it is the layout matrix above, with Engram on host versus GPU, measured at fixed concurrency. Open <a href="https://github.com/vllm-project/vllm/pull/56512">PR #56512</a> adds asynchronous prefetch for offloaded lookups, shares a single host table across DP replicas through <code class="language-plaintext highlighter-rouge">/dev/shm</code>, and adds Engram DP sharding; when it lands, the DP8 row changes.</p> <p><strong>References</strong></p> <ul> <li><a href="https://docs.vllm.ai/en/latest/serving/data_parallel_deployment/">vLLM data parallel deployment</a>, <a href="https://docs.vllm.ai/en/latest/serving/expert_parallel_deployment/">expert parallel deployment</a>, <a href="https://docs.vllm.ai/en/latest/deployment/docker/">docker deployment</a>, <a href="https://docs.vllm.ai/en/latest/serving/parallelism_scaling/">parallelism scaling</a>, <a href="https://docs.vllm.ai/en/latest/features/tool_calling/">tool calling</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/rust/README.md">vLLM Rust frontend README</a>; <a href="https://github.com/vllm-project/recipes/pull/957">recipes PR #957</a></li> <li>vLLM source, as of 2026-09-11: <code class="language-plaintext highlighter-rouge">vllm/envs.py</code>, <code class="language-plaintext highlighter-rouge">vllm/engine/arg_utils.py</code>, <code class="language-plaintext highlighter-rouge">vllm/config/{vllm,cache,engram,multimodal,model}.py</code>, <code class="language-plaintext highlighter-rouge">vllm/tokenizers/deepseek_v41_encoding.py</code>, <code class="language-plaintext highlighter-rouge">vllm/tool_parsers/__init__.py</code>, <code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/common/engram.py</code>, <code class="language-plaintext highlighter-rouge">vllm/models/deepseek_v4_1/nvidia/{model,vl_model}.py</code>, <code class="language-plaintext highlighter-rouge">vllm/compilation/breakable_cudagraph.py</code></li> <li><a href="https://github.com/vllm-project/vllm/pull/56512">PR #56512</a>, <a href="https://github.com/vllm-project/vllm/pull/56334">PR #56334</a>, <a href="https://github.com/vllm-project/vllm/pull/56366">PR #56366</a></li> </ul> <hr/> <h4 id="vllm-day-0-status"><strong>vLLM Day-0 Status</strong></h4> <h5 id="what-landed-what-is-open"><strong>What landed, what is open</strong></h5> <p>Support arrived in three PRs over two days: frontends in <a href="https://github.com/vllm-project/vllm/pull/56208">#56208</a> (2026-09-10), model definitions in <a href="https://github.com/vllm-project/vllm/pull/56228">#56228</a> (2026-09-10), and the keystone with registry, config, kernels, and tests in <a href="https://github.com/vllm-project/vllm/pull/56214">#56214</a> (merged 2026-09-11). Everything else is tracked in <a href="https://github.com/vllm-project/vllm/issues/56400">issue #56400</a> and the <a href="https://github.com/vllm-project/vllm/issues/56217">kernel tracker #56217</a>. Neither the <a href="https://docs.vllm.ai/en/latest/models/supported_models/">supported models page</a> nor the vLLM blog mentions V4.1 yet.</p> <table> <thead> <tr> <th>Open item</th> <th>PR or issue</th> <th>Why it matters here</th> </tr> </thead> <tbody> <tr> <td>Attention megakernel plus FP4 main KV</td> <td><a href="https://github.com/vllm-project/vllm/pull/56344">#56344</a></td> <td>fuses norm, RoPE, sparse attention, inverse RoPE, and FP8 cast into one FlashMLA kernel and adds the <code class="language-plaintext highlighter-rouge">nvfp4_ds_mla</code> cache; its own GB200 numbers show a fused decode kernel at 22.7 µs versus 43.1 µs for the split chain</td> </tr> <tr> <td>SWA bounded replay</td> <td><a href="https://github.com/vllm-project/vllm/pull/56227">#56227</a></td> <td>the prefill halving from CED</td> </tr> <tr> <td>DeepGEMM sparse MQA logits</td> <td><a href="https://github.com/vllm-project/vllm/pull/56254">#56254</a></td> <td>the layer-20 full-range indexer scan; the PR’s GB300 microbenchmark shows 6.56x to 12.6x over dense for decode at 512K context and roughly break-even at 32K</td> </tr> <tr> <td>DeepSelect top-k</td> <td><a href="https://github.com/vllm-project/vllm/pull/56464">#56464</a></td> <td>119 µs versus 613 µs at batch 256 and 1M KV on GB200, per the PR</td> </tr> <tr> <td>Engram prefetch, shared host table, DP sharding</td> <td><a href="https://github.com/vllm-project/vllm/pull/56512">#56512</a></td> <td>the DP8 host-memory multiplier</td> </tr> <tr> <td>EPLB with DSpark</td> <td><a href="https://github.com/vllm-project/vllm/pull/56387">#56387</a></td> <td>EPLB currently fails when the DSpark drafter is on</td> </tr> <tr> <td>Mega-mHC, Mega-Gate</td> <td><a href="https://github.com/vllm-project/vllm/issues/56217">#56217</a></td> <td>the fused kernels the report describes</td> </tr> </tbody> </table> <h5 id="bugs-worth-knowing-on-an-nvidia-node"><strong>Bugs worth knowing on an NVIDIA node</strong></h5> <ul> <li><a href="https://github.com/vllm-project/vllm/issues/56389">#56389</a>: the <code class="language-plaintext highlighter-rouge">dsv4_topk</code> Triton router hits an illegal memory access above 256 sequences under high concurrency on H20 with TP8, EP8, and DSpark. With the 1024-per-rank default above, this is the first thing to bound if you see a crash under load.</li> <li><a href="https://github.com/vllm-project/vllm/issues/56443">#56443</a>: DSpark warmup asserts on H200 with the Marlin MXFP4 MoE backend, which is what SM90 falls back to. Not a B300 issue, but it says the SM90 path is less traveled.</li> <li><a href="https://github.com/vllm-project/vllm/issues/56461">#56461</a>: cannot serve on SM120 at all, from a block-size mismatch. Irrelevant to B300 but indicative of how fresh the geometry code is.</li> </ul> <h5 id="the-image"><strong>The image</strong></h5> <p>The tag in the command, <code class="language-plaintext highlighter-rouge">deepseekv41-flash-0909</code>, was pushed to Docker Hub on 2026-09-10; the bare <code class="language-plaintext highlighter-rouge">deepseekv41-flash</code> tag the recipe prose mentions does not exist. The image labels its build commit as unknown, users report an in-image version string pointing at a commit that no longer resolves on GitHub (a staging branch deleted once V4.1 merged, per <a href="https://github.com/vllm-project/vllm/pull/56512">#56512</a>), and it was created about a day before the keystone PR merged. Practically: the container is not <code class="language-plaintext highlighter-rouge">main</code>, and a fix you read on GitHub may not be in it. The recipe’s <code class="language-plaintext highlighter-rouge">min_vllm_version</code> of 0.30.0 is unreleased; v0.29.0 (2026-09-09) does not mention V4.1.</p> <p><strong>References</strong></p> <ul> <li><a href="https://github.com/vllm-project/vllm/issues/56400">Tracking issue #56400</a>, <a href="https://github.com/vllm-project/vllm/issues/56217">kernel tracker #56217</a>, PRs and issues as linked</li> <li><a href="https://hub.docker.com/r/vllm/vllm-openai/tags?name=deepseekv41">Docker Hub tags for vllm/vllm-openai</a>, <a href="https://github.com/vllm-project/vllm/releases/tag/v0.29.0">vLLM v0.29.0 release</a></li> </ul> <hr/> <h4 id="where-to-look-for-performance"><strong>Where to Look for Performance</strong></h4> <p>No one has published a throughput number for this model on B300 with vLLM. The only public vLLM measurement of any kind is an AMD <a href="https://github.com/vllm-project/vllm/issues/56506">RFC #56506</a> on 8x MI355X at TP4 with DSpark: 35.9 output tokens per second at concurrency 1 with a 25.6 ms inter-token latency, rising to 469 tokens per second at concurrency 32 with ITL only 1.9x worse. The author’s reading is the right one: a device whose ITL barely moves while throughput climbs 13x is not saturated; it is paying a large fixed cost per step. Their trace counts about 14,020 kernel launches per decode step, 85% of each layer’s 350 launches in the mHC block, on a 4x4 matrix. That is ROCm with an eager-mode trace, so the wall-clock figures do not transfer to B300; the launch count and its composition do, because a CUDA graph replays the sequence it captured rather than merging kernels.</p> <p>Everything below is a hypothesis, ordered by how much I expect it to matter, each tied to a place in the code to confirm or refute it.</p> <p><strong>H1: decode is launch-bound, not byte-bound, and mHC is the launch count.</strong> The byte floor computed earlier is about 1 ms per step at small batch. If a single-stream decode step on B300 lands anywhere near 20 ms, the gap is fixed cost. First check: count kernels per step with <code class="language-plaintext highlighter-rouge">nsys</code> on one engine core, and see what fraction sits in the TileLang mHC pre and post kernels and the 20-iteration Sinkhorn loop. The fix path is the Mega-mHC integration on the kernel tracker; the measurement tells you how much it is worth before it lands.</p> <p><strong>H2: the Engram lookup is a random-access host read in the critical path.</strong> By default every token gathers 24 rows of 256 FP8 bytes from a pinned host table over UVA, twice per token, at layers 1 and 14. The bandwidth is nothing; the latency is a PCIe round trip per gather with no prefetch on main. Check: run the layout matrix with <code class="language-plaintext highlighter-rouge">--engram-config '{"cpu_offload": false}'</code> on a layout where the table fits, and compare ITL at fixed concurrency. If the gap is large, the prefetch PR is the fix; if it is small, leave the tables on the host and spend the HBM on batch.</p> <p><strong>H3: DP8 is the wrong layout for this model until Engram shards across DP.</strong> Beyond the host-memory multiplier, DP8 replicates the 7.6 GB dense-weight read on every rank each step, while TP8 divides it, and that read is most of the byte floor. Check: TEP8 versus DP8 versus two TP4 replicas at the same concurrency per node. SGLang’s verified 8x B300 layout is two TP4 replicas, a data point rather than a verdict.</p> <p><strong>H4: the MoE backend that <code class="language-plaintext highlighter-rouge">auto</code> picks matters on SM100.</strong> The MegaMoE backends (<code class="language-plaintext highlighter-rouge">deep_gemm_mega_moe</code>, <code class="language-plaintext highlighter-rouge">flashinfer_moe_ep_mega_deep_gemm</code>, <code class="language-plaintext highlighter-rouge">flashinfer_moe_ep_mega_cutedsl</code>) exist for exactly this checkpoint shape and require EP plus SM100. Which one <code class="language-plaintext highlighter-rouge">auto</code> resolves to is not documented. Check the startup log for the selected backend, then sweep <code class="language-plaintext highlighter-rouge">--kernel-config '{"moe_backend": ...}'</code> across the three at a saturating batch, where the expert GEMMs dominate.</p> <p><strong>H5: DSpark helps at low concurrency and hurts at high, and adaptive verification is what makes one config survive both.</strong> The engine’s own doc says so, and SGLang turns DSpark off in its throughput cell. Check: ITL at concurrency 1, 8, 32, 128 with DSpark off, on with fixed verification, and on with adaptive verification. Watch the per-position acceptance counters, since acceptance is workload-dependent and V4.1’s numbers are unpublished.</p> <p><strong>H6: the layer-20 full-range indexer scan is the context-scaling cost.</strong> Everything else in decode is bounded by the 512-entry selection, the candidate pool, or the window. Check: ITL at 8K, 128K, and 512K context at fixed batch. The DeepGEMM logits and DeepSelect PRs target exactly this, and the DeepGEMM PR’s own numbers put the crossover near 32K context, with the sparse path pure overhead below it.</p> <p><strong>H7: the admission defaults are too generous for the step cost curve.</strong> With 1024 sequences per rank and 16,384 batched tokens, the scheduler will happily build steps far past the point where every expert is touched, trading ITL for nothing. Check: sweep <code class="language-plaintext highlighter-rouge">--max-num-seqs</code> from 32 to 1024 per rank and plot goodput against an ITL bound, as in the metrics section of the inference post. The recipe’s PD variant caps at 32 for a reason, and the known router crash above 256 is a second reason.</p> <p><strong>What to leave alone for now.</strong> KV capacity: it is not the constraint on this node, so the FP4 cache PR is a memory win that mostly matters at 1M context with hundreds of sequences per GPU. Chunked prefill and prefix caching: both are on by default and neither is model-specific. torch.compile: not available for this model.</p> <p>The order that follows: <code class="language-plaintext highlighter-rouge">nsys</code> on one engine core at concurrency 1 (H1); the layout matrix (H2, H3); the MoE backend sweep at saturating batch (H4); the DSpark and admission sweeps against an ITL bound (H5, H7); the context sweep last (H6). vLLM’s Prometheus histograms give ITL and TTFT per phase without instrumentation.</p> <p><strong>References</strong></p> <ul> <li><a href="https://github.com/vllm-project/vllm/issues/56506">RFC #56506, DeepSeek-V4.1-Flash performance on ROCm</a></li> <li><a href="https://docs.vllm.ai/en/latest/features/speculative_decoding/adaptive_verification/">vLLM adaptive verification doc</a>, <a href="https://docs.vllm.ai/en/latest/serving/expert_parallel_deployment/">EP deployment doc</a></li> <li><a href="https://github.com/vllm-project/vllm/pull/56254">PR #56254</a>, <a href="https://github.com/vllm-project/vllm/pull/56464">PR #56464</a>, <a href="https://github.com/vllm-project/vllm/pull/56512">PR #56512</a>, <a href="https://github.com/vllm-project/vllm/issues/56217">tracker #56217</a></li> <li><a href="/blog/2026/llm-inference-systems/">LLM inference systems post, metrics section</a></li> </ul> <hr/> <h4 id="other-vendors-briefly"><strong>Other Vendors, Briefly</strong></h4> <p>vLLM is the baseline here, but it is worth knowing what everyone else shipped in the same 48 hours, as of 2026-09-11.</p> <table> <thead> <tr> <th>Stack</th> <th>Status</th> <th>Notes</th> </tr> </thead> <tbody> <tr> <td><a href="https://www.lmsys.org/blog/2026-09-10-deepseek-v41">SGLang</a></td> <td>day-0 via preview image <code class="language-plaintext highlighter-rouge">lmsysorg/sglang:dev-dsv41</code>; upstream PR still open</td> <td>verified cells for GB300, H200, B200, B300, MI350X; 8x B300 runs as two TP4, EP4 replicas; DSpark in the low-latency cell only; opt-in decoder bounded replay (reported 1.56x prefill on 8x H200); Engram host offload via an env var, reported plus 36% KV capacity on 4x GB300 (<a href="https://docs.sglang.io/cookbook/autoregressive/DeepSeek/DeepSeek-V4_1">cookbook</a>)</td> </tr> <tr> <td>NVIDIA Dynamo</td> <td>frontend and parsers merged (<a href="https://github.com/ai-dynamo/dynamo/pull/14690">#14690</a>); SGLang-backed recipes on 8x GB200 in an open PR (<a href="https://github.com/ai-dynamo/dynamo/pull/14682">#14682</a>)</td> <td>the PR states neither target is benchmarked</td> </tr> <tr> <td>NVIDIA TensorRT-LLM</td> <td>nothing for V4.1 found in the repo</td> <td>V4 support exists</td> </tr> <tr> <td>AMD</td> <td>through the frameworks</td> <td>vLLM ROCm recipe verified on MI350X with <code class="language-plaintext highlighter-rouge">VLLM_USE_BREAKABLE_CUDAGRAPH=1</code> and the AITER MXFP4 MoE backend; the RFC above is the only public performance data</td> </tr> <tr> <td>llama.cpp, Ollama</td> <td>conversion PR open upstream (<a href="https://github.com/ggml-org/llama.cpp/pull/28696">#28696</a>); Ollama offers a cloud tag only</td> <td>community forks report a few tokens per second with expert streaming</td> </tr> <tr> <td>DeepSeek’s own <a href="https://github.com/deepseek-ai/deepseek-recipe">deepseek-recipe</a></td> <td>Rust and Python prompt encoder and output parser</td> <td>no inference engine; vLLM’s Rust tool-call rendering was aligned to it in <a href="https://github.com/vllm-project/vllm/pull/56260">#56260</a></td> </tr> </tbody> </table> <p>One cross-vendor discrepancy to carry in your head: DeepSeek’s reference encoder and the tech report map the named reasoning efforts to low 50, high 75, max 100; vLLM’s port maps low 25, high 50, xhigh 75, max 100, defaulting to 50 with thinking on (<code class="language-plaintext highlighter-rouge">vllm/tokenizers/deepseek_v41_encoding.py</code>, as of 2026-09-11). A benchmark that sends <code class="language-plaintext highlighter-rouge">reasoning_effort: "high"</code> is asking for different things on the two stacks. Send an integer.</p> <p><strong>References</strong></p> <ul> <li><a href="https://www.lmsys.org/blog/2026-09-10-deepseek-v41">SGLang and Miles day-0 post</a>, <a href="https://docs.sglang.io/cookbook/autoregressive/DeepSeek/DeepSeek-V4_1">SGLang cookbook</a></li> <li>Dynamo <a href="https://github.com/ai-dynamo/dynamo/pull/14690">#14690</a>, <a href="https://github.com/ai-dynamo/dynamo/pull/14682">#14682</a>; llama.cpp <a href="https://github.com/ggml-org/llama.cpp/pull/28696">#28696</a>; <a href="https://github.com/deepseek-ai/deepseek-recipe">deepseek-recipe</a></li> <li><a href="https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/blob/main/encoding/encoding.py">DeepSeek reference encoder</a></li> </ul> <hr/> <h4 id="takeaways"><strong>Takeaways</strong></h4> <ul> <li><strong>The model is built to cache less.</strong> Four layers write main KV, at 2.5 entries per token, in FP4; the decoder’s KV is projected from the encoder so prefill runs half the network; the sliding-window caches are replayed rather than persisted. The paper’s 890 bytes per token is the HBM cost on DeepSeek’s stack.</li> <li><strong>vLLM today stores twice that.</strong> The fp8 layout is 584 bytes per entry, FP4 is an open PR, and bounded replay is an open PR. Size off vLLM’s numbers, and expect both to move.</li> <li><strong>The byte floor on 8x B300 is about 1 ms per decode step; the only public measurement is 25x that, on AMD.</strong> The gap is fixed per-step cost, and the trace points at mHC’s kernel count. Measure launches before bytes.</li> <li><strong>DP8 multiplies Engram.</strong> Default placement is one full 189 GiB copy per DP rank in pinned host memory, eight copies for this command. TP8 or DP sharding turns that into one. This is the first experiment.</li> <li><strong>Several flags are inert or redundant in this exact command.</strong> The encoder-parallel flag needs TP above 1; the tokenizer mode is auto-selected; the privileged flag is there for RDMA.</li> <li><strong>The admission defaults are large.</strong> 1024 sequences per rank and 16,384 tokens per step, on a model whose step cost stops improving once every expert is touched, with a known router crash above 256 sequences.</li> <li><strong>The image is not main.</strong> Built from a deleted staging branch, a day before the keystone merge, with its build commit unlabeled. Read PRs with that in mind.</li> </ul> <hr/> <h4 id="test-yourself"><strong>Test Yourself</strong></h4> <p>Try each from memory before reading the answer.</p> <p><strong>1. Why does the model activate 8B parameters per token in prefill but 16B in decode?</strong> The causal encoder-decoder projects the decoder’s global KV from the encoder’s final hidden state, so a prompt token only needs to run the 20 encoder layers to populate the cache. Decode runs all 40. Half the layers means half the active experts.</p> <p><strong>2. What do the three CSA2 modes share and what do they compute?</strong> Full computes main KV, indexer K, and top-K itself. Reindex reuses main KV and indexer K from the last Full layer but computes its own indexer Q and a fresh top-K. Reuse reuses main KV and the latest top-K and runs no indexer. All three compute their own query and sliding-window KV.</p> <p><strong>3. How many main KV entries does one token add across the network, and where does the number come from?</strong> 2.5: the three encoder source layers (2, 8, 14) at compression ratio 2 contribute half an entry each, and the decoder source layer (20) at ratio 1 contributes one. From <code class="language-plaintext highlighter-rouge">kv_source_layer_ids</code> and <code class="language-plaintext highlighter-rouge">compress_ratios</code> in config.json.</p> <p><strong>4. What does the hierarchical sparse indexer make constant in context length, and what does it leave linear?</strong> The per-query cost of the four decoder Reindex layers, which score only the 16,384-position candidate pool published by layer 20. Layer 20’s own full-range scan stays linear in context.</p> <p><strong>5. What is single-pass mHC, in one sentence, and why does it matter for kernels?</strong> Block \(l\) mixes its input with the previous block’s coefficient \(A_{l-1}\) instead of its own \(A_l\), which removes the reduction dependency and lets residual update, input mixing, and coefficient prediction fuse into one pass over the residual, halving activation traffic.</p> <p><strong>6. What does an Engram lookup cost per token in bytes, and why is it still a performance concern?</strong> About 12 KB: 24 gathers of 256 FP8 values per module, two modules. The concern is placement and latency, not bandwidth: the tables are 189 GiB, the addresses are random, and vLLM’s default reads them from pinned host memory with no prefetch on main.</p> <p><strong>7. Under the recipe’s DP8 command, how many copies of the Engram table exist and where?</strong> Eight, one per DP rank, each the full 188.8 GiB, in pinned host memory, because TP is 1 (no sharding) and <code class="language-plaintext highlighter-rouge">embedding_across_dp</code> defaults to false.</p> <p><strong>8. Which flag in the command does nothing, and why?</strong> <code class="language-plaintext highlighter-rouge">--mm-encoder-tp-mode data</code>, because the ViT’s data-parallel path is only taken when the tensor-parallel world size exceeds 1, and this command has TP 1.</p> <p><strong>9. Why is a 1 ms byte floor consistent with a measured 25 ms ITL, and what would you measure first?</strong> Because the floor counts bytes and the measurement includes fixed per-step cost. The AMD trace counted about 14,000 kernel launches per step, 85% of a layer’s launches in the mHC block. Measure the launch count per step on one engine core before touching anything else.</p> <p><strong>10. Why does SGLang turn DSpark off in its high-throughput recipe?</strong> A DSpark step has a fixed cost that does not shrink with accept length; at large batch the GPU is no longer memory-bound, so rejected draft tokens are compute wasted and the fixed step cost stops paying for itself. vLLM’s adaptive verification exists to make one setting survive across that crossover.</p> <hr/> <h4 id="wrapping-up"><strong>Wrapping up</strong></h4> <p>The thing to keep from this post is a shape, not a number. DeepSeek-V4.1-Flash is an answer to the question “what is the least state a long-context model can carry per token,” and every component in it is one term of that answer: share the cache across layers, compress it along the sequence, store it in four bits, project the top half from the bottom half, and replay the window instead of saving it. The recipe command is a generic MoE throughput layout dropped onto that model, and the two do not quite agree. It replicates the one large component that does not shard on its own, it leaves the model’s headline cache format on the table because the kernel has not landed, and it admits far more work per step than the step cost curve rewards.</p> <p>None of that is a criticism of a day-old recipe. It is the map of where the time probably goes, and each item comes with a way to check it. The floor is a millisecond; the first job is to find out how far above it the node actually runs, and why.</p> <p>If you find a mistake anywhere in here, please let me know and I’ll fix it.</p>]]></content><author><name></name></author><category term="GPU"/><category term="ML"/><summary type="html"><![CDATA[A briefing on DeepSeek-V4.1-Flash for anyone about to tune it on vLLM, covering the new architecture piece by piece (CED, CSA2, the hierarchical indexer, FP4 KV, mHC, Engram, DSpark), what every flag in the recipe command does, what vLLM has and has not landed yet, and where to look first on an 8x B300 node]]></summary></entry><entry><title type="html">How Luminal Works - E-Graphs, Equality Saturation, and Search</title><link href="https://monishver11.github.io/blog/2026/luminal-egraphs-search/" rel="alternate" type="text/html" title="How Luminal Works - E-Graphs, Equality Saturation, and Search"/><published>2026-09-10T13:00:00+00:00</published><updated>2026-09-10T13:00:00+00:00</updated><id>https://monishver11.github.io/blog/2026/luminal-egraphs-search</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/luminal-egraphs-search/"><![CDATA[<p>Hello all. This post is about <a href="https://github.com/luminal-ai/luminal">Luminal</a>, an inference compiler written in Rust, and about the single idea the whole thing is built on.</p> <p>Nearly every compiler you have used works by rewriting. It finds a pattern, replaces it with something better, and moves on. Luminal refuses to do that. When it discovers that two programs are equivalent, it does not pick one; it records the equality and keeps both. Do that for every rule it knows, over and over until nothing new can be learned, and you end up with a data structure that holds an enormous set of equivalent programs at once. Only then does the compiler choose, and it chooses by compiling candidates and timing them on the actual device.</p> <p>That is the thesis. The rest of the design follows from it, and a lot of things that look strange at first, the tiny operator set, the symbolic shapes, the fact that a matmul is not an operation, all stop looking strange once you see what the search needs from them.</p> <p>The plan:</p> <ul> <li>The one idea: why rewriting destroys information, and what equality saturation does instead</li> <li>The map: the pipeline end to end, and what you actually write</li> <li>E-graphs from first principles: e-nodes, e-classes, congruence, and saturation</li> <li>egglog: rules instead of passes, read on a real Luminal rule</li> <li>Extraction and search: why choosing is the hard part, and why Luminal measures</li> <li>When things compile: ahead-of-time, just-in-time, and what “shape-specific kernels compiled at runtime” means</li> <li>The core and runtime boundary, and where a backend plugs in</li> <li>Where this sits next to torch.compile, XLA, TVM, and tinygrad</li> <li>Running it on a Mac</li> <li>Takeaways, and a question bank over everything above</li> </ul> <p>I’m assuming you know roughly what a computation graph is and have seen a GPU kernel before, at the level of my <a href="/blog/2025/gpu-notes/">GPU notes</a>. No Rust knowledge is needed; the few snippets are short and I’ll explain them. Everything quoted from the codebase was checked against commit <code class="language-plaintext highlighter-rouge">d18376d1</code> as of 2026-09-10, and Luminal moves fast, so treat file paths as pointers rather than promises.</p> <p>Let’s get started.</p> <hr/> <h4 id="the-one-idea"><strong>The One Idea</strong></h4> <h5 id="rewriting-throws-information-away"><strong>Rewriting throws information away</strong></h5> <p>Take the expression \((a \times 2) / 2\) and give a compiler two perfectly good rules:</p> <ol> <li>Multiplying by a power of two is a left shift: \(x \times 2 \rightarrow x \ll 1\).</li> <li>Multiplying and then dividing by the same value cancels: \((x \times y) / y \rightarrow x\).</li> </ol> <p>Rule 1 is a real optimization; shifts are cheaper than multiplies on most hardware. Apply it and you get \((a \ll 1) / 2\). You have made the program faster and you have also made it impossible to reach the correct answer, which is just \(a\). Rule 2 can no longer fire, because the multiply it was looking for is gone.</p> <p>Apply rule 2 first and you get \(a\), which is better than anything rule 1 could produce. So the rules are order-dependent, and the right order depends on the expression. This is the <strong>phase ordering problem</strong>, and it is not a corner case. It is the central difficulty in optimizing compilers, and it gets worse as you add rules, because every new rule interacts with all the existing ones.</p> <p>The usual answer is to make rules conservative. Only fire when you are sure it helps. But “sure it helps” is a judgment that depends on everything downstream, so the guards get complicated, and special-cased, and numerous. Luminal’s README makes exactly this complaint about the traditional stacks: destructive rules must only fire when a benefit is certain, and that requirement is what makes them balloon.</p> <p>If you want to see what that balloon looks like in production, I wrote up one instance of it in detail: <a href="/blog/2026/silu-mul-fp8-block-quant-compile-vLLM/">vLLM’s pattern matching pipeline</a> registers sixteen structurally distinct FX graph patterns to cover a single fusion, because the pattern matcher matches exactly and each combination of flags produces a different graph. Every one of those sixteen is correct. The count is not a bug; it is the cost of exact, destructive, one-directional matching.</p> <h5 id="equality-saturation-does-not-choose"><strong>Equality saturation does not choose</strong></h5> <p><a href="https://cseweb.ucsd.edu/~lerner/papers/popl09.html">Tate, Stepp, Tatlock, and Lerner (POPL 2009)</a> proposed the alternative. Instead of an optimization being “replace this with that”, an optimization becomes “this equals that”. The compiler applies its rules repeatedly, and each application adds equality information to the intermediate representation rather than mutating it. Run until no rule can add anything new, and the IR is <strong>saturated</strong>: it now encodes many optimized versions of the input program simultaneously. Only after that does a separate step pick one.</p> <p>On our example, saturation records that \((a \times 2)/2\), \((a \ll 1)/2\), and \(a\) are all the same thing. Nothing was lost, no order was committed to, and the choice of which one to emit is made at the end with full knowledge of all three.</p> <p>The data structure that makes this affordable is the <strong>e-graph</strong>, and we’ll build one by hand shortly. The short version is that it compresses an exponentially large set of equivalent programs into something roughly the size of the original, by sharing.</p> <h5 id="why-an-ml-compiler-wants-this"><strong>Why an ML compiler wants this</strong></h5> <p>Two reasons, and they compound.</p> <p>The first is that ML graph optimization is where phase ordering bites hardest. <a href="https://arxiv.org/abs/2101.01332">Yang et al.</a> made the case directly: production frameworks apply graph substitutions sequentially and are therefore sensitive to order, and they explore only a small fragment of the exponential space of equivalent graphs. Applying all substitutions at once via equality saturation, they found graphs up to 16% faster while spending on average 48 times less time optimizing.</p> <p>The second is that on modern accelerators nobody actually knows which version is faster. Whether to fuse two kernels, whether to call a vendor library or emit your own, which tiling to use, all of it depends on cache behavior, occupancy, and launch overhead in ways that defeat static cost models. If your compiler has already committed to a rewrite, that question never gets asked. If it has kept every alternative alive, you can just run them and find out.</p> <p>Luminal takes the second option to its logical end. Its README puts the position bluntly: “The best heuristic is no heuristic.” It searches instead.</p> <p><strong>References</strong></p> <ul> <li><a href="https://cseweb.ucsd.edu/~lerner/papers/popl09.html">Equality Saturation: a New Approach to Optimization, Tate et al., POPL 2009</a> (<a href="https://homes.cs.washington.edu/~ztatlock/pubs/eqsat-tate-popl09.pdf">PDF</a>)</li> <li><a href="https://arxiv.org/abs/2101.01332">Equality Saturation for Tensor Graph Superoptimization, Yang et al.</a></li> <li><a href="https://github.com/luminal-ai/luminal">Luminal README</a></li> <li><a href="/blog/2026/silu-mul-fp8-block-quant-compile-vLLM/">SiLU+Mul+FP8 Block Quant Pattern Matching Pipeline</a> (the destructive-matching cost, worked out)</li> </ul> <hr/> <h4 id="the-map"><strong>The Map</strong></h4> <p>Before any details, the shape of the whole thing.</p> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/luminal-egraphs-search/luminal-pipeline.svg" sizes="95vw"/> <img src="/assets/img/luminal-egraphs-search/luminal-pipeline.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="The Luminal compilation pipeline" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The path from a line of Rust to a running kernel. The core crate stops at the saturated e-graph; everything right of the dashed line belongs to the runtime. The red loop is the part that makes Luminal unusual: candidates are compiled and timed rather than scored by a model. The purple box is what keeps that cost one-time. Editable source: <a href="/assets/img/luminal-egraphs-search/luminal-pipeline.excalidraw">luminal-pipeline.excalidraw</a>. </div> <p>Five stages, and it is worth naming them now because the vocabulary recurs:</p> <table> <thead> <tr> <th>Stage</th> <th>What it produces</th> <th>Where it lives</th> </tr> </thead> <tbody> <tr> <td>Front end</td> <td>an HLIR graph of primitive ops</td> <td><code class="language-plaintext highlighter-rouge">src/frontend/</code></td> </tr> <tr> <td>Lowering to egglog</td> <td>an egglog program</td> <td><code class="language-plaintext highlighter-rouge">src/egglog_utils/</code></td> </tr> <tr> <td>Saturation</td> <td>one e-graph per dynamic-dimension bucket</td> <td><code class="language-plaintext highlighter-rouge">Graph::build_search_space</code> in <code class="language-plaintext highlighter-rouge">src/graph.rs</code></td> </tr> <tr> <td>Extraction and search</td> <td>one selected LLIR program per bucket</td> <td><code class="language-plaintext highlighter-rouge">src/search/</code>, driven by the runtime</td> </tr> <tr> <td>Load and execute</td> <td>device kernels, compiled and launched</td> <td><code class="language-plaintext highlighter-rouge">crates/luminal_metal/</code>, <code class="language-plaintext highlighter-rouge">crates/luminal_cuda_lite/</code></td> </tr> </tbody> </table> <p>HLIR is the high-level IR, the graph of primitives you built. LLIR is the low-level IR, a graph of backend ops that map onto real kernels. Saturation is the step that turns one into a space containing many of the other.</p> <h5 id="what-you-actually-write"><strong>What you actually write</strong></h5> <p>Here is a complete Luminal program, from the repository’s <code class="language-plaintext highlighter-rouge">examples/simple</code>:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">luminal</span><span class="p">::</span><span class="nn">prelude</span><span class="p">::</span><span class="o">*</span><span class="p">;</span>

<span class="k">let</span> <span class="k">mut</span> <span class="n">cx</span> <span class="o">=</span> <span class="nn">Graph</span><span class="p">::</span><span class="nf">new</span><span class="p">();</span>
<span class="k">let</span> <span class="n">a</span> <span class="o">=</span> <span class="n">cx</span><span class="nf">.tensor</span><span class="p">((</span><span class="mi">3</span><span class="p">,</span> <span class="mi">1</span><span class="p">));</span>
<span class="k">let</span> <span class="n">b</span> <span class="o">=</span> <span class="n">cx</span><span class="nf">.tensor</span><span class="p">((</span><span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">));</span>

<span class="k">let</span> <span class="n">c</span> <span class="o">=</span> <span class="n">a</span><span class="nf">.matmul</span><span class="p">(</span><span class="n">b</span><span class="p">)</span><span class="nf">.output</span><span class="p">();</span>

<span class="k">let</span> <span class="k">mut</span> <span class="n">rt</span> <span class="o">=</span> <span class="n">cx</span><span class="nf">.compile</span><span class="p">(</span><span class="nn">ReferenceRuntime</span><span class="p">::</span><span class="nf">default</span><span class="p">(),</span> <span class="nn">CompileOptions</span><span class="p">::</span><span class="nf">default</span><span class="p">());</span>

<span class="n">rt</span><span class="nf">.set_data</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="nd">vec!</span><span class="p">[</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">2.0</span><span class="p">,</span> <span class="mf">3.0</span><span class="p">]);</span>
<span class="n">rt</span><span class="nf">.set_data</span><span class="p">(</span><span class="n">b</span><span class="p">,</span> <span class="nd">vec!</span><span class="p">[</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">2.0</span><span class="p">,</span> <span class="mf">3.0</span><span class="p">,</span> <span class="mf">3.0</span><span class="p">]);</span>
<span class="n">rt</span><span class="nf">.execute</span><span class="p">(</span><span class="o">&amp;</span><span class="n">cx</span><span class="py">.dyn_map</span><span class="p">);</span>

<span class="nd">println!</span><span class="p">(</span><span class="s">"Result: {:?}"</span><span class="p">,</span> <span class="n">rt</span><span class="nf">.get_f32</span><span class="p">(</span><span class="n">c</span><span class="p">));</span>
</code></pre></div></div> <p>Three things about this are worth slowing down on.</p> <p><strong>Nothing computes until <code class="language-plaintext highlighter-rouge">execute</code>.</strong> <code class="language-plaintext highlighter-rouge">cx.tensor((3, 1))</code> does not allocate a 3-by-1 array. It adds a node to a graph and hands you back a <code class="language-plaintext highlighter-rouge">GraphTensor</code>, which as of this commit is a small <code class="language-plaintext highlighter-rouge">Copy</code> struct holding a node index, a pointer to the graph, a <code class="language-plaintext highlighter-rouge">ShapeTracker</code>, and a dtype (<a href="https://github.com/luminal-ai/luminal/blob/main/src/frontend/tensor.rs"><code class="language-plaintext highlighter-rouge">src/frontend/tensor.rs</code></a>). It holds no data. <code class="language-plaintext highlighter-rouge">a.matmul(b)</code> does no arithmetic; it appends nodes. This is lazy execution, but unlike libraries that bolt laziness onto an eager core, here <em>everything</em> is built this way, so the compiler always sees the entire network.</p> <p><strong>Data comes after the computation.</strong> You define the graph, compile it, and only then say what the inputs are. That inversion is deliberate: the shape of the computation is a compile-time fact, the contents of the tensors are a runtime one, and separating them lets you re-run with new data without rebuilding anything.</p> <p><strong><code class="language-plaintext highlighter-rouge">compile</code> returns the runtime.</strong> It is not a mutation of the graph. <code class="language-plaintext highlighter-rouge">Graph::compile</code> takes a runtime and options and gives you back a prepared runtime holding a selected program. The graph itself stays as the specification.</p> <h5 id="fifteen-operations"><strong>Fifteen operations</strong></h5> <p>Everything reduces to a small primitive set. The math primitives, as the README groups them:</p> <table> <thead> <tr> <th>Group</th> <th>Ops</th> </tr> </thead> <tbody> <tr> <td>Unary</td> <td><code class="language-plaintext highlighter-rouge">Log2</code>, <code class="language-plaintext highlighter-rouge">Exp2</code>, <code class="language-plaintext highlighter-rouge">Sin</code>, <code class="language-plaintext highlighter-rouge">Sqrt</code>, <code class="language-plaintext highlighter-rouge">Recip</code></td> </tr> <tr> <td>Binary</td> <td><code class="language-plaintext highlighter-rouge">Add</code>, <code class="language-plaintext highlighter-rouge">Mul</code>, <code class="language-plaintext highlighter-rouge">Mod</code>, <code class="language-plaintext highlighter-rouge">LessThan</code></td> </tr> <tr> <td>Other</td> <td><code class="language-plaintext highlighter-rouge">SumReduce</code>, <code class="language-plaintext highlighter-rouge">MaxReduce</code>, <code class="language-plaintext highlighter-rouge">Iota</code>, <code class="language-plaintext highlighter-rouge">Gather</code>, <code class="language-plaintext highlighter-rouge">Scatter</code>, <code class="language-plaintext highlighter-rouge">Cast</code></td> </tr> </tbody> </table> <p>Fifteen. You will also find a handful of structural entries alongside them in the <code class="language-plaintext highlighter-rouge">HLIROps</code> tuple in <a href="https://github.com/luminal-ai/luminal/blob/main/src/hlir.rs"><code class="language-plaintext highlighter-rouge">src/hlir.rs</code></a>: <code class="language-plaintext highlighter-rouge">Input</code>, <code class="language-plaintext highlighter-rouge">Output</code>, <code class="language-plaintext highlighter-rouge">Constant</code>, <code class="language-plaintext highlighter-rouge">CustomOpKind</code>, and the loop markers <code class="language-plaintext highlighter-rouge">LoopStart</code>, <code class="language-plaintext highlighter-rouge">LoopEnd</code>, <code class="language-plaintext highlighter-rouge">LoopInput</code>, and friends. Those are not arithmetic; they are how the graph marks its boundaries and its repeated structure. The arithmetic really is fifteen ops.</p> <p>Everything else is derived in the front end before the compiler ever sees it, and the derivations are exactly what you would write on paper:</p> <table> <thead> <tr> <th>You write</th> <th>The graph gets</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">a - b</code></td> <td><code class="language-plaintext highlighter-rouge">a + (-b)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">a / b</code></td> <td><code class="language-plaintext highlighter-rouge">a * recip(b)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">x.exp()</code></td> <td><code class="language-plaintext highlighter-rouge">exp2(x * (1 / ln 2))</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">x.log()</code></td> <td><code class="language-plaintext highlighter-rouge">log2(x) * ln 2</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">x.softmax(d)</code></td> <td>subtract the max, <code class="language-plaintext highlighter-rouge">exp</code>, sum, reciprocal, multiply</td> </tr> </tbody> </table> <p>That last row is worth noticing. Softmax is not an operation in Luminal. It is a shape of subgraph. So is layer norm, so is attention, so is a matmul.</p> <h5 id="a-matmul-is-not-an-operation"><strong>A matmul is not an operation</strong></h5> <p>This is the part that surprises people, so let’s do it concretely. <code class="language-plaintext highlighter-rouge">matmul</code> for the 2D case, from <a href="https://github.com/luminal-ai/luminal/blob/main/src/frontend/matmul.rs"><code class="language-plaintext highlighter-rouge">src/frontend/matmul.rs</code></a>, is essentially three lines:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">mul</span> <span class="o">=</span> <span class="k">self</span><span class="nf">.expand_dim</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">n</span><span class="p">)</span> <span class="o">*</span> <span class="n">rhs</span><span class="nf">.permute</span><span class="p">((</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span><span class="nf">.expand_dim</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">m</span><span class="p">);</span>
<span class="k">let</span> <span class="n">ret</span> <span class="o">=</span> <span class="n">mul</span><span class="nf">.sum</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span>
</code></pre></div></div> <p>Broadcast both operands to a common \((m, n, k)\) shape, multiply elementwise, sum over the last axis. That is the textbook definition of matrix multiplication, written out, and it is exactly what lands in the graph.</p> <p>For <code class="language-plaintext highlighter-rouge">(3, 1)</code> times <code class="language-plaintext highlighter-rouge">(1, 4)</code>, the resulting HLIR graph is <strong>five nodes</strong>: two <code class="language-plaintext highlighter-rouge">Input</code>s, one <code class="language-plaintext highlighter-rouge">Mul</code>, one <code class="language-plaintext highlighter-rouge">SumReduce</code>, one <code class="language-plaintext highlighter-rouge">Output</code>. The <code class="language-plaintext highlighter-rouge">expand_dim</code> and the <code class="language-plaintext highlighter-rouge">permute</code> contribute no nodes at all.</p> <p>That last fact deserves its own beat. <strong>Movement operations are free.</strong> A permute, a broadcast, a reshape, a slice: none of them add work, because none of them move data. They change the <code class="language-plaintext highlighter-rouge">ShapeTracker</code> attached to the <code class="language-plaintext highlighter-rouge">GraphTensor</code>, which records dimensions and strides. Reading a transposed tensor is reading the same buffer with the indices swapped. The compiler sees strides, not copies, and a later kernel simply indexes accordingly.</p> <p>So the graph the optimizer receives is maximally decomposed. There is no <code class="language-plaintext highlighter-rouge">MatMul</code> node to protect, no fused softmax to preserve. Every structure that a normal framework would hand-write as a kernel is, here, just a subgraph that the rewrite rules are free to recognize, or not, in whatever grouping turns out to be fastest. Flash Attention is not a special case in Luminal; it is a fusion the search is allowed to find.</p> <p><strong>References</strong></p> <ul> <li><a href="https://github.com/luminal-ai/luminal">Luminal source</a>, files as cited, as of commit <code class="language-plaintext highlighter-rouge">d18376d1</code>, 2026-09-10</li> <li><a href="https://docs.luminalai.com/docs/introduction">Luminal documentation</a></li> </ul> <hr/> <h4 id="e-graphs-from-first-principles"><strong>E-Graphs From First Principles</strong></h4> <p>This is the section that makes the rest of the post legible, so we’ll build the structure from nothing.</p> <h5 id="terms-and-the-problem-with-sets-of-terms"><strong>Terms, and the problem with sets of terms</strong></h5> <p>Start with an expression as a tree. \((a \times 2) / 2\) is a <code class="language-plaintext highlighter-rouge">Div</code> node whose children are a <code class="language-plaintext highlighter-rouge">Mul</code> node and the constant 2; the <code class="language-plaintext highlighter-rouge">Mul</code> node’s children are \(a\) and 2.</p> <p>Now suppose we want to keep every equivalent form of that expression. We know three: the original, the shifted version \((a \ll 1)/2\), and \(a\). Storing them as three separate trees works, but it does not scale. If an expression has ten independent subexpressions and each has three equivalent forms, there are \(3^{10}\), about 59,000, whole trees to store, and almost all of them share almost all of their structure. Store them separately and you drown.</p> <p>The e-graph is the fix, and it is fundamentally a compression scheme.</p> <h5 id="e-nodes-and-e-classes"><strong>E-nodes and e-classes</strong></h5> <p>Two definitions, and everything follows.</p> <p>An <strong>e-class</strong> is a set of things that are known to be equal to each other. It is an equivalence class.</p> <p>An <strong>e-node</strong> is an operator together with a list of children, where each child is an <strong>e-class</strong>, not another e-node. This is the whole trick. In an ordinary tree, <code class="language-plaintext highlighter-rouge">Mul</code>’s children are specific expressions. In an e-graph, <code class="language-plaintext highlighter-rouge">Mul</code>’s children are <em>sets</em> of expressions, and the e-node stands for the multiply of any member of the first set by any member of the second.</p> <p>An <strong>e-graph</strong> is a collection of e-classes, each holding one or more e-nodes, plus a designated root class.</p> <p>To read a program out of an e-graph, start at the root class, pick any one e-node from it, then recursively pick one e-node from each of that e-node’s child classes. Every set of choices yields a valid term, and every term the e-graph represents arises from some set of choices. That is what “it holds many programs at once” means, concretely: it is a choice per class, and the terms are the combinations.</p> <p>The counting follows immediately. If the root class has two e-nodes, and one of its children’s classes has three, and those choices are independent, you have six terms stored as five e-nodes. Add one e-node to one class and you can double the number of programs represented while growing the structure by one. That multiplicative behavior is why the compression works, and it is also, as we’ll see, why choosing well is hard.</p> <h5 id="congruence-the-property-that-makes-it-an-e-graph"><strong>Congruence, the property that makes it an e-graph</strong></h5> <p>If an e-graph were only “sets of equal things”, a union-find would do. The extra property, and the reason the <a href="https://arxiv.org/abs/2004.03082">egg paper</a> describes an e-graph as efficiently representing a <strong>congruence relation</strong>, is this:</p> \[a \equiv b \quad \Longrightarrow \quad f(a) \equiv f(b)\] <p>Equality has to propagate upward through operators. If you learn that \(x \times 2\) equals \(x \ll 1\), then anything built on top of \(x \times 2\) is equal to the same thing built on top of \(x \ll 1\), automatically, without any rule firing. Maintaining that closure as merges happen is the hard engineering, and it is exactly what the egg paper’s <strong>rebuilding</strong> technique makes fast: rather than restoring the invariant after every single merge, defer and restore it in batches, which the paper reports gives asymptotic speedups in practice.</p> <h5 id="doing-it-by-hand"><strong>Doing it by hand</strong></h5> <p>Let’s saturate our example. Rules: \(x \times 2 \rightarrow x \ll 1\) and \((x \times y)/y \rightarrow x\).</p> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/luminal-egraphs-search/egraph-rewrite.svg" sizes="95vw"/> <img src="/assets/img/luminal-egraphs-search/egraph-rewrite.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="An e-graph before and after two rewrites" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Dashed boxes are e-classes, solid boxes inside them are e-nodes, and arrows point from an e-node to the e-class of each child. Panel 1: the initial e-graph, one e-node per class, representing exactly one program. Panel 2: the shift rule fires and its result joins the class it was proved equal to, so that class now holds two e-nodes, and the multiply is still there. Panel 3: the cancellation rule fires on the root, merging the root class with the class holding a. Three programs, none of them lost. Note the red edges in panel 3: merging two classes can create a cycle, which is why extraction has to check for one. Editable source: <a href="/assets/img/luminal-egraphs-search/egraph-rewrite.excalidraw">egraph-rewrite.excalidraw</a>. </div> <p>Walking it in words:</p> <p><strong>Initial.</strong> Build the tree, one e-node per e-class. Class \(c_a\) holds the leaf \(a\). Class \(c_2\) holds the constant 2. Class \(c_m\) holds <code class="language-plaintext highlighter-rouge">Mul(</code>\(c_a\)<code class="language-plaintext highlighter-rouge">, </code>\(c_2\)<code class="language-plaintext highlighter-rouge">)</code>. The root class \(c_r\) holds <code class="language-plaintext highlighter-rouge">Div(</code>\(c_m\)<code class="language-plaintext highlighter-rouge">, </code>\(c_2\)<code class="language-plaintext highlighter-rouge">)</code>. Four classes, four e-nodes, one program represented.</p> <p><strong>Rule 1 fires.</strong> The left side \(x \times 2\) matches the e-node in \(c_m\), binding \(x\) to \(c_a\). The right side is \(x \ll 1\), so we add an e-node <code class="language-plaintext highlighter-rouge">Shl(</code>\(c_a\)<code class="language-plaintext highlighter-rouge">, </code>\(c_1\)<code class="language-plaintext highlighter-rouge">)</code> and, crucially, we <strong>union</strong> it with the class we matched, \(c_m\), rather than replacing anything. Now \(c_m\) holds two e-nodes. The multiply is still there. Two programs represented.</p> <p><strong>Rule 2 fires.</strong> The left side \((x \times y)/y\) matches the root: the <code class="language-plaintext highlighter-rouge">Div</code>’s first child class \(c_m\) contains a <code class="language-plaintext highlighter-rouge">Mul</code> whose second child class is \(c_2\), and the <code class="language-plaintext highlighter-rouge">Div</code>’s second child class is also \(c_2\). Binding \(x\) to \(c_a\), the right side is just \(x\), so we union the root class \(c_r\) with \(c_a\). Those two classes become one.</p> <p><strong>Saturation.</strong> Try every rule again. Nothing new is learned, so we are at a fixed point. Done.</p> <p>The final e-graph represents three programs: \((a \times 2)/2\), \((a \ll 1)/2\), and \(a\). No order was chosen. No information was destroyed. Both rules fired and neither blocked the other.</p> <p>That last sentence is the whole reason this machinery exists. Compare it to the destructive version, where firing rule 1 first permanently prevented rule 2.</p> <h5 id="saturation-is-a-fixed-point-sometimes-with-a-budget"><strong>Saturation is a fixed point, sometimes with a budget</strong></h5> <p>“Saturate” means run rules until no rule can add anything new. Sometimes that terminates quickly. Sometimes the rule set is such that it does not terminate at all; commutativity plus associativity can generate structure forever. Real systems therefore cap it, by iteration count, node count, or time, and accept a partially saturated e-graph. That is fine: a partial saturation still represents more programs than a destructive pipeline ever could, and the search downstream simply has fewer candidates.</p> <p>Luminal expresses its budget as an explicit <strong>schedule</strong>, which we’ll look at next.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2004.03082">egg: Fast and Extensible Equality Saturation, Willsey et al., POPL 2021</a></li> <li><a href="https://cseweb.ucsd.edu/~lerner/papers/popl09.html">Equality Saturation: a New Approach to Optimization, Tate et al., POPL 2009</a></li> </ul> <hr/> <h4 id="egglog-rules-instead-of-passes"><strong>egglog: Rules Instead of Passes</strong></h4> <p>Luminal does not implement e-graphs itself. It depends on <a href="https://github.com/egraphs-good/egglog">egglog</a>, pinned to a specific git revision in its <code class="language-plaintext highlighter-rouge">Cargo.toml</code>.</p> <h5 id="why-a-language-and-not-a-library"><strong>Why a language and not a library</strong></h5> <p>The egg library lets you write rewrite rules in Rust. egglog goes further: it is a language, and its contribution, per <a href="https://arxiv.org/abs/2304.04332">Zhang et al. (PLDI 2023)</a>, is unifying Datalog with equality saturation. From Datalog it takes efficient incremental execution, cooperating analyses, and lattice-based reasoning; from equality saturation it takes term rewriting, congruence closure, and extraction.</p> <p>The Datalog half matters more than it sounds. A pure rewrite system can only say “this term equals that term”. A compiler needs to say things like “the dtype of this node is f32”, “these list elements are all identical”, “this subgraph was already recognized as a rotary embedding”. Those are <strong>relations</strong>, and being able to derive them incrementally, have rules depend on them, and have them cooperate with the equality reasoning is what lets a real backend be expressed as rules at all.</p> <h5 id="a-real-rule-read-closely"><strong>A real rule, read closely</strong></h5> <p>Here is an actual Luminal rule, from <code class="language-plaintext highlighter-rouge">src/egglog_utils/matmul_flattening/squeeze.egg</code>, trimmed of comments. Its job is to strip a leading dimension of size 1 off a multiply-then-sum pair, so that downstream two-dimensional matmul rules, the ones that can hand the work to cuBLAS, become applicable.</p> <div class="language-lisp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nv">rule</span>
    <span class="p">(</span>
        <span class="p">(</span><span class="nb">=</span> <span class="nv">?mul</span> <span class="p">(</span><span class="nv">Op</span> <span class="p">(</span><span class="nv">Mul</span> <span class="nv">?mul_shape</span> <span class="nv">?a_stride</span> <span class="nv">?b_stride</span> <span class="nv">?mul_out_stride</span><span class="p">)</span>
                    <span class="p">(</span><span class="nv">ICons</span> <span class="nv">?a</span> <span class="p">(</span><span class="nv">ICons</span> <span class="nv">?b</span> <span class="p">(</span><span class="nv">INil</span><span class="p">)))))</span>
        <span class="p">(</span><span class="nb">=</span> <span class="nv">?sum</span> <span class="p">(</span><span class="nv">Op</span> <span class="p">(</span><span class="nv">Sum</span> <span class="nv">?sum_shape</span> <span class="nv">?k</span> <span class="nv">?sum_in_stride</span> <span class="nv">?k_stride</span> <span class="nv">?sum_out_stride</span><span class="p">)</span>
                    <span class="p">(</span><span class="nv">ICons</span> <span class="nv">?mul</span> <span class="p">(</span><span class="nv">INil</span><span class="p">))))</span>

        <span class="p">(</span><span class="nb">=</span> <span class="nv">?sum_shape</span> <span class="p">(</span><span class="nv">ECons</span> <span class="p">(</span><span class="nv">MNum</span> <span class="mi">1</span><span class="p">)</span> <span class="nv">?rest_sum_shape</span><span class="p">))</span>
        <span class="p">(</span><span class="nv">!=</span> <span class="nv">?rest_sum_shape</span> <span class="p">(</span><span class="nv">ENil</span><span class="p">))</span>
        <span class="p">(</span><span class="nb">=</span> <span class="nv">?rest_sum_len</span> <span class="p">(</span><span class="nv">len</span> <span class="nv">?rest_sum_shape</span><span class="p">))</span>
        <span class="p">(</span><span class="nb">&gt;=</span> <span class="nv">?rest_sum_len</span> <span class="mi">2</span><span class="p">)</span>

        <span class="p">(</span><span class="nb">=</span> <span class="nv">?mul_shape</span> <span class="p">(</span><span class="nv">ECons</span> <span class="p">(</span><span class="nv">MNum</span> <span class="mi">1</span><span class="p">)</span> <span class="nv">?rest_mul_shape</span><span class="p">))</span>
        <span class="p">(</span><span class="nb">=</span> <span class="nv">?a_stride</span> <span class="p">(</span><span class="nv">ECons</span> <span class="nv">?as0</span> <span class="nv">?rest_as</span><span class="p">))</span>
        <span class="o">...</span>
        <span class="p">(</span><span class="nb">=</span> <span class="nv">?dt</span> <span class="p">(</span><span class="nv">dtype</span> <span class="nv">?a</span><span class="p">))</span>
    <span class="p">)</span>
    <span class="p">(</span>
        <span class="p">(</span><span class="k">let</span> <span class="nv">?new_mul</span> <span class="p">(</span><span class="nv">Op</span> <span class="p">(</span><span class="nv">Mul</span> <span class="nv">?rest_mul_shape</span> <span class="nv">?rest_as</span> <span class="nv">?rest_bs</span> <span class="nv">?rest_mos</span><span class="p">)</span>
                          <span class="p">(</span><span class="nv">ICons</span> <span class="nv">?a</span> <span class="p">(</span><span class="nv">ICons</span> <span class="nv">?b</span> <span class="p">(</span><span class="nv">INil</span><span class="p">)))))</span>
        <span class="p">(</span><span class="k">let</span> <span class="nv">?new_sum</span> <span class="p">(</span><span class="nv">Op</span> <span class="p">(</span><span class="nv">Sum</span> <span class="nv">?rest_sum_shape</span> <span class="nv">?k</span> <span class="nv">?rest_sis</span> <span class="nv">?k_stride</span> <span class="nv">?rest_sos</span><span class="p">)</span>
                          <span class="p">(</span><span class="nv">ICons</span> <span class="nv">?new_mul</span> <span class="p">(</span><span class="nv">INil</span><span class="p">))))</span>
        <span class="p">(</span><span class="nb">union</span> <span class="nv">?sum</span> <span class="nv">?new_sum</span><span class="p">)</span>
        <span class="p">(</span><span class="nb">set</span> <span class="p">(</span><span class="nv">dtype</span> <span class="nv">?new_mul</span><span class="p">)</span> <span class="nv">?dt</span><span class="p">)</span>
        <span class="p">(</span><span class="nb">set</span> <span class="p">(</span><span class="nv">dtype</span> <span class="nv">?new_sum</span><span class="p">)</span> <span class="nv">?dt</span><span class="p">)</span>
    <span class="p">)</span>
    <span class="ss">:ruleset</span> <span class="nv">matmul_flatten</span>
    <span class="ss">:name</span> <span class="s">"batch-collapse squeeze dim=1"</span>
<span class="p">)</span>
</code></pre></div></div> <p>A rule has two halves. The first parenthesized block is the <strong>query</strong>: facts that must hold for the rule to fire. The second is the <strong>action</strong>: what to assert when it does.</p> <p>Reading the query top to bottom. Find a <code class="language-plaintext highlighter-rouge">Mul</code> and a <code class="language-plaintext highlighter-rouge">Sum</code> where the <code class="language-plaintext highlighter-rouge">Sum</code> consumes the <code class="language-plaintext highlighter-rouge">Mul</code>; <code class="language-plaintext highlighter-rouge">ICons</code> and <code class="language-plaintext highlighter-rouge">INil</code> are cons-list constructors for the input list, so <code class="language-plaintext highlighter-rouge">(ICons ?mul (INil))</code> means “a one-element input list containing the multiply”. Require that the <code class="language-plaintext highlighter-rouge">Sum</code>’s output shape begins with a literal 1, and that what remains is at least two dimensions, so we do not collapse below 2D. Require the same leading 1 on the <code class="language-plaintext highlighter-rouge">Mul</code>. Destructure each stride list to peel off its first element. Look up the dtype of <code class="language-plaintext highlighter-rouge">?a</code> from the <code class="language-plaintext highlighter-rouge">dtype</code> relation, which other rules populate.</p> <p>Then the action. Build a new <code class="language-plaintext highlighter-rouge">Mul</code> and a new <code class="language-plaintext highlighter-rouge">Sum</code> over the shortened shapes and strides. And then the line that is the entire point of this post:</p> <div class="language-lisp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nb">union</span> <span class="nv">?sum</span> <span class="nv">?new_sum</span><span class="p">)</span>
</code></pre></div></div> <p>Not “replace <code class="language-plaintext highlighter-rouge">?sum</code>”. Not “delete the old one”. <strong>Union.</strong> It asserts that the old summation and the new one are the same value, and drops both into one e-class. The four-dimensional spelling and the three-dimensional spelling now coexist, and downstream rules, including the ones that only match 2D shapes, can work from whichever they need. The comment in the file notes the rule fires recursively, 4D to 3D to 2D, and every intermediate spelling stays available.</p> <p>The last two lines propagate the dtype relation onto the new terms, so the analysis stays consistent as the e-graph grows. That is the Datalog half doing its job.</p> <h5 id="the-schedule"><strong>The schedule</strong></h5> <p>Rules are grouped into named <strong>rulesets</strong>, and Luminal specifies exactly how they run. The main schedule, from <code class="language-plaintext highlighter-rouge">src/egglog_utils/mod.rs</code>, is roughly:</p> <div class="language-lisp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nv">saturate</span> <span class="p">(</span><span class="nv">seq</span>
    <span class="p">(</span><span class="nv">saturate</span> <span class="p">(</span><span class="nv">seq</span>
        <span class="p">(</span><span class="nv">saturate</span> <span class="nv">expr</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">saturate</span> <span class="nv">dtype_prop</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">matmul_flatten</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">kernel_lower</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">direct_kernel</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">kernel_specialize</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">buffer_reuse</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">matmul_backend</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">glumoe</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">fusion_pair</span><span class="p">)</span>
    <span class="p">))</span>
    <span class="p">(</span><span class="nv">saturate</span> <span class="p">(</span><span class="nv">seq</span>
        <span class="p">(</span><span class="nv">saturate</span> <span class="nv">expr</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">saturate</span> <span class="nv">dtype_prop</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">fusion_grow</span><span class="p">)</span>
        <span class="p">(</span><span class="nv">run</span> <span class="nv">fusion_merge</span><span class="p">)</span>
    <span class="p">))</span>
<span class="p">))</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">(run r)</code> applies ruleset <code class="language-plaintext highlighter-rouge">r</code> once. <code class="language-plaintext highlighter-rouge">(saturate r)</code> applies it until it stops learning. <code class="language-plaintext highlighter-rouge">(seq ...)</code> runs its arguments in order. So the reading is: bring the algebraic rules and the dtype analysis to a fixed point, then apply the lowering and kernel-discovery rules once each, and repeat that whole block until it stabilizes; then do the same for the fusion growth rules; then repeat the pair.</p> <p>The ordering here is a performance decision, not a correctness one. The comment in the source explains it: producer rules create the raw alternatives that fusion later consumes, so saturating discovery before growing fusions avoids re-running expensive pair-discovery scans on every iteration. Because the rules are non-destructive, a different schedule would find a different amount, but it could not find something <em>wrong</em>.</p> <p>After the main cycles come cleanup phases. This is where the <code class="language-plaintext highlighter-rouge">Runtime::CLEANUP_HLIR</code> flag from <code class="language-plaintext highlighter-rouge">src/op.rs</code> comes in: for a real backend, the raw HLIR spellings are deleted from the e-graph after saturation, so only lowered, executable LLIR remains extractable. The reference runtime, whose LLIR <em>is</em> HLIR, sets the flag to false and keeps them. Backend lowering rules can also mark an HLIR term as <strong>subsumed</strong>, which tells the extractor to stop offering it as a choice. That is how a backend says “there is no such thing as an unlowered multiply in my output”.</p> <h5 id="the-contributor-rule-that-falls-out-of-all-this"><strong>The contributor rule that falls out of all this</strong></h5> <p>The repository’s <code class="language-plaintext highlighter-rouge">AGENTS.md</code> states a boundary that only makes sense once you have read the above: all graph pattern matching and op selection must be expressed in egglog rewrites, and no Rust-side passes may search for op patterns, fuse kernels, or rewrite extracted graphs after egglog has run. If a backend wants a fused op, it adds the match as a rule and lets extraction produce it.</p> <p>That is not stylistic. A Rust post-pass would be destructive by construction, and it would sit outside the search, so its decisions could never be measured against the alternatives. One such pass and the whole property is gone.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2304.04332">Better Together: Unifying Datalog and Equality Saturation, Zhang et al., PLDI 2023</a></li> <li><a href="https://github.com/egraphs-good/egglog">egglog on GitHub</a></li> <li>Luminal <code class="language-plaintext highlighter-rouge">src/egglog_utils/</code>, <code class="language-plaintext highlighter-rouge">src/op.rs</code>, <code class="language-plaintext highlighter-rouge">AGENTS.md</code>, as of commit <code class="language-plaintext highlighter-rouge">d18376d1</code>, 2026-09-10</li> </ul> <hr/> <h4 id="extraction-and-search"><strong>Extraction and Search</strong></h4> <p>Saturation gives you a space. It does not give you a program. Getting one out is called <strong>extraction</strong>, and it is where the difficulty moved to.</p> <h5 id="a-genome-is-one-choice-per-e-class"><strong>A genome is one choice per e-class</strong></h5> <p>Recall how you read a term out of an e-graph: pick one e-node from the root class, then one from each child class, recursively. So a complete selection is a function from e-classes to e-nodes, and Luminal represents exactly that. The type is <code class="language-plaintext highlighter-rouge">IndexedChoiceSet</code> in <code class="language-plaintext highlighter-rouge">src/egglog_utils/mod.rs</code>, and its documentation calls it a <strong>genome</strong>, which tells you where this is going.</p> <p>The size of the space follows directly. If \(C\) is the set of e-classes the extractor is allowed to choose in, the number of distinct selections is</p> \[\prod_{c \in C} |c|\] <table> <tbody> <tr> <td>where $$</td> <td>c</td> <td>\(is the number of e-nodes in class\)c\(. Luminal computes precisely this, capped so it cannot overflow, in `count_choice_sets_up_to`. A product over classes is a multiplicative space: it does not grow with the size of your model, it grows with the number of places where the compiler found more than one way to do something. Add one rule that offers an alternative in fifty places and you have multiplied the space by\)2^{50}$$.</td> </tr> </tbody> </table> <p>Not every combination is legal, though, and the reason is visible in panel 3 of the figure above. Merging two e-classes can produce a cycle, where a class contains an e-node that eventually depends on that same class. A term is finite, so a selection that follows a cycle does not name a program at all. Luminal’s approach, per the comment in <code class="language-plaintext highlighter-rouge">src/egglog_utils/mod.rs</code>, is to draw a complete random genome and then repair only the cycles in the reachable part of it, leaving every legal choice outside those cycles untouched so that specialization stays a measured decision rather than a repaired one.</p> <h5 id="choosing-optimally-is-np-hard"><strong>Choosing optimally is NP-hard</strong></h5> <p>You might hope to just assign a cost to each e-node and pick the cheapest tree bottom-up. That works if the cost function is additive and you ignore sharing. Real cost functions are not, because two branches of the program may share a subexpression and you should pay for it once, and that turns extraction into a global optimization problem.</p> <p>The complexity is settled. <a href="https://dl.acm.org/doi/10.1145/3689801">Fast and Optimal Extraction for Sparse Equality Graphs (OOPSLA 2024)</a> states that e-graph extraction is NP-hard in general and, further, hard to approximate within any constant ratio; existing tools therefore rely either on integer linear programming, which is optimal but slow, or on heuristics, which are fast but not optimal. The paper’s own contribution is a parameterized algorithm exploiting low treewidth. Tate et al. had already formulated extraction as an ILP back in 2009.</p> <p>So there is no clean way out. And Luminal’s situation is worse than the general one, because it does not even have a trustworthy cost function.</p> <h5 id="which-is-why-luminal-measures"><strong>Which is why Luminal measures</strong></h5> <p>On a GPU, the cost of a candidate depends on occupancy, register pressure, cache behavior, memory coalescing, launch overhead, and how the vendor’s library happens to be tuned for that exact shape on that exact architecture. Any static number you assign is a guess. Luminal’s answer is to stop guessing: <strong>compile the candidate and time it.</strong></p> <p>The mechanism is a genetic search, and <code class="language-plaintext highlighter-rouge">src/search/genetic.rs</code> implements it as a pull-style state machine. The runtime asks for a candidate, evaluates it however it likes, and reports back one of three outcomes: <code class="language-plaintext highlighter-rouge">Measured</code> with a metric, <code class="language-plaintext highlighter-rouge">Rejected</code> if it could not even be compiled or exceeded a resource cap, or <code class="language-plaintext highlighter-rouge">Invalid</code> if evaluation started but produced nothing usable. The state machine owns everything else: generations, mutation, deduplication of genomes and of programs, budgets, timeouts, and progress reporting.</p> <p>The loop, in outline. Draw random genomes to seed a population. Have the runtime measure each. Keep the best. Produce the next generation by mutating the survivors, where a mutation is simply changing which e-node is selected in some class. Measure, keep, repeat, until the budget runs out. The defaults live in <code class="language-plaintext highlighter-rouge">CompileOptions</code> in <code class="language-plaintext highlighter-rouge">src/graph.rs</code>:</p> <table> <thead> <tr> <th>Option</th> <th>Default</th> <th>What it controls</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">limit</code></td> <td>100</td> <td>total candidate programs evaluated</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">generation_size</code></td> <td>10</td> <td>offspring per generation</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">mutations</code></td> <td>10</td> <td>e-class choices flipped per offspring</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">trials</code></td> <td>3</td> <td>timing runs per candidate</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">keep_best</code></td> <td>1</td> <td>survivors kept as parents</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">candidate_timeout</code></td> <td>60 s</td> <td>budget covering compile plus run, per candidate</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">execution_timeout</code></td> <td>1 s</td> <td>cap on a single timing trial</td> </tr> </tbody> </table> <p>A hundred candidates is not a large number against a space that can be astronomically large, and that is the honest tradeoff. Genetic search does not find the optimum; it finds something good, quickly, using real measurements rather than a model that might be wrong in an unknown direction. If you want more, <code class="language-plaintext highlighter-rouge">limit</code> is a knob, and <code class="language-plaintext highlighter-rouge">restart_stagnation</code> escalates mutation counts and resamples fresh genomes when a run stops improving.</p> <p>Two details worth knowing. Candidates are deduplicated both by genome and by the resulting program, because different choices often extract to the same graph. And <code class="language-plaintext highlighter-rouge">early_stop_factor</code>, off by default, lets a runtime abandon timing a candidate once its running mean has already lost to the best by a given margin, which stops the search from spending its trial budget confirming that something is slow.</p> <h5 id="dimension-buckets-searching-more-than-once"><strong>Dimension buckets: searching more than once</strong></h5> <p>Real inference has dynamic shapes. Sequence length changes every request; batch size changes with load. Luminal models these as <strong>symbolic dimensions</strong>, so a shape can be <code class="language-plaintext highlighter-rouge">(s, 4096)</code> or <code class="language-plaintext highlighter-rouge">(b, h, w + 3)</code>, with the symbols carried through the graph rather than erased.</p> <p>Symbolic is good for correctness and bad for specialization: a kernel that must work for any \(s\) cannot bake in loop bounds. So Luminal lets you declare <strong>buckets</strong> over a dynamic dimension, and then does the whole pipeline once per bucket combination. <code class="language-plaintext highlighter-rouge">DimBucket</code> in <code class="language-plaintext highlighter-rouge">src/graph.rs</code> is an inclusive range with an optional representative value used during profiling, and <code class="language-plaintext highlighter-rouge">build_search_space</code> produces one saturated e-graph per combination of buckets. The search then selects one program per bucket, and at execution time the runtime dispatches on the actual value.</p> <p>Concretely, if you bucket sequence length into short, medium, and long, you get three e-graphs, three searches, and three specialized programs. Each was measured at a representative length inside its own range, so each is specialized to shapes it will actually see. Buckets must not overlap, and the code asserts it.</p> <p><strong>References</strong></p> <ul> <li><a href="https://dl.acm.org/doi/10.1145/3689801">Fast and Optimal Extraction for Sparse Equality Graphs, OOPSLA 2024</a> (<a href="https://cse.hkust.edu.hk/~parreaux/publication/oopsla24a/">open version</a>)</li> <li>Luminal <code class="language-plaintext highlighter-rouge">src/search/</code>, <code class="language-plaintext highlighter-rouge">src/graph.rs</code>, <code class="language-plaintext highlighter-rouge">src/egglog_utils/mod.rs</code>, as of commit <code class="language-plaintext highlighter-rouge">d18376d1</code>, 2026-09-10</li> </ul> <hr/> <h4 id="when-things-compile"><strong>When Things Compile</strong></h4> <p>The word “compile” does four different jobs in a Luminal discussion, and untangling them answers both the ahead-of-time versus just-in-time question and the README line about shape-specific kernels.</p> <h5 id="four-things-called-compilation"><strong>Four things called compilation</strong></h5> <table> <thead> <tr> <th>Sense</th> <th>What happens</th> <th>When</th> </tr> </thead> <tbody> <tr> <td>Building the crate</td> <td><code class="language-plaintext highlighter-rouge">cargo</code> turns Rust into a binary</td> <td>before you ship</td> </tr> <tr> <td>Building the graph</td> <td>your <code class="language-plaintext highlighter-rouge">matmul</code> calls append HLIR nodes</td> <td>when your program runs, before <code class="language-plaintext highlighter-rouge">compile</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Graph::compile</code></td> <td>saturate, search, select, load</td> <td>once, at process startup</td> </tr> <tr> <td>Device compilation</td> <td>generated kernel source becomes machine code</td> <td>inside <code class="language-plaintext highlighter-rouge">Graph::compile</code></td> </tr> </tbody> </table> <p>The last row is the one people mean by JIT, and it is real. The Metal backend hands shader source to <code class="language-plaintext highlighter-rouge">new_library_with_source</code> at runtime (<code class="language-plaintext highlighter-rouge">crates/luminal_metal/src/kernel/ops.rs</code>). The CUDA backend hands C source to NVRTC, with architecture flags chosen from the detected compute capability (<code class="language-plaintext highlighter-rouge">crates/luminal_cuda_lite/src/lib.rs</code>). Neither kernel exists as text until the compiler decides what it wants.</p> <h5 id="so-which-is-it"><strong>So which is it?</strong></h5> <p>Luminal is <strong>ahead-of-time in philosophy and just-in-time in mechanism</strong>, and the two are not in tension because they refer to different clocks.</p> <p>Ahead-of-time here means: relative to your model’s execution, everything is decided in advance. The full network is a static graph, so devices, dtypes, memory allocation, fusion, and kernel selection are all compile-time facts. There is no interpreter in the loop, no per-op dispatch, no Python at inference. The README states the policy directly: “push everything to compile time and leave nothing to run time.”</p> <p>Just-in-time here means: relative to the operating system process, the machine code is produced while the process is running. It has to be, because the compiler cannot know until search time which kernels it wants, and it cannot know until it sees your dimension buckets what shapes to specialize them for.</p> <p>Contrast that with <code class="language-plaintext highlighter-rouge">torch.compile</code>, which is just-in-time in a stronger sense: <a href="https://dl.acm.org/doi/10.1145/3620665.3640366">TorchDynamo</a> hooks Python bytecode as it executes, captures what it can into an FX graph, falls back to the interpreter when it cannot, and recompiles when guards fail. Luminal has no interpreter to fall back to and no guards to fail. The graph is complete or it does not exist.</p> <h5 id="shape-specific-kernels-compiled-at-runtime-decoded"><strong>“Shape-specific kernels compiled at runtime”, decoded</strong></h5> <p>Now that line reads cleanly. A dynamic dimension is symbolic in the graph. You declare buckets over it. Within a bucket the dimension is pinned to a representative, so the emitted kernel can hardcode loop trip counts, tile sizes, and unroll factors for that shape rather than reading them from a parameter and branching. That specialization is only possible once the bucket values are known, which is at process runtime, and the resulting source is then compiled by NVRTC or the Metal compiler on the spot.</p> <p>The payoff is that a generically written network gets hyper-specific machine code, and you did not write the specialization.</p> <h5 id="artifacts-paying-for-the-search-once"><strong>Artifacts: paying for the search once</strong></h5> <p>Searching a hundred candidates, each of which must be compiled and timed, is not free. If you had to do it on every process start, the AOT story would be a lie.</p> <p>You do not. <code class="language-plaintext highlighter-rouge">src/graph/artifact.rs</code> serializes the selected schedule: which program was chosen for which bucket, fingerprinted so it can be validated against the e-graph it came from. The CUDA backend goes further in <code class="language-plaintext highlighter-rouge">crates/luminal_cuda_lite/src/artifact.rs</code>, capturing the compiled module images themselves, keyed by a signature covering the target architecture, the NVRTC options, and the NVRTC version, so a mismatched toolchain cannot silently load stale binaries.</p> <p>The intended shape of a deployment, then: search once on a machine like the target, save the artifact, ship it, and load it. Startup becomes a deserialize and a module load rather than a search.</p> <p><strong>References</strong></p> <ul> <li><a href="https://dl.acm.org/doi/10.1145/3620665.3640366">PyTorch 2: Faster Machine Learning Through Dynamic Python Bytecode Transformation and Graph Compilation, ASPLOS 2024</a> (<a href="https://docs.pytorch.org/assets/pytorch2-2.pdf">PDF</a>)</li> <li>Luminal <code class="language-plaintext highlighter-rouge">src/graph/artifact.rs</code>, <code class="language-plaintext highlighter-rouge">crates/luminal_cuda_lite/</code>, <code class="language-plaintext highlighter-rouge">crates/luminal_metal/</code>, as of commit <code class="language-plaintext highlighter-rouge">d18376d1</code>, 2026-09-10</li> </ul> <hr/> <h4 id="the-core-and-runtime-boundary"><strong>The Core and Runtime Boundary</strong></h4> <p>One structural fact explains how the codebase is laid out, and it is stated plainly in <code class="language-plaintext highlighter-rouge">AGENTS.md</code>: <strong>the core’s compile pipeline ends at egglog saturation.</strong></p> <p><code class="language-plaintext highlighter-rouge">Graph::build_search_space</code> produces a <code class="language-plaintext highlighter-rouge">SearchSpace</code>, which is one saturated e-graph per bucket combination plus the registered ops. <code class="language-plaintext highlighter-rouge">Runtime::compile</code> owns everything after that: search strategy, profiling, and loading. The core never runs a search itself. The genetic machinery in <code class="language-plaintext highlighter-rouge">luminal::search</code> is offered as utilities that a runtime may use, compose, or ignore.</p> <p>The three runtimes in the tree take three different options, which is a good demonstration that the boundary is real:</p> <table> <thead> <tr> <th>Runtime</th> <th>Strategy</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">ReferenceRuntime</code></td> <td><code class="language-plaintext highlighter-rouge">extract_one_selected</code>, no search</td> <td>It is the semantic reference. Its LLIR is HLIR, executed directly, so there is nothing to rank. Used to validate correctness.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MetalRuntime</code></td> <td>the stock <code class="language-plaintext highlighter-rouge">genetic_search</code> utility</td> <td>Nothing special to do between steps, so it takes the batteries-included path.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">CudaRuntime</code></td> <td>drives the state machine explicitly</td> <td>It has its own concerns, graph capture, resource caps, module caching, so it steps the search by hand in <code class="language-plaintext highlighter-rouge">crates/luminal_cuda_lite/src/search.rs</code>.</td> </tr> </tbody> </table> <p>A new backend therefore needs to supply four things: op definitions for the ops it can execute, egglog rewrite rules that lower HLIR into those ops, an implementation of <code class="language-plaintext highlighter-rouge">Runtime::compile</code> that picks a program from the space, and code that loads and executes the selected LLIR. It does not need to reimplement the graph, the shape tracker, the primitives, or the search.</p> <p>That last point is the real argument for the tiny operator set. Fifteen primitives is not minimalism for its own sake; it is roughly the number of things a new backend has to be able to express before it works at all. The <a href="https://docs.luminalai.com/docs/why">Luminal docs</a> make the same case for dtypes, devices, and even autograd: keep the core small and add capability back through rules and compilers, in composable pieces.</p> <p><strong>References</strong></p> <ul> <li>Luminal <code class="language-plaintext highlighter-rouge">AGENTS.md</code>, <code class="language-plaintext highlighter-rouge">src/op.rs</code>, <code class="language-plaintext highlighter-rouge">src/search/mod.rs</code>, <code class="language-plaintext highlighter-rouge">crates/luminal_cuda_lite/src/search.rs</code>, as of commit <code class="language-plaintext highlighter-rouge">d18376d1</code>, 2026-09-10</li> <li><a href="https://docs.luminalai.com/docs/why">Why Luminal</a></li> </ul> <hr/> <h4 id="where-this-sits"><strong>Where This Sits</strong></h4> <p>Every stack below faces the same problem, which is that a high-level graph has to become machine code and there are many ways to do it. They differ on how the rewrites are written, whether writing one destroys the alternative, who picks the final program, and when the kernel text becomes machine code.</p> <table> <thead> <tr> <th> </th> <th>Rewrites expressed as</th> <th>Destructive?</th> <th>Who picks the final program</th> <th>Kernel machine code produced</th> </tr> </thead> <tbody> <tr> <td>torch.compile / Inductor</td> <td>Python passes over an FX graph, plus a registered pattern matcher</td> <td>yes</td> <td>the pass order, fixed by the developer</td> <td>at runtime; Inductor emits Triton for GPU and C++ for CPU</td> </tr> <tr> <td>XLA</td> <td>HLO passes, target-independent then target-dependent</td> <td>yes</td> <td>the pass pipeline, plus library pattern matching in the backend</td> <td>at compile time via LLVM, from emitted LLVM IR</td> </tr> <tr> <td>TVM with Ansor</td> <td>graph-level passes, plus schedules sampled from a hierarchical space</td> <td>yes at graph level; the search is over schedules, not graphs</td> <td>evolutionary search guided by a learned cost model</td> <td>at tuning time, per operator</td> </tr> <tr> <td>tinygrad</td> <td>kernel optimizations applied to a lowered AST</td> <td>yes, per candidate</td> <td>BEAM search over kernel implementations, timed on device, cached</td> <td>at runtime</td> </tr> <tr> <td>Luminal</td> <td>egglog rules that union rather than replace</td> <td><strong>no</strong></td> <td>genetic search over e-graph extractions, timed on device</td> <td>at runtime via NVRTC or the Metal compiler</td> </tr> </tbody> </table> <p>Three observations.</p> <p><strong>Luminal is the only row whose rewrites are non-destructive.</strong> That is the actual novelty. Everything else in the table has to be careful about rule ordering because a rewrite forecloses alternatives; Luminal does not, which is why its rule authors can be aggressive and why <code class="language-plaintext highlighter-rouge">AGENTS.md</code> can forbid Rust post-passes outright.</p> <p><strong>Search is not novel; searching the graph is.</strong> TVM’s <a href="https://arxiv.org/abs/2006.06762">Ansor</a> uses evolutionary search with a learned cost model, and tinygrad’s <a href="https://docs.tinygrad.org/mnist/">BEAM search</a> tries many implementations and keeps what is fastest on your hardware, caching the result. Both search <strong>within</strong> an operator, over schedules or kernel parameters, after the graph structure has already been fixed by destructive passes. Luminal’s search space is the graph structure itself, including which operators exist at all. That is why fusions like Flash Attention are reachable rather than hand-written.</p> <p><strong>Cost model versus stopwatch.</strong> Ansor learns a cost model because tuning every candidate on hardware is expensive; the model is an approximation that buys throughput. tinygrad and Luminal both measure directly and cache or serialize the answer. Luminal’s <code class="language-plaintext highlighter-rouge">trials</code> default of 3 and its per-candidate timeout are the visible price of that choice.</p> <p><strong>References</strong></p> <ul> <li><a href="https://dl.acm.org/doi/10.1145/3620665.3640366">PyTorch 2, ASPLOS 2024</a></li> <li><a href="https://openxla.org/xla/architecture">XLA architecture</a></li> <li><a href="https://arxiv.org/abs/2006.06762">Ansor: Generating High-Performance Tensor Programs for Deep Learning, OSDI 2020</a></li> <li><a href="https://docs.tinygrad.org/mnist/">tinygrad documentation</a></li> </ul> <hr/> <h4 id="running-it-on-a-mac"><strong>Running It on a Mac</strong></h4> <p>Everything below was run on an Apple silicon Mac on 2026-09-10.</p> <p>Install Rust if you have not:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">--proto</span> <span class="s1">'=https'</span> <span class="nt">--tlsv1</span>.2 <span class="nt">-sSf</span> https://sh.rustup.rs | sh
</code></pre></div></div> <p>Then open a new shell, or source the environment file in your current one:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">.</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.cargo/env"</span>
</code></pre></div></div> <p>Clone and run the hello world, which uses <code class="language-plaintext highlighter-rouge">ReferenceRuntime</code> and is portable CPU code with no GPU involved:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/luminal-ai/luminal <span class="o">&amp;&amp;</span> <span class="nb">cd </span>luminal <span class="o">&amp;&amp;</span> cargo run <span class="nt">--release</span> <span class="nt">-p</span> simple
</code></pre></div></div> <p>Two things to expect. The first build is slow, because egglog is pulled from a pinned git revision and compiled from source. And <code class="language-plaintext highlighter-rouge">examples/simple/src/main.rs</code> calls <code class="language-plaintext highlighter-rouge">display_graph</code>, which encodes the graph into a URL and opens a browser tab at the Luminal visualizer; delete that line if you would rather it did not.</p> <p>For an actual model on Apple silicon, Metal is wired up for the Qwen example:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cargo run <span class="nt">--release</span> <span class="nt">-p</span> qwen <span class="nt">--features</span> metal
</code></pre></div></div> <p>The first run downloads Qwen3-4B, converts the safetensors weights, compiles the Metal graph, and generates text. Every other example in the tree, llama, gemma, paged_llama, whisper, yolo, and flux among them, is CUDA-only as of this commit.</p> <p>If you want to watch the compiler work, the environment flags are documented in the README. <code class="language-plaintext highlighter-rouge">SEARCH_LOG=1</code> prints per-bucket progress and best-so-far metrics, <code class="language-plaintext highlighter-rouge">EGGLOG_LOG=1</code> prints e-graph build and schedule diagnostics, and <code class="language-plaintext highlighter-rouge">LUMINAL_LOG=1</code> turns on everything.</p> <hr/> <h4 id="takeaways"><strong>Takeaways</strong></h4> <ul> <li><strong>A destructive rewrite forecloses alternatives.</strong> Applying \(x \times 2 \rightarrow x \ll 1\) makes \((x \times y)/y \rightarrow x\) unable to fire. That is the phase ordering problem, and it is why production rule sets grow guards until they are unmaintainable.</li> <li><strong>Equality saturation records equalities instead of applying rewrites.</strong> Nothing is deleted, so no order is committed to, and the saturated IR encodes many optimized versions at once.</li> <li><strong>An e-graph is the data structure that makes this affordable.</strong> E-nodes have e-classes as children, so one structure represents a multiplicative number of programs, and congruence propagates equality upward for free.</li> <li><strong>Luminal writes every rewrite as an egglog rule ending in <code class="language-plaintext highlighter-rouge">union</code></strong>, and forbids Rust-side graph passes, because such a pass would be destructive and would sit outside the search.</li> <li> <table> <tbody> <tr> <td><strong>Choosing a program out of the e-graph is the hard part.</strong> A selection is one e-node per e-class, the space is $$\prod_{c}</td> <td>c</td> <td>$$, and optimal extraction is NP-hard and hard to approximate within any constant.</td> </tr> </tbody> </table> </li> <li><strong>Luminal picks by measuring, not modelling.</strong> A genetic search hands candidates to the runtime, which compiles and times them on the real device. Defaults are 100 candidates, generations of 10, 3 trials each.</li> <li><strong>The primitive set is small because backends have to implement it.</strong> Fifteen math ops, with subtraction, division, exp, log, softmax, and matmul all derived in the front end. Movement ops are free because they live in the shape tracker.</li> <li><strong>AOT and JIT are both true, on different clocks.</strong> Everything about the model is decided before it runs; the kernel machine code is produced while the process runs, by NVRTC or the Metal compiler, specialized to a dimension bucket.</li> <li><strong>Artifacts make the search a one-time cost.</strong> Serialize the selected schedule and the compiled modules, ship them, load them.</li> </ul> <hr/> <h4 id="test-yourself"><strong>Test Yourself</strong></h4> <p>Try each from memory before reading the answer.</p> <p><strong>1. Give a concrete pair of rewrite rules that interfere, and say what goes wrong.</strong> \(x \times 2 \rightarrow x \ll 1\) and \((x \times y)/y \rightarrow x\), on \((a \times 2)/2\). Fire the first and the multiply is gone, so the second cannot match and you are stuck with \((a \ll 1)/2\) instead of \(a\). The correct order depends on the expression, which is the phase ordering problem.</p> <p><strong>2. What is an e-node, precisely, and how does it differ from a tree node?</strong> An operator together with a list of children, where each child is an e-class rather than a specific term. A tree node points at one subexpression; an e-node points at a set of equivalent subexpressions and stands for the operation applied to any member.</p> <p><strong>3. How do you read a single program out of an e-graph?</strong> Pick one e-node from the root e-class, then recursively pick one e-node from each of its children’s e-classes. Every complete set of choices yields a valid term.</p> <p><strong>4. What does congruence mean here, and why does a plain union-find not suffice?</strong> Congruence is that \(a \equiv b\) implies \(f(a) \equiv f(b)\), so equality propagates upward through operators. A union-find tracks equality of leaves but does not close it under application; maintaining that closure as merges happen is the extra work, and egg’s rebuilding technique is what makes it fast.</p> <p><strong>5. In the squeeze rule, what does <code class="language-plaintext highlighter-rouge">(union ?sum ?new_sum)</code> do, and what would a normal compiler have written there instead?</strong> It asserts the two summations are equal and merges their e-classes, keeping both spellings available. A normal compiler would replace the old node with the new one, deleting the 4D form permanently.</p> <p><strong>6. What is the difference between <code class="language-plaintext highlighter-rouge">(run r)</code> and <code class="language-plaintext highlighter-rouge">(saturate r)</code> in a schedule, and why does Luminal use both?</strong> <code class="language-plaintext highlighter-rouge">(run r)</code> applies a ruleset once; <code class="language-plaintext highlighter-rouge">(saturate r)</code> applies it to a fixed point. Algebraic rules and the dtype analysis are saturated because they are cheap and need to be complete; the expensive lowering and fusion-discovery rules are run once per outer iteration to avoid re-running large joins.</p> <p><strong>7. What is a genome in Luminal’s search, and how large is the space it lives in?</strong> A choice of one e-node per searchable e-class, stored as <code class="language-plaintext highlighter-rouge">IndexedChoiceSet</code>. The space has \(\prod_{c} |c|\) members, the product over classes of the number of e-nodes in each.</p> <p><strong>8. Why does Luminal not just assign costs to e-nodes and extract the cheapest tree?</strong> Because a good cost function has to account for shared subexpressions, which makes extraction a global optimization problem that is NP-hard and hard to approximate within any constant; and because on a GPU no static cost estimate is trustworthy anyway. So it compiles candidates and times them.</p> <p><strong>9. What are the three outcomes a runtime can report for a candidate, and how do they differ?</strong> <code class="language-plaintext highlighter-rouge">Measured</code> with a metric, which is ranked and counts against the budget. <code class="language-plaintext highlighter-rouge">Rejected</code>, meaning it was not viable before evaluation, for example it failed to compile or blew a resource cap, and does not count against the budget. <code class="language-plaintext highlighter-rouge">Invalid</code>, meaning evaluation started but produced no usable metric, which counts against the budget but is never ranked.</p> <p><strong>10. What is a dimension bucket, and what does declaring one actually cause to happen?</strong> An inclusive range over a symbolic dimension with a representative value. Declaring buckets causes <code class="language-plaintext highlighter-rouge">build_search_space</code> to produce one saturated e-graph per bucket combination, the search to select one program per bucket, and the runtime to dispatch on the real value at execution time.</p> <p><strong>11. Explain “shape-specific kernels compiled at runtime” in one sentence each for “shape-specific” and “at runtime”.</strong> Shape-specific: within a bucket the dynamic dimension is pinned to a representative, so trip counts, tiles, and unroll factors can be baked into the emitted source rather than branched on. At runtime: that source only exists once the compiler has searched and the bucket values are known, so NVRTC or the Metal compiler turns it into machine code inside the running process.</p> <p><strong>12. Is Luminal AOT or JIT? Defend the answer.</strong> Both, on different clocks. AOT relative to the model: the entire network is a static graph, so devices, dtypes, allocation, fusion, and kernel choice are settled before a single number moves, and there is no interpreter at inference. JIT relative to the process: kernel machine code is generated while the process runs. Unlike <code class="language-plaintext highlighter-rouge">torch.compile</code>, there is no eager fallback and no guard-triggered recompile.</p> <p><strong>13. Why does a matmul produce no <code class="language-plaintext highlighter-rouge">MatMul</code> node, and why is that good for the search?</strong> The front end expands it into a broadcast multiply plus a sum reduce, with the broadcast and permute absorbed into the shape tracker. Because there is no matmul node to protect, the rewrite rules are free to regroup that arithmetic into whatever kernel is fastest, including fusing it with its neighbours, rather than being forced to preserve a hand-drawn boundary.</p> <p><strong>14. Why does <code class="language-plaintext highlighter-rouge">AGENTS.md</code> forbid Rust passes that rewrite the extracted graph?</strong> Such a pass would necessarily be destructive, and it would run after the search, so its decisions could never be compared against the alternatives it destroyed. One of them and the non-destructive property of the whole pipeline is gone.</p> <p><strong>15. What does <code class="language-plaintext highlighter-rouge">ReferenceRuntime</code> search, and why?</strong> Nothing. It extracts one program and runs it. Its LLIR is HLIR executed directly, so it is a semantic reference for validating that everything the search selects computes the same answer, and there is nothing to rank.</p> <hr/> <h4 id="wrapping-up"><strong>Wrapping up</strong></h4> <p>If one thing survives from this post, make it the shape of the trade. A conventional compiler spends its intelligence deciding <em>whether</em> to apply a rewrite, and it has to be right, because applying one destroys the alternative. Luminal spends nothing there. It applies every rewrite it knows, keeps all of them, and moves the entire difficulty to the end, where the question is no longer “will this help?” but “which of these is fastest?”, and that question can be answered with a stopwatch instead of a heuristic.</p> <p>Everything else is downstream. The operator set is small because backends must implement it and rules must match against it. Movement ops are free because a rewrite should be able to regroup arithmetic without paying for copies. Shapes are symbolic so the compiler keeps visibility, and bucketed so it can still specialize. The core stops at saturation because choosing is the backend’s business. Artifacts exist because measuring a hundred candidates is only acceptable if you do it once.</p> <p>Whether it wins is a separate question from whether it is well-designed, and it is a question about numbers I have not measured. What I can say is that the design is unusually coherent: pull on any thread and you end up back at the same sentence, which is that a rewrite should add information rather than remove it.</p> <p>If you find a mistake anywhere in here, please let me know and I’ll fix it.</p>]]></content><author><name></name></author><category term="GPU"/><category term="ML"/><summary type="html"><![CDATA[A first-principles walk through Luminal, the Rust inference compiler that replaces destructive rewrite passes with equality saturation, and picks its final program by measuring candidates on real hardware]]></summary></entry><entry><title type="html">Large-Scale LLM Training Systems - How and Why</title><link href="https://monishver11.github.io/blog/2026/llm-training-systems/" rel="alternate" type="text/html" title="Large-Scale LLM Training Systems - How and Why"/><published>2026-09-02T18:00:00+00:00</published><updated>2026-09-02T18:00:00+00:00</updated><id>https://monishver11.github.io/blog/2026/llm-training-systems</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/llm-training-systems/"><![CDATA[<p>Hello all. This post is the training-side companion to the <a href="/blog/2026/llm-inference-systems/">LLM inference systems post</a>: how the systems that train large language models across thousands of GPUs are put together, and why they are shaped the way they are.</p> <p>The inference post had one organizing fact, that generating a token is cheap on arithmetic and expensive on memory traffic, and everything in a serving engine followed from it. Training has its own organizing fact, and it is a different one. A training step does three times the arithmetic of a forward pass, holds an optimizer’s worth of state for every parameter, and keeps every intermediate activation alive until the backward pass consumes it. So training is not bandwidth-bound the way decode is; it is <strong>capacity-bound first and communication-bound second</strong>. The model’s state does not fit on one GPU, so it has to be split, and once it is split, every step has to move bytes between GPUs to stitch it back together. Nearly every technique below, mixed precision, activation recomputation, ZeRO, tensor and pipeline parallelism, is an answer to one of two questions: how do we make the state smaller, and how do we hide the communication that splitting it creates.</p> <p>The angle is general rather than framework-specific. Ideas are anchored in the papers and official documents that introduced them, Megatron-LM, ZeRO, GPipe, PipeDream, the FSDP and DDP papers, the FP8 and mixed precision papers, and the Llama 3 report, which is the running real-world example because it publishes its actual parallelism configuration, its measured utilization, and its failure statistics. Every number quoted here was checked against a linked source, and each major section ends with its references. Arithmetic derived from those numbers is worked out in the open so you can check it.</p> <p>The plan:</p> <ul> <li>Why training is a different shape from inference, and where the \(6N\) comes from</li> <li>The numbers that drive everything: the hardware, the bytes per parameter, and activation memory</li> <li>Mixed precision: fp16, bf16, loss scaling, FP8</li> <li>Activation recomputation, and why it is a throughput optimization</li> <li>Communication primitives: the collectives, the ring algorithm, and the cost model</li> <li>Data parallelism: DDP, gradient accumulation, ZeRO, FSDP, and offloading</li> <li>Tensor parallelism: column-then-row splits and what the all-reduces cost</li> <li>Sequence, context, and expert parallelism</li> <li>Pipeline parallelism: microbatches, the bubble, and the schedules</li> <li>Composing them: 3D and 4D parallelism, and how a real run is laid out</li> <li>MFU and HFU: the utilization numbers and how to compute them in both directions</li> <li>What breaks at scale</li> <li>Takeaways</li> <li>Test yourself: a question bank over everything above</li> </ul> <p>I’m assuming you’re comfortable with what a transformer is and with backpropagation at the level of the <a href="/blog/2026/mlp-forward-backward/">MLP forward and backward post</a>. This is a long one, but the sections build in order, and the question bank at the end is there to check what stuck.</p> <p>Let’s get started.</p> <hr/> <h4 id="why-training-is-a-different-shape"><strong>Why Training Is a Different Shape</strong></h4> <p>Start with a side-by-side, because the two workloads run the same model and could hardly be more different:</p> <table> <thead> <tr> <th> </th> <th>Inference (decode)</th> <th>Training</th> </tr> </thead> <tbody> <tr> <td>FLOPs per token</td> <td>\(\approx 2N\)</td> <td>\(\approx 6N\)</td> </tr> <tr> <td>What fills memory</td> <td>weights and the KV cache</td> <td>weights, gradients, optimizer state, and saved activations</td> </tr> <tr> <td>First bottleneck</td> <td>HBM bandwidth (the weight read per step)</td> <td>HBM capacity (the state does not fit)</td> </tr> <tr> <td>Second bottleneck</td> <td>latency of the small collectives under TP</td> <td>interconnect bandwidth for the sharded state</td> </tr> <tr> <td>Batch</td> <td>as large as the latency budget allows</td> <td>as large as memory allows, then gradient accumulation</td> </tr> <tr> <td>Arithmetic intensity</td> <td>roughly the batch size, low</td> <td>high; big batched GEMMs in both directions</td> </tr> <tr> <td>Utilization metric</td> <td>fraction of bandwidth, tokens/s per replica</td> <td>MFU, fraction of peak FLOP/s</td> </tr> </tbody> </table> <p>Two rows carry the rest of the post: the FLOPs row and the memory row.</p> <h5 id="where-the-6n-comes-from"><strong>Where the \(6N\) comes from</strong></h5> <p>\(N\) is the parameter count excluding the embedding table. A forward pass costs about \(2N\) FLOPs per token, because a token passing through a linear layer with \(k \times n\) weights is a \(1 \times k\) row times a \(k \times n\) matrix, which is \(2kn\) FLOPs, two per parameter, one multiply and one add. Summed over every weight matrix in the model that is \(2N\). This is <a href="https://arxiv.org/abs/2001.08361">Kaplan et al., Eq. 2.2</a>, which also carries an attention term, \(2 \, n_{layer} \, n_{ctx} \, d_{model}\), that grows with context and stays small until contexts get long. The <a href="/blog/2026/transformer-block-accounting/">transformer block accounting post</a> has the op-by-op ledger.</p> <p>The backward pass costs about twice the forward. Kaplan states it as an approximation, and the reason is worth having exactly, because it is the answer to “why 6 and not 4”. Take one linear layer \(u = A z\) with \(A\) of shape \(m \times n\), and let \(g = \partial L / \partial u\) be the gradient arriving from downstream. The backward pass has to produce two things:</p> \[\frac{\partial L}{\partial A} = g \, z^\top \qquad (m \times n), \qquad\qquad \frac{\partial L}{\partial z} = A^\top g \qquad (n \times 1)\] <p>The first is the weight gradient, an outer product of the incoming gradient with the saved input. The second is the input gradient, the transposed weight times the incoming gradient, which is what gets handed to the layer below. (Both are derived as equations (11) and (12) of the <a href="/blog/2026/mlp-forward-backward/">MLP post</a>.) Over a batch of tokens each of these is a matrix multiply with exactly the same \(m\), \(n\), \(k\) dimensions as the forward multiply, so each costs the same \(2\) FLOPs per parameter per token that the forward did. Two of them make \(4N\), and \(2N + 4N = 6N\).</p> <p>So the shape of a training step is three matmuls per weight matrix where inference has one, and one of the three, the weight gradient, has no counterpart in inference at all. Keep that outer product in mind; it is the reason the backward pass needs the forward pass’s inputs, which is the reason activations have to be saved, which is where most of the memory goes.</p> <h5 id="the-organizing-idea"><strong>The organizing idea</strong></h5> <p>Inference is a bandwidth problem: decode reads every weight once per token and does almost nothing with it. Training is a <strong>memory and communication</strong> problem: the state per parameter is many times the weight itself, the activations scale with the batch, none of it fits on one device, and every way of splitting it across devices creates traffic. The rest of this post is that sentence, unpacked.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2001.08361">Scaling Laws for Neural Language Models - Kaplan et al.</a></li> <li><a href="/blog/2026/mlp-forward-backward/">A Simple MLP - Forward Pass, Backward Pass, and Every Derivative</a> (the outer-product and transposed-matmul shapes)</li> <li><a href="/blog/2026/transformer-block-accounting/">Transformer Block FLOPs and Parameters Calculations</a></li> </ul> <hr/> <h4 id="the-numbers-that-drive-everything"><strong>The Numbers That Drive Everything</strong></h4> <p>Before any mechanism, the numbers. Three of them: the hardware and its bandwidth ladder, the bytes a training run holds per parameter, and the bytes it holds per token of activation. Every later section quotes these. All figures were checked against the linked sources.</p> <h5 id="the-hardware"><strong>The hardware</strong></h5> <p>The H100 SXM is the reference GPU, since Llama 3 was trained on 80 GB H100s in 8-GPU NVLink servers (<a href="https://arxiv.org/abs/2407.21783">Llama 3 paper, Section 3.3.1</a>); NVIDIA’s own DGX H100 supplies the node-level numbers:</p> <table> <thead> <tr> <th> </th> <th>H100 SXM</th> <th>Source</th> </tr> </thead> <tbody> <tr> <td>Memory</td> <td>80 GB HBM3</td> <td><a href="https://www.nvidia.com/en-us/data-center/h100/">H100 page</a></td> </tr> <tr> <td>HBM bandwidth</td> <td>3.35 TB/s</td> <td><a href="https://www.nvidia.com/en-us/data-center/h100/">H100 page</a></td> </tr> <tr> <td>BF16 tensor core, dense</td> <td>\(\approx 989\) TFLOPS (1,979 “with sparsity”)</td> <td><a href="https://www.nvidia.com/en-us/data-center/h100/">H100 page</a>, footnote</td> </tr> <tr> <td>FP8 tensor core, dense</td> <td>\(\approx 1{,}979\) TFLOPS</td> <td>same</td> </tr> <tr> <td>NVLink (4th gen, 18 links)</td> <td>900 GB/s total, 450 GB/s per direction</td> <td><a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">Hopper in-depth</a></td> </tr> <tr> <td>PCIe Gen5 x16</td> <td>128 GB/s total, 64 GB/s per direction</td> <td><a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">Hopper in-depth</a></td> </tr> <tr> <td>Node</td> <td>8 GPUs, all-to-all over NVSwitch</td> <td><a href="https://docs.nvidia.com/dgx/dgxh100-user-guide/introduction-to-dgxh100.html">DGX H100 guide</a></td> </tr> <tr> <td>Network</td> <td>8 ConnectX-7 at up to 400 Gb/s InfiniBand, one per GPU</td> <td><a href="https://docs.nvidia.com/dgx/dgxh100-user-guide/introduction-to-dgxh100.html">DGX H100 guide</a></td> </tr> </tbody> </table> <p>Two reading notes. The marketing TFLOPS assume 2:4 structured sparsity; the datasheet footnote says the dense numbers are half, and dense is what training does. And NVLink’s 900 GB/s is the sum of both directions across 18 links at 25 GB/s each way, so a one-way stream sees 450 GB/s; the same applies to PCIe’s 128 GB/s.</p> <p>What matters for parallelism is not any one number but the <strong>ladder</strong> they form, in bandwidth available to one GPU in one direction:</p> <table> <thead> <tr> <th>Link</th> <th>One-way bandwidth per GPU</th> <th>Relative to HBM</th> </tr> </thead> <tbody> <tr> <td>HBM3 (on-package)</td> <td>3,350 GB/s</td> <td>1</td> </tr> <tr> <td>NVLink (within the node)</td> <td>450 GB/s</td> <td>about 1/7</td> </tr> <tr> <td>InfiniBand NDR (across nodes)</td> <td>400 Gb/s = 50 GB/s</td> <td>about 1/67</td> </tr> <tr> <td>PCIe Gen5 (to the host)</td> <td>64 GB/s</td> <td>about 1/52</td> </tr> </tbody> </table> <p>Each rung down is roughly an order of magnitude. Every parallelism decision in this post is, in the end, a decision about which traffic goes on which rung, and the rule that falls out is stated in the composition section: the chattiest split goes on the fastest link.</p> <h5 id="the-models"><strong>The models</strong></h5> <p>The Llama 3 family is the running example, because its architecture table is public (<a href="https://arxiv.org/abs/2407.21783">Llama 3 paper, Table 3</a>) and its training configuration is too:</p> <table> <thead> <tr> <th> </th> <th>8B</th> <th>70B</th> <th>405B</th> </tr> </thead> <tbody> <tr> <td>Layers \(L\)</td> <td>32</td> <td>80</td> <td>126</td> </tr> <tr> <td>\(d_{model}\) (the paper’s \(h\))</td> <td>4096</td> <td>8192</td> <td>16384</td> </tr> <tr> <td>Attention heads \(a\)</td> <td>32</td> <td>64</td> <td>128</td> </tr> <tr> <td>KV heads</td> <td>8</td> <td>8</td> <td>8</td> </tr> <tr> <td>head_dim</td> <td>128</td> <td>128</td> <td>128</td> </tr> <tr> <td>FFN dim</td> <td>14336</td> <td>28672</td> <td>53248</td> </tr> </tbody> </table> <p>I’ll use the nominal parameter counts, \(8 \times 10^9\), \(70 \times 10^9\), \(405 \times 10^9\), in the arithmetic.</p> <h5 id="bytes-per-parameter-the-number-that-decides-every-design"><strong>Bytes per parameter: the number that decides every design</strong></h5> <p>A model trained in mixed precision with Adam holds, for every parameter (<a href="https://arxiv.org/abs/1910.02054">ZeRO paper, Section 3.1</a>):</p> <table> <thead> <tr> <th>Component</th> <th>Precision</th> <th>Bytes</th> <th>Why it exists</th> </tr> </thead> <tbody> <tr> <td>Weights</td> <td>bf16 or fp16</td> <td>2</td> <td>what the forward and backward pass actually multiply with</td> </tr> <tr> <td>Gradients</td> <td>bf16 or fp16</td> <td>2</td> <td>the \(g z^\top\) from the previous section</td> </tr> <tr> <td>Master weights</td> <td>fp32</td> <td>4</td> <td>the authoritative copy the optimizer updates (next subsection)</td> </tr> <tr> <td>Adam first moment \(m\)</td> <td>fp32</td> <td>4</td> <td>running mean of the gradient</td> </tr> <tr> <td>Adam second moment \(v\)</td> <td>fp32</td> <td>4</td> <td>running mean of the squared gradient</td> </tr> <tr> <td><strong>Total</strong></td> <td> </td> <td><strong>16</strong></td> <td> </td> </tr> </tbody> </table> <p>The ZeRO paper calls the optimizer’s share \(K = 12\) bytes (master copy plus two moments) and writes the total as \(2\Psi + 2\Psi + K\Psi = 16\Psi\) for \(\Psi\) parameters. <strong>Sixteen bytes per parameter</strong> is the number to carry around. Running the Llama 3 sizes through it:</p> <table> <thead> <tr> <th>Model</th> <th>Static training state at 16 B/param</th> <th>Weights alone at bf16</th> </tr> </thead> <tbody> <tr> <td>8B</td> <td>\(8 \times 10^9 \times 16 = 128\) GB</td> <td>16 GB</td> </tr> <tr> <td>70B</td> <td>\(70 \times 10^9 \times 16 = 1.12\) TB</td> <td>140 GB</td> </tr> <tr> <td>405B</td> <td>\(405 \times 10^9 \times 16 = 6.48\) TB</td> <td>810 GB</td> </tr> </tbody> </table> <p>The 8B model serves comfortably on one 80 GB H100 with 16 GB of weights, and its training state alone does not fit on that same GPU before a single activation exists. That is the whole motivation for ZeRO and FSDP, and it is why “does it fit” is the first question a training run asks and “is it fast” only the second. Note also what this table does <em>not</em> depend on: batch size, sequence length, or the number of GPUs. Static state scales with \(N\) and nothing else.</p> <h5 id="why-the-master-weights-are-fp32"><strong>Why the master weights are fp32</strong></h5> <p>Adam’s update is (<a href="https://arxiv.org/abs/1412.6980">Kingma and Ba, Algorithm 1</a>):</p> \[w \leftarrow w - \alpha \, \frac{\hat m}{\sqrt{\hat v} + \varepsilon}\] <p>The problem is not computing the update; it is <em>adding</em> it. bf16 stores 7 fraction bits (<a href="https://arxiv.org/abs/1905.12322">Kalamkar et al., Table 1</a>), so two adjacent bf16 values near \(w\) differ by about \(2^{-7} \approx 0.8\%\) of \(w\), and under round-to-nearest any addend smaller than half that gap, about \(2^{-8} \approx 0.4\%\) of \(w\) in relative terms, rounds back to \(w\) itself. The update is not applied inaccurately; it is not applied at all. The same argument is made for fp16 in the original mixed precision paper: with 10 fraction bits, an update smaller than \(2^{-11}\) of the weight, a ratio of 2048, can vanish when the addition right-shifts it out of the mantissa (<a href="https://arxiv.org/abs/1710.03740">Micikevicius et al., Section 3.1</a>). A later study of bf16 training puts it directly: nearest rounding of the weight update often cancels small updates (<a href="https://arxiv.org/abs/2010.06192">Zamirai et al.</a>).</p> <p>fp32 has 23 fraction bits, so its threshold is \(2^{-24} \approx 6 \times 10^{-8}\) relative, sixteen bits lower. Keeping the copy the optimizer updates in fp32 is what lets small updates accumulate over thousands of steps instead of being rounded away; the bf16 weights the forward pass uses are re-cast from it after every step. That one design choice costs 4 of the 16 bytes.</p> <h5 id="activation-memory"><strong>Activation memory</strong></h5> <p>The static state is only half the story, and often the smaller half. The backward pass needs the forward pass’s inputs (the \(z\) in \(g z^\top\), plus whatever the nonlinearities and softmaxes need), so the forward pass has to keep them. <a href="https://arxiv.org/abs/2205.05198">Korthikanti et al.</a> count them exactly for a standard transformer layer stored in 16-bit, with \(s\) the sequence length, \(b\) the microbatch size, \(h\) the hidden size, and \(a\) the number of attention heads (their Eq. 1):</p> \[\text{activation bytes per layer} = s\,b\,h\left(34 + 5\,\frac{a\,s}{h}\right)\] <p>The two terms are different animals:</p> <table> <thead> <tr> <th>Term</th> <th>Bytes</th> <th>What it is</th> </tr> </thead> <tbody> <tr> <td>\(34\,sbh\)</td> <td>linear in \(s\)</td> <td>inputs to the QKV and output projections, the MLP inputs and its \(4h\)-wide intermediate, the norm inputs, residual and dropout masks (the paper’s breakdown: 11 from attention, 19 from the MLP, 4 from the two norms)</td> </tr> <tr> <td>\(5\,a\,s^2 b\)</td> <td>quadratic in \(s\)</td> <td>the attention score matrices: \(QK^\top\) in 16-bit, the softmax output in 16-bit, and its dropout mask in 1 byte, for every head</td> </tr> </tbody> </table> <p>Run Llama 3 70B through it at \(s = 8192\), \(b = 1\), \(h = 8192\), \(a = 64\). Then \(sbh = 8192 \times 8192 = 6.7 \times 10^7\), and:</p> \[34\,sbh = 2.28 \text{ GB}, \qquad 5\,a\,s^2 b = 5 \times 64 \times 8192^2 = 21.5 \text{ GB}\] <p>per layer, or 23.8 GB per layer and about <strong>1.9 TB across 80 layers, for a single 8K-token sequence</strong>, with no parallelism and no recomputation. The score term is 90% of it, which is why every modern stack kills that term one way or another: FlashAttention never writes the \(s \times s\) matrices to memory in the first place (<a href="https://arxiv.org/abs/2205.14135">Dao et al.</a>), and selective recomputation, covered in its own section, drops them and recomputes them in the backward pass. Even with the quadratic term gone, \(34\,sbh\) per layer is 182 GB for one sequence of the 70B model, which is why activations, not weights, are usually what caps the microbatch size.</p> <h5 id="the-asymmetry-that-shapes-everything"><strong>The asymmetry that shapes everything</strong></h5> <p>Put the two accountings side by side:</p> <table> <thead> <tr> <th> </th> <th>Scales with</th> <th>Fixed by</th> <th>Attacked by</th> </tr> </thead> <tbody> <tr> <td>Static state (\(16N\))</td> <td>\(N\) only</td> <td>the model</td> <td>sharding across data-parallel ranks (ZeRO, FSDP); splitting the model (TP, PP)</td> </tr> <tr> <td>Activations</td> <td>\(b \times s \times L\)</td> <td>the batch</td> <td>recomputation; sequence and context parallelism; smaller microbatches</td> </tr> </tbody> </table> <p>Sharding fixes the first and recomputation fixes the second, and a real run needs both. This split is the reason the parallelism sections below keep asking two separate questions of each technique: what does it do to the \(16N\), and what does it do to the activations.</p> <p><strong>References</strong></p> <ul> <li><a href="https://www.nvidia.com/en-us/data-center/h100/">NVIDIA H100 page</a>, <a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">NVIDIA Hopper architecture in-depth</a>, <a href="https://docs.nvidia.com/dgx/dgxh100-user-guide/introduction-to-dgxh100.html">DGX H100 user guide</a></li> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a></li> <li><a href="https://arxiv.org/abs/1910.02054">ZeRO: Memory Optimizations Toward Training Trillion Parameter Models - Rajbhandari et al.</a></li> <li><a href="https://arxiv.org/abs/1412.6980">Adam - Kingma and Ba</a></li> <li><a href="https://arxiv.org/abs/1710.03740">Mixed Precision Training - Micikevicius et al.</a>, <a href="https://arxiv.org/abs/1905.12322">A Study of BFLOAT16 for Deep Learning Training - Kalamkar et al.</a>, <a href="https://arxiv.org/abs/2010.06192">Revisiting BFloat16 Training - Zamirai et al.</a></li> <li><a href="https://arxiv.org/abs/2205.05198">Reducing Activation Recomputation in Large Transformer Models - Korthikanti et al.</a></li> <li><a href="https://arxiv.org/abs/2205.14135">FlashAttention - Dao et al.</a></li> </ul> <hr/> <h4 id="mixed-precision"><strong>Mixed Precision</strong></h4> <p>Every byte count above assumed the forward and backward passes run in 16-bit while the optimizer runs in 32-bit. This section is why that split, and what changes at 8 bits.</p> <h5 id="the-formats"><strong>The formats</strong></h5> <table> <thead> <tr> <th>Format</th> <th>Sign / exponent / fraction bits</th> <th>Largest normal</th> <th>Smallest normal</th> <th>Source</th> </tr> </thead> <tbody> <tr> <td>fp32</td> <td>1 / 8 / 23</td> <td>\(3.4 \times 10^{38}\)</td> <td>\(1.2 \times 10^{-38}\)</td> <td><a href="https://arxiv.org/abs/1905.12322">Kalamkar et al., Table 1</a></td> </tr> <tr> <td>fp16</td> <td>1 / 5 / 10</td> <td>\(65{,}504\)</td> <td>\(6.1 \times 10^{-5}\)</td> <td>same</td> </tr> <tr> <td>bf16</td> <td>1 / 8 / 7</td> <td>\(3.4 \times 10^{38}\)</td> <td>\(1.2 \times 10^{-38}\)</td> <td>same</td> </tr> <tr> <td>FP8 E4M3</td> <td>1 / 4 / 3</td> <td>\(448\)</td> <td>\(2^{-6} \approx 0.016\)</td> <td><a href="https://arxiv.org/abs/2209.05433">FP8 paper, Table 1</a></td> </tr> <tr> <td>FP8 E5M2</td> <td>1 / 5 / 2</td> <td>\(57{,}344\)</td> <td>\(2^{-14} \approx 6.1 \times 10^{-5}\)</td> <td>same</td> </tr> </tbody> </table> <p>Read the table by columns. The exponent bits set the <strong>range</strong>, the fraction bits set the <strong>precision</strong>, and every format below fp32 has given up one to keep the other. bf16 keeps fp32’s 8 exponent bits and pays with 7 fraction bits, so it can represent the same span of magnitudes as fp32, at roughly two to three significant decimal digits. fp16 does the opposite: 10 fraction bits, but only 5 exponent bits and a ceiling of 65,504.</p> <h5 id="the-scheme"><strong>The scheme</strong></h5> <p>The mixed precision recipe that every large run uses is four rules (<a href="https://arxiv.org/abs/1710.03740">Micikevicius et al.</a>, <a href="https://docs.nvidia.com/deeplearning/performance/mixed-precision-training/index.html">NVIDIA mixed precision guide</a>):</p> <ol> <li>Keep an fp32 master copy of the weights, for the rounding reason given in the previous section.</li> <li>Cast to 16-bit for the forward and backward passes, so the matmuls run on the fast tensor-core path and the activations cost half the bytes.</li> <li>Accumulate inside each matmul in fp32. The tensor cores take 16-bit inputs and can produce 32-bit outputs (<a href="https://docs.nvidia.com/deeplearning/performance/mixed-precision-training/index.html">NVIDIA guide, Section 2.2</a>), so the long dot products that make up a GEMM do not lose precision as they sum.</li> <li>Compute the optimizer update in fp32, against the master copy, then re-cast to 16-bit for the next step.</li> </ol> <p>The rule of thumb behind all four: <strong>matmul operands in low precision; reductions and updates in fp32.</strong> A matmul touches each input once and is tolerant of noise in it; a reduction adds thousands of terms and a weight update accumulates thousands of steps, and both are where small errors compound.</p> <h5 id="fp16-and-loss-scaling"><strong>fp16 and loss scaling</strong></h5> <p>fp16’s problem is not the weights, which sit comfortably in range; it is the <strong>gradients</strong>, which are routinely tiny. The mixed precision paper found that a sizable share of gradient values in their networks fell below fp16’s smallest subnormal, \(2^{-24}\), and simply became zero (<a href="https://arxiv.org/abs/1710.03740">Section 3.1</a>). The fix is loss scaling (<a href="https://arxiv.org/abs/1710.03740">Section 3.2</a>): multiply the loss by a factor \(S\) before the backward pass. By the chain rule every gradient is then multiplied by \(S\) too, shifting the whole distribution up into representable range; the weight gradients are divided by \(S\) again <em>before</em> anything that depends on their true magnitude, gradient clipping, weight decay, the optimizer step. Nothing about the optimization changes; only the intermediate representation does.</p> <p>The catch is picking \(S\): too small and gradients still underflow, too large and they overflow to infinity. <strong>Dynamic loss scaling</strong> picks it empirically, and the PyTorch implementation makes the algorithm concrete (<a href="https://docs.pytorch.org/docs/stable/amp.html"><code class="language-plaintext highlighter-rouge">torch.amp.GradScaler</code></a>):</p> <table> <thead> <tr> <th>Event</th> <th>Action</th> <th>PyTorch default</th> </tr> </thead> <tbody> <tr> <td>Start</td> <td>begin at a large scale</td> <td><code class="language-plaintext highlighter-rouge">init_scale</code> \(= 2^{16} = 65{,}536\)</td> </tr> <tr> <td>Gradients contain inf or NaN</td> <td>skip the optimizer step, multiply the scale by <code class="language-plaintext highlighter-rouge">backoff_factor</code></td> <td>\(0.5\)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">growth_interval</code> consecutive clean steps</td> <td>multiply the scale by <code class="language-plaintext highlighter-rouge">growth_factor</code></td> <td>\(2000\) steps, factor \(2.0\)</td> </tr> </tbody> </table> <p>The NVIDIA guide describes the same loop with the same constants (<a href="https://docs.nvidia.com/deeplearning/performance/mixed-precision-training/index.html">Section 2.3.2</a>), and Megatron-LM’s defaults are a starting scale of \(2^{32}\) with a 1000-step window (<code class="language-plaintext highlighter-rouge">--initial-loss-scale</code>, <code class="language-plaintext highlighter-rouge">--loss-scale-window</code> in <a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/training/arguments.py"><code class="language-plaintext highlighter-rouge">megatron/training/arguments.py</code></a>). The skipped step is the important detail: an overflow means the gradients for that step are garbage, so the step is discarded rather than applied, and the scale backs off so the next one is clean.</p> <h5 id="bf16-needs-none-of-this"><strong>bf16 needs none of this</strong></h5> <p>bf16 has fp32’s exponent range, so gradients that fit in fp32 fit in bf16; the loss-scaling machinery exists for a range problem that bf16 does not have (<a href="https://arxiv.org/abs/1905.12322">Kalamkar et al., Section 2</a>). The trade is precision: 7 fraction bits instead of 10. The empirical finding across the industry is that training cares far more about range than about those three bits, which is why bf16 is the modern default and fp16 is the legacy path. In one sentence: <strong>bf16 trades mantissa for exponent, and training wanted the exponent.</strong></p> <h5 id="fp8"><strong>FP8</strong></h5> <p>At 8 bits there are not enough bits to keep both, so the FP8 paper defines two formats and assigns them by tensor (<a href="https://arxiv.org/abs/2209.05433">Micikevicius et al. 2022, Section 3.1</a>): <strong>E4M3</strong> (max 448) for weights and activations, which need precision more than range, and <strong>E5M2</strong> (max 57,344) for gradients, which need range for the same reason fp16 gradients did. Ada and Hopper tensor cores support both (<a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">Hopper in-depth</a>), and the FP8 tensor core rate is double the BF16 rate.</p> <p>A ceiling of 448 is tight enough that the paper says outright that some networks cannot be trained without per-tensor <strong>scaling factors</strong> (Section 4.3): before casting a tensor to FP8, multiply it by a scale chosen so its largest magnitude lands near the format’s maximum, and undo the scale after the matmul. Choosing that scale needs the tensor’s maximum absolute value, its amax, and there are two ways to get it:</p> <ul> <li><strong>Delayed scaling</strong> uses the amax history from <em>previous</em> iterations rather than the current tensor’s, on the assumption that a tensor’s range changes slowly. This avoids a full pass over the tensor (and the resulting synchronization) before every cast. NVIDIA’s Transformer Engine keeps a sliding window of past amax values (<code class="language-plaintext highlighter-rouge">amax_history_len</code>, default 1024 in its recipe) and sets the scale to the format maximum divided by the window’s max (<a href="https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/features/low_precision_training/fp8_delayed_scaling/fp8_delayed_scaling.html">TE delayed scaling</a>, <a href="https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/api/common.html">recipe API</a>). Values that exceed the stale range clip.</li> <li><strong>Fine-grained scaling</strong> shrinks the group that shares a scale so one outlier cannot flatten a whole tensor. DeepSeek-V3 scales activations per \(1 \times 128\) tile and weights per \(128 \times 128\) block, computes the scales online from the current tensor, and, notably, uses E4M3 for every tensor including gradients, arguing that the finer groups recover enough range to spend the bits on mantissa (<a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3, Section 3.3.2</a>). They also found that the H800’s FP8 tensor cores accumulate with only about 14 bits of precision, so they promote partial sums to fp32 registers every 128 elements, which is rule 3 above enforced by hand.</li> </ul> <p>DeepSeek-V3 is also a good list of what stays in higher precision even in an FP8 run (<a href="https://arxiv.org/abs/2412.19437">Section 3.3.1 and 3.3.3</a>): the embedding, the output head, the MoE router, the normalization layers, and the attention operators all stay in bf16 or fp32; the master weights and the gradient accumulators are fp32; only the Adam moments are allowed down to bf16.</p> <h5 id="which-hardware-runs-what"><strong>Which hardware runs what</strong></h5> <table> <thead> <tr> <th>Precision</th> <th>First tensor-core support</th> <th>Notes</th> </tr> </thead> <tbody> <tr> <td>bf16</td> <td>Ampere (A100)</td> <td><a href="https://developer.nvidia.com/blog/nvidia-ampere-architecture-in-depth/">Ampere in-depth</a></td> </tr> <tr> <td>FP8 (E4M3, E5M2)</td> <td>Ada (compute capability 8.9) and Hopper (9.0)</td> <td>A100 has no FP8 tensor cores (<a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">Hopper in-depth</a>, <a href="https://images.nvidia.com/aem-dam/Solutions/geforce/ada/nvidia-ada-gpu-architecture.pdf">Ada whitepaper</a>); per-tensor scaling in software</td> </tr> <tr> <td>FP4 (NVFP4, MXFP4)</td> <td>Blackwell</td> <td>blocks of 16 (NVFP4, E4M3 scale) or 32 (MXFP4, power-of-two scale) values share a hardware-applied scale, “micro-tensor scaling” (<a href="https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/">NVFP4 post</a>, <a href="https://www.nvidia.com/en-us/data-center/technologies/blackwell-architecture/">Blackwell page</a>)</td> </tr> </tbody> </table> <p>The progression is the same story each time: fewer bits per element, and the range problem that creates gets solved by scaling, first per tensor in software, then per block in hardware.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/1710.03740">Mixed Precision Training - Micikevicius et al.</a>, <a href="https://docs.nvidia.com/deeplearning/performance/mixed-precision-training/index.html">NVIDIA Train With Mixed Precision guide</a>, <a href="https://docs.pytorch.org/docs/stable/amp.html">PyTorch AMP and GradScaler</a></li> <li><a href="https://arxiv.org/abs/1905.12322">A Study of BFLOAT16 for Deep Learning Training - Kalamkar et al.</a></li> <li><a href="https://arxiv.org/abs/2209.05433">FP8 Formats for Deep Learning - Micikevicius et al.</a>, <a href="https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/features/low_precision_training/fp8_delayed_scaling/fp8_delayed_scaling.html">Transformer Engine FP8 delayed scaling</a>, <a href="https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/examples/fp8_primer.html">Transformer Engine FP8 primer</a></li> <li><a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3 Technical Report</a></li> <li><a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">NVIDIA Hopper architecture in-depth</a>, <a href="https://developer.nvidia.com/blog/nvidia-ampere-architecture-in-depth/">NVIDIA Ampere architecture in-depth</a>, <a href="https://images.nvidia.com/aem-dam/Solutions/geforce/ada/nvidia-ada-gpu-architecture.pdf">Ada GPU architecture whitepaper</a>, <a href="https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/">Introducing NVFP4</a></li> <li><a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/training/arguments.py">Megatron-LM arguments</a></li> </ul> <hr/> <h4 id="activation-recomputation"><strong>Activation Recomputation</strong></h4> <p>The activation accounting said a single 8K sequence of the 70B model wants 1.9 TB of saved activations. Recomputation, also called activation or gradient checkpointing, is the answer: do not save everything, and recompute what you dropped when the backward pass needs it.</p> <h5 id="full-recomputation"><strong>Full recomputation</strong></h5> <p>The original formulation is <a href="https://arxiv.org/abs/1604.06174">Chen et al., 2016</a>. Keep only the activations at chosen layer boundaries (checkpoints), free everything in between after the forward pass, and when the backward pass reaches a segment, re-run its forward from the saved boundary to regenerate the interior, then proceed. With \(n\) layers and checkpoints every \(\sqrt{n}\) layers, peak memory drops from \(O(n)\) to \(O(\sqrt{n})\) at the cost of one extra forward pass per step; they measured roughly 30% extra runtime.</p> <p>In the \(6N\) accounting, the extra forward is \(2N\) on top of \(6N\), so full recomputation costs <strong>about 33% more FLOPs</strong>, \(6N \to 8N\), in exchange for holding only one layer’s activations at a time plus the boundaries. Megatron’s measurements on large transformers land in the same place: 30 to 40% overhead (<a href="https://arxiv.org/abs/2205.05198">Korthikanti et al., abstract</a>).</p> <h5 id="selective-recomputation"><strong>Selective recomputation</strong></h5> <p>The insight of <a href="https://arxiv.org/abs/2205.05198">Korthikanti et al.</a> is that the 33% is buying more than it needs to. Look back at the per-layer formula, \(sbh(34 + 5as/h)\). The two terms have opposite economics:</p> <table> <thead> <tr> <th>Term</th> <th>Memory</th> <th>FLOPs to recompute</th> <th>Ratio</th> </tr> </thead> <tbody> <tr> <td>\(5\,a\,s^2 b\), the attention scores (\(QK^\top\), softmax, dropout, attention over \(V\))</td> <td>large, quadratic in \(s\)</td> <td>small: a few operations per score element</td> <td>recompute these</td> </tr> <tr> <td>\(34\,sbh\), the matmul inputs and outputs</td> <td>linear in \(s\)</td> <td>large: the GEMMs themselves</td> <td>keep these</td> </tr> </tbody> </table> <p>So <strong>selective recomputation</strong> drops only the attention score tensors and recomputes just that block in the backward pass. For GPT-3-sized shapes the score term is \(5as/h = 80\) against the fixed 34, so this removes about 70% of activation memory for about 2.7% more FLOPs (Section 5 of the paper). Megatron-LM exposes it as <code class="language-plaintext highlighter-rouge">--recompute-activations</code>, which sets the recompute granularity to <code class="language-plaintext highlighter-rouge">selective</code> with the core attention module as the default target (<a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/training/arguments.py"><code class="language-plaintext highlighter-rouge">megatron/training/arguments.py</code></a>, <a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/core/transformer/transformer_config.py"><code class="language-plaintext highlighter-rouge">transformer_config.py</code></a>). And note the overlap with FlashAttention, which never materializes the scores at all: a stack using a fused attention kernel already has this term gone, and what selective recomputation then means in practice is whatever the kernel chooses to recompute inside its own backward pass (FlashAttention recomputes the scores from \(Q\), \(K\), and the saved softmax statistics, for the same reason: cheap to redo, expensive to store).</p> <p>The paper’s own numbers make the comparison concrete, on a 22B-parameter layer with 8-way tensor parallelism (<a href="https://arxiv.org/abs/2205.05198">Table 4</a>):</p> <table> <thead> <tr> <th>Configuration</th> <th>Time overhead per layer</th> </tr> </thead> <tbody> <tr> <td>Full recomputation</td> <td>39%</td> </tr> <tr> <td>Selective recomputation</td> <td>7%</td> </tr> <tr> <td>Sequence parallelism alone (next section)</td> <td>\(-3\)%</td> </tr> <tr> <td>Selective recomputation with sequence parallelism</td> <td>4%</td> </tr> </tbody> </table> <h5 id="per-layer-activation-memory-the-whole-table"><strong>Per-layer activation memory, the whole table</strong></h5> <p>The paper tracks how each technique changes the per-layer count. With \(t\)-way tensor parallelism and, later, sequence parallelism, the formulas are:</p> <table> <thead> <tr> <th>Configuration</th> <th>Activation bytes per layer</th> <th>Llama 3 70B, \(s = 8192\), \(b = 1\), \(t = 8\)</th> </tr> </thead> <tbody> <tr> <td>No parallelism (Eq. 1)</td> <td>\(sbh\left(34 + 5\dfrac{as}{h}\right)\)</td> <td>23.8 GB</td> </tr> <tr> <td>Tensor parallelism (Eq. 2)</td> <td>\(sbh\left(10 + \dfrac{24}{t} + 5\dfrac{as}{ht}\right)\)</td> <td>3.5 GB</td> </tr> <tr> <td>Tensor plus sequence parallelism (Eq. 4)</td> <td>\(\dfrac{sbh}{t}\left(34 + 5\dfrac{as}{h}\right)\)</td> <td>3.0 GB</td> </tr> <tr> <td>Plus selective recomputation (Table 2)</td> <td>\(\dfrac{34\,sbh}{t}\)</td> <td>0.29 GB</td> </tr> </tbody> </table> <p>The 10 in the tensor-parallel row is the part of the layer that tensor parallelism does not split (the norm and dropout inputs, replicated on every rank), which is exactly what sequence parallelism goes after in the next section. Across 80 layers the last row is about 23 GB per 8K sequence per GPU, down from 1.9 TB. That is the difference between a run that cannot start and one with room for a real microbatch.</p> <h5 id="why-recomputation-is-a-throughput-decision-not-just-a-memory-one"><strong>Why recomputation is a throughput decision, not just a memory one</strong></h5> <p>The cost side of recomputation is easy to state: 33% more FLOPs for full, a few percent for selective. The benefit side is subtler than “it fits”. FLOPs per token are fixed at \(6N\), so a training run’s throughput is set by how efficiently it executes those FLOPs, and that efficiency depends on the shapes of the GEMMs, which depend on the microbatch. Activation memory is what caps the microbatch. So recomputation is a trade of FLOPs for memory <em>that buys back larger, more efficient GEMMs</em>, and it pays off when the efficiency gain exceeds the recompute overhead. The same paper’s headline measurement is the sourced version of this: on a 530B model over 2,240 A100s, switching from full recomputation to selective recomputation with sequence parallelism raised model FLOPs utilization from 42.1% to 54.2% (<a href="https://arxiv.org/abs/2205.05198">abstract</a>). That is a 29% throughput gain from changing nothing about the model and nothing about the hardware, only what gets stored.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/1604.06174">Training Deep Nets with Sublinear Memory Cost - Chen et al.</a></li> <li><a href="https://arxiv.org/abs/2205.05198">Reducing Activation Recomputation in Large Transformer Models - Korthikanti et al.</a></li> <li><a href="https://arxiv.org/abs/2205.14135">FlashAttention - Dao et al.</a></li> <li><a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/training/arguments.py">Megatron-LM arguments</a>, <a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/core/transformer/transformer_config.py">Megatron Core <code class="language-plaintext highlighter-rouge">TransformerConfig</code></a></li> </ul> <hr/> <h4 id="communication-primitives"><strong>Communication Primitives</strong></h4> <p>Every parallelism scheme in the rest of this post is a pattern built from a handful of collective operations, and every cost estimate is a count of how many bytes those collectives move over which link. So before any of the schemes, the primitives themselves: what each one does, what each one costs, and the one algorithm, the ring, that the cost formulas come from.</p> <h5 id="the-eight-collectives"><strong>The eight collectives</strong></h5> <p>In all of the following there are \(p\) ranks (GPUs), and \(S\) is the size in bytes of the full vector involved.</p> <table> <thead> <tr> <th>Collective</th> <th>Before</th> <th>After</th> <th>Bytes each rank sends, bandwidth-optimal</th> </tr> </thead> <tbody> <tr> <td>Broadcast</td> <td>one rank holds \(x\)</td> <td>every rank holds \(x\)</td> <td> </td> </tr> <tr> <td>Scatter</td> <td>one rank holds \([a, b, c, d]\)</td> <td>rank \(i\) holds chunk \(i\)</td> <td> </td> </tr> <tr> <td>Gather</td> <td>rank \(i\) holds chunk \(i\)</td> <td>one rank holds \([a, b, c, d]\)</td> <td> </td> </tr> <tr> <td><strong>All-gather</strong></td> <td>rank \(i\) holds chunk \(i\) (\(S/p\) bytes)</td> <td>every rank holds all \(S\) bytes</td> <td>\(S\,\dfrac{p-1}{p}\)</td> </tr> <tr> <td>Reduce</td> <td>every rank holds a full vector</td> <td>one rank holds the elementwise sum</td> <td> </td> </tr> <tr> <td><strong>All-reduce</strong></td> <td>every rank holds a full vector</td> <td>every rank holds the sum</td> <td>\(2S\,\dfrac{p-1}{p}\)</td> </tr> <tr> <td><strong>Reduce-scatter</strong></td> <td>every rank holds a full vector</td> <td>rank \(i\) holds slice \(i\) of the sum</td> <td>\(S\,\dfrac{p-1}{p}\)</td> </tr> <tr> <td><strong>All-to-all</strong></td> <td>rank \(i\) holds row \(i\) of a matrix of chunks</td> <td>rank \(i\) holds column \(i\) (a transpose)</td> <td>\(S\,\dfrac{p-1}{p}\)</td> </tr> </tbody> </table> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/collectives.svg" sizes="95vw"/> <img src="/assets/img/llm-training/collectives.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Distributed collective primitives" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The eight collectives, before and after, on four devices. Letters are data chunks, numbers are values being summed, and the reduce-scatter row shows four vectors being summed elementwise with device i keeping slice i of the result. Editable source: <a href="/assets/img/llm-training/collectives.excalidraw">collectives.excalidraw</a>. </div> <p>The four bold rows are the ones training uses constantly. All-reduce is the data-parallel gradient sync and the tensor-parallel sync. All-gather and reduce-scatter are what ZeRO and FSDP use to assemble and disassemble sharded state, and what sequence parallelism uses at its boundaries. All-to-all is the expert-parallel dispatch in mixture-of-experts models.</p> <h5 id="the-ring-algorithm-and-where-p-1p-comes-from"><strong>The ring algorithm, and where \((p-1)/p\) comes from</strong></h5> <p>The volume column deserves a derivation, because the same factor shows up in every communication estimate for the rest of the post and it is not obvious where it comes from. The picture first, since the whole derivation is a walk through it:</p> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/ring-tree-allreduce.svg" sizes="95vw"/> <img src="/assets/img/llm-training/ring-tree-allreduce.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Tree versus ring all-reduce" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> All-reduce on four devices d0 to d3. Device d0 holds the vector a, cut into four chunks a0, a1, a2, a3; d1 holds b, d2 holds c, d3 holds d, cut the same way. Aj is the sum of chunk j across devices (aj + bj + cj + dj), and the goal is A = (A0, A1, A2, A3) on every device. Top: the tree reduces whole vectors up to a root and broadcasts the result down. Bottom: the ring moves one chunk per step; after the reduce-scatter each device owns one finished chunk (the diagonal), and after the all-gather every device owns all four. Editable source: <a href="/assets/img/llm-training/ring-tree-allreduce.excalidraw">ring-tree-allreduce.excalidraw</a>. </div> <p><strong>The setup.</strong> There are \(p\) devices (\(p = 4\) in the figure) and each holds its own vector of \(S\) bytes: \(d_0\) holds \(a\), \(d_1\) holds \(b\), and so on. In training, “its own vector” is that rank’s local gradient. Every vector is cut into \(p\) equal chunks of \(S/p\) bytes each, so \(a = (a_0, a_1, a_2, a_3)\), and in the ring’s left grid a row is a device and a column is a chunk index. The all-reduce has to produce the elementwise sum \(A = a + b + c + d\) on every device. Since the sum is elementwise, it can be done chunk by chunk: \(A_j = a_j + b_j + c_j + d_j\), and the right grid, every device holding \(A_0\) to \(A_3\), is the finished state.</p> <p>The devices form a logical ring, \(d_0 \to d_1 \to d_2 \to d_3 \to d_0\), and each device only ever sends to its right neighbor and receives from its left. The algorithm has two phases.</p> <p><strong>Phase 1, reduce-scatter: \(p - 1\) steps.</strong> Give each device one chunk index to end up owning: device \(j\) will own chunk \(j\), which is the diagonal in the middle grid. Follow one chunk’s journey. \(A_1\) will end on \(d_1\), and its trip starts on the device <em>after</em> it: \(d_2\) sends its \(c_1\) to \(d_3\), which adds its own \(d_1\) and sends \(c_1 + d_1\) on to \(d_0\), which adds \(a_1\) and sends \(a_1 + c_1 + d_1\) to \(d_1\), which adds \(b_1\) and now holds \(A_1\). Three hops, three additions, one finished chunk. Every other chunk makes the same trip one device over: \(A_0\)’s trip starts at \(d_1\) and ends at \(d_0\), \(A_2\)’s starts at \(d_3\) and ends at \(d_2\), \(A_3\)’s starts at \(d_0\) and ends at \(d_3\). Because the four trips are staggered, at every step each device sends exactly one chunk and receives exactly one, so all four links are busy at once; that is the trick that makes the ring efficient. After \(p - 1 = 3\) steps the middle grid holds: device \(j\) has the complete \(A_j\), and the other chunks on each device are stale partial sums that are no longer needed.</p> <p>Count the bytes for one device in this phase: \(p - 1\) steps, and in each step it sends one chunk of \(S/p\) bytes to its right neighbor and receives one from its left. Counting the sent side (the received side is identical):</p> \[(p-1)\,\frac{S}{p} = S\,\frac{p-1}{p}\] <p>That is the reduce-scatter row of the table.</p> <p><strong>Phase 2, all-gather: \(p - 1\) more steps.</strong> Now the finished chunks circulate. In the first step \(d_1\) sends \(A_1\) to \(d_2\), \(d_2\) sends \(A_2\) to \(d_3\), and so on around the ring; in each later step every device forwards the chunk it just received. After \(p - 1\) steps each chunk has visited every device, and the right grid holds \(A_0\) to \(A_3\) everywhere. The byte count is the same as phase 1: \(p - 1\) chunks of \(S/p\) sent per device, \(S(p-1)/p\), the all-gather row.</p> <p><strong>Total.</strong> Two phases of \(p - 1\) steps, each step moving one \(S/p\)-byte chunk per device:</p> \[2\,(p-1)\,\frac{S}{p} = 2S\,\frac{p-1}{p}\] <p>sent by each device, and the same amount received. The \((p-1)/p\) now has a plain reading: the answer has \(p\) chunks, and in each phase a device already holds one of them (the chunk it is reducing, or the finished chunk it owns), so it only has to move the other \(p - 1\). For \(p = 4\) that is \(3/4\) of \(S\) per phase and \(1.5\,S\) in total; for \(p = 8\), \(7/8\) and \(1.75\,S\); as \(p\) grows it approaches \(2S\). So <strong>a ring all-reduce costs each device about \(2S\) of traffic no matter how many devices take part</strong>, which is what lets it scale.</p> <p><strong>What “bandwidth-optimal” means.</strong> <a href="https://www.cs.fsu.edu/~xyuan/paper/09jpdc.pdf">Patarasuk and Yuan</a> prove that <em>any</em> all-reduce algorithm, however clever, must have at least one device that sends or receives \(2S(p-1)/p\) bytes (their Lemma 4, stated in data items). The ring hits that bound exactly, on every device at once. So if the time of a collective is set by how many bytes have to pass through the busiest link, nothing can beat the ring; it is optimal in the bandwidth term. That statement is only about bytes. It says nothing about how many sequential steps the algorithm takes, and that is exactly where the ring is weak, as the cost model below shows.</p> <p><strong>The tree, for contrast.</strong> The top half of the figure reduces the same four vectors up a binary tree: \(d_0\) and \(d_1\) send \(a\) and \(b\) (all four chunks at once) to an intermediate node that forms \(a + b\), likewise \(c + d\), then the root forms \(A_0\) to \(A_3\) and broadcasts it back down. It finishes in \(2\log_2 p\) hops, 4 for four devices against the ring’s \(2(p-1) = 6\), and 6 against 14 for eight. But every hop moves a whole \(S\)-byte vector, and the node under the root handles several vectors’ worth, so its busiest link carries more than the ring’s \(2S(p-1)/p\). Fewer, larger hops: latency-friendly and bandwidth-heavy, the mirror image of the ring.</p> <p>The identity the ring makes visible, <strong>all-reduce \(=\) reduce-scatter \(+\) all-gather</strong>, is more than an implementation detail. It is the hinge the entire ZeRO family turns on: if an all-reduce is already two phases with a moment in the middle where each device holds one fully reduced chunk (the diagonal), then a device can do useful work on its chunk <em>between the phases</em>, and that is exactly where the sharded optimizer step goes. We’ll see it in the data parallelism section.</p> <h5 id="the-cost-model-latency-bandwidth-throughput"><strong>The cost model: latency, bandwidth, throughput</strong></h5> <p>Three words that are easy to mix up, so a mental model first. Think of a link between two GPUs as a water pipe.</p> <table> <thead> <tr> <th>Term</th> <th>Symbol</th> <th>What it is</th> <th>Pipe picture</th> </tr> </thead> <tbody> <tr> <td><strong>Latency</strong></td> <td>\(\alpha\)</td> <td>the fixed time between sending a message and its first byte arriving, whatever its size: handshake, kernel launch, switch hops</td> <td>how long after you open the tap before any water comes out the far end</td> </tr> <tr> <td><strong>Bandwidth</strong></td> <td>\(B\)</td> <td>the rate at which the link carries bytes once they are flowing, a property of the hardware: 450 GB/s per direction for NVLink, 50 GB/s for 400 Gb/s InfiniBand</td> <td>the pipe’s diameter</td> </tr> <tr> <td><strong>Throughput</strong></td> <td> </td> <td>what you actually achieved: bytes delivered divided by the time it took, including the waiting</td> <td>how much water you collected per second, from tap-open to tap-close</td> </tr> </tbody> </table> <p>Bandwidth is the ceiling; throughput is what a particular transfer got. For one message of \(n\) bytes, the time is</p> \[T(n) = \alpha + \frac{n}{B}\] <p>and the throughput is \(n / T(n)\). A tiny message spends almost all of its time in \(\alpha\) and its throughput is a small fraction of \(B\); a huge message spends almost all of its time in \(n/B\) and its throughput approaches \(B\). The two terms are equal at \(n = \alpha B\), the message size at which half the time is waiting: for every microsecond of latency on a 450 GB/s link, that crossover is \(450\) KB. Messages far below \(\alpha B\) are <strong>latency-bound</strong> (the link is mostly idle), messages far above it are <strong>bandwidth-bound</strong> (the link is mostly full). Note that \(n\) here is one message; it is not the \(S\) of the previous subsection, which was the whole vector being reduced.</p> <p>Now apply this to the ring. It runs \(2(p-1)\) sequential steps, and each step is one message of \(S/p\) bytes, so:</p> \[T_{\text{ring}} = 2(p-1)\left(\alpha + \frac{S}{pB}\right) = \underbrace{2(p-1)\,\alpha}_{\text{latency term}} + \underbrace{2\,\frac{p-1}{p}\,\frac{S}{B}}_{\text{bandwidth term}}\] <p>The bandwidth term is the optimal one from above. The latency term is paid once per step, and there are \(2(p-1)\) steps, so it grows linearly with the number of devices; Patarasuk and Yuan say exactly this, that the ring is optimal in the bandwidth term but not the latency term. A tree pays \(\alpha\) only \(2\log_2 p\) times. Which term matters depends on \(S\), and the dividing line comes from setting the two terms equal: \(2(p-1)\alpha = 2\frac{p-1}{p}\frac{S}{B}\) gives \(S = p\,\alpha B\), the single-message crossover \(\alpha B\) scaled by \(p\) because each step’s message is only \(S/p\):</p> <table> <thead> <tr> <th>Regime</th> <th>When</th> <th>What \(T_{\text{ring}}\) looks like</th> <th>What helps</th> </tr> </thead> <tbody> <tr> <td>Latency-bound</td> <td>\(S\) is small, well under \(p\,\alpha B\)</td> <td>\(\approx 2(p-1)\alpha\): independent of \(S\), grows with \(p\)</td> <td>send fewer messages: <strong>bucket</strong> many small tensors into one collective; use tree-shaped algorithms with fewer steps</td> </tr> <tr> <td>Bandwidth-bound</td> <td>\(S\) is large, well over \(p\,\alpha B\)</td> <td>\(\approx 2S/B\): independent of \(p\), proportional to \(S\)</td> <td>move fewer bytes (smaller dtype, sharding), use a faster link, or overlap the transfer with compute</td> </tr> </tbody> </table> <p>Read the two “independent of” notes slowly, because they are the point. In the latency-bound regime, making the vector smaller does nothing and adding devices makes it worse. In the bandwidth-bound regime, adding devices costs nothing extra (that is the ring’s optimality at work) and the only lever is the number of bytes over the link’s speed.</p> <p><strong>Why gradient bucketing matters.</strong> A transformer’s gradient is not one vector; it is hundreds of separate tensors, one per weight matrix, bias, and norm, and many of them are tiny: a norm’s weight vector on the 70B model is \(8192\) elements, 16 KB in bf16, far below any plausible \(\alpha B\). All-reducing each tensor on its own would pay the full latency term, \(2(p-1)\alpha\), hundreds of times per step, and most of those calls would be pure waiting. Bucketing concatenates the tensors into a few large buffers and runs one collective per buffer. PyTorch’s DDP uses 25 MiB buckets by default; at that size each collective is deep in the bandwidth-bound regime, so the total time is close to (total gradient bytes) \(/ B\) no matter how many tensors the model has. The next section shows how DDP does this on the fly during the backward pass.</p> <h5 id="a-worked-number"><strong>A worked number</strong></h5> <p>To set the scale, all-reduce the 70B model’s bf16 gradient. Every rank holds its own copy, \(S = 70 \times 10^9 \times 2 = 140\) GB.</p> <p><em>Eight GPUs in one node, over NVLink.</em> Each chunk is \(S/p = 140/8 = 17.5\) GB. The reduce-scatter is 7 steps of one chunk each, \(7 \times 17.5 = 122.5\) GB sent per rank; the all-gather is another 7 steps and another 122.5 GB; total \(245\) GB sent and 245 GB received per rank, which is the formula’s \(2 \times 7/8 \times 140\). Every rank sends over its own link at the same time, so the collective takes as long as one rank’s traffic, not the sum over ranks: \(245 / 450 \approx 0.54\) s at 450 GB/s per direction. The latency term is invisible here; 14 steps of \(\alpha\) are microseconds against half a second.</p> <p><em>Sixty-four GPUs across eight nodes, over 400 Gb/s InfiniBand.</em> First the units: InfiniBand is quoted in gigabits per second, and 400 Gb/s is \(400 / 8 = 50\) GB/s per direction, which is the figure the bandwidth ladder uses and the one to divide by. Each chunk is \(140/64 = 2.19\) GB; 63 steps per phase, \(63 \times 2.19 = 138\) GB per phase, \(276\) GB in total per rank, the formula’s \(2 \times 63/64 \times 140\). The per-rank bytes barely moved (the ring is bandwidth-optimal, so adding ranks did not add traffic), but the link is nine times slower than NVLink’s 450 GB/s: \(276 / 50 \approx 5.5\) s.</p> <p>Half a second to five seconds, per step, for one collective. Those are the numbers that have to be hidden behind compute for data parallelism to work at all, and hiding them is the next section’s main subject.</p> <h5 id="overlap-is-the-whole-game"><strong>Overlap is the whole game</strong></h5> <p>A collective that runs while the GPU is otherwise idle is fully exposed: its time adds directly to the step. A collective that runs while the GPU computes something else is free, up to the bandwidth of the link. Every well-designed training system is built around finding compute to hide each collective behind. Stated here only as a principle; each of the following is worked out in its own section:</p> <ul> <li>DDP launches the all-reduce of one bucket of gradients while the backward pass is still computing the gradients of earlier layers.</li> <li>FSDP all-gathers the parameters of layer \(i+1\) while layer \(i\) computes.</li> <li>Pipeline parallelism sends activations to the next stage asynchronously while the current stage keeps working on the next microbatch.</li> <li>Tensor parallelism is the hardest to hide, which is why it is placed on the fastest link rather than overlapped away.</li> </ul> <p>If a system is not overlapping, the communication is visible directly as a gap in a profiler timeline, with the GPU’s compute streams empty while the network interface is busy. Most of what follows is arranging for those gaps not to exist.</p> <p><strong>References</strong></p> <ul> <li><a href="https://www.cs.fsu.edu/~xyuan/paper/09jpdc.pdf">Bandwidth Optimal All-reduce Algorithms for Clusters of Workstations - Patarasuk and Yuan, JPDC 2009</a></li> <li><a href="https://andrew.gibiansky.com/blog/machine-learning/baidu-allreduce/">Bringing HPC Techniques to Deep Learning - Gibiansky (the ring all-reduce walkthrough)</a></li> <li><a href="https://docs.nvidia.com/deeplearning/nccl/user-guide/docs/usage/collectives.html">NCCL collective operations</a> (the semantics of each primitive)</li> <li><a href="https://arxiv.org/abs/2006.15704">PyTorch Distributed: Experiences on Accelerating Data Parallel Training - Li et al.</a> (bucketing)</li> </ul> <hr/> <h4 id="data-parallelism"><strong>Data Parallelism</strong></h4> <p>Data parallelism is the oldest and simplest way to use more GPUs: give each one a copy of the model and a different slice of the batch. It is also where the 16-bytes-per-parameter problem bites hardest, and the ZeRO family is the response. This section builds up from the plain version.</p> <h5 id="vocabulary-batch-microbatch-and-the-letters"><strong>Vocabulary: batch, microbatch, and the letters</strong></h5> <p>From here on the word “batch” is doing several jobs, and the parallelism sections that follow lean on the distinctions, so here they are in one place:</p> <table> <thead> <tr> <th>Term</th> <th>Symbol</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td>Global batch</td> <td> </td> <td>the data consumed per <strong>optimizer step</strong>, across all ranks; the number the optimizer cares about. Llama 3 uses 16M tokens (<a href="https://arxiv.org/abs/2407.21783">Table 4</a>)</td> </tr> <tr> <td>Data-parallel degree</td> <td>\(D\)</td> <td>how many ranks process <em>different</em> data in parallel; each holds a full replica (DDP) or a shard of one (ZeRO, FSDP)</td> </tr> <tr> <td>Batch per rank</td> <td>global \(/ D\)</td> <td>the share of the global batch one data-parallel rank is responsible for each step; Llama 3’s “batch per DP rank” of 32 is 32 sequences of 8,192 tokens</td> </tr> <tr> <td>Hidden dimension</td> <td>\(h\)</td> <td>the width of one token’s activation row: a token is a row of \(h\) values, and every matmul in the layer multiplies rows of this width (\(h = 8192\) for Llama 3 70B). Not a batch quantity, but it sits next to the two below in every formula</td> </tr> <tr> <td>Microbatch</td> <td>a tensor of shape \((b, s, h)\)</td> <td>what one rank pushes through <strong>one forward and backward pass</strong> at a time: \(b\) sequences, each of \(s\) tokens, each token a row of \(h\) values, so \(b \times s\) token rows go through the matmuls together. These are the \(b\), \(s\), \(h\) of the activation formula, and the microbatch is what activation memory limits</td> </tr> <tr> <td>Accumulation steps</td> <td>\(k\)</td> <td>microbatches per rank per optimizer step, so batch per rank \(= b \times k\); under pipeline parallelism these same microbatches are the pipeline’s \(m\)</td> </tr> <tr> <td>Ranks in a collective</td> <td>\(p\)</td> <td>the number of participants in whatever collective is being costed (the primitives section); for a data-parallel gradient sync, \(p = D\). The pipeline section reuses \(p\) for the number of stages, and says so</td> </tr> </tbody> </table> <p>The one relation to keep in mind: <strong>global batch \(= D \times b \times k\)</strong> sequences, or \(D \times b \times k \times s\) tokens. The optimizer sees the left side; activation memory sees only the one \((b, s, h)\) microbatch in flight; communication is paid once per step, whatever \(k\) is.</p> <h5 id="distributed-data-parallel"><strong>Distributed data parallel</strong></h5> <p>In plain distributed data parallel (DDP), every rank holds a full replica of the model, the optimizer state included. Each step, every rank runs the forward and backward pass on its own microbatch, producing a local gradient. The gradients are then <strong>averaged across ranks with an all-reduce</strong>, so that every rank holds the same gradient, the gradient of the loss over the whole global batch. Every rank then applies the same optimizer update to its identical copy of the weights, and the replicas stay in lockstep without ever exchanging weights (<a href="https://arxiv.org/abs/2006.15704">Li et al., Section 3</a>).</p> <p>The mechanics that make this fast are in PyTorch’s implementation, and they are a direct application of the cost model above:</p> <ul> <li><strong>Hooks, not a barrier.</strong> DDP registers an autograd hook on every parameter. When that parameter’s gradient is computed during the backward pass, the hook fires and marks it ready. There is no “wait for the whole backward pass, then all-reduce everything.”</li> <li><strong>Buckets.</strong> Parameters are grouped into buckets of a fixed size, 25 MiB by default (<code class="language-plaintext highlighter-rouge">bucket_cap_mb</code>, <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.parallel.DistributedDataParallel.html">DDP docs</a>), in roughly the <em>reverse</em> of the model’s parameter order, because the backward pass produces gradients for the last layers first. When every gradient in a bucket is ready, DDP launches one asynchronous all-reduce for the whole bucket.</li> <li><strong>Overlap.</strong> Because the all-reduce of a ready bucket runs while autograd is still computing gradients for earlier layers, most of the communication hides behind the backward pass. Only the last bucket’s all-reduce is necessarily exposed. The paper reports that this overlap, plus the bucketing, is what turns a naive implementation into a scalable one (<a href="https://arxiv.org/abs/2006.15704">Section 3.2.3</a>).</li> </ul> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/ddp-overlap.svg" sizes="95vw"/> <img src="/assets/img/llm-training/ddp-overlap.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="DDP gradient bucketing and overlap" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> One data-parallel step on one rank. The backward pass produces gradients for the last layer first; as each bucket fills, its all-reduce launches on a separate stream and overlaps the backward computation still in flight. With gradient accumulation over k microbatches, the all-reduces are skipped for the first k minus 1 backward passes and run once. Editable source: <a href="/assets/img/llm-training/ddp-overlap.excalidraw">ddp-overlap.excalidraw</a>. </div> <p>The cost of DDP per step is one all-reduce over the entire gradient, \(2S(p-1)/p\) per rank with \(S\) the gradient bytes, most of it hidden. The memory cost is the problem: every rank holds the full 16 bytes per parameter, and adding ranks does nothing about it. A 70B model needs 1.12 TB <em>per GPU</em> under plain DDP, whether there are 8 GPUs or 8,000.</p> <h5 id="gradient-accumulation"><strong>Gradient accumulation</strong></h5> <p>Before fixing the memory problem, one knob that is independent of it. The batch size a run wants for optimization reasons (Llama 3 uses 16M tokens per batch, <a href="https://arxiv.org/abs/2407.21783">Table 4</a>) is far larger than what fits through a GPU in one forward pass, since activations scale with the batch. Gradient accumulation decouples the two.</p> <p>The gradient of a mean loss over a batch is the mean of the gradients over its parts. If the global batch is split into \(k\) equal microbatches, then:</p> \[\nabla L_{\text{batch}} = \frac{1}{k}\sum_{j=1}^{k} \nabla L_{\text{microbatch}\,j}\] <p>So a rank can run the forward and backward pass \(k\) times, on \(k\) different microbatches, <em>adding</em> each backward pass’s gradient into the same gradient buffer instead of zeroing it, and only then take one optimizer step on the accumulated sum (scaled by \(1/k\), or equivalently with the per-microbatch losses pre-scaled). Three things follow:</p> <ul> <li><strong>Memory.</strong> Only one microbatch’s activations are alive at a time, so the activation footprint is that of a microbatch, not the batch. The gradient buffer is the same size regardless. This is the entire point.</li> <li><strong>Effective batch.</strong> The optimizer sees a batch of \(\text{DP} \times \text{microbatch size} \times k\). Megatron-LM’s <code class="language-plaintext highlighter-rouge">--global-batch-size</code> and <code class="language-plaintext highlighter-rouge">--micro-batch-size</code> flags are exactly this pair, with \(k\) derived from them and the data-parallel degree (<a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/training/arguments.py"><code class="language-plaintext highlighter-rouge">megatron/training/arguments.py</code></a>).</li> <li><strong>Communication.</strong> The gradient all-reduce should happen once per optimizer step, not once per microbatch. Under DDP, every backward pass would trigger the bucketed all-reduces by default, so DDP provides a <code class="language-plaintext highlighter-rouge">no_sync()</code> context manager that disables them; run the first \(k-1\) microbatches inside it and the last one outside, and the collective runs once, amortized over \(k\) microbatches (<a href="https://arxiv.org/abs/2006.15704">Li et al., Section 3.2.4</a>). FSDP has the same context manager with one twist: inside <code class="language-plaintext highlighter-rouge">no_sync()</code> the accumulated gradients are kept <em>unsharded</em> on every rank, so the memory saving of sharding is temporarily given up in exchange for the communication saving (<a href="https://docs.pytorch.org/docs/stable/fsdp.html">FSDP docs</a>).</li> </ul> <p>There is one more identity worth stating now and returning to later: under pipeline parallelism, the microbatches that fill the pipeline <em>are</em> the gradient-accumulation microbatches. The pipeline’s \(m\) and the accumulation’s \(k\) are the same number, which is why choosing it is a throughput decision (the bubble) and a memory decision (activations in flight) at once.</p> <h5 id="zero-removing-the-redundancy"><strong>ZeRO: removing the redundancy</strong></h5> <p>Look at what the \(p\) replicas in DDP are holding. Every rank has the same optimizer state, the same master weights, the same gradients after the all-reduce, and the same weights. Everything but the activations is replicated \(p\) times. ZeRO (<a href="https://arxiv.org/abs/1910.02054">Rajbhandari et al.</a>) partitions that state so each rank holds only \(1/p\) of it, in three stages of increasing aggressiveness. Let \(D\) be the data-parallel degree and \(\Psi\) the parameter count:</p> <table> <thead> <tr> <th>Stage</th> <th>What is partitioned</th> <th>Bytes per parameter per rank</th> <th>Communication per step, relative to DDP</th> </tr> </thead> <tbody> <tr> <td>Baseline DDP</td> <td>nothing</td> <td>\(16\)</td> <td>\(1\times\) (one all-reduce, \(2\Psi\) elements)</td> </tr> <tr> <td>ZeRO-1 (\(P_{os}\))</td> <td>optimizer state: master weights, \(m\), \(v\)</td> <td>\(4 + \dfrac{12}{D}\)</td> <td>\(1\times\)</td> </tr> <tr> <td>ZeRO-2 (\(P_{os+g}\))</td> <td>plus gradients</td> <td>\(2 + \dfrac{14}{D}\)</td> <td>\(1\times\)</td> </tr> <tr> <td>ZeRO-3 (\(P_{os+g+p}\))</td> <td>plus parameters</td> <td>\(\dfrac{16}{D}\)</td> <td>\(1.5\times\) (\(3\Psi\) elements)</td> </tr> </tbody> </table> <p>The memory column is the paper’s Figure 1 and Section 5; the communication column is its Section 7. What the mechanics look like at each stage:</p> <p><strong>ZeRO-1</strong> is the ring identity from the previous section put to work. Instead of an all-reduce, do a <strong>reduce-scatter</strong> of the gradients: afterwards rank \(i\) holds the fully summed gradient for its \(1/D\) slice of the parameters and nothing else. Rank \(i\) owns the master weights and Adam moments for exactly that slice, so it runs the optimizer step on its slice alone. Then <strong>all-gather</strong> the updated 16-bit weights so every rank has the full model again for the next forward pass. Reduce-scatter plus all-gather moves \(\Psi + \Psi = 2\Psi\) elements, the same as the all-reduce it replaced, and the optimizer state, 12 of the 16 bytes, is now divided by \(D\). Nearly a 4x memory reduction for free, which is why this stage is close to universal; Megatron-LM’s <code class="language-plaintext highlighter-rouge">--use-distributed-optimizer</code> is the same idea.</p> <p><strong>ZeRO-2</strong> notices that after the reduce-scatter a rank only needs the gradient of its own slice, so gradients can be reduced bucket by bucket during the backward pass and discarded once they are reduced to their owner. The gradient’s 2 bytes drop to \(2/D\). The communication volume is unchanged.</p> <p><strong>ZeRO-3</strong> partitions the parameters themselves, so no rank ever holds the full model. That means a rank has to <strong>all-gather</strong> each layer’s parameters right before it needs them in the forward pass, run the layer, and free the gathered copy; then all-gather them <em>again</em> in the backward pass, since the weights were freed after the forward. Two all-gathers of \(\Psi\) plus the reduce-scatter of \(\Psi\) is \(3\Psi\) elements, and \(3\Psi / 2\Psi\) is the \(1.5\times\). In exchange, everything, all 16 bytes, is divided by \(D\).</p> <p>Run the 70B model on 64 GPUs through the table (16-bit gradients, \(\Psi = 70 \times 10^9\)):</p> <table> <thead> <tr> <th>Stage</th> <th>Static memory per GPU</th> </tr> </thead> <tbody> <tr> <td>DDP</td> <td>\(16 \times 70 = 1{,}120\) GB</td> </tr> <tr> <td>ZeRO-1</td> <td>\((4 + 12/64) \times 70 = 293\) GB</td> </tr> <tr> <td>ZeRO-2</td> <td>\((2 + 14/64) \times 70 = 155\) GB</td> </tr> <tr> <td>ZeRO-3</td> <td>\((16/64) \times 70 = 17.5\) GB</td> </tr> </tbody> </table> <p>Only ZeRO-3 fits in 80 GB, and it fits with room for activations. Notice that ZeRO-1 and ZeRO-2 are capped by the 4 and 2 bytes that stay replicated, so no number of GPUs makes them fit a 70B model on an 80 GB card; only ZeRO-3 scales the <em>whole</em> footprint with \(D\). And notice the flip side: at \(D = 8\), ZeRO-3 gives \(140\) GB per GPU, which still does not fit, so a single node cannot train a 70B model with data parallelism alone. That is the seam where model parallelism, the next two sections, takes over.</p> <h5 id="fsdp-zero-3-as-the-pytorch-engine-builds-it"><strong>FSDP: ZeRO-3 as the PyTorch engine builds it</strong></h5> <p>PyTorch’s <code class="language-plaintext highlighter-rouge">FullyShardedDataParallel</code> is the same idea as ZeRO’s third stage, and its paper describes the engineering that makes the \(1.5\times\) communication survivable (<a href="https://arxiv.org/abs/2304.11277">Zhao et al.</a>):</p> <ul> <li><strong>Units and flat parameters.</strong> The model is divided into units, typically one transformer layer each. All the parameters of a unit are flattened into one contiguous 1-D <code class="language-plaintext highlighter-rouge">FlatParameter</code>, which is chunked evenly across ranks. One unit is one all-gather, which keeps the collective count low and each collective large, per the cost model.</li> <li><strong>The lifecycle.</strong> For each unit, in order: all-gather its flat parameter, run the forward, free the peer shards. In the backward pass: all-gather again, run the backward, <strong>reduce-scatter</strong> the gradient so each rank keeps the shard it owns, free. After the backward, each rank holds sharded parameters, sharded gradients, and sharded optimizer state, and runs the optimizer step on its shard.</li> <li><strong>Prefetching.</strong> The all-gather for unit \(i+1\) is issued <em>before</em> unit \(i\)’s compute finishes (forward prefetch), and in the backward pass the next unit’s all-gather is issued ahead of the current unit’s reduce-scatter (backward prefetch, <a href="https://arxiv.org/abs/2304.11277">Sections 3.3.2 and 3.3.3</a>). This is the overlap that hides the extra all-gather. Without it, every layer would wait on its own parameters arriving over the network, and the extra \(\Psi\) of communication would be fully exposed. The sentence to remember: <strong>FSDP is viable because the gather for the next layer overlaps the compute of the current one.</strong></li> </ul> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/fsdp-lifecycle.svg" sizes="95vw"/> <img src="/assets/img/llm-training/fsdp-lifecycle.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="FSDP unit lifecycle with prefetch" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The FSDP lifecycle of three units on one rank. Each unit's parameters are all-gathered just before use and freed after, in both passes; gradients are reduce-scattered in the backward pass. The next unit's all-gather is prefetched during the current unit's compute so the communication stream stays ahead of the compute stream. Editable source: <a href="/assets/img/llm-training/fsdp-lifecycle.excalidraw">fsdp-lifecycle.excalidraw</a>. </div> <p>Two practical variants are worth knowing.</p> <p><strong>Keep the weights after the forward pass.</strong> Llama 3 uses FSDP as its outermost parallelism and does <em>not</em> reshard the parameters after the forward pass, keeping the gathered weights around so the backward pass needs no second all-gather (<a href="https://arxiv.org/abs/2407.21783">Section 3.3.2</a>): communication back down to \(2\Psi\), memory back up by the gathered weights, a trade that makes sense when the other parallelism dimensions have already made each rank’s share of the weights small.</p> <p><strong>Hybrid sharding.</strong> Full sharding across all \(W\) ranks puts the \(3\Psi\) of all-gather and reduce-scatter traffic on whatever link connects the farthest ranks, which across nodes is the slow one. Hybrid sharding (<a href="https://arxiv.org/abs/2304.11277">Zhao et al., Section 3.2</a>) picks a <strong>sharding factor</strong> \(F\) smaller than \(W\), say \(F = 8\) for one node, shards each parameter only within groups of \(F\) ranks, and <em>replicates</em> across the \(W/F\) groups. Two things change:</p> <ul> <li><em>Memory.</em> Each rank now holds \(1/F\) of the state rather than \(1/W\): the 16 bytes per parameter are divided by 8, not by 64. For the 70B model that is 140 GB per GPU, which does not fit, so on its own hybrid sharding suits models that are only somewhat too large to replicate; for larger models it is combined with tensor and pipeline parallelism, which shrink each group’s share first.</li> <li><em>Communication.</em> The all-gathers and the reduce-scatter, the traffic that made ZeRO-3 cost \(1.5\times\) DDP, now run <strong>inside the node over NVLink</strong>. What crosses the network is only the last step of the gradient reduction: after each group has reduce-scattered, every rank holds a \(\Psi/F\) gradient shard that must still be averaged with the same shard on the other \(W/F\) replicas, one all-reduce of \(\Psi/F\) elements. The paper states the equivalence directly: the single reduce-scatter over all ranks becomes a reduce-scatter within each sharded group followed by an all-reduce within each replicated group.</li> </ul> <p>Put numbers on it for the 70B model on 64 GPUs. Fully sharded across all 64, each rank moves about \(3 \times 140 \times 63/64 = 413\) GB per step, much of it across InfiniBand at 50 GB/s, on the order of 8 s if exposed. Hybrid with \(F = 8\): the in-node part is \(3 \times 140 \times 7/8 = 368\) GB per rank at 450 GB/s, about 0.8 s, and the cross-node part is an all-reduce of a \(140/8 = 17.5\) GB shard across 8 replicas, \(2 \times 7/8 \times 17.5 = 31\) GB per rank at 50 GB/s, about 0.6 s. The slow link carries a twelfth of the bytes it did, at the price of holding eight times the state per GPU. That is the trade hybrid sharding offers, and it is the same rule as everywhere else in this post: put the heavy traffic on the fast link.</p> <h5 id="offloading"><strong>Offloading</strong></h5> <p>If the state still does not fit, it can leave the GPU. ZeRO-Offload keeps the 16-bit parameters and the forward and backward computation on the GPU but moves the fp32 master weights, the Adam moments, and the 16-bit gradients to host memory, where a CPU implementation of Adam runs the update (<a href="https://arxiv.org/abs/2101.06840">Ren et al.</a>). ZeRO-Infinity extends the same idea on top of ZeRO-3, spilling parameters and optimizer state across GPU, CPU, and NVMe (<a href="https://arxiv.org/abs/2104.07857">Rajbhandari et al. 2021</a>).</p> <p>The price is on the bandwidth ladder: PCIe Gen5 moves 64 GB/s per direction, seven times less than NVLink and fifty times less than HBM. Offloading is the right call when the run is capacity-bound and has wall-clock to spare (fine-tuning a large model on few GPUs); it is the wrong call when the run is already communication-bound, because it adds the slowest link in the machine to the critical path.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2006.15704">PyTorch Distributed: Experiences on Accelerating Data Parallel Training - Li et al.</a>, <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.parallel.DistributedDataParallel.html">DistributedDataParallel docs</a></li> <li><a href="https://arxiv.org/abs/1910.02054">ZeRO: Memory Optimizations Toward Training Trillion Parameter Models - Rajbhandari et al.</a></li> <li><a href="https://arxiv.org/abs/2304.11277">PyTorch FSDP: Experiences on Scaling Fully Sharded Data Parallel - Zhao et al.</a>, <a href="https://docs.pytorch.org/docs/stable/fsdp.html">FSDP docs</a></li> <li><a href="https://arxiv.org/abs/2101.06840">ZeRO-Offload - Ren et al.</a>, <a href="https://arxiv.org/abs/2104.07857">ZeRO-Infinity - Rajbhandari et al.</a></li> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a></li> <li><a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/training/arguments.py">Megatron-LM arguments</a></li> </ul> <hr/> <h4 id="tensor-parallelism"><strong>Tensor Parallelism</strong></h4> <p>Data parallelism splits the batch. Tensor parallelism (TP) splits the model, one weight matrix at a time, so that each rank holds a slice of every matrix and the ranks cooperate on every layer. The scheme every framework uses is Megatron-LM’s (<a href="https://arxiv.org/abs/1909.08053">Shoeybi et al.</a>), and its cleverness is entirely in <em>which way</em> each matrix is cut.</p> <h5 id="the-mlp-column-then-row"><strong>The MLP: column, then row</strong></h5> <p>The transformer MLP is two matmuls with a nonlinearity between them:</p> \[Y = \mathrm{GeLU}(X A)\, B\] <p>with \(X\) of shape \((\text{tokens} \times h)\), \(A\) of shape \((h \times 4h)\), and \(B\) of shape \((4h \times h)\). Suppose two ranks. There are two ways to cut \(A\) in half:</p> <p><strong>Cut \(A\) along its rows</strong> (the \(h\) dimension, the one the matmul contracts over). Then \(X\) has to be cut the same way, and \(XA = X_1 A_1 + X_2 A_2\): each rank produces a <em>partial sum</em>, and the two have to be added before the GeLU, because</p> \[\mathrm{GeLU}(X_1 A_1 + X_2 A_2) \neq \mathrm{GeLU}(X_1 A_1) + \mathrm{GeLU}(X_2 A_2)\] <p>GeLU is nonlinear, so it cannot be pushed through a sum. A row split forces a synchronization in the middle of the block.</p> <p><strong>Cut \(A\) along its columns</strong> (the \(4h\) dimension). Then \(XA = [\,X A_1 \;\; X A_2\,]\): each rank takes the <em>full</em> \(X\) and produces its own half of the columns, and since GeLU is elementwise it applies to each half independently: \([\,\mathrm{GeLU}(X A_1) \;\; \mathrm{GeLU}(X A_2)\,]\). <strong>No communication.</strong> This is why the first matrix is split column-wise (<a href="https://arxiv.org/abs/1909.08053">Shoeybi et al., Section 3, Eq. 1 to 3</a>), and it is the detail the whole design rests on.</p> <p>Now the second matmul. The output of the first is column-split, which means each rank holds a slice of the \(4h\) dimension, and that is exactly the dimension \(B\) contracts over. So cut \(B\) <strong>along its rows</strong> to match: rank \(i\) computes \(Y_i = \mathrm{GeLU}(X A_i)\, B_i\) from purely local data, and</p> \[Y = Y_1 + Y_2\] <p>One <strong>all-reduce</strong> sums the partials and every rank holds \(Y\). Column-then-row is the pairing that puts the single unavoidable sync at the end of the block rather than in the middle of it.</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/tp-mlp.svg" sizes="95vw"/> <img src="/assets/img/llm-training/tp-mlp.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Tensor-parallel MLP: column split then row split" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The tensor-parallel MLP on two ranks. A is split by columns so each rank computes its own GeLU with no communication; B is split by rows so each rank's partial product is summed by one all-reduce. f and g are the two conjugate operators described next. Editable source: <a href="/assets/img/llm-training/tp-mlp.excalidraw">tp-mlp.excalidraw</a>. </div> <h5 id="the-f-and-g-operators"><strong>The \(f\) and \(g\) operators</strong></h5> <p>Megatron writes the communication as two operators placed at the block’s boundaries (<a href="https://arxiv.org/abs/1909.08053">Figure 3</a>):</p> <table> <thead> <tr> <th>Operator</th> <th>Forward pass</th> <th>Backward pass</th> <th>Where</th> </tr> </thead> <tbody> <tr> <td>\(f\)</td> <td>identity (every rank already has the full \(X\))</td> <td><strong>all-reduce</strong></td> <td>entering the block</td> </tr> <tr> <td>\(g\)</td> <td><strong>all-reduce</strong> (sum the \(Y_i\))</td> <td>identity</td> <td>leaving the block</td> </tr> </tbody> </table> <p>They are called conjugates, and the reason each has the backward it has is worth seeing rather than memorizing. \(g\)’s backward is an identity because \(Y = Y_1 + Y_2\) means \(\partial L / \partial Y_i = \partial L / \partial Y\) for every \(i\); each rank already holds the incoming gradient and just uses it. \(f\)’s backward is an all-reduce because \(X\) was <strong>used by every rank</strong>, each computing its own \(X A_i\), and a value used in several places accumulates the gradient from each use. That is the fork rule from the <a href="/blog/2026/layernorm-rmsnorm/">LayerNorm post</a>, applied across GPUs: \(\partial L / \partial X\) is the sum over ranks of each rank’s local contribution, and summing across ranks is an all-reduce.</p> <p>So the MLP block costs one all-reduce in the forward pass and one in the backward pass.</p> <h5 id="attention-split-by-heads"><strong>Attention: split by heads</strong></h5> <p>Multi-head attention has the same two-matmul shape, and heads make the split natural. The query, key, and value projections are cut <strong>column-wise</strong>, so that rank \(i\) ends up holding <em>whole heads</em>: with \(a\) heads and \(t\) ranks, \(a/t\) complete heads per rank, each with its full \(Q\), \(K\), and \(V\) for those heads. Everything inside attention, the scores, the softmax over keys, the weighted sum over values, happens <em>within</em> a head, so each rank runs attention for its own heads with no communication at all. The output projection \(W_O\) contracts over the concatenated-heads dimension, which is exactly the dimension that is now sharded, so it is cut <strong>row-wise</strong>, and one all-reduce sums the ranks’ partial outputs (<a href="https://arxiv.org/abs/1909.08053">Shoeybi et al., Figure 3b</a>). Same \(f\) and \(g\), same one-all-reduce-per-direction.</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/tp-attention.svg" sizes="95vw"/> <img src="/assets/img/llm-training/tp-attention.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Tensor-parallel attention: split by heads" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Tensor-parallel attention on two ranks. The QKV projections are column-split so each rank owns complete heads and runs attention for them locally; the output projection is row-split and one all-reduce sums the partial outputs. Editable source: <a href="/assets/img/llm-training/tp-attention.excalidraw">tp-attention.excalidraw</a>. </div> <p>Putting the two blocks together, a tensor-parallel transformer layer costs <strong>two all-reduces in the forward pass and two in the backward pass</strong>, four communication operations per layer (<a href="https://arxiv.org/abs/1909.08053">Figure 4</a>). The layer norms, dropouts, and residual additions that sit between the blocks are not split; every rank computes them redundantly on the full hidden state, which is cheaper than communicating and is the redundancy sequence parallelism will remove below.</p> <h5 id="what-the-four-all-reduces-cost"><strong>What the four all-reduces cost</strong></h5> <p>Each all-reduce is over the hidden state for one microbatch: a \(b \times s \times h\) tensor, \(S = 2\,b\,s\,h\) bytes in bf16. From the ring analysis, each rank sends and receives \(2S(t-1)/t\) per all-reduce, and there are four per layer, so per layer per rank per microbatch:</p> \[4 \times 2 \times 2\,b\,s\,h \times \frac{t-1}{t} \;=\; 16\,b\,s\,h\,\frac{t-1}{t} \text{ bytes}\] <p>Take the 70B model (\(h = 8192\), 80 layers) with \(b \times s = 8192\) tokens per microbatch and \(t = 8\). One hidden-state tensor is \(2 \times 8192 \times 8192 = 134\) MB. Then:</p> <table> <thead> <tr> <th>Quantity</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Per all-reduce, per rank</td> <td>\(2 \times 134 \text{ MB} \times 7/8 = 235\) MB</td> </tr> <tr> <td>Per layer (4 all-reduces)</td> <td>\(0.94\) GB</td> </tr> <tr> <td>Per microbatch, 80 layers</td> <td><strong>75 GB</strong> sent and received per rank</td> </tr> <tr> <td>Time on NVLink, 450 GB/s per direction</td> <td>\(\approx 0.17\) s</td> </tr> <tr> <td>Time on PCIe Gen5, 64 GB/s</td> <td>\(\approx 1.2\) s</td> </tr> <tr> <td>Time on 400 Gb/s InfiniBand, 50 GB/s</td> <td>\(\approx 1.5\) s</td> </tr> </tbody> </table> <p>Compare that with the compute for the same microbatch: \(6 \times 70 \times 10^9 \times 8192 = 3.4 \times 10^{15}\) FLOPs spread over 8 GPUs at 989 TFLOPS each is about \(0.43\) s at 100% utilization and about \(1.1\) s at a realistic 40%.</p> <h5 id="why-tensor-parallelism-stays-inside-the-node"><strong>Why tensor parallelism stays inside the node</strong></h5> <p>The table above is the whole argument, but it is worth walking through slowly, because “TP stays in the node” is the single most consequential placement rule in this post and it should feel inevitable rather than remembered.</p> <p><strong>First, the traffic is on the critical path.</strong> The four all-reduces per layer are not background work. Each one sits between two matmuls that depend on it: the row-parallel matmul produces partial sums, the all-reduce adds them, and the <em>next</em> block cannot start until the sum exists, because its input is that sum. So every layer, twice per direction, the GPUs stop computing and wait for a collective to finish. Unlike the data-parallel gradient sync, which can hide behind the backward pass, there is very little compute to hide a tensor-parallel all-reduce behind. Its time adds almost directly to the step. (Megatron has a <code class="language-plaintext highlighter-rouge">--tp-comm-overlap</code> option that recovers part of it by splitting the matmul and overlapping pieces, but the dependency is structural.)</p> <p><strong>Second, there are a lot of them.</strong> Four per layer times 80 layers is 320 all-reduces per microbatch, in the forward and backward passes together, each on a hidden-state tensor of \(b \times s \times h\) elements. That is what produced the 75 GB per rank per microbatch above.</p> <p><strong>Third, put the bytes on each rung of the bandwidth ladder</strong> and compare with the compute of the same microbatch:</p> <table> <thead> <tr> <th>Link</th> <th>One-way bandwidth</th> <th>Time for the 75 GB</th> <th>Against about 1.1 s of compute</th> </tr> </thead> <tbody> <tr> <td>NVLink, inside the node</td> <td>450 GB/s</td> <td>0.17 s</td> <td>a sizable tax, partly recoverable by overlap</td> </tr> <tr> <td>PCIe Gen5</td> <td>64 GB/s</td> <td>1.2 s</td> <td>longer than the compute itself</td> </tr> <tr> <td>InfiniBand, across nodes</td> <td>50 GB/s</td> <td>1.5 s</td> <td>longer than the compute itself</td> </tr> </tbody> </table> <p>Inside the node the all-reduces cost a noticeable fraction of the step and are worth engineering around. Across nodes they would take longer than all of the arithmetic they are interleaved with; the GPUs would spend most of every layer waiting on the network, and adding more of them would make it worse. That is the sense in which tensor parallelism cannot leave the node: not that it is impossible, but that its 320 synchronizations per microbatch are only affordable on a link fast enough to make each one short.</p> <p><strong>Fourth, this is what the Megatron authors found, and said.</strong> Their communication analysis gives the per-layer tensor-parallel volume as \(8bsh(t-1)/t\) per device per microbatch, and they state that when \(t\) is larger than the number of GPUs in a single node, performing tensor parallelism across the slower inter-node links can be impractical (<a href="https://arxiv.org/abs/2104.04473">Narayanan et al., Section 3.2</a>). They then measured it, sweeping tensor-parallel and pipeline-parallel sizes for a fixed 64-GPU budget, and throughput per GPU peaks when the tensor-parallel size equals the number of GPUs in a node, 8 on their DGX A100 nodes, and falls off beyond it (<a href="https://arxiv.org/abs/2104.04473">Section 5.4.1, Figure 13</a>).</p> <p><strong>So the rule.</strong> Tensor parallelism is set to at most the number of GPUs that share NVLink, 8 on a DGX-class node, and never more. If the model still does not fit at TP 8, the answer is a different dimension, pipeline parallelism, whose communication is one activation per stage boundary rather than four all-reduces per layer, and which therefore tolerates the slow link. Llama 3’s configuration table has TP at exactly 8 in every row for this reason, and the composition section builds the rest of the placement on top of it.</p> <p>The next section picks up the redundancy this leaves behind, the norm and dropout activations replicated on every rank, and the two other dimensions that split along the sequence and across experts.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/1909.08053">Megatron-LM: Training Multi-Billion Parameter Language Models Using Model Parallelism - Shoeybi et al.</a></li> <li><a href="https://arxiv.org/abs/2104.04473">Efficient Large-Scale Language Model Training on GPU Clusters Using Megatron-LM - Narayanan et al.</a></li> <li><a href="https://github.com/NVIDIA/Megatron-LM/blob/main/README.md">Megatron-LM README</a></li> <li><a href="/blog/2026/layernorm-rmsnorm/">LayerNorm and RMSNorm - the fork rule</a></li> </ul> <hr/> <h4 id="sequence-context-and-expert-parallelism"><strong>Sequence, Context, and Expert Parallelism</strong></h4> <p>The four dimensions so far, data, tensor, and (next section) pipeline parallelism, split the batch, the matrices, and the layer stack. Three more dimensions split along axes those leave untouched, and each exists because one of the earlier accountings left a term behind:</p> <table> <thead> <tr> <th>Dimension</th> <th>What it splits</th> <th>The term it attacks</th> <th>Communication it adds</th> </tr> </thead> <tbody> <tr> <td>Sequence parallelism (SP)</td> <td>the norm and dropout activations between tensor-parallel blocks, along the sequence, within a TP group</td> <td>the \(10\,sbh\) that TP replicates on every rank</td> <td>none beyond TP’s (an all-reduce becomes a reduce-scatter plus an all-gather)</td> </tr> <tr> <td>Context parallelism (CP)</td> <td>the whole sequence, for every layer, across a CP group</td> <td>the \(b \times s\) activation scaling when \(s\) itself is too large</td> <td>\(K\) and \(V\) exchanged between ranks in attention</td> </tr> <tr> <td>Expert parallelism (EP)</td> <td>the experts of a mixture-of-experts layer across an EP group</td> <td>the parameter count of a model whose weights are mostly experts each token never touches</td> <td>two all-to-alls per MoE layer, dispatch and combine</td> </tr> </tbody> </table> <h5 id="sequence-parallelism-the-10-that-tensor-parallelism-leaves-behind"><strong>Sequence parallelism: the 10 that tensor parallelism leaves behind</strong></h5> <p>Look again at the per-layer activation formula with \(t\)-way tensor parallelism from the recomputation section, \(sbh(10 + 24/t + 5as/(ht))\). The \(24/t\) and the attention term shrink with \(t\); the <strong>10</strong> does not. Those 10 bytes per element are the activations in the parts of the layer tensor parallelism does not touch, the layer norm inputs, the dropout masks, the residual stream, which every rank computes and stores in full. At \(t = 8\) they are more than a third of the remaining activation memory.</p> <p><a href="https://arxiv.org/abs/2205.05198">Korthikanti et al.</a> observe that those operations are <strong>pointwise along the sequence</strong>: a layer norm of token 5 does not depend on token 6. So the non-tensor-parallel regions can be split along the sequence dimension instead, each rank holding \(s/t\) tokens of the full hidden state. The catch is at the boundaries, where the tensor-parallel matmuls need the whole sequence:</p> <table> <thead> <tr> <th>Boundary</th> <th>Without sequence parallelism</th> <th>With sequence parallelism</th> </tr> </thead> <tbody> <tr> <td>Entering a TP block (\(f\))</td> <td>identity forward, all-reduce backward</td> <td><strong>all-gather</strong> forward (assemble the full sequence), <strong>reduce-scatter</strong> backward</td> </tr> <tr> <td>Leaving a TP block (\(g\))</td> <td>all-reduce forward, identity backward</td> <td><strong>reduce-scatter</strong> forward (sum the partials <em>and</em> hand each rank its sequence slice), <strong>all-gather</strong> backward</td> </tr> </tbody> </table> <p>The paper names the new pair \(g\) and \(\bar g\). The point is that an all-reduce <em>is</em> a reduce-scatter plus an all-gather, so replacing one all-reduce with one of each moves exactly the same number of bytes: <strong>sequence parallelism costs no extra communication</strong>, and the paper’s measurement is that it is slightly <em>faster</em> (the \(-3\)% in the recomputation section’s table), since the pointwise ops now run on \(1/t\) of the data. What it buys is the 10 becoming \(10/t\), so the per-layer formula becomes \(\frac{sbh}{t}(34 + 5as/h)\): every term divided by \(t\). In Megatron-LM it is the <code class="language-plaintext highlighter-rouge">--sequence-parallel</code> flag, and it only makes sense together with tensor parallelism, which is why the arguments file disables it when \(t = 1\).</p> <p>The phrase to hold onto: <em>same communication volume, strictly less activation memory.</em></p> <h5 id="context-parallelism-when-the-sequence-is-the-problem"><strong>Context parallelism: when the sequence is the problem</strong></h5> <p>Sequence parallelism splits only the cheap regions between tensor-parallel blocks. Context parallelism (CP) splits the <strong>input sequence itself</strong>, so that with \(\text{CP} = c\) each rank holds a contiguous chunk of \(s/c\) tokens through <em>every</em> layer. Almost every operation in a transformer layer is per-token, the norms, the MLP, the QKV and output projections, so they all run unchanged on a chunk and never need to know the other chunks exist (<a href="https://docs.nvidia.com/megatron-core/developer-guide/latest/user-guide/features/context_parallel.html">Megatron Core context parallelism</a>). Activation memory per rank, including the quadratic attention term, drops by \(c\). That is the memory story, and it is why CP is the dimension that unlocks 128K-token training: Llama 3 uses \(\text{CP} = 16\) for its long-context stage and 1 elsewhere (<a href="https://arxiv.org/abs/2407.21783">Table 4</a>).</p> <p>The one operation that is <em>not</em> per-token is attention. Each query has to see the keys and values of the whole sequence, and under CP the \(K\) and \(V\) for the other chunks live on other ranks. There are three ways to bring them together, and all three are in production:</p> <table> <thead> <tr> <th>Method</th> <th>Mechanism</th> <th>Communication</th> <th>Trade</th> </tr> </thead> <tbody> <tr> <td><strong>Ring</strong> (<a href="https://arxiv.org/abs/2310.01889">Ring Attention</a>; Megatron <code class="language-plaintext highlighter-rouge">p2p</code>)</td> <td>ranks pass their \(K\) and \(V\) chunk to the next rank around a ring, \(c-1\) times; each rank accumulates attention against each visiting block with the online-softmax rescaling FlashAttention uses, while the next block is already in flight</td> <td>point-to-point, overlapped with attention compute</td> <td>fully hidden if a block’s attention takes longer than its transfer; more complex with irregular masks</td> </tr> <tr> <td><strong>All-gather</strong> (Llama 3; Megatron <code class="language-plaintext highlighter-rouge">all_gather</code>)</td> <td>all-gather the full \(K\) and \(V\) first, then run ordinary attention for the local queries</td> <td>one all-gather of \(K\) and \(V\) per layer, on the critical path</td> <td>simple, and any mask works, including the per-document masks Llama 3 needs</td> </tr> <tr> <td><strong>All-to-all</strong> (<a href="https://arxiv.org/abs/2309.14509">DeepSpeed-Ulysses</a>; Megatron <code class="language-plaintext highlighter-rouge">a2a</code>)</td> <td>an all-to-all re-shards \(Q\), \(K\), \(V\) from sequence-sharded to <strong>head</strong>-sharded, so each rank gets the full sequence for \(1/c\) of the heads, computes full attention for them, and a second all-to-all re-shards the output back along the sequence</td> <td>two all-to-alls per layer, \(4sh/c\) bytes-equivalent per link</td> <td>lowest volume, but \(c\) cannot exceed the number of (KV) heads</td> </tr> </tbody> </table> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/cp-attention.svg" sizes="95vw"/> <img src="/assets/img/llm-training/cp-attention.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Context parallelism: ring and all-gather attention" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Context parallelism on four ranks. Every rank owns one chunk of the sequence for all layers. For attention, the ring variant rotates each rank's K and V block around the ring over three steps, accumulating attention block by block; the all-gather variant collects every K and V first and then runs attention for the local queries. Editable source: <a href="/assets/img/llm-training/cp-attention.excalidraw">cp-attention.excalidraw</a>. </div> <p>Llama 3’s choice of the all-gather method, with its latency deliberately exposed, is the instructive one, because the report explains the arithmetic (<a href="https://arxiv.org/abs/2407.21783">Section 3.3.2</a>). Under grouped-query attention the \(K\) and \(V\) tensors are much smaller than \(Q\) (Llama 3 70B has 8 KV heads against 64 query heads), so the gathered bytes are small; and attention’s compute grows as \(O(s^2)\) while the gather grows as \(O(s)\), so at 128K tokens the gather is a rounding error next to the attention it feeds. Two further details from the same paragraph are worth knowing. First, a causal mask makes the work uneven: the chunk at the end of the sequence attends to everything, the chunk at the start to almost nothing. Llama 3 balances it by cutting the sequence into \(2c\) chunks and giving rank \(i\) chunks \(i\) and \(2c - 1 - i\), one light and one heavy; Megatron Core does the same kind of balancing for its ring implementation. Second, GQA helps CP directly: the Megatron docs note that MQA and GQA reduce the CP communication volume for exactly this reason, only the few KV heads travel.</p> <p>Where CP sits in the hierarchy follows from its traffic: per layer, like TP, but overlappable or small, unlike TP. Llama 3 places it second, \([\text{TP}, \text{CP}, \text{PP}, \text{DP}]\), and the total GPU count is \(\text{TP} \times \text{CP} \times \text{PP} \times \text{DP}\) (<a href="https://docs.nvidia.com/megatron-core/developer-guide/latest/user-guide/features/context_parallel.html">Megatron Core docs</a>).</p> <h5 id="the-naming-collision-settled"><strong>The naming collision, settled</strong></h5> <p>“Sequence parallelism” is used for both of the previous two ideas, and the collision is a genuine source of confusion:</p> <table> <thead> <tr> <th> </th> <th>Megatron sequence parallelism</th> <th>Context parallelism</th> </tr> </thead> <tbody> <tr> <td>Introduced in</td> <td><a href="https://arxiv.org/abs/2205.05198">Korthikanti et al. 2022</a></td> <td><a href="https://arxiv.org/abs/2105.13120">Li et al. 2021</a>, which calls it “sequence parallelism”; <a href="https://arxiv.org/abs/2310.01889">Ring Attention</a>; <a href="https://arxiv.org/abs/2309.14509">DeepSpeed-Ulysses</a>, which also calls it sequence parallelism</td> </tr> <tr> <td>What is split along the sequence</td> <td>only the norm and dropout activations between TP blocks, within a TP group</td> <td>the input and every activation, for the whole network</td> </tr> <tr> <td>Attention</td> <td>unchanged: each rank has its heads’ full \(K\) and \(V\)</td> <td>each rank has \(K\) and \(V\) for its own chunk only and needs everyone else’s</td> </tr> <tr> <td>Purpose</td> <td>activation memory, at no extra communication</td> <td>sequences too long for one GPU at all</td> </tr> </tbody> </table> <p>When a paper or a flag says “sequence parallel”, check which of the two it means: if it is a companion to tensor parallelism and costs nothing, it is the first; if it is about long context and touches attention, it is the second, and modern frameworks call that one context parallelism.</p> <h5 id="expert-parallelism-splitting-a-model-that-is-mostly-experts"><strong>Expert parallelism: splitting a model that is mostly experts</strong></h5> <p>A mixture-of-experts (MoE) layer replaces the single dense MLP with \(E\) separate MLPs, the <strong>experts</strong>, and a small <strong>router</strong> that picks, for each token, the \(k\) experts that token will go through; the token’s output is the gate-weighted sum of those \(k\) experts’ outputs. GShard used top-2 routing (<a href="https://arxiv.org/abs/2006.16668">Lepikhin et al.</a>), the Switch Transformer argued for top-1 (<a href="https://arxiv.org/abs/2101.03961">Fedus et al.</a>), and DeepSeek-V3 routes each token to 8 of 256 routed experts plus 1 shared expert that every token uses (<a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3, Section 4.2</a>). The point of the design is the ratio it creates: compute per token scales with \(k\), parameters scale with \(E\). DeepSeek-V3 has 671B parameters and activates 37B per token.</p> <p>That ratio is a problem for everything in this post that assumed parameters and FLOPs go together. <strong>All 671B parameters must be resident</strong>, because any token may route to any expert, so the \(16N\) accounting applies to the full count while the \(6N\) per token applies only to the active slice. And tensor parallelism is a poor fit for experts: each expert is a small MLP (DeepSeek-V3’s have an intermediate width of 2048, against 7168 for the hidden size), so slicing it eight ways produces GEMMs too thin to run efficiently while still paying TP’s collectives; Megatron’s MoE guide recommends an expert tensor-parallel degree of 1 for fine-grained MoE models for exactly this reason (<a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/core/transformer/moe/README.md">Megatron Core MoE README</a>).</p> <p><strong>Expert parallelism (EP)</strong> is the natural split instead: keep each expert whole and place different experts on different GPUs, \(E / \text{EP}\) per rank. DeepSeek-V3 deploys its 256 routed experts uniformly over 64 GPUs across 8 nodes, four experts per GPU per layer (<a href="https://arxiv.org/abs/2412.19437">Section 4.2</a>). The consequence is that tokens have to travel to their experts, and that traffic is the <strong>all-to-all</strong> from the primitives table:</p> <ol> <li><strong>Dispatch.</strong> After the router decides, every rank sends each of its tokens’ hidden vectors to the ranks hosting that token’s \(k\) experts. Since every rank sends to every other rank, this is one all-to-all.</li> <li><strong>Compute.</strong> Each rank runs its resident experts on whatever tokens arrived, one grouped or batched GEMM per expert.</li> <li><strong>Combine.</strong> A second all-to-all sends each expert output back to the token’s home rank, which sums the \(k\) results with the router’s gate weights.</li> </ol> <p>The backward pass mirrors both, so an MoE layer costs two all-to-alls forward and two backward. GShard framed the same movement as an all-to-all resharding on TPUs (<a href="https://arxiv.org/abs/2006.16668">Section 3.3.2</a>); Megatron-LM’s <code class="language-plaintext highlighter-rouge">--moe-token-dispatcher-type alltoall</code> and the DeepEP-backed dispatcher are the GPU versions, with <code class="language-plaintext highlighter-rouge">--expert-model-parallel-size</code> setting the EP degree (<a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/core/transformer/moe/README.md">MoE README</a>).</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/ep-all-to-all.svg" sizes="95vw"/> <img src="/assets/img/llm-training/ep-all-to-all.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Expert parallelism: dispatch, expert compute, combine" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Expert parallelism on four ranks with eight experts, two per rank, and top-2 routing. The router on each rank picks two experts per token; a dispatch all-to-all carries each token's hidden vector to the ranks holding those experts, each rank runs its resident experts on what arrived, and a combine all-to-all returns the outputs to be gate-weighted and summed at home. Editable source: <a href="/assets/img/llm-training/ep-all-to-all.excalidraw">ep-all-to-all.excalidraw</a>. </div> <p><strong>What it costs.</strong> Each token’s \(h\)-vector is sent to \(k\) experts and \(k\) results come back, so per MoE layer a rank sends roughly \(2\,k\,b\,s\,h\) bytes-per-element, less whatever fraction of experts happen to be local. For DeepSeek-V3, \(h = 7168\) and \(k = 8\) in bf16 is \(8 \times 7168 \times 2 = 115\) KB per token per layer each way, and there are 58 MoE layers (61 layers with the first three dense). Compare tensor parallelism’s four all-reduces at \(t = 8\), \(16 \times 7168 \times 2 \times 7/8 = 200\) KB per token per layer: the same order of magnitude, but EP’s traffic crosses <strong>nodes</strong>, since 64 experts’ worth of GPUs do not fit in one. The report is candid about the result: cross-node expert parallelism gave a compute-to-communication ratio of about 1:1 (<a href="https://arxiv.org/abs/2412.19437">Section 3.2.1</a>), which is why the two engineering investments of that run, DualPipe’s overlap of a forward chunk’s dispatch and combine with a backward chunk’s compute, and custom all-to-all kernels, both exist. Two tricks in those kernels are worth knowing because they are the bandwidth ladder applied to routing (<a href="https://arxiv.org/abs/2412.19437">Section 3.2.2</a>): <strong>node-limited routing</strong> caps each token at 4 destination nodes so that InfiniBand carries at most 4 copies of it, and each copy is sent once over InfiniBand to a same-index GPU on the target node and then forwarded over NVLink (160 GB/s on their H800 nodes against 50 GB/s for InfiniBand) to the GPUs that actually host the experts. With that structure a token could reach 13 experts for the same cross-node cost as its 8.</p> <p><strong>Load balance.</strong> A synchronous step ends when the busiest GPU finishes, so if the router sends 30% of tokens to one expert, the GPU hosting it becomes the straggler for every step. Three families of remedy:</p> <table> <thead> <tr> <th>Remedy</th> <th>Mechanism</th> <th>Where</th> </tr> </thead> <tbody> <tr> <td>Expert capacity</td> <td>cap the tokens an expert may take per batch at \((\text{tokens}/E) \times\) a capacity factor; overflow tokens skip the layer (Switch reports typically under 1% dropped at factor 1.0 to 1.25)</td> <td><a href="https://arxiv.org/abs/2006.16668">GShard</a>, <a href="https://arxiv.org/abs/2101.03961">Switch Transformer</a>; Megatron <code class="language-plaintext highlighter-rouge">--moe-expert-capacity-factor</code></td> </tr> <tr> <td>Auxiliary loss</td> <td>add a differentiable loss that penalizes uneven routing, with a small coefficient (Switch uses \(10^{-2}\))</td> <td>same; Megatron <code class="language-plaintext highlighter-rouge">--moe-router-load-balancing-type aux_loss</code></td> </tr> <tr> <td>Auxiliary-loss-free</td> <td>add a per-expert bias to the routing score used for top-\(k\) selection only, and nudge it up or down after each step depending on whether the expert was under- or over-loaded; the gate weights themselves are untouched, so the loss is not distorted</td> <td><a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3, Section 2.1.2</a>, bias update rate \(10^{-3}\); Megatron <code class="language-plaintext highlighter-rouge">--moe-router-enable-expert-bias</code></td> </tr> </tbody> </table> <p>Megatron also supports “dropless” MoE, where no token is ever discarded and the expert GEMMs are sized to whatever arrives, at the cost of dynamic shapes.</p> <p><strong>How EP composes.</strong> The older constraint was \(\text{EP} \leq \text{DP}\): an expert-parallel group was carved out of a data-parallel group, since the attention layers on those ranks are replicas of each other while their experts are different. Megatron Core’s “parallel folding” removes that coupling by giving the attention layers and the MoE layers separate parallelism layouts, \(\text{TP} \times \text{CP} \times \text{DP} \times \text{PP}\) for attention and \(\text{ETP} \times \text{EP} \times \text{EDP} \times \text{PP}\) for the experts, so that a run can use high TP and CP where attention wants them and high EP with no TP where the experts want it (<a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/core/transformer/moe/README.md">MoE README</a>). DeepSeek-V3 is the existence proof of the shape this produces: 16-way pipeline parallelism, 64-way expert parallelism across 8 nodes, ZeRO-1 data parallelism, and no tensor parallelism at all (<a href="https://arxiv.org/abs/2412.19437">Section 3.2</a>). The ordering rule survives, but the dimension on the fast link changes with the model: for a dense model it is TP, for a fine-grained MoE it is the expert all-to-all.</p> <h5 id="the-three-side-by-side"><strong>The three, side by side</strong></h5> <table> <thead> <tr> <th> </th> <th>Sequence parallelism</th> <th>Context parallelism</th> <th>Expert parallelism</th> </tr> </thead> <tbody> <tr> <td>Group</td> <td>the TP group</td> <td>a CP group, inside the PP stage</td> <td>an EP group, usually spanning nodes</td> </tr> <tr> <td>What each rank holds</td> <td>\(s/t\) tokens of the norm and dropout activations, full tokens elsewhere</td> <td>\(s/c\) tokens of everything</td> <td>\(E/\text{EP}\) whole experts, all tokens’ attention</td> </tr> <tr> <td>Collectives per layer</td> <td>all-gather and reduce-scatter at each TP boundary</td> <td>ring point-to-point, all-gather, or all-to-all, in attention only</td> <td>two all-to-alls forward, two backward</td> </tr> <tr> <td>Memory effect</td> <td>activations \(\div t\) across the whole layer</td> <td>activations \(\div c\), including the attention scores</td> <td>parameters \(\div \text{EP}\) for the experts</td> </tr> <tr> <td>Turn it on when</td> <td>always, with TP</td> <td>the sequence is too long for one rank</td> <td>the model is a mixture of experts</td> </tr> </tbody> </table> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2205.05198">Reducing Activation Recomputation in Large Transformer Models - Korthikanti et al.</a></li> <li><a href="https://arxiv.org/abs/2105.13120">Sequence Parallelism: Long Sequence Training from System Perspective - Li et al.</a>, <a href="https://arxiv.org/abs/2310.01889">Ring Attention with Blockwise Transformers - Liu et al.</a>, <a href="https://arxiv.org/abs/2309.14509">DeepSpeed-Ulysses - Jacobs et al.</a></li> <li><a href="https://docs.nvidia.com/megatron-core/developer-guide/latest/user-guide/features/context_parallel.html">Megatron Core context parallelism</a>, <a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/core/transformer/moe/README.md">Megatron Core MoE README</a>, <a href="https://github.com/NVIDIA/Megatron-LM/blob/main/megatron/training/arguments.py">Megatron-LM arguments</a></li> <li><a href="https://arxiv.org/abs/2006.16668">GShard - Lepikhin et al.</a>, <a href="https://arxiv.org/abs/2101.03961">Switch Transformers - Fedus et al.</a></li> <li><a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3 Technical Report</a></li> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a></li> </ul> <hr/> <h4 id="pipeline-parallelism"><strong>Pipeline Parallelism</strong></h4> <p>Tensor parallelism splits every layer across GPUs. Pipeline parallelism (PP) splits the <em>stack</em> of layers: with \(p\) stages, stage 1 holds the first \(L/p\) layers, stage 2 the next \(L/p\), and so on, each stage on its own GPU (or its own tensor-parallel group of GPUs). Data flows through the stages the way it flows through the layers: stage \(i\) runs its layers on an input and sends the output activation to stage \(i+1\); in the backward pass, gradients flow the other way.</p> <h5 id="why-the-communication-is-cheap"><strong>Why the communication is cheap</strong></h5> <p>The only thing crossing a stage boundary is the hidden state at that point in the network: one \(b \times s \times h\) tensor forward and its gradient backward, per microbatch, sent point-to-point from one GPU to one other (<a href="https://arxiv.org/abs/2104.04473">Narayanan et al., Section 3.2</a>; GPipe makes the same point, that only activations at partition boundaries need to move, <a href="https://arxiv.org/abs/1811.06965">Section 2.3</a>). Compare that with tensor parallelism, which all-reduces a tensor of the <em>same size</em> four times per layer. Pipeline parallelism pays once per <strong>stage</strong>, however many layers the stage contains, and it pays with a single send rather than a collective. In the 70B example, TP moved 75 GB per rank per microbatch; a 16-stage pipeline moves one 134 MB activation per boundary, and can do it asynchronously while the stage works on the next microbatch. That difference in volume is why pipeline parallelism is the split that goes <strong>across nodes</strong>, on the slow link.</p> <h5 id="microbatches-and-the-bubble"><strong>Microbatches and the bubble</strong></h5> <p>The problem with a pipeline is that a naive one is almost entirely idle. If the whole batch went through stage 1, then stage 2, and so on, only one stage would ever be working. So the batch is cut into \(m\) <strong>microbatches</strong>, and the stages work on different microbatches at the same time: stage 1 finishes microbatch 1 and hands it to stage 2, then starts on microbatch 2 while stage 2 works on microbatch 1, and so on. In steady state every stage is busy on a different microbatch.</p> <p>The waste is at the ends. Let \(t_f\) and \(t_b\) be the time for one stage to run the forward and backward pass of one microbatch. The ideal time for the batch, with every stage always busy, would be \(m(t_f + t_b)\). But at the start, stage \(p\) cannot begin until microbatch 1 has traversed the \(p - 1\) stages before it, and at the end, stage 1 cannot finish its backward work until the last microbatch’s gradient has come back through the \(p - 1\) stages after it. Fill and drain together idle each stage for \((p-1)(t_f + t_b)\), the <strong>pipeline bubble</strong>, and its size relative to the useful work is (<a href="https://arxiv.org/abs/2104.04473">Narayanan et al., Section 2.2.1</a>):</p> \[\frac{t_{\text{bubble}}}{t_{\text{ideal}}} = \frac{(p-1)(t_f + t_b)}{m\,(t_f + t_b)} = \frac{p-1}{m}\] <p>Two forms of this are in circulation and both are right; they differ in the denominator. Megatron’s \((p-1)/m\) is bubble over <em>ideal</em> time, the overhead. GPipe’s \(O\!\left(\frac{K-1}{M+K-1}\right)\) (<a href="https://arxiv.org/abs/1811.06965">Section 2.3</a>, with \(K\) stages and \(M\) microbatches) is bubble over <em>total</em> time, the idle fraction. Evaluating both:</p> <table> <thead> <tr> <th>\(p\) stages</th> <th>\(m\) microbatches</th> <th>Overhead \((p-1)/m\)</th> <th>Idle fraction \((p-1)/(m+p-1)\)</th> </tr> </thead> <tbody> <tr> <td>8</td> <td>8</td> <td>87.5%</td> <td>46.7%</td> </tr> <tr> <td>8</td> <td>32</td> <td>21.9%</td> <td>17.9%</td> </tr> <tr> <td>8</td> <td>64</td> <td>10.9%</td> <td>9.9%</td> </tr> <tr> <td>16</td> <td>64</td> <td>23.4%</td> <td>19.0%</td> </tr> </tbody> </table> <p>The rule that falls out is \(m \gg p\): GPipe found the overhead negligible once \(M \geq 4K\), and Megatron’s later work chases the bubble further still. Since the microbatches of the pipeline are the microbatches of gradient accumulation, \(m\) is bounded above by the global batch: \(m = \text{batch per data-parallel rank} / \text{microbatch size}\), so a larger pipeline needs a larger batch to keep its bubble small, which is one of the pressures toward the 16M-token batches large runs use.</p> <h5 id="schedules"><strong>Schedules</strong></h5> <p>The bubble fraction is the same for every schedule below; what differs is how many microbatches’ activations are alive at once, which is what decides whether the run fits.</p> <p><strong>GPipe</strong> (<a href="https://arxiv.org/abs/1811.06965">Huang et al.</a>) runs all \(m\) forwards, then all \(m\) backwards. Simple, but every microbatch’s activations are stashed until its backward pass arrives, so activation memory scales with \(m\), which directly fights the \(m \gg p\) rule.</p> <p><strong>1F1B</strong>, one-forward-one-backward, comes from PipeDream (<a href="https://arxiv.org/abs/1806.03377">Narayanan et al. 2019, Section 3.3</a>), which coined the term. After a warm-up in which each stage runs a few forwards, every stage alternates: one forward on a new microbatch, one backward on an old one. A backward pass frees that microbatch’s activations, so at most \(p\) microbatches are in flight at any moment, and memory scales with \(p\) instead of \(m\). PipeDream itself was asynchronous (it kept multiple weight versions to avoid ever flushing the pipeline). The synchronous variant, <strong>PipeDream-Flush</strong> (<a href="https://arxiv.org/abs/2006.09503">Narayanan et al. 2020, Section 3.2</a>), keeps the 1F1B order, uses a single weight version, and flushes the pipeline at the end of each batch for the optimizer step. That is the schedule Megatron-LM uses by default (<a href="https://arxiv.org/abs/2104.04473">Section 2.2.1</a>): <strong>same bubble as GPipe, activation memory of \(p\) microbatches instead of \(m\).</strong> It is strictly better, and it is the baseline everything else improves on.</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/pp-schedules.svg" sizes="95vw"/> <img src="/assets/img/llm-training/pp-schedules.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="GPipe versus 1F1B pipeline schedules" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Four stages, eight microbatches. Top: GPipe runs all forwards then all backwards, so stage 1 holds eight microbatches' activations at its peak. Bottom: 1F1B interleaves them after a warm-up, holding at most four. The shaded cells are the bubble, and its total size is the same in both. Editable source: <a href="/assets/img/llm-training/pp-schedules.excalidraw">pp-schedules.excalidraw</a>. </div> <p>One consequence of 1F1B worth knowing: the memory is uneven across stages. Stage 1 must hold activations for \(p\) microbatches while the last stage holds only 1. With \(L/p\) layers per stage, the first stage’s \(p\) in-flight microbatches add up to a full \(L\) layers’ worth of activations, which is why the activation-memory analyses quote the first stage as the binding constraint (<a href="https://arxiv.org/abs/2205.05198">Korthikanti et al., Section 4.2.3</a>).</p> <p><strong>Interleaved 1F1B</strong> (<a href="https://arxiv.org/abs/2104.04473">Narayanan et al. 2021, Section 2.2.2</a>) shrinks the bubble itself. Instead of one contiguous block of layers, give each device \(v\) smaller, non-adjacent chunks (with 16 layers and 4 devices, device 1 gets layers 1-2 and 9-10 rather than 1-4). Each chunk’s forward and backward is \(v\) times shorter, so the fill and drain are \(v\) times shorter, and the bubble becomes:</p> \[\frac{1}{v}\cdot\frac{p-1}{m}\] <p>The cost is that each microbatch now crosses \(v\) times as many stage boundaries, so point-to-point communication goes up by \(v\), and \(m\) must be a multiple of \(p\). This is the schedule Llama 3 trains with, and its report writes the bubble in exactly this form, \((\text{PP}-1)/(V \cdot M)\) (<a href="https://arxiv.org/abs/2407.21783">Section 3.3.2</a>).</p> <p><strong>Zero-bubble schedules</strong> attack the last of it with the observation from the very first section: the backward pass is <em>two</em> matmuls, \(\partial L / \partial z = A^\top g\) and \(\partial L / \partial A = g z^\top\), and they have different urgency. The input gradient is on the critical path, since the previous stage is waiting for it. The weight gradient is needed only by the optimizer step at the end of the batch. So split the backward into \(B\) (input gradient) and \(W\) (weight gradient) and schedule \(W\) into the bubbles (<a href="https://arxiv.org/abs/2401.10241">Qi et al.</a>). Their ZB-H1 schedule cuts the bubble to a third of 1F1B’s at the same memory; ZB-H2 eliminates it entirely at the cost of more activations in flight. The one obstacle is the optimizer step’s own synchronization (the global gradient-norm for clipping is an all-reduce across stages that would stall the pipeline), which they replace with a post-hoc validation and a rollback if it fails. DeepSeek-V3’s <strong>DualPipe</strong> goes further, feeding microbatches from both ends of the pipeline and overlapping each forward chunk with a backward chunk’s compute and communication; its bubble is \((\text{PP}/2 - 1)(F\&amp;B + B - 3W)\) against 1F1B’s \((\text{PP}-1)(F+B)\), paid for with two copies of the parameters (<a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3, Section 3.2.1 and Table 2</a>).</p> <p>Whether you need these depends on where the bubble sits in your budget. With \(p = 16\), \(v = 8\), \(m = 32\), the interleaved bubble is \(15/256 \approx 6\%\); zero-bubble buys back that last few percent. For most runs the useful hierarchy is: 1F1B as the baseline, interleaving when \(p\) is large, zero-bubble when the last percent is worth the complexity.</p> <h5 id="why-pipeline-parallelism-is-awkward-for-inference"><strong>Why pipeline parallelism is awkward for inference</strong></h5> <p>In training there are \(m\) microbatches to fill the pipe, and \(m\) is under your control. In decode there is one token per sequence per step, so a request has nothing with which to fill the stages behind it; the only “microbatches” are other concurrent requests, and the pipeline sits in its bubble whenever there are too few of them. That is why serving keeps tensor parallelism within a node and scales out with data-parallel replicas, and treats pipeline parallelism as a fit-the-model tool rather than a throughput tool (the <a href="/blog/2026/llm-inference-systems/">inference post</a> covers that side).</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2104.04473">Efficient Large-Scale Language Model Training on GPU Clusters Using Megatron-LM - Narayanan et al.</a></li> <li><a href="https://arxiv.org/abs/1811.06965">GPipe - Huang et al.</a></li> <li><a href="https://arxiv.org/abs/1806.03377">PipeDream: Generalized Pipeline Parallelism for DNN Training - Narayanan et al.</a>, <a href="https://arxiv.org/abs/2006.09503">Memory-Efficient Pipeline-Parallel DNN Training (PipeDream-Flush and 2BW) - Narayanan et al.</a></li> <li><a href="https://arxiv.org/abs/2401.10241">Zero Bubble Pipeline Parallelism - Qi et al.</a></li> <li><a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3 Technical Report</a></li> <li><a href="https://arxiv.org/abs/2205.05198">Reducing Activation Recomputation in Large Transformer Models - Korthikanti et al.</a></li> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a></li> </ul> <hr/> <h4 id="composing-them-3d-and-4d-parallelism"><strong>Composing Them: 3D and 4D Parallelism</strong></h4> <p>No single dimension gets a large model trained. Tensor parallelism cannot leave the node, pipeline parallelism wastes a bubble per stage, and data parallelism does nothing for a model that does not fit. Real runs use all of them at once, and the number of GPUs is their product:</p> \[\text{GPUs} = \text{TP} \times \text{CP} \times \text{PP} \times \text{DP}\] <p>(times an expert-parallel degree for mixture-of-experts models). The question is which dimension goes where in the hardware, and the previous sections have already answered it, one dimension at a time.</p> <h5 id="the-ordering-rule"><strong>The ordering rule</strong></h5> <p>Put the four dimensions’ communication side by side, per microbatch unless stated:</p> <table> <thead> <tr> <th>Dimension</th> <th>What it moves</th> <th>How often</th> <th>Can it hide behind compute?</th> </tr> </thead> <tbody> <tr> <td>TP</td> <td>4 all-reduces of the \(b \times s \times h\) hidden state</td> <td><strong>per layer</strong></td> <td>barely; each sits between two dependent matmuls</td> </tr> <tr> <td>CP</td> <td>\(K\) and \(V\) blocks around a ring</td> <td>per layer, attention only</td> <td>yes, by design, behind block-wise attention</td> </tr> <tr> <td>PP</td> <td>one \(b \times s \times h\) activation (and its gradient), point to point</td> <td>per <strong>stage boundary</strong></td> <td>yes, asynchronously, behind the next microbatch</td> </tr> <tr> <td>DP</td> <td>the gradient reduce, plus the parameter gather if sharded: \(2\Psi\) to \(3\Psi\) elements</td> <td>once per <strong>step</strong></td> <td>yes, behind the backward pass (and the next forward, for prefetched gathers)</td> </tr> </tbody> </table> <p>Reading down the rows, the traffic gets less frequent and more overlappable, and that ordering is the placement rule: <strong>the chattiest dimension goes on the fastest link.</strong> TP on NVLink inside a node, PP across nodes on the network, DP outermost on whatever is left, with CP slotted between TP and PP when it is in use. Llama 3 states the rule and the order explicitly: the innermost parallelism needs the highest bandwidth and lowest latency and is kept within a server, so they place the dimensions in the order \([\text{TP}, \text{CP}, \text{PP}, \text{DP}]\), with DP outermost because it tolerates the network’s latency thanks to asynchronous prefetching of weights and reduction of gradients (<a href="https://arxiv.org/abs/2407.21783">Section 3.3.2</a>). Megatron’s guidelines say the same in two takeaways: use tensor parallelism up to the number of GPUs in a server, then pipeline parallelism to scale across servers (<a href="https://arxiv.org/abs/2104.04473">Narayanan et al., Section 3.2</a>).</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-training/parallelism-layout.svg" sizes="95vw"/> <img src="/assets/img/llm-training/parallelism-layout.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="3D parallelism placed on four 8-GPU nodes" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Placement on four 8-GPU nodes. Tensor parallelism spans the eight GPUs of a node over NVLink; pipeline stages are whole nodes connected point to point over the network; data-parallel replicas are groups of nodes whose gradients are reduced once per step. Editable source: <a href="/assets/img/llm-training/parallelism-layout.excalidraw">parallelism-layout.excalidraw</a>. </div> <h5 id="what-a-real-run-looks-like"><strong>What a real run looks like</strong></h5> <p>Llama 3’s report gives the actual configurations of its 405B pre-training, which is rare and worth studying line by line (<a href="https://arxiv.org/abs/2407.21783">Table 4</a>):</p> <table> <thead> <tr> <th>GPUs</th> <th>TP</th> <th>CP</th> <th>PP</th> <th>DP</th> <th>Seq. len.</th> <th>Batch per DP rank</th> <th>Tokens per batch</th> <th>TFLOPs per GPU</th> <th>BF16 MFU</th> </tr> </thead> <tbody> <tr> <td>8,192</td> <td>8</td> <td>1</td> <td>16</td> <td>64</td> <td>8,192</td> <td>32</td> <td>16M</td> <td>430</td> <td>43%</td> </tr> <tr> <td>16,384</td> <td>8</td> <td>1</td> <td>16</td> <td>128</td> <td>8,192</td> <td>16</td> <td>16M</td> <td>400</td> <td>41%</td> </tr> <tr> <td>16,384</td> <td>8</td> <td>16</td> <td>16</td> <td>8</td> <td>131,072</td> <td>16</td> <td>16M</td> <td>380</td> <td>38%</td> </tr> </tbody> </table> <p>Check the arithmetic and the rule against it. \(8 \times 1 \times 16 \times 64 = 8{,}192\) and \(8 \times 16 \times 16 \times 8 = 16{,}384\), as required. TP is 8, the size of a node, in every row: it never leaves NVLink. PP is 16 in every row, so one model replica spans \(8 \times 16 = 128\) GPUs, sixteen nodes, and everything beyond that is DP. When the sequence length goes to 128K, CP goes to 16, and since the GPU count is fixed, DP drops from 128 to 8 to make room. Tokens per batch stay at 16M throughout: in the first row that is \(64 \times 32 \times 8{,}192 = 16.8\)M, in the third \(8 \times 16 \times 131{,}072 = 16.8\)M. And the utilization drifts down as DP grows, which the report attributes to the smaller batch per DP rank needed to keep the token count fixed: fewer microbatches per pipeline, a larger bubble.</p> <p>The per-GPU memory picture at this scale is instructive. One replica holds the 405B model over 128 GPUs, so the bf16 weights are \(810 / 128 \approx 6.3\) GB per GPU, and the remaining 14 bytes per parameter, sharded by FSDP across the 64 or 128 data-parallel ranks as well, are under a gigabyte. Static state, the thing that dominated the single-GPU discussion, has become small; what fills the 80 GB now is activations for the \(p\) microbatches a 1F1B stage keeps in flight, plus the communication buffers. At scale the memory problem turns into a scheduling problem.</p> <h5 id="how-to-choose"><strong>How to choose</strong></h5> <p>The Megatron paper’s guidelines, restated as a procedure (<a href="https://arxiv.org/abs/2104.04473">Section 3</a>):</p> <ol> <li><strong>Tensor parallelism up to the node</strong>, and no further. Use as little as makes the per-GPU slice of weights and activations fit, since each doubling thins the per-rank GEMMs and adds collectives; in practice 8 for large models.</li> <li><strong>Pipeline parallelism just large enough</strong> that, with TP, the model fits. Every extra stage adds bubble, so the model-parallel product \(\text{TP} \times \text{PP}\) should be the smallest that fits the parameters and activations.</li> <li><strong>Everything else to data parallelism.</strong> It scales best, because its one collective per step hides behind the backward pass.</li> <li><strong>Then tune the microbatch and \(m\).</strong> Raise the number of microbatches until the bubble is a few percent, and set the microbatch size as large as memory allows so the GEMMs are efficient; the global batch follows as \(\text{DP} \times \text{microbatch} \times m\).</li> </ol> <p>Context parallelism enters when the sequence length forces it, and expert parallelism when the model is a mixture of experts, as the previous section laid out; the DeepSeek-V3 layout of \(\text{PP} = 16\) and \(\text{EP} = 64\) across 8 nodes with no tensor parallelism (<a href="https://arxiv.org/abs/2412.19437">Section 3.2</a>) is what the same procedure produces when the expert all-to-all, not the TP all-reduce, is the traffic that needs the fast link.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a></li> <li><a href="https://arxiv.org/abs/2104.04473">Efficient Large-Scale Language Model Training on GPU Clusters Using Megatron-LM - Narayanan et al.</a></li> <li><a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3 Technical Report</a></li> </ul> <hr/> <h4 id="mfu-and-hfu"><strong>MFU and HFU</strong></h4> <p>Everything above is in service of one number, and it is the one leadership asks about: what fraction of the hardware’s peak arithmetic is the run actually using.</p> <h5 id="the-definition-and-why-tokens-per-second-is-in-it"><strong>The definition, and why tokens per second is in it</strong></h5> <p>Model FLOPs utilization (MFU) was defined by the PaLM paper as the ratio of the observed throughput, in tokens per second, to the theoretical maximum throughput the system could reach at its peak FLOP rate (<a href="https://arxiv.org/abs/2204.02311">Chowdhery et al., Section 4.1</a>). The theoretical maximum is easy to write down, because the FLOPs per token are a constant of the model: \(6N\) for the forward and backward pass. So:</p> \[\text{max tokens/s} = \frac{\text{GPUs} \times \text{peak FLOP/s per GPU}}{6N} \qquad\Longrightarrow\qquad \text{MFU} = \frac{6N \times \text{tokens/s}}{\text{GPUs} \times \text{peak FLOP/s per GPU}}\] <p>Tokens per second appears because it is the thing you can <em>measure</em>: a training loop knows how many tokens it consumed and how long it took. The FLOPs it did are not observed, they are inferred, by multiplying the token rate by the model’s fixed cost per token. The numerator is therefore “the FLOPs the model <em>needed</em>”, counted from the token rate, and the denominator is “the FLOPs the hardware <em>could</em> have done”. PaLM’s Appendix B adds the attention term to the numerator, \(6N + 12\,L\,H\,Q\,T\) per token with \(L\) layers, \(H\) heads, \(Q\) head dimension, and \(T\) sequence length; for PaLM it moved the answer from 45.7% to 46.2%, and it grows with \(T\).</p> <p><strong>Hardware FLOPs utilization (HFU)</strong> is the other number, and the distinction matters: HFU counts every FLOP the hardware executed, including the recomputation from activation checkpointing, against peak. MFU counts only the FLOPs the model needed. So \(\text{HFU} \geq \text{MFU}\) always, with equality when nothing is recomputed, and the gap is the recompute tax made visible. PaLM ran with full recomputation and reported 46.2% MFU against 57.8% HFU; Megatron’s selective-recompute 1T run reported 56.3% MFU against 57.0% HFU (<a href="https://arxiv.org/abs/2205.05198">Korthikanti et al., Table 5</a>), a gap of under one point. Quoting HFU when the question was MFU inflates the answer by exactly the amount of work that was not useful, and PaLM’s paper points out that earlier reports, including Megatron’s 52%, were computed the hardware way.</p> <h5 id="both-directions-worked"><strong>Both directions, worked</strong></h5> <p><strong>Measured to MFU.</strong> Llama 3’s second configuration reports 400 TFLOPs per GPU, and dense BF16 peak is about 989 TFLOPS, so \(400 / 989 = 40.4\%\) against the report’s 41% (its TFLOPs figure is itself rounded) (<a href="https://arxiv.org/abs/2407.21783">Table 4</a>); the first row’s 430 gives 43%. Now go through tokens instead. At 400 TFLOPs on 16,384 GPUs the run does \(6.55 \times 10^{18}\) useful FLOP/s, and a 405B model costs \(6 \times 405 \times 10^9 = 2.43 \times 10^{12}\) FLOPs per token, so:</p> \[\text{tokens/s} = \frac{6.55 \times 10^{18}}{2.43 \times 10^{12}} \approx 2.7 \times 10^{6}\] <p>A 16.8M-token batch is then about 6.2 s per step, and the paper’s 15.6T pre-training tokens would take \(15.6 \times 10^{12} / 2.7 \times 10^{6} \approx 5.8 \times 10^{6}\) s, about 67 days of step time at that stage’s rate (the actual run also had a long-context stage at lower MFU and a ramp-up at smaller batch, so this is the order of magnitude, not the schedule).</p> <p><strong>MFU to expected throughput.</strong> Suppose a 70B model on 1,024 H100s reports 12,000 tokens/s. Then \(6 \times 70 \times 10^9 \times 12{,}000 = 5.0 \times 10^{15}\) FLOP/s achieved, against \(1{,}024 \times 989 \times 10^{12} = 1.0 \times 10^{18}\) peak: an MFU of <strong>0.5%</strong>. Nothing is that bad by accident; something is broken. Running it the other way says what to expect: at 40% MFU those GPUs should deliver \(0.4 \times 1.0 \times 10^{18} / (4.2 \times 10^{11}) \approx 965{,}000\) tokens/s, eighty times more. Being able to do this estimate in both directions from three numbers, model size, GPU count, token rate, is the fastest sanity check there is on any training run.</p> <h5 id="reference-points"><strong>Reference points</strong></h5> <p>Published, verified figures, so you know what “good” looks like:</p> <table> <thead> <tr> <th>Run</th> <th>Scale</th> <th>Utilization</th> <th>Source</th> </tr> </thead> <tbody> <tr> <td>GPT-3 175B</td> <td> </td> <td>21.3% MFU (as computed by PaLM)</td> <td><a href="https://arxiv.org/abs/2204.02311">PaLM, Table 3</a></td> </tr> <tr> <td>Gopher 280B</td> <td> </td> <td>32.5% MFU</td> <td>same</td> </tr> <tr> <td>Megatron-Turing NLG 530B</td> <td> </td> <td>30.2% MFU</td> <td>same</td> </tr> <tr> <td>Megatron 1T GPT</td> <td>3,072 A100</td> <td>52% of peak, hardware FLOPs</td> <td><a href="https://arxiv.org/abs/2104.04473">Narayanan et al.</a></td> </tr> <tr> <td>PaLM 540B</td> <td>6,144 TPU v4</td> <td>46.2% MFU, 57.8% HFU</td> <td><a href="https://arxiv.org/abs/2204.02311">PaLM, Section 4.1</a></td> </tr> <tr> <td>Megatron 530B with selective recompute and SP</td> <td>2,240 A100</td> <td>54.2% MFU</td> <td><a href="https://arxiv.org/abs/2205.05198">Korthikanti et al.</a></td> </tr> <tr> <td>MegaScale 175B</td> <td>12,288 GPUs</td> <td>55.2% MFU</td> <td><a href="https://arxiv.org/abs/2402.15627">Jiang et al.</a></td> </tr> <tr> <td>Llama 3 405B</td> <td>8,192 to 16,384 H100</td> <td>38 to 43% BF16 MFU</td> <td><a href="https://arxiv.org/abs/2407.21783">Llama 3, Table 4</a></td> </tr> <tr> <td>Megatron-LM on H100 clusters</td> <td> </td> <td>up to 47% MFU</td> <td><a href="https://github.com/NVIDIA/Megatron-LM/blob/main/README.md">Megatron-LM README</a></td> </tr> </tbody> </table> <p>The band for a well-tuned large dense run is roughly 40 to 55% MFU, and it has moved up over time as recomputation got selective and communication got overlapped. Where the rest goes, in the order you would check:</p> <table> <thead> <tr> <th>Loss</th> <th>Why it shows up</th> <th>Section</th> </tr> </thead> <tbody> <tr> <td>Exposed communication</td> <td>collectives not overlapped; TP on the wrong link; too little compute per collective</td> <td>primitives, data and tensor parallelism</td> </tr> <tr> <td>Pipeline bubble</td> <td>\(m\) too small relative to \(p\)</td> <td>pipeline parallelism</td> </tr> <tr> <td>Thin GEMMs</td> <td>microbatch too small for the tensor cores to reach peak; TP degree too high</td> <td>recomputation, tensor parallelism</td> </tr> <tr> <td>Recomputation</td> <td>the HFU-to-MFU gap; full instead of selective</td> <td>recomputation</td> </tr> <tr> <td>Stragglers and stalls</td> <td>one slow rank, or a starved dataloader, idling every collective</td> <td>what breaks</td> </tr> <tr> <td>Kernel inefficiency</td> <td>attention or norm kernels far from roofline</td> <td>the <a href="/blog/2026/fa3-worklog/">FlashAttention worklog</a></td> </tr> <tr> <td>Wrong peak in the denominator</td> <td>the “with sparsity” TFLOPS number</td> <td>the numbers section</td> </tr> </tbody> </table> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2204.02311">PaLM: Scaling Language Modeling with Pathways - Chowdhery et al.</a></li> <li><a href="https://arxiv.org/abs/2205.05198">Reducing Activation Recomputation in Large Transformer Models - Korthikanti et al.</a></li> <li><a href="https://arxiv.org/abs/2104.04473">Efficient Large-Scale Language Model Training on GPU Clusters Using Megatron-LM - Narayanan et al.</a></li> <li><a href="https://arxiv.org/abs/2402.15627">MegaScale: Scaling Large Language Model Training to More Than 10,000 GPUs - Jiang et al.</a></li> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a>, <a href="https://github.com/NVIDIA/Megatron-LM/blob/main/README.md">Megatron-LM README</a></li> </ul> <hr/> <h4 id="what-breaks-at-scale"><strong>What Breaks at Scale</strong></h4> <p>A short section, because the mechanisms are simple even though the engineering is not. The numbers are Llama 3’s, from the most detailed public account of a large run’s operations (<a href="https://arxiv.org/abs/2407.21783">Section 3.3.4</a>).</p> <p><strong>Failures are routine, not exceptional.</strong> In a 54-day snapshot of 405B pre-training on 16K GPUs, Llama 3 saw 466 job interruptions, 47 planned and 419 unexpected. About 78% of the unexpected ones were attributed to confirmed or suspected hardware, and GPU problems alone were 58.7% of them (faulty GPUs 30.1%, HBM3 failures 17.2%). That is roughly eight unexpected interruptions per day. The run still achieved over 90% effective training time, which was possible only because restarts were automatic; the paper says manual intervention was needed just three times. Everything below is in service of making an interruption cheap.</p> <p><strong>Stragglers.</strong> A synchronous step ends when its slowest participant finishes, so one GPU that is thermally throttling, or one link with errors, slows every collective it is part of and therefore every GPU in the job. The failure is silent, since nothing crashes; detection is by per-rank timing, and Llama 3 built tooling that prioritizes suspect communications from selected process groups to find slow-but-functioning ranks.</p> <p><strong>Checkpoints.</strong> A checkpoint of the full training state is the 16 bytes per parameter again: about 1.12 TB for a 70B model, 6.5 TB for the 405B. Written synchronously from every rank, that stalls the run for as long as the write takes. Llama 3’s storage tier sustained 2 TB/s with a 7 TB/s peak, sized precisely so that checkpoints could be written quickly enough to be frequent; each GPU’s share is 1 MB to 4 GB depending on the model, and it is written sharded, one piece per rank, rather than gathered (<a href="https://arxiv.org/abs/2407.21783">Section 3.3.1</a>). The frequency is set by the interruption rate: with eight interruptions a day, an hour between checkpoints costs an average of half an hour of lost work per interruption.</p> <p><strong>Loss spikes.</strong> Large runs occasionally see the loss jump and sometimes fail to recover. PaLM reports about 20 such spikes, despite gradient clipping, and its remedy is the standard one: restart from a checkpoint roughly 100 steps before the spike and skip 200 to 500 data batches, after which the spike did not recur; they verified the same batches did not spike from a different checkpoint, so it was the combination of data and state, not bad data alone (<a href="https://arxiv.org/abs/2204.02311">PaLM, Section 5.1</a>). The everyday defenses are gradient clipping by global norm (<a href="https://arxiv.org/abs/1211.5063">Pascanu et al.</a>) and, under fp16, the loss scaler’s skip-on-overflow.</p> <p><strong>Nondeterminism.</strong> Two runs of the same job with the same seed are not bit-identical, and the usual explanation, floating-point addition is not associative and reductions run in parallel, is only half of it. The reduction <em>order</em> changes with the batch composition and the communication topology: a kernel that reduces over a different tile pattern for a different batch size produces different bits, so the same sequence gets a different result depending on what else is in the batch. The Thinking Machines write-up on inference makes this precise as a lack of batch invariance in the kernels, and shows it by getting 80 distinct completions from 1,000 greedy runs of the same prompt (<a href="https://thinkingmachines.ai/blog/defeating-nondeterminism-in-llm-inference/">Defeating Nondeterminism in LLM Inference</a>). In training this is usually harmless, but it matters when a divergence has to be reproduced to be debugged, and it is the same class of issue that showed up in the <a href="/blog/2026/eagle-test-dp/">EAGLE data-parallel test</a>. Determinism is buyable, at a cost in kernel choice and speed.</p> <p><strong>The dataloader.</strong> Trivially overlooked and a common reason a run sits at half the MFU it should: if the input pipeline (reading, tokenizing, packing, shipping to the GPU) cannot keep up with the step time, every step waits on it, and the GPU profile shows idle time at the start of each step that no parallelism setting will fix. It is the first thing to rule out when MFU is low and the collectives look healthy.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a></li> <li><a href="https://arxiv.org/abs/2204.02311">PaLM: Scaling Language Modeling with Pathways - Chowdhery et al.</a></li> <li><a href="https://arxiv.org/abs/1211.5063">On the difficulty of training Recurrent Neural Networks - Pascanu et al.</a></li> <li><a href="https://thinkingmachines.ai/blog/defeating-nondeterminism-in-llm-inference/">Defeating Nondeterminism in LLM Inference - Thinking Machines</a></li> </ul> <hr/> <h4 id="takeaways"><strong>Takeaways</strong></h4> <p>The things worth carrying out of this post, each one traceable to a section above:</p> <ul> <li><strong>\(6N\) for training, \(2N\) for inference.</strong> The backward pass is two matmuls per weight, the outer product \(g z^\top\) for the weights and \(A^\top g\) for the input, each a forward’s worth of work. The weight gradient is why activations must be saved.</li> <li><strong>16 bytes per parameter</strong> for bf16 with Adam: 2 weights, 2 gradients, 4 master weights, 4 and 4 for the moments. Static state scales with \(N\) only; an 8B model’s state does not fit on an 80 GB GPU. This is the number that motivates ZeRO, FSDP, and model parallelism.</li> <li><strong>Master weights are fp32</strong> because a bf16 weight cannot absorb an update below about \(2^{-8}\) of its own size; the update rounds to nothing. fp32 pushes that floor to \(2^{-24}\).</li> <li><strong>bf16 trades mantissa for exponent</strong> and needs no loss scaling; fp16 needs it because its gradients underflow. FP8 uses E4M3 forward and E5M2 for gradients, needs scaling factors, and accumulates in fp32.</li> <li><strong>Activations scale with \(b \times s \times L\)</strong>, and per layer they are \(sbh(34 + 5as/h)\). The quadratic term is the attention scores, and selective recomputation (or a fused attention kernel) removes it for a few percent of FLOPs; full recomputation costs a third.</li> <li><strong>A ring all-reduce costs each rank \(2S(p-1)/p\)</strong>, because it is a reduce-scatter plus an all-gather, each moving the \(p-1\) chunks a rank does not already own. That identity is what ZeRO’s sharded optimizer step exploits, and the \((p-1)/p\) is why the cost is flat in \(p\).</li> <li><strong>ZeRO 1, 2, 3 shard optimizer state, gradients, and parameters</strong>, for \(4 + 12/D\), \(2 + 14/D\), and \(16/D\) bytes per parameter. Stages 1 and 2 cost no extra communication; stage 3 costs \(1.5\times\) for the second parameter gather, and FSDP hides it by prefetching the next layer’s gather behind the current layer’s compute.</li> <li><strong>Gradient accumulation</strong> sums \(k\) microbatch gradients before one optimizer step: activations of one microbatch at a time, the collective once per step, and under pipeline parallelism the same microbatches fill the pipe.</li> <li><strong>Tensor parallelism splits column-then-row</strong> so the nonlinearity needs no sync, costs four all-reduces per layer on the critical path, and stays inside the node.</li> <li><strong>Sequence parallelism</strong> splits the norm and dropout regions along the sequence for the same communication and less memory. <strong>Context parallelism</strong> splits the sequence itself and brings keys and values together by ring, all-gather, or all-to-all. <strong>Expert parallelism</strong> keeps each expert whole on one GPU and moves tokens to experts with a dispatch and a combine all-to-all, which becomes the dominant traffic in MoE training.</li> <li><strong>The pipeline bubble is \((p-1)/m\)</strong> of ideal time. 1F1B keeps it with \(p\) microbatches in flight instead of GPipe’s \(m\); interleaving divides it by \(v\); zero-bubble schedules fill it with deferred weight-gradient work.</li> <li><strong>Chattiest on the fastest link</strong>: TP on NVLink, then CP, then PP across nodes, then DP outermost. Llama 3 runs \([8, 1, 16, 64]\) on 8,192 GPUs at 43% MFU.</li> <li><strong>MFU \(= 6N \cdot \text{tokens/s} / (\text{GPUs} \cdot \text{peak})\)</strong>, tokens per second because that is what you can measure. HFU also counts recomputation, so it is never lower. Good large dense runs sit around 40 to 55%, and the estimate runs in both directions from three numbers.</li> <li><strong>At scale, failures are hourly</strong>, and the run survives them with automatic restart, sharded asynchronous checkpoints, and straggler detection.</li> </ul> <hr/> <h4 id="test-yourself"><strong>Test Yourself</strong></h4> <p>Everything above, as questions, grouped the way the sections are. Try each from memory before reading its answer; where a question says <em>numbers</em>, do the arithmetic. Every answer traces back to a section and its references.</p> <h5 id="shape-and-memory"><strong>Shape and memory</strong></h5> <p><strong>1. Why \(6N\) per token for training and \(2N\) for inference? Name the two backward matmuls and their shapes.</strong> Forward is \(2N\): two FLOPs per parameter per token. Backward computes, per weight matrix, the weight gradient \(\partial L/\partial A = g z^\top\) (an outer product, \(m \times n\)) and the input gradient \(\partial L/\partial z = A^\top g\) (a transposed matmul, \(n \times 1\)), each the same size multiply as the forward, so \(4N\); total \(6N\). Inference has no backward.</p> <p><strong>2. Numbers: bytes per parameter for bf16 with Adam, and does an 8B model’s static state fit on one 80 GB H100?</strong> 2 (weights) + 2 (gradients) + 4 (fp32 master) + 4 (\(m\)) + 4 (\(v\)) = 16. \(8 \times 10^9 \times 16 = 128\) GB. No, and that is before any activation.</p> <p><strong>3. Why are master weights fp32? What exactly goes wrong without them?</strong> The optimizer update is <em>added</em> to the weight, and bf16 has 7 fraction bits, so any update below about \(2^{-8} \approx 0.4\%\) of the weight rounds away to nothing under round-to-nearest; the update is cancelled, not approximated. fp32’s 23 bits move that floor to \(2^{-24}\).</p> <p><strong>4. What scales with \(N\), what scales with \(b \times s\), and why does the distinction decide the technique?</strong> Static state (weights, gradients, optimizer) scales with \(N\) only, so sharding across ranks or splitting the model fixes it. Activations scale with \(b \times s \times L\), so recomputation, smaller microbatches, and sequence or context parallelism fix them. A real run needs both.</p> <p><strong>5. Numbers: activation memory for the Llama 3 8B architecture at microbatch 4, sequence 8192, 16-bit, no parallelism, no recomputation.</strong> \(h = 4096\), \(a = 32\), \(L = 32\). \(sbh = 8192 \times 4 \times 4096 = 1.34 \times 10^8\); \(5as/h = 5 \times 32 \times 8192 / 4096 = 320\); per layer \(1.34 \times 10^8 \times (34 + 320) = 47.5\) GB; times 32 layers about 1.5 TB. With the score term removed (FlashAttention or selective recompute) the \(34\,sbh\) alone is 4.6 GB per layer, 146 GB total.</p> <h5 id="precision"><strong>Precision</strong></h5> <p><strong>6. fp16 versus bf16: bits, ranges, and which needs loss scaling and why.</strong> fp16 is 1/5/10 with a maximum of 65,504 and a smallest normal of \(6.1 \times 10^{-5}\); bf16 is 1/8/7 with fp32’s range. Gradients underflow fp16’s range, so fp16 needs loss scaling; bf16 does not, because the range problem does not exist there.</p> <p><strong>7. How does dynamic loss scaling work, and what happens on an overflow?</strong> Multiply the loss by a scale \(S\) before backward, unscale the gradients before clipping and the update. Start large (\(2^{16}\) in PyTorch), double after 2,000 consecutive clean steps; on an inf or NaN, skip that optimizer step entirely and halve the scale.</p> <p><strong>8. FP8 E4M3 versus E5M2: which for what, and why does that follow from the bit layout?</strong> E4M3 (max 448, 3 fraction bits) for weights and activations, which need precision more than range; E5M2 (max 57,344, 2 fraction bits) for gradients, which need range for the same reason fp16 gradients underflow. Both need per-tensor or per-block scaling because 448 is a tight ceiling.</p> <p><strong>9. What is delayed scaling and what does it avoid?</strong> Choosing the FP8 scale factor from the amax history of previous iterations instead of the current tensor, on the assumption that ranges change slowly; it avoids a full pass over the tensor (and a synchronization) before every cast. DeepSeek-V3 instead scales online per \(1 \times 128\) tile and \(128 \times 128\) block.</p> <p><strong>10. What stays fp32 no matter what? State the rule, not the list.</strong> Reductions and updates in fp32, matmul operands in low precision. Concretely: master weights, the optimizer moments and update, tensor-core accumulators, the loss and gradient-norm reductions, and norm and softmax statistics.</p> <p><strong>11. Which architecture generation gates bf16, FP8, FP4?</strong> bf16 tensor cores from Ampere (A100); FP8 from Ada (compute capability 8.9) and Hopper (9.0), absent on A100; FP4 with hardware block scaling (NVFP4, MXFP4) from Blackwell.</p> <h5 id="sharding"><strong>Sharding</strong></h5> <p><strong>12. ZeRO stages 1, 2, 3: what is sharded, memory per GPU, communication relative to DDP.</strong> 1: optimizer state, \(4 + 12/D\), same as DDP. 2: plus gradients, \(2 + 14/D\), same. 3: plus parameters, \(16/D\), \(1.5\times\).</p> <p><strong>13. Why is ZeRO-3 \(1.5\times\) the communication of DDP? Where does the extra half come from?</strong> DDP’s all-reduce is a reduce-scatter plus an all-gather, \(2\Psi\). ZeRO-3 must all-gather each layer’s parameters before its forward, free them, and all-gather them <em>again</em> before its backward, plus reduce-scatter the gradients: \(3\Psi\). The extra \(\Psi\) is the second parameter gather.</p> <p><strong>14. Why is FSDP usable at all given that overhead?</strong> Prefetching: the all-gather for layer \(i+1\) is issued while layer \(i\) computes (forward prefetch), and likewise in the backward pass, so the extra gather hides behind compute instead of sitting on the critical path.</p> <p><strong>15. Numbers: 70B model, 64 GPUs, bf16 with Adam. Static memory per GPU under DDP, ZeRO-1, 2, 3, and which fit in 80 GB?</strong> DDP \(16 \times 70 = 1{,}120\) GB; ZeRO-1 \((4 + 12/64) \times 70 = 293\) GB; ZeRO-2 \((2 + 14/64) \times 70 = 155\) GB; ZeRO-3 \(70 \times 16 / 64 = 17.5\) GB. Only ZeRO-3 fits; stages 1 and 2 are capped by the replicated 4 and 2 bytes.</p> <p><strong>16. What does gradient accumulation buy, and what must you do in DDP or FSDP for it to actually save communication?</strong> The gradient of a mean is the mean of the gradients, so \(k\) microbatch backward passes summed into one buffer give the gradient of a \(k\) times larger batch with one microbatch’s activations alive at a time. Wrap the first \(k-1\) in <code class="language-plaintext highlighter-rouge">no_sync()</code> so the all-reduce (DDP) or reduce-scatter (FSDP) runs once per step; in FSDP that keeps unsharded gradients on every rank meanwhile.</p> <p><strong>17. When is ZeRO-Offload the right call, and when is it wrong?</strong> Right when the run is capacity-bound with wall-clock to spare (few GPUs, large model). Wrong when it is already communication-bound, since it puts PCIe at 64 GB/s per direction, the slowest link in the machine, on the critical path.</p> <h5 id="tensor-and-pipeline-parallelism"><strong>Tensor and pipeline parallelism</strong></h5> <p><strong>18. For \(Y = \mathrm{GeLU}(XA)B\), why is \(A\) split by columns and \(B\) by rows? What breaks the other way?</strong> A column split of \(A\) gives each rank whole output columns, and GeLU is elementwise, so it applies locally with no sync. A row split would produce partial sums \(X_1A_1 + X_2A_2\) that must be added <em>before</em> the GeLU, since GeLU of a sum is not the sum of GeLUs. \(B\) is then split by rows because its contraction dimension is the one already sharded; the partial \(Y_i\) are summed by one all-reduce at the end.</p> <p><strong>19. Numbers: all-reduces per transformer layer under TP, and the bytes for \(h = 8192\), 8192 tokens, bf16, \(t = 8\); time on NVLink versus PCIe.</strong> Four per layer: two forward (\(g\) after attention and after the MLP), two backward (\(f\)). One hidden state is \(8192 \times 8192 \times 2 = 134\) MB; per all-reduce per rank \(2 \times 134 \times 7/8 = 235\) MB; per layer 0.94 GB; over 80 layers 75 GB per microbatch per rank. At 450 GB/s per direction (NVLink) 0.17 s; at 64 GB/s (PCIe Gen5) 1.2 s; against roughly 1.1 s of compute at 40% MFU.</p> <p><strong>20. What does sequence parallelism add, and what is its exact benefit?</strong> It splits the norm and dropout regions, which TP replicates on every rank, along the sequence, and converts the all-reduce at each TP boundary into a reduce-scatter plus an all-gather. Same communication volume, activation memory divided by \(t\) across the whole layer: the 10 in \(sbh(10 + 24/t + \ldots)\) becomes \(10/t\). It is not a communication reduction.</p> <p><strong>21. Numbers: derive the pipeline bubble and evaluate for \((p, m) = (8, 8), (8, 64), (16, 64)\).</strong> Fill and drain idle each stage for \((p-1)(t_f + t_b)\) against \(m(t_f + t_b)\) of useful work: \((p-1)/m\) of ideal time, or \((p-1)/(m+p-1)\) of total time. \(7/8 = 87.5\%\) (47% of total); \(7/64 = 10.9\%\) (9.9%); \(15/64 = 23.4\%\) (19%).</p> <p><strong>22. GPipe versus 1F1B: same bubble, different what? Why is 1F1B strictly better?</strong> Same bubble fraction, different activation memory: GPipe holds all \(m\) microbatches’ activations until the backward phase, 1F1B holds at most \(p\), because each backward frees a microbatch and the schedule alternates. Since \(m \gg p\) is required for a small bubble, memory that scales with \(p\) instead of \(m\) is strictly better.</p> <p><strong>23. What does interleaved 1F1B do to the bubble, and what does it cost?</strong> With \(v\) non-adjacent layer chunks per device the bubble becomes \((p-1)/(v\,m)\); it costs \(v\) times the point-to-point communication and requires \(m\) to be a multiple of \(p\).</p> <p><strong>24. Why is PP awkward for inference in a way it is not for training?</strong> Training has \(m\) microbatches to fill the pipe, chosen freely. Decode produces one token per sequence per step, so only concurrent requests can act as microbatches; with too few, the pipeline sits in its bubble. Serving uses TP within a node and DP replicas across.</p> <p><strong>25. The rule for assigning parallelism dimensions to the hardware, in one sentence, with justification.</strong> Chattiest on the fastest link: TP does four all-reduces of the hidden state per layer, so NVLink inside the node; PP sends one activation per stage boundary per microbatch, point to point and asynchronous, so across nodes; DP reduces gradients once per step, overlapped with the backward, so outermost. Llama 3’s order is \([\text{TP}, \text{CP}, \text{PP}, \text{DP}]\).</p> <p><strong>25b. Three ways to do attention under context parallelism, and why Llama 3 chose the one with exposed latency.</strong> Ring: pass \(K\) and \(V\) blocks around the CP ring, overlapped with block-wise attention. All-gather: collect all \(K\) and \(V\), then attend locally. All-to-all (Ulysses): re-shard from sequence to heads, attend on full sequences for a subset of heads, re-shard back. Llama 3 uses all-gather because under GQA the gathered \(K\) and \(V\) are small and attention is \(O(s^2)\) against the gather’s \(O(s)\), and because it supports document masks easily.</p> <p><strong>25c. Why expert parallelism instead of tensor parallelism for a mixture of experts, and what does it communicate?</strong> Every expert must be resident since any token may route anywhere, but each expert is a small MLP, so slicing it produces thin GEMMs that still pay TP’s collectives. EP keeps experts whole and places different experts on different ranks; tokens travel to their \(k\) experts and back through a dispatch all-to-all and a combine all-to-all per MoE layer, roughly \(2khb\) bytes per token per layer in bf16, and that traffic crosses nodes.</p> <p><strong>25d. Three ways to keep expert load balanced.</strong> Expert capacity with a capacity factor (overflow tokens skip the layer); an auxiliary load-balancing loss; and DeepSeek-V3’s auxiliary-loss-free bias, added to the routing score for top-\(k\) selection only and nudged after each step.</p> <h5 id="collectives-and-utilization"><strong>Collectives and utilization</strong></h5> <p><strong>26. Ring all-reduce volume per rank, and why is all-reduce exactly twice reduce-scatter?</strong> \(2S(p-1)/p\). The ring does a reduce-scatter (each rank ends with one fully summed chunk) and then an all-gather (the chunks circulate), each \(p-1\) steps of \(S/p\) bytes; each phase alone is \(S(p-1)/p\).</p> <p><strong>27. What is gradient bucketing and in what regime does it help?</strong> Fusing many small gradient tensors into one collective of about 25 MiB. It helps in the latency-bound regime, where the per-message \(\alpha\) term dominates because the model has thousands of small tensors.</p> <p><strong>28. Where does DDP overlap communication with compute? Where does FSDP?</strong> DDP launches a bucket’s all-reduce as soon as its gradients are ready, while autograd is still computing earlier layers’ gradients. FSDP prefetches the next unit’s parameter all-gather during the current unit’s forward or backward compute, and reduce-scatters gradients as the backward proceeds.</p> <p><strong>29. Define MFU and HFU. Which is larger with recomputation on, and why?</strong> MFU is the FLOPs the model needed, \(6N\) per token times the token rate, over the hardware’s peak. HFU is every FLOP the hardware executed, recomputation included, over peak. HFU is never smaller, and the gap is the recompute tax; PaLM was 46.2% MFU against 57.8% HFU.</p> <p><strong>30. Numbers: 1,024 H100s, 70B model, 40% MFU. Tokens per second, and time for \(10^{12}\) tokens?</strong> \(0.4 \times 1{,}024 \times 989 \times 10^{12} / (6 \times 70 \times 10^9) \approx 965{,}000\) tokens/s; \(10^{12} / 9.65 \times 10^5 \approx 1.04 \times 10^6\) s, about 12 days of step time.</p> <p><strong>31. Numbers: you measure 18% MFU. Five candidate causes, in the order you would check.</strong> The dataloader (idle at the start of every step); exposed communication (gaps on the compute stream while NICs are busy, TP on the wrong link); the pipeline bubble (\(m\) too small for \(p\)); thin GEMMs (microbatch too small, TP degree too high) or full recomputation instead of selective (check HFU against MFU); a straggler (per-rank step-time histogram). Also confirm the denominator uses the dense, not “with sparsity”, peak.</p> <h5 id="operations"><strong>Operations</strong></h5> <p><strong>32. Why are stragglers so damaging, and how would you detect one?</strong> Every synchronous collective finishes when its slowest rank does, so one slow GPU slows all of them, silently. Per-rank step and collective timing, and tooling that flags process groups with suspect communication times.</p> <p><strong>33. Numbers: checkpoint size for a 70B model with full optimizer state, what it does to cadence, and the fix.</strong> \(70 \times 10^9 \times 16 = 1.12\) TB. Written synchronously it stalls the run for the write time, which pushes toward infrequent checkpoints, which raises the cost of every one of the roughly daily hardware interruptions. The fix is sharded (one piece per rank), asynchronous checkpoints to a storage tier fast enough for them to be frequent; Llama 3’s sustained 2 TB/s.</p> <p><strong>34. Four causes of a loss spike, and a mitigation for each.</strong> A pathological data batch (skip it: PaLM restarts 100 steps back and skips 200 to 500 batches); a learning rate too high for the current state (clip by global norm, warm up, decay); fp16 overflow (the loss scaler skips the step and backs off); a bad interaction of state and data (roll back to a checkpoint; PaLM showed the same batches did not spike from a different checkpoint).</p> <p><strong>35. Why is a large run not bit-reproducible, and when does it matter?</strong> Reduction order varies with topology and batch composition, and floating-point addition is not associative, so the same computation yields different bits under a different batch or placement; kernels that are not batch-invariant are the sharpest version of this. It usually does not matter, but it does when reproducing a divergence to debug it or when promising identical outputs for identical inputs.</p> <hr/> <h4 id="wrapping-up"><strong>Wrapping up</strong></h4> <p>If one habit survives from this post, let it be the accounting: before reasoning about any part of a training system, ask what the bytes are, where they live, and which link they have to cross. Sixteen bytes per parameter is why the state does not fit. The activation formula is why the microbatch is small. The ring’s \(2S(p-1)/p\) is what every collective costs. And the bandwidth ladder, HBM to NVLink to the network, an order of magnitude per rung, is why tensor parallelism stays in the node, pipeline parallelism goes across nodes, and data parallelism sits outside everything. Mixed precision, recomputation, ZeRO, and the parallelism schedules are all the same two moves, make the state smaller or hide the traffic, applied at different layers, and MFU is the number that tells you how well the hiding worked.</p> <p>The other thing worth keeping is how much of this is visible in one public run. Llama 3’s table of parallelism configurations, its 38 to 43% MFU, and its 419 unexpected interruptions in 54 days are the abstractions above made concrete, and reading that section of the report with this post’s vocabulary in hand is the best exercise I know for making the material stick.</p> <p>If you find a mistake anywhere in here, please let me know and I’ll fix it.</p>]]></content><author><name></name></author><category term="GPU"/><category term="ML"/><summary type="html"><![CDATA[How large-scale LLM training systems are designed, from the 16 bytes per parameter and mixed precision to collectives, ZeRO and FSDP, tensor, sequence, context, expert, and pipeline parallelism, 4D composition, MFU, and what breaks at scale]]></summary></entry><entry><title type="html">LLM Inference Systems - From the Roofline to vLLM Internals</title><link href="https://monishver11.github.io/blog/2026/llm-inference-systems/" rel="alternate" type="text/html" title="LLM Inference Systems - From the Roofline to vLLM Internals"/><published>2026-08-23T21:00:00+00:00</published><updated>2026-08-23T21:00:00+00:00</updated><id>https://monishver11.github.io/blog/2026/llm-inference-systems</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/llm-inference-systems/"><![CDATA[<p>Hello all. This post is a top-to-bottom tour of LLM inference systems: what happens between a request arriving at a serving engine and tokens streaming back out, and why the systems are shaped the way they are.</p> <p>The angle throughout is performance. Almost every design decision in a modern inference engine traces back to one fact: generating a token is cheap on arithmetic and expensive on memory traffic. Once that fact is internalized, the whole stack, paged KV caches, continuous batching, speculative decoding, disaggregated prefill, stops looking like a pile of unrelated tricks and starts looking like one idea applied at different layers. So we’ll build the performance model first, with real numbers, and then walk the stack with that model in hand.</p> <p>Concepts come first, but everything is anchored in a real engine: vLLM’s V1 architecture. It’s open source, it’s the engine whose design documents actually explain their reasoning, and pointing at the file that implements an idea keeps the discussion honest. One convention up front: vLLM moves fast, so every vLLM file path, symbol name, and default quoted here was verified against <code class="language-plaintext highlighter-rouge">main</code> as of 2026-08-21, and I’ve kept that stamp next to the claims most likely to move. Papers and official docs are linked inline where each claim is made, and each major section ends with its references.</p> <p>The plan:</p> <ul> <li>The problem: autoregressive generation, the KV cache, and the two phases</li> <li>The numbers that drive everything: GPU specs, the roofline, and three formulas</li> <li>Metrics: TTFT, ITL, TPOT, goodput, and how vLLM measures them</li> <li>Batching: static, continuous, chunked</li> <li>KV cache management and PagedAttention</li> <li>Prefix caching</li> <li>Scheduling in vLLM V1</li> <li>Speculative decoding</li> <li>Distributed inference: TP, PP, DP, EP</li> <li>Disaggregated prefill and KV transfer</li> <li>Attention kernels</li> <li>Quantization and MoE</li> <li>The engine at runtime: processes, CUDA graphs, torch.compile, sampling</li> <li>Test yourself: a question bank over everything above</li> </ul> <p>I’m assuming you’re comfortable with what a transformer is and does; we won’t re-derive attention here. This is a long one, but the sections build on each other in order, and the question bank at the end is there so you can check what stuck.</p> <p>Let’s get started.</p> <hr/> <h4 id="the-problem"><strong>The Problem</strong></h4> <h5 id="autoregressive-generation"><strong>Autoregressive generation</strong></h5> <p>An LLM generates text one token at a time, and each new token depends on all the tokens before it. That single property sets up everything that follows. Producing \(m\) output tokens requires at least \(m\) sequential forward passes; there is no way to parallelize across the output positions of a single request, because position \(t+1\)’s input includes position \(t\)’s output.</p> <p>It has a second consequence that is easy to miss and that a serving engine cannot ignore: <strong>output length is unknown at admission time</strong>. When a request arrives, the engine does not know whether it will generate 5 tokens or 5,000, which means it cannot know how long the request will occupy memory or how much of it. Every memory-management decision in this post is downstream of that uncertainty. A generation ends when the model emits a stop condition; in vLLM the recorded finish reasons are <code class="language-plaintext highlighter-rouge">stop</code> (EOS token or stop string), <code class="language-plaintext highlighter-rouge">length</code> (hit the token limit), and <code class="language-plaintext highlighter-rouge">abort</code> (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">docs/design/metrics.md</a>).</p> <h5 id="the-kv-cache"><strong>The KV cache</strong></h5> <p>Inside each transformer layer, attention needs the key and value projections of every token in the prefix. Recomputing those for the whole prefix on every step would make step \(t\) cost \(O(t)\) redundant work, so instead the engine stores them: the <strong>KV cache</strong> holds the K and V vectors for every token, at every layer, for every live request, and each decode step appends one more token’s worth (<a href="https://arxiv.org/abs/2211.05102">Pope et al.</a>, <a href="https://kipp.ly/transformer-inference-arithmetic/">kipply’s inference arithmetic</a>). This is the classic time-for-space trade, and the space is substantial; we’ll put exact numbers on it in the next section, but for a sense of scale, a single 8K-token request on Llama 3 70B carries 2.5 GiB of cache.</p> <p>The KV cache is <em>state</em>. That one word is why inference serving is a systems problem and not just a kernels problem: requests arrive, grow a variable-sized allocation at an unpredictable rate, and leave at an unpredictable time, and the engine has to pack them into fixed GPU memory without fragmenting it or running out.</p> <h5 id="two-phases-one-cache"><strong>Two phases, one cache</strong></h5> <p>Serving one request has two distinct phases, and they could hardly be more different:</p> <table> <thead> <tr> <th> </th> <th>Prefill</th> <th>Decode</th> </tr> </thead> <tbody> <tr> <td>Input per step</td> <td>the whole prompt (\(P\) tokens)</td> <td>1 token per sequence</td> </tr> <tr> <td>Purpose</td> <td>populate the KV cache, emit the first token</td> <td>extend the KV cache, emit the next token</td> </tr> <tr> <td>Arithmetic intensity</td> <td>\(\approx P\) FLOP/byte</td> <td>\(\approx B\) (batch size) FLOP/byte</td> </tr> <tr> <td>Regime</td> <td>compute-bound</td> <td>memory-bound until \(B\) nears the ridge point</td> </tr> </tbody> </table> <p><strong>Prefill</strong> processes the entire prompt in one forward pass. All \(P\) tokens flow through the model together, every matrix multiply is fat, and the GPU’s compute units are the bottleneck. <strong>Decode</strong> processes one token per sequence per step. The matrix multiplies are skinny (one row per sequence), and the bottleneck flips: the step spends its time streaming the model’s weights out of GPU memory, not doing math on them. vLLM’s own docs use exactly this framing, compute-bound prefill and memory-bound decode (<a href="https://github.com/vllm-project/vllm/blob/main/docs/configuration/optimization.md">docs/configuration/optimization.md</a>).</p> <p>Why the flip happens is worth spelling out, because it’s the load-bearing fact of the whole post. Each forward pass must read every weight of the model from GPU memory, whether it processes 1 token or 10,000. Prefill amortizes that weight read over \(P\) tokens of useful work. A decode step amortizes it over 1 token per sequence, so the only way to amortize it at all is batching: with \(B\) sequences decoding together, one weight read serves \(B\) tokens. That’s the origin of the arithmetic-intensity row in the table (made precise in the next section): prefill’s intensity scales with prompt length, decode’s with batch size.</p> <p>There’s a catch inside the batching remedy, though. Weight reads amortize over the batch, but <strong>KV cache reads do not</strong>: each sequence attends over its <em>own</em> cache, so KV traffic grows with \(B \times \text{context length}\) while weight traffic stays flat. Per decode step the engine moves roughly the weight bytes plus \(B \times n \times \text{KV-bytes-per-token}\) for \(B\) tokens of output. At long contexts the KV term dominates, which is why long-context decode stays memory-bound even at batch sizes that would otherwise saturate compute, and why so much of this post is about making KV bytes smaller, shareable, or better placed.</p> <h5 id="the-engines-inner-loop"><strong>The engine’s inner loop</strong></h5> <p>With those pieces named, a serving engine’s job is easy to state. It maintains a pool of in-flight requests, and it loops: pick which requests run this step, allocate KV memory for the tokens about to be computed, run <strong>one</strong> forward pass for the whole batch, sample one token per sequence, hand back outputs, repeat. Everything in this post lives inside that loop: the metrics measure it, the scheduler drives it, the KV manager feeds it, and the kernels are what it launches.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2211.05102">Efficiently Scaling Transformer Inference - Pope et al.</a></li> <li><a href="https://kipp.ly/transformer-inference-arithmetic/">Transformer Inference Arithmetic - kipply</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/configuration/optimization.md">vLLM optimization docs</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">vLLM metrics design doc</a></li> </ul> <hr/> <h4 id="the-numbers-that-drive-everything"><strong>The Numbers That Drive Everything</strong></h4> <p>Before any mechanisms, the numbers. This section is the reference the rest of the post leans on: the hardware, the one performance model worth internalizing, and three formulas. None of it is hard, but the later sections quote these figures constantly, so it pays to have them in one place. All figures were verified against the linked sources, accessed 2026-08-21.</p> <h5 id="the-hardware"><strong>The hardware</strong></h5> <p>The two GPUs that anchor every example in this post:</p> <table> <thead> <tr> <th> </th> <th>A100 SXM 80GB</th> <th>H100 SXM</th> </tr> </thead> <tbody> <tr> <td>Memory</td> <td>80 GB HBM2e</td> <td>80 GB HBM3</td> </tr> <tr> <td>Bandwidth</td> <td>2,039 GB/s</td> <td>3.35 TB/s</td> </tr> <tr> <td>BF16 dense</td> <td>312 TFLOPS</td> <td>~1000 TFLOPS</td> </tr> <tr> <td>BF16 “with sparsity”</td> <td>624 TFLOPS</td> <td>1,979 TFLOPS</td> </tr> <tr> <td>FP8 dense</td> <td>n/a (no FP8 tensor cores)</td> <td>~2000 TFLOPS</td> </tr> <tr> <td>SMs</td> <td>108</td> <td>132</td> </tr> <tr> <td>L2 cache</td> <td>40 MB</td> <td>50 MB</td> </tr> <tr> <td>Ridge point (dense BF16 / bandwidth)</td> <td>~153 FLOP/B</td> <td>~300 FLOP/B</td> </tr> </tbody> </table> <p>Sources: <a href="https://www.nvidia.com/en-us/data-center/a100/">NVIDIA A100 page</a>, <a href="https://www.nvidia.com/en-us/data-center/h100/">NVIDIA H100 page</a>, <a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">Hopper architecture in-depth</a> (which gives the H100 SM count, L2 size, and dense TFLOPS; its table rounds to 1000/2000), <a href="https://developer.nvidia.com/blog/nvidia-ampere-architecture-in-depth/">Ampere architecture in-depth</a> (A100 SMs and L2). One trap worth flagging: marketing pages quote the “with sparsity” number, which assumes 2:4 structured sparsity in the weights. Dense workloads, which is what LLM inference is, get half of it. When someone quotes 1,979 TFLOPS for an H100, the usable number is ~1000.</p> <p>A quick sketch of the machine behind the table, since the vocabulary recurs. A GPU is an array of <strong>streaming multiprocessors</strong> (SMs); work launches as a grid of thread blocks, blocks are assigned to SMs, and threads execute in <strong>warps</strong> of 32 (SIMT). Each H100 SM has 256 KB of registers and 256 KB of combined shared memory and L1 (of which up to 228 KB is configurable as shared memory); chip-wide sit the 50 MB L2 and the 80 GB of HBM3. <strong>Occupancy</strong> is “the ratio of active warps per multiprocessor to the maximum number of warps” (<a href="https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/index.html">CUDA Best Practices Guide</a>), and it matters because it’s the GPU’s latency-hiding mechanism: when one warp stalls on a memory access, the SM simply issues from another resident warp, so having many warps resident keeps the SM busy through stalls. Hopper also added three features worth knowing by name: <strong>TMA</strong> (Tensor Memory Accelerator), a per-SM DMA engine for asynchronous bulk copies between global and shared memory; <strong>thread block clusters</strong> with distributed shared memory, letting co-scheduled blocks on different SMs access each other’s shared memory; and <strong>FP8 tensor cores</strong> (E4M3 and E5M2 formats), which halve storage per element and double throughput relative to FP16/BF16 (<a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">Hopper in-depth</a>).</p> <h5 id="the-roofline"><strong>The roofline</strong></h5> <p>One model organizes everything: the roofline (<a href="https://dl.acm.org/doi/10.1145/1498765.1498785">Williams, Waterman, Patterson, CACM 2009</a>). Define a kernel’s <strong>arithmetic intensity</strong> as the FLOPs it performs per byte it moves from DRAM:</p> \[I = \frac{\text{FLOPs}}{\text{bytes moved}}\] <p>Then the performance it can attain is capped by whichever binds first, memory bandwidth or peak compute:</p> \[\text{attainable FLOP/s} = \min(\text{peak FLOP/s},\; I \times \text{bandwidth})\] <p>The crossover is the <strong>ridge point</strong>, peak FLOP/s divided by bandwidth. For H100 dense BF16 that’s roughly \(10^{15} / 3.35 \times 10^{12} \approx 300\) FLOP per byte. Below the ridge a kernel is memory-bound: the compute units are starved, and optimizing the math does nothing; the only lever is moving fewer bytes (fuse kernels, shrink the dtype, batch more work per byte). Above the ridge it’s compute-bound, and the levers flip: tensor core utilization, occupancy, scheduling.</p> <p>Now map LLM inference onto it, and the two-phase table from the previous section becomes quantitative:</p> <ul> <li><strong>Decode</strong> has \(I \approx B\): a batch of \(B\) sequences does \(B\) tokens of work per weight read. On an H100 that means decode is memory-bound until batch size approaches ~300, and at, say, batch 64 the attainable fraction of peak is roughly \(64/300 \approx 21\%\). In practice the crossover sits even higher than 300, because the KV-read traffic that grows with batch and context keeps adding bytes to the denominator.</li> <li><strong>Prefill</strong> has \(I \approx P\), the prompt length. Any realistic prompt puts it well above the ridge: prefill is compute-bound almost by construction.</li> <li><strong>Elementwise ops</strong> (norms, activations, RoPE) have \(I = O(1)\): a few FLOPs per element read and written, independent of size. Alone, they are hopelessly memory-bound, which is the entire case for kernel fusion: fused into a neighboring op, they ride on bytes that were already moving. (This is the same accounting that makes a standalone LayerNorm memory-bound; I went through that op’s arithmetic in the <a href="/blog/2026/layernorm-rmsnorm/">LayerNorm and RMSNorm post</a>.)</li> </ul> <p>One more roofline remark that will matter in the quantization section: the dtype moves <em>both</em> lines. FP8 doubles peak FLOP/s (the roof rises) and halves the bytes per element (the kernel’s intensity rises). Both shifts push a kernel toward compute-bound.</p> <h5 id="the-models"><strong>The models</strong></h5> <p>The Llama 3 family is the running example, because its architecture table is public and its sizes span the practical range (<a href="https://arxiv.org/abs/2407.21783">Llama 3 paper, Table 3</a>):</p> <table> <thead> <tr> <th> </th> <th>8B</th> <th>70B</th> <th>405B</th> </tr> </thead> <tbody> <tr> <td>Layers \(L\)</td> <td>32</td> <td>80</td> <td>126</td> </tr> <tr> <td>\(d_{model}\)</td> <td>4096</td> <td>8192</td> <td>16384</td> </tr> <tr> <td>Q heads</td> <td>32</td> <td>64</td> <td>128</td> </tr> <tr> <td>KV heads</td> <td>8</td> <td>8</td> <td>8</td> </tr> <tr> <td>head_dim</td> <td>128</td> <td>128</td> <td>128</td> </tr> <tr> <td>FFN dim</td> <td>14336</td> <td>28672</td> <td>53248</td> </tr> <tr> <td>Vocab</td> <td>128K</td> <td>128K</td> <td>128K</td> </tr> </tbody> </table> <p>Notice the KV-heads row: 8 across the whole family. That’s grouped-query attention (GQA), and it exists precisely to shrink the KV cache; the formula below shows by how much.</p> <h5 id="formula-1-kv-bytes-per-token"><strong>Formula 1: KV bytes per token</strong></h5> \[\text{KV bytes per token} = 2 \times L \times H_{kv} \times d_{head} \times b\] <p>where the leading 2 counts K and V, \(H_{kv}\) is the number of KV heads, and \(b\) is bytes per element (<a href="https://kipp.ly/transformer-inference-arithmetic/">kipply</a>). Running the Llama 3 numbers at bf16 (\(b = 2\)):</p> <table> <thead> <tr> <th>Model</th> <th>Per token</th> <th>At 8K context</th> </tr> </thead> <tbody> <tr> <td>8B</td> <td>\(2 \cdot 32 \cdot 8 \cdot 128 \cdot 2\) = <strong>128 KiB</strong></td> <td>1.0 GiB</td> </tr> <tr> <td>70B</td> <td>\(2 \cdot 80 \cdot 8 \cdot 128 \cdot 2\) = <strong>320 KiB</strong></td> <td>2.5 GiB</td> </tr> <tr> <td>405B</td> <td>\(2 \cdot 126 \cdot 8 \cdot 128 \cdot 2\) = <strong>504 KiB</strong></td> <td>4.0 GiB</td> </tr> </tbody> </table> <p>These are the numbers to keep loaded. A single long-context request on the 70B model carries gigabytes of state, and note what the formula does <em>not</em> contain: the number of query heads. GQA is why: 64 query heads share 8 KV heads on the 70B model, an 8x saving already baked in. With full multi-head attention (\(H_{kv} = 64\)), the 70B figure would be 2.5 MiB per token and 20 GiB per 8K request, which is the difference between a GPU holding a healthy batch of requests in cache and holding a handful.</p> <h5 id="formula-2-flops-per-token"><strong>Formula 2: FLOPs per token</strong></h5> \[\text{FLOPs per token} \approx 2N \;+\; 2 \, L \, n_{ctx} \, d_{model}\] <p>This is <a href="https://arxiv.org/abs/2001.08361">Kaplan et al., Eq. 2.2</a>, and both terms are worth deriving rather than memorizing.</p> <p><strong>The \(2N\) term.</strong> Start from the general matmul count: multiplying an \(m \times k\) matrix by a \(k \times n\) matrix costs \(2mnk\) FLOPs, the 2 counting the multiply and the add of each multiply-accumulate. A single token passing through a linear layer is the \(m = 1\) case: a \(1 \times k\) row times a \(k \times n\) weight matrix costs \(2kn\) FLOPs, and \(kn\) is exactly the number of parameters in that weight matrix. So one token through one linear layer costs <strong>2 FLOPs per parameter</strong>, and since a forward pass is one trip through every weight matrix in the model (the QKV and output projections, the FFN matrices, all \(L\) layers of them), the totals just add up: roughly 2 FLOPs per parameter overall, hence \(2N\). If you want the op-by-op ledger behind this, block by block with the bytes counted too, I’ve worked through it in the <a href="/blog/2026/transformer-block-accounting/">transformer block accounting post</a>.</p> <p><strong>What \(N\) is.</strong> \(N\) is the parameter count <em>excluding the embedding table</em>, and the exclusion has a mechanical justification: the input embedding is a table lookup, not a matmul, so its parameters contribute zero FLOPs. The output side is different, and worth being precise about since it’s a common point of confusion: the final logits projection (hidden state times the \(d_{model} \times V\) unembedding matrix) <em>is</em> a real matmul. The \(\approx\) absorbs it because it’s one matrix against the \(L\) layers’ worth in \(N\): for Llama 3 8B it’s \(2 \cdot 4096 \cdot 128256 \approx 1.1\) GFLOP per token against \(2N \approx 16\) GFLOP, about 7%, and the fraction shrinks as models grow (about 1.5% at 70B).</p> <p><strong>The attention term.</strong> Attention against the cached context is the one part of the forward pass that is <em>not</em> a multiply through a fixed weight matrix; its operand is the KV cache, and its cost grows with context. Per layer, the new token’s query is dotted against all \(n_{ctx}\) cached keys and the resulting weights are applied to the \(n_{ctx}\) values, work proportional to \(n_{ctx} \, d_{model}\); Kaplan’s accounting puts the total at \(2 \, L \, n_{ctx} \, d_{model}\) per token. It stays small until contexts get long: at 8K context on Llama 3 8B it’s \(2 \cdot 32 \cdot 8192 \cdot 4096 \approx 2.1\) GFLOP against the 16 GFLOP of \(2N\), about 13%, but it scales linearly with \(n_{ctx}\) while \(2N\) stays fixed.</p> <p><strong>And the \(6N\) you’ll see quoted for training.</strong> Same \(N\), different pass structure. Training costs roughly \(6N\) per token: the \(2N\) forward, plus a backward pass that costs about twice the forward, because each layer’s backward computes <em>two</em> matmul-shaped quantities, the gradient with respect to the weights and the gradient with respect to the input, each about a forward’s worth of work. (Those are exactly the outer-product and transposed-matmul shapes derived in the <a href="/blog/2026/mlp-forward-backward/">MLP forward and backward post</a>.) Inference has no backward pass, so it’s the \(2N\) forward only.</p> <h5 id="formula-3-the-decode-floor"><strong>Formula 3: the decode floor</strong></h5> <p>This is the punchline the previous two set up. A decode step must read every weight byte, so, however good the kernels are:</p> \[\text{minimum decode step time} = \frac{\text{weight bytes}}{\text{memory bandwidth}}\] <p>Worked example, Llama 3 8B in bf16 on one H100: 16 GB of weights over 3.35 TB/s gives</p> \[\frac{16 \times 10^9}{3.35 \times 10^{12}} \approx 4.8 \text{ ms per step}\] <p>so single-stream decode is capped at roughly <strong>210 tokens/s regardless of kernel quality</strong>. Now compare the arithmetic for that same step: \(2N = 16 \times 10^9\) FLOPs at \(10^{15}\) FLOP/s is about <strong>16 µs</strong>, roughly 300x less than the memory time. The compute is essentially free; the step <em>is</em> the weight read. Every decode-latency technique in this post, batching, speculative decoding, tensor parallelism, quantization, is an attack on this one number: more useful tokens per weight read, or fewer weight bytes per read, or the read split across more GPUs.</p> <h5 id="constants-and-defaults-worth-pinning"><strong>Constants and defaults worth pinning</strong></h5> <ul> <li>Bytes per element: bf16/fp16 = 2, fp8/int8 = 1, int4 = 0.5.</li> <li>Weights at bf16 are roughly 2 bytes per parameter: the 8B model is 16 GB, 70B is 140 GB, 405B is 810 GB, before any KV cache, activations, or CUDA context. The 70B model doesn’t even fit on one 80 GB GPU; that observation is where the distributed section starts.</li> <li>vLLM defaults, from <code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code> as of 2026-08-21: <code class="language-plaintext highlighter-rouge">gpu_memory_utilization = 0.92</code> (older docs say 0.9; the current code says 0.92), and a KV block size of 16 tokens (<code class="language-plaintext highlighter-rouge">CacheConfig.DEFAULT_BLOCK_SIZE</code>, applied when unset; platform or attention-backend preferences can override it, <code class="language-plaintext highlighter-rouge">vllm/platforms/interface.py</code>). Both matter in the KV cache section.</li> </ul> <h5 id="why-re-read-the-weights-every-step-at-all"><strong>Why re-read the weights every step at all?</strong></h5> <p>A fair objection to formula 3, and a question worth settling before moving on: the weights never change during inference, so why does every decode step pay to “read” them again? Can’t they just stay where they are?</p> <p>They <em>do</em> stay where they are, in HBM; nothing re-uploads them from the CPU between steps. The read the floor charges is a different trip: from HBM to the compute units. Arithmetic happens in registers on the SMs, and the total on-chip storage, the 50 MB L2 plus each SM’s registers and shared memory, adds up to roughly 100 MB on an H100, against 16 GB of weights for even the small model. The weights physically cannot live next to the ALUs. So each layer’s matrices must stream on-chip, get used, and be overwritten by the next layer’s matrices, every single forward pass; “keeping the weights fixed” is exactly what already happens, and it doesn’t help, because the cost was never re-loading them onto the GPU, it was moving them across the last few centimeters. Caches don’t rescue this either: within one batch-1 decode step each weight is touched exactly once, and a value used once gains nothing from any cache tier. The only reuse available is <em>across sequences in the same step</em>, which is precisely what batching exploits.</p> <p>Framed this way, the remedies from the rest of the post sort themselves: get more tokens out of each streaming pass (batching, speculative decoding), make the stream smaller (weight quantization), or split the stream across more HBM stacks in parallel (tensor parallelism). That’s the whole menu, and it’s the roofline’s memory-bound prescription, reduce or amortize bytes, applied to the one byte stream that can’t be skipped.</p> <p><strong>References</strong></p> <ul> <li><a href="https://www.nvidia.com/en-us/data-center/a100/">NVIDIA A100 page</a>, <a href="https://www.nvidia.com/en-us/data-center/h100/">NVIDIA H100 page</a></li> <li><a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">NVIDIA Hopper architecture in-depth</a>, <a href="https://developer.nvidia.com/blog/nvidia-ampere-architecture-in-depth/">NVIDIA Ampere architecture in-depth</a></li> <li><a href="https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/index.html">CUDA C++ Best Practices Guide</a></li> <li><a href="https://dl.acm.org/doi/10.1145/1498765.1498785">Roofline: An Insightful Visual Performance Model - Williams, Waterman, Patterson</a></li> <li><a href="https://arxiv.org/abs/2407.21783">The Llama 3 Herd of Models</a></li> <li><a href="https://arxiv.org/abs/2001.08361">Scaling Laws for Neural Language Models - Kaplan et al.</a></li> <li><a href="https://kipp.ly/transformer-inference-arithmetic/">Transformer Inference Arithmetic - kipply</a></li> </ul> <hr/> <h4 id="metrics"><strong>Metrics</strong></h4> <p>Before any more machinery, we need to fix what “fast” means. Every metric here is one of two kinds. <strong>Latency</strong> is one request’s experience: how long did <em>this</em> user wait. <strong>Throughput</strong> is the operator’s aggregate: how much work the whole system completes per second. The two pull against each other, and this section is about measuring each honestly.</p> <p>One piece of vocabulary first: latency is a distribution over many samples, not a number, and <strong>p99</strong> names the value that 99% of samples fall below (so p99 ITL = 200 ms means 1 gap in 100 was worse than that). Serving is judged at p99 rather than the mean because a request generating 500 tokens samples the gap distribution 500 times: even a 1-in-100 stall hits essentially every long request, and the user feels the freeze, not the average. That is why the targets later in this section are stated at percentiles.</p> <p>The definitions:</p> <table> <thead> <tr> <th>Metric</th> <th>Stands for</th> <th>Definition</th> <th>What to watch</th> </tr> </thead> <tbody> <tr> <td>TTFT</td> <td>time to first token</td> <td>request sent to first token received</td> <td>queueing plus prefill; client-side it also includes tokenization</td> </tr> <tr> <td>ITL</td> <td>inter-token latency</td> <td>gap between consecutive tokens</td> <td>the tail (p99) exposes prefill insertions and preemptions</td> </tr> <tr> <td>TPOT</td> <td>time per output token</td> <td>\((\text{E2EL} - \text{TTFT}) / (\text{output tokens} - 1)\)</td> <td>a per-request average; hides stalls</td> </tr> <tr> <td>E2EL</td> <td>end-to-end latency</td> <td>request sent to last token</td> <td>\(= \text{TTFT} + \sum \text{ITL}\)</td> </tr> <tr> <td>Throughput</td> <td> </td> <td>tokens/s or requests/s completed</td> <td>meaningless without a latency constraint</td> </tr> <tr> <td>Goodput</td> <td> </td> <td>requests/s meeting <strong>all</strong> stated SLOs</td> <td>the comparable headline number (<a href="https://arxiv.org/abs/2401.09670">DistServe</a>)</td> </tr> </tbody> </table> <h5 id="itl-versus-tpot"><strong>ITL versus TPOT</strong></h5> <p>These two sound interchangeable and are not. ITL is a <em>sample per decode step</em>: every gap between consecutive tokens is one measurement, so ITL has a distribution and a meaningful p99. TPOT is a <em>per-request mean</em>: total decode time divided by tokens generated. If a request stalls once for 800 ms mid-generation, that stall is one enormous ITL sample, while TPOT smears it across a few hundred tokens and barely moves. When hunting scheduling pathologies, ITL p99 is the metric that talks.</p> <p>And the classic pathology to keep in mind: <strong>admitting a prompt helps that request’s TTFT and hurts everyone else’s ITL</strong>, because its prefill tokens ride in the same forward pass as the in-flight decodes and stretch the step for all of them. Every scheduler decision in this post sits somewhere on that trade-off.</p> <h5 id="tokenss-versus-requestss"><strong>Tokens/s versus requests/s</strong></h5> <p>The throughput row lists two units because they answer different questions. <strong>Tokens/s</strong> counts the engine’s actual work (a 900-token generation is 100x the work of a 9-token one, and is weighted accordingly), which makes it the unit for comparing engines and kernels. <strong>Requests/s</strong> is what capacity planning buys: how many users one replica serves. Converting between them requires the workload’s average tokens per request, so the two agree only on a fixed workload. One convention to check in any benchmark: whether “tokens” means output tokens only or prompt plus output; both get reported, and on prefill-heavy workloads the difference is large.</p> <h5 id="goodput"><strong>Goodput</strong></h5> <p>Raw throughput has a flaw that makes benchmark headlines misleading: <strong>any engine can buy tokens/s with batch size, at the cost of latency</strong>. One decode step reads the weights once and yields one token per sequence, so a bigger batch means more tokens per step; but the step also gets slower as the batch grows, so every user’s ITL worsens at the same time (a decoding user gets exactly one token per step, so their ITL <em>is</em> the step time, and each added sequence makes the step longer by adding its own KV reads, sampling work, and FLOPs on top of the fixed weight read). The same engine on the same GPU can report 3000 or 5000 tok/s depending on how much latency it is configured to inflict, which is why a tokens/s number with no latency constraint attached is not comparable across systems.</p> <p>Goodput is the fix (<a href="https://arxiv.org/abs/2401.09670">DistServe</a>): state the latency requirements first, then count only the requests that met <em>all</em> of them. With requirements “TTFT under 500 ms and TPOT under 50 ms,” a request that finished but took 900 ms to first token counts <strong>zero</strong>. Now push batch size up: raw throughput keeps rising, but more and more requests miss their bounds and drop out of the count, so goodput rises, peaks, and then falls. That peak is the honest operating point, and goodput, together with the exact requirements behind it, is the number to ask for when someone quotes a benchmark.</p> <h5 id="slos"><strong>SLOs</strong></h5> <p>Those stated requirements are <strong>SLOs</strong> (service-level objectives); an SLO written into a contract with consequences is an <strong>SLA</strong>. For an LLM service they are typically a TTFT bound and an ITL or TPOT bound, stated at a percentile (p99 under some threshold, not the mean). The values come from the product, not the engine: interactive chat needs a fast first token and a stream that outpaces reading, while an offline pipeline may need no per-token bound at all and should simply run for raw throughput. Once SLOs are fixed, tuning has a concrete objective: raise batch and load until the bound percentile approaches its limit, which is exactly operating at maximum goodput. The scheduling section’s knobs are the levers this procedure adjusts.</p> <h5 id="how-vllm-measures"><strong>How vLLM measures</strong></h5> <p>Definitions are only half the story; where the timestamps come from decides what the numbers mean. vLLM measures on both sides of the wire, and the two views differ in instructive ways.</p> <p><strong>Client side.</strong> The <code class="language-plaintext highlighter-rouge">vllm bench serve</code> benchmark records per-request timing in <code class="language-plaintext highlighter-rouge">RequestFuncOutput</code> (<code class="language-plaintext highlighter-rouge">vllm/benchmarks/lib/endpoint_request_func.py</code>, as of 2026-08-21): a timestamp per SSE chunk, with the first content chunk marking TTFT. Two subtleties are worth knowing. ITL here is per <em>chunk</em>, not per token, because servers may bundle several tokens into one chunk; the true token count comes from the response’s <code class="language-plaintext highlighter-rouge">usage</code> field. And client-measured TTFT includes network, HTTP handling, tokenization, queueing, and prefill, the entire stack. <code class="language-plaintext highlighter-rouge">calculate_metrics</code> in <code class="language-plaintext highlighter-rouge">vllm/benchmarks/serve.py</code> then computes TPOT as \((\text{latency} - \text{ttft})/(\text{output len} - 1)\), and the <code class="language-plaintext highlighter-rouge">--goodput ttft:500 tpot:50</code> flag counts a request only if every listed SLO passes.</p> <p><strong>Engine side.</strong> The V1 engine core emits per-request events, <code class="language-plaintext highlighter-rouge">QUEUED</code>, <code class="language-plaintext highlighter-rouge">SCHEDULED</code>, <code class="language-plaintext highlighter-rouge">PREEMPTED</code> (<code class="language-plaintext highlighter-rouge">vllm/v1/engine/__init__.py</code>), plus a per-iteration batch timestamp, and the frontend assembles them into intervals in <code class="language-plaintext highlighter-rouge">vllm/v1/metrics/stats.py</code> (<code class="language-plaintext highlighter-rouge">IterationStats</code>): queue time is QUEUED to first SCHEDULED, prefill is first SCHEDULED to first token, decode is first token to last token, and inference is SCHEDULED to last token. Three deliberate choices in that design are worth calling out:</p> <ul> <li><strong>TTFT is measured from the frontend’s <code class="language-plaintext highlighter-rouge">arrival_time</code>, a wall clock that starts at tokenization</strong>, precisely so that input processing is included (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">docs/design/metrics.md</a>). Monotonic timestamps are never compared across processes, since they have no shared origin.</li> <li><strong>Preemptions are included, not reset.</strong> If a request is preempted mid-decode and resumed, the gap stays inside its decode and inference intervals; only the first SCHEDULED event sets the scheduled timestamp. This is the right call for SLO-facing metrics: the user experienced that gap, so the histogram should contain it.</li> <li>A request counts as “prefilling” until its first output token arrives (<code class="language-plaintext highlighter-rouge">is_prefilling</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/engine/output_processor.py</code>), which keeps the phase boundary consistent with what the client observes.</li> </ul> <p>The exported Prometheus histograms follow directly: <code class="language-plaintext highlighter-rouge">vllm:time_to_first_token_seconds</code>, <code class="language-plaintext highlighter-rouge">vllm:inter_token_latency_seconds</code>, <code class="language-plaintext highlighter-rouge">vllm:request_time_per_output_token_seconds</code>, <code class="language-plaintext highlighter-rouge">vllm:e2e_request_latency_seconds</code>, plus per-phase <code class="language-plaintext highlighter-rouge">request_queue_time</code>, <code class="language-plaintext highlighter-rouge">request_prefill_time</code>, and <code class="language-plaintext highlighter-rouge">request_decode_time</code> (<code class="language-plaintext highlighter-rouge">vllm/v1/metrics/loggers.py</code>).</p> <p>The delta between client TTFT and engine TTFT is itself a diagnostic: it isolates everything outside the engine, network, HTTP, client-side stream handling. If the two diverge, the problem isn’t the scheduler.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2401.09670">DistServe - Zhong et al.</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">vLLM metrics design doc</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/benchmarks/lib/endpoint_request_func.py</code>, <code class="language-plaintext highlighter-rouge">vllm/benchmarks/serve.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/metrics/stats.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/metrics/loggers.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/engine/output_processor.py</code></li> </ul> <hr/> <h4 id="batching"><strong>Batching</strong></h4> <p>Batching is the first and biggest lever on the decode floor, and its history in serving systems is short enough to tell in three steps.</p> <p><strong>Static batching</strong> is the naive version: assemble a batch of requests, run it until every request finishes, then assemble the next one. Two things are wasted. Requests that finish early leave dead slots in the batch, doing nothing until the longest request drains; and arriving requests wait for the entire batch to complete before they can start. With generation lengths varying by orders of magnitude across requests, both wastes are large.</p> <p><strong>Continuous batching</strong> fixes this by re-forming the batch <em>every step</em>. This is Orca’s iteration-level scheduling (<a href="https://www.usenix.org/conference/osdi22/presentation/yu">Yu et al., OSDI ‘22</a>), and the name says exactly what changed: the unit the scheduler hands to the GPU shrinks from “a whole generation” to “one iteration,” a single forward pass producing one token per sequence. After every iteration, control returns to the scheduler, which removes sequences that just finished and admits waiting ones before launching the next iteration. A finished request’s slot is refilled at the very next step instead of idling until the batch drains, and a new arrival waits at most one step to start rather than one full batch.</p> <p>The two diagrams below (from <a href="https://www.anyscale.com/blog/continuous-batching-llm-inference">Anyscale’s continuous batching post</a>) are the cleanest mental model I’ve seen for this. Read each grid as: one row per batch slot, one column per model step (\(T_1, T_2, \ldots\)), yellow cells are prompt tokens, blue cells are generated tokens, red is the end-of-sequence token, and a white cell is a step that slot spent doing nothing.</p> <div class="row justify-content-center"> <div class="col-sm-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-inference/static-batching-480.webp 480w,/assets/img/llm-inference/static-batching-800.webp 800w,/assets/img/llm-inference/static-batching-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/llm-inference/static-batching.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="static batching" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Static batching. Left: the batch mid-flight. Right: S3 finishes at T5 and S1 at T6, but their slots sit empty (white) until the longest sequence, S2, finally finishes at T8; only then can the next batch start. Credits: <a href="https://www.anyscale.com/blog/continuous-batching-llm-inference">Anyscale, "How continuous batching enables 23x throughput in LLM inference"</a>. </div> <div class="row justify-content-center"> <div class="col-sm-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-inference/continuous-batching-480.webp 480w,/assets/img/llm-inference/continuous-batching-800.webp 800w,/assets/img/llm-inference/continuous-batching-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/llm-inference/continuous-batching.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="continuous batching" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Continuous batching, same workload. The moment a sequence emits END, its slot is handed to a waiting request at the next step: S5, S6, and S7 begin their (yellow) prefills in slots that static batching left idle. No white cells. Credits: <a href="https://www.anyscale.com/blog/continuous-batching-llm-inference">Anyscale</a>. </div> <p>Making one forward pass serve this mixed population takes one more idea, and it’s Orca’s second contribution: <strong>selective batching</strong>. The obstacle is that the sequences in a step are at different lengths and even different phases, one slot might be prefilling 512 prompt tokens while three others decode 1 token each, and tensors of such ragged shapes don’t stack into the neat batched matmul that batching normally relies on. Orca’s observation is that the ops in a transformer layer split into two classes, and only one of them cares. The linear layers, norms, and activations are <em>per-token</em>: they apply the same weights to each token’s vector independently, with no interaction between tokens, so the raggedness is irrelevant; flatten all the tokens from all sequences into one long \([\text{total tokens} \times d_{model}]\) matrix and run them as a single fused matmul, exactly as if they were one sequence. Attention is the exception: each token must attend over <em>its own sequence’s</em> K and V, of its own length, so sequences can’t be flattened together there. Hence “selective”: batch every op except attention across the flattened tokens, then split at the attention boundary and let each sequence attend against its own cache. This is precisely the shape of vLLM’s fused step from the previous section, prompt chunks and decodes flattened into one forward pass, with the per-sequence work pushed into the attention kernel (the kernels section shows how PagedAttention handles the raggedness).</p> <p><strong>Chunked prefill</strong> addresses the tension continuous batching creates. Once prompts and decodes share steps, a 10K-token prompt entering the batch means one step must process 10K tokens instead of a few dozen, and every co-batched decode inherits that multi-hundred-millisecond step as a frozen token stream; this is exactly the ITL-tail pathology from the metrics section. Sarathi-Serve’s fix (<a href="https://arxiv.org/abs/2403.02310">Agrawal et al.</a>) is to stop treating a prompt as atomic. vLLM V1 has it on by default (<a href="https://github.com/vllm-project/vllm/blob/main/docs/configuration/optimization.md">docs/configuration/optimization.md</a>), built from three interlocking pieces:</p> <ul> <li><strong>A per-step token budget.</strong> Each step may run at most <code class="language-plaintext highlighter-rouge">max_num_batched_tokens</code> tokens through the fused forward pass (the numbers section’s defaults: 2048 to 16384 depending on GPU and entrypoint). Since step time grows with tokens processed, capping tokens per step caps the duration of every step, and a bound on step time is a bound on every ITL sample: the budget is the knob that decides the worst-case gap any user can experience.</li> <li><strong>Decodes are scheduled first.</strong> Within a step’s budget, every running sequence gets its 1 decode token before any prompt gets anything. Decodes are tiny (a batch of 200 running requests consumes just 200 tokens of budget), so this is cheap, and it guarantees in-flight streams never stall behind a newcomer’s prompt: their ITL is protected by construction.</li> <li><strong>Prefill gets the leftovers, in chunks.</strong> Whatever budget remains after the decodes goes to waiting prompts, and a prompt larger than the leftover is <em>chunked</em>: it runs partway this step and continues next step. With a budget of 8192 and 200 running decodes, a 20K-token prompt gets 7992 tokens processed in its first step, then continues across the following steps until done; the KV cache accumulates across chunks exactly as it would have in one shot, so nothing is recomputed.</li> </ul> <p>Put together: every step is decodes plus at most a budget’s worth of prefill, so steps stay uniformly sized and ITL stays flat, while the long prompt’s TTFT stretches over the few extra steps its chunks now take. The spike doesn’t vanish; it’s amortized, and the budget (plus a per-request chunk cap we’ll meet in the scheduling section) sets the exchange rate between the long request’s TTFT and everyone else’s ITL.</p> <p>There’s a fourth step in this progression, running prefill and decode on <em>different GPUs entirely</em> (<a href="https://arxiv.org/abs/2311.18677">Splitwise</a>, <a href="https://arxiv.org/abs/2401.09670">DistServe</a>), which removes the interference class rather than bounding it. That one gets its own section later, because it needs the KV-transfer machinery in between.</p> <p><strong>References</strong></p> <ul> <li><a href="https://www.usenix.org/conference/osdi22/presentation/yu">Orca: A Distributed Serving System for Transformer-Based Generative Models - Yu et al., OSDI ‘22</a></li> <li><a href="https://arxiv.org/abs/2403.02310">Taming Throughput-Latency Tradeoff in LLM Inference with Sarathi-Serve - Agrawal et al.</a></li> <li><a href="https://arxiv.org/abs/2311.18677">Splitwise - Patel et al.</a>, <a href="https://arxiv.org/abs/2401.09670">DistServe - Zhong et al.</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/configuration/optimization.md">vLLM optimization docs</a></li> </ul> <hr/> <h4 id="kv-cache-management-and-pagedattention"><strong>KV Cache Management and PagedAttention</strong></h4> <h5 id="the-problem-contiguous-allocation-wastes-most-of-the-memory"><strong>The problem: contiguous allocation wastes most of the memory</strong></h5> <p>Continuous batching made the batch fluid, and in doing so it turned KV memory into the binding constraint: the batch can only be as large as the caches it can hold. So how the cache is allocated matters enormously, and the pre-vLLM answer was bad.</p> <p>The naive scheme reserves one contiguous KV region per request, sized for the maximum possible length, because the engine doesn’t know the output length up front (the admission-time uncertainty from the first section). That wastes memory three ways: <strong>reservation</strong> for tokens that are never generated, <strong>internal fragmentation</strong> in the unused tail of each region, and <strong>external fragmentation</strong> in the holes between differently-sized regions that nothing fits into. The vLLM team measured existing systems wasting <strong>60 to 80%</strong> of their KV memory this way (<a href="https://vllm.ai/blog/2023-06-20-vllm">vLLM launch blog</a>).</p> <h5 id="the-idea-paging-lifted-straight-from-operating-systems"><strong>The idea: paging, lifted straight from operating systems</strong></h5> <p>PagedAttention (<a href="https://arxiv.org/abs/2309.06180">Kwon et al., SOSP 2023</a>) applies the oldest trick in the OS book. Chop KV memory into fixed-size <strong>blocks</strong> (16 tokens each by default; <code class="language-plaintext highlighter-rouge">CacheConfig.DEFAULT_BLOCK_SIZE</code>, <code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code>). A request’s cache is a list of possibly non-contiguous blocks, and a per-request <strong>block table</strong> maps logical block \(i\) to a physical block id, exactly like a page table maps virtual to physical pages. The attention kernel is co-designed to read KV through that one level of indirection (covered in the kernels section), which is what makes the scheme cheap enough to use.</p> <p>The accounting flips immediately. Blocks are allocated on demand as the sequence grows, so reservation waste disappears. All blocks are the same size, so external fragmentation is <em>impossible</em>: any free block fits any request. What remains is internal fragmentation in each sequence’s last partial block, bounded by \(\text{block\_size} - 1 = 15\) token slots per sequence; the launch blog puts the total waste under 4%, versus the 60 to 80% before. The paper’s claimed result: 2-4x serving throughput over FasterTransformer and Orca at the same latency, larger at longer sequences (<a href="https://arxiv.org/abs/2309.06180">paper abstract</a>). Nearly all of that is just the bigger batch the reclaimed memory affords.</p> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-inference/paged-kv.svg" sizes="95vw"/> <img src="/assets/img/llm-inference/paged-kv.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Paged KV cache: logical blocks, block table, physical pool" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The paging indirection for one request. Its 54 tokens form 4 logical blocks (the last one partial, the only internal fragmentation there is); the block table maps each logical block to a physical block id; and the physical pool holds those blocks scattered wherever free space happened to be, interleaved with another request's blocks and with free blocks that still carry old cached contents. Editable source: <a href="/assets/img/llm-inference/paged-kv.excalidraw">paged-kv.excalidraw</a>. </div> <h5 id="how-many-blocks-exist-startup-memory-accounting"><strong>How many blocks exist: startup memory accounting</strong></h5> <p>The block pool’s size is decided once, at startup, by measurement rather than estimation. From <code class="language-plaintext highlighter-rouge">determine_available_memory</code> in <code class="language-plaintext highlighter-rouge">vllm/v1/worker/gpu_worker.py</code> and <code class="language-plaintext highlighter-rouge">request_memory</code> in <code class="language-plaintext highlighter-rouge">vllm/v1/worker/utils.py</code>, as of 2026-08-21:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>requested        = total_gpu_memory * gpu_memory_utilization   # default 0.92
kv_cache_memory  = requested - weights - peak_activation - non_torch
                   [- cudagraph estimate, if enabled]
num_blocks       = kv_cache_memory / page_size_bytes
</code></pre></div></div> <p>Two details matter. <code class="language-plaintext highlighter-rouge">gpu_memory_utilization</code> multiplies <strong>total</strong> device memory, not free memory, so a GPU shared with another process will overcommit unless the setting is lowered (or <code class="language-plaintext highlighter-rouge">kv_cache_memory_bytes</code> is set to bypass the calculation entirely). And peak activation memory is <em>measured by running an actual dummy forward pass</em> (<code class="language-plaintext highlighter-rouge">profile_run()</code>), not modeled, which is the robust choice given how many configuration knobs affect it.</p> <p>The per-block byte cost follows the KV formula from the numbers section, restated per block per layer (<code class="language-plaintext highlighter-rouge">page_size_bytes</code>, <code class="language-plaintext highlighter-rouge">FullAttentionSpec</code> in <code class="language-plaintext highlighter-rouge">vllm/v1/kv_cache_interface.py</code>):</p> \[\text{page\_size\_bytes} = 2 \times \text{block\_size} \times H_{kv} \times d_{head} \times b\] <p>For Llama 3 8B at bf16 with block size 16: \(2 \cdot 16 \cdot 8 \cdot 128 \cdot 2 = 64\) KiB per block per layer, times 32 layers = <strong>2 MiB of pool memory per 16-token block</strong>, consistent with the 128 KiB/token figure from earlier. At startup vLLM divides the pool by the blocks a maximum-length request would need and logs the result as “Maximum concurrency for … requests” (<code class="language-plaintext highlighter-rouge">get_max_concurrency_for_kv_cache_config</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_utils.py</code>); it’s the first line to read when debugging capacity.</p> <h5 id="the-three-layers-of-the-v1-design"><strong>The three layers of the V1 design</strong></h5> <p>Before the data structures, a quick cast of characters, because the rest of this section leans on them. vLLM V1 splits serving across processes (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/arch_overview.md">docs/design/arch_overview.md</a>): an <strong>API server</strong> process handles HTTP and tokenization, and an <strong>engine core</strong> process runs the serving loop itself. Inside the engine core lives the <strong>scheduler</strong>: every step, it decides which requests run and how many tokens each gets, and, since KV memory is the resource those decisions hinge on, it also owns all the KV bookkeeping below. The <strong>workers</strong> (one process driving each GPU) hold the model weights and the physical KV pool, and execute the fused forward pass they are told to run. The division of labor to hold onto: the scheduler <em>decides</em> and does its bookkeeping on the CPU; the workers <em>execute</em> on the GPU; and what crosses between them each step is a small instruction packet, which requests to run, how many tokens each, and which physical block ids to write KV into. The full architecture, why the processes are split this way and what else lives where, gets its own section near the end of the post (The Engine at Runtime); this much is enough for everything here.</p> <p>With the roles fixed, the memory machinery is three layers:</p> <table> <thead> <tr> <th>Layer</th> <th>Class / file (as of 2026-08-21)</th> <th>Job</th> </tr> </thead> <tbody> <tr> <td>Block bookkeeping</td> <td><code class="language-plaintext highlighter-rouge">KVCacheBlock</code>, <code class="language-plaintext highlighter-rouge">BlockPool</code> (<code class="language-plaintext highlighter-rouge">vllm/v1/core/block_pool.py</code>)</td> <td>every block object preallocated at init; tracks <code class="language-plaintext highlighter-rouge">ref_cnt</code> and <code class="language-plaintext highlighter-rouge">block_hash</code>; free blocks live on an intrusive doubly-linked list</td> </tr> <tr> <td>Per-request logic</td> <td><code class="language-plaintext highlighter-rouge">KVCacheManager</code> (<code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_manager.py</code>)</td> <td><code class="language-plaintext highlighter-rouge">get_computed_blocks()</code> (prefix-cache hits), <code class="language-plaintext highlighter-rouge">allocate_slots()</code>, <code class="language-plaintext highlighter-rouge">free()</code>; called by the <strong>scheduler</strong>, not the worker</td> </tr> <tr> <td>GPU-side mapping</td> <td>block table (<code class="language-plaintext highlighter-rouge">vllm/v1/worker/block_table.py</code>)</td> <td>logical-to-physical indices fed to the attention kernels; append-only in V1</td> </tr> </tbody> </table> <p>One field in that first row needs defining now, because everything below uses it: <strong><code class="language-plaintext highlighter-rouge">ref_cnt</code> is a block’s reference count, the number of live requests currently using that block</strong>. It exists because blocks can be <em>shared</em>: prefix caching (the next section’s subject) lets several requests point at the same physical block instead of each computing its own copy, and a shared block can only be recycled once nobody points at it, i.e., when its count is back to zero.</p> <p>Two design choices in that table repay attention. First, every <code class="language-plaintext highlighter-rouge">KVCacheBlock</code> is preallocated and the free list is a hand-rolled intrusive doubly-linked list: the scheduler’s hot loop performs allocation and free at very high rates, so this avoids Python object churn, and the intrusive links give O(1) removal from the <em>middle</em> of the free list, which the prefix cache needs when a cache hit pulls a freed block back into service. Second, <strong>the scheduler owns all allocation decisions</strong>; the worker just receives block ids inside its per-step instructions. Memory is the scarce resource that admission and preemption decisions hinge on, so the policy lives in one place, the engine core, and the worker stays a dumb executor.</p> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-inference/free-queue.svg" sizes="95vw"/> <img src="/assets/img/llm-inference/free-queue.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="The block pool free queue as an intrusive doubly-linked list" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The free queue. Every block on it has ref count 0 but keeps its bytes and its hash; allocation pops from the HEAD (evicting that block's old cached identity), freed blocks append at the TAIL, and a prefix-cache hit can "touch" a block out of the middle, lifting it back into use with its neighbors relinking around it in O(1) thanks to the intrusive prev/next pointers. The touch and the head-to-tail LRU ordering are exactly the mechanics the next two subsections walk through. Editable source: <a href="/assets/img/llm-inference/free-queue.excalidraw">free-queue.excalidraw</a>. </div> <h5 id="allocate-and-free-step-by-step"><strong>allocate and free, step by step</strong></h5> <p>For a new request, the scheduler first calls <code class="language-plaintext highlighter-rouge">get_computed_blocks()</code> to find the longest cached prefix (next section), then <code class="language-plaintext highlighter-rouge">allocate_slots()</code>:</p> <ol> <li>Compute how many new blocks are needed; if the pool cannot cover it, return failure (which triggers preemption, covered in the scheduling section).</li> <li>“Touch” the cache-hit blocks: increment <code class="language-plaintext highlighter-rouge">ref_cnt</code> and remove them from the free queue, so they cannot be evicted while in use.</li> <li>Pop fresh blocks from the head of the free queue, evicting each popped block’s old cached identity if it had one.</li> <li>Register every block that fills completely in the hash map immediately, so that another request in the <em>same batch</em> can already reuse it.</li> </ol> <p>A running request repeats the same flow minus the prefix lookup: fill the last partial block, allocate more as its computed-token count grows. On finish, blocks whose <code class="language-plaintext highlighter-rouge">ref_cnt</code> drops to zero are appended to the free queue <strong>in reverse order, deepest block first</strong>, a small trick with a purpose that will make sense in the prefix-caching section: the block least likely to be reused is placed closest to eviction (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/prefix_caching.md">docs/design/prefix_caching.md</a>).</p> <p>The subtle and load-bearing fact: <strong>freeing a block does not erase it</strong>. A freed block keeps its contents and its hash while sitting in the free queue, and only actually dies when a later allocation pops it and overwrites it. Freed-but-intact blocks are what the prefix cache is made of.</p> <h5 id="preemption-free-everything-recompute-later"><strong>Preemption: free everything, recompute later</strong></h5> <p>Now the failure path. Mid-serving, some running request needs one more block for its next token, and <code class="language-plaintext highlighter-rouge">allocate_slots()</code> reports the pool is empty. The engine can’t partially refuse (a request either gets its block or can’t run this step), so one running request must give up its memory so the rest can make progress. The scheduler picks that request, the <strong>victim</strong> (which one it picks is a policy question, covered in the scheduling section), and <code class="language-plaintext highlighter-rouge">Scheduler._preempt_request</code> (<code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code>) does three things to it:</p> <ol> <li><strong>All of the victim’s blocks are freed</strong> back to the free queue. Per the free semantics above, their contents and hashes stay intact for now; they’ve just become reclaimable by others.</li> <li><strong>Its progress counter is zeroed</strong> (<code class="language-plaintext highlighter-rouge">num_computed_tokens = 0</code>). The victim keeps its <em>tokens</em>, the prompt and everything it has generated so far live in the request’s CPU-side state; what the engine records is that no KV cache exists for them anymore.</li> <li><strong>It moves to the front of the waiting queue</strong>, first in line to be re-admitted once memory frees up.</li> </ol> <p>Recovery, at re-admission, is recomputation: the victim’s full token sequence (prompt plus its already-generated output) runs through prefill again to rebuild the KV that was thrown away. In the good case even that is cheap: if the victim’s freed blocks are still sitting in the free queue un-overwritten, the prefix-cache lookup finds them and the “recompute” is mostly a cache hit on its own old blocks.</p> <p>V0 had a second mechanism, swapping a preempted request’s blocks out to CPU memory and back, and V1 deleted it along with the <code class="language-plaintext highlighter-rouge">--swap-space</code> flag and its metrics (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">docs/design/metrics.md</a>): once blocks became shared, deduplicated state under prefix caching, tracking swapped shared blocks was more machinery than it was worth, while free-and-recompute needs nothing beyond what this section already built.</p> <div class="row justify-content-center"> <div class="col-sm-11 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/llm-inference/request-lifecycle.svg" sizes="95vw"/> <img src="/assets/img/llm-inference/request-lifecycle.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="One request's blocks through admission, decode, and preemption" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> One request's blocks through the whole lifecycle. Admission finds half the prompt already cached (those blocks are touched, ref count up) and pops fresh blocks for the rest; decode grows the cache one block at a time; preemption frees all of them to the free-queue tail, deepest first, zeroes the progress counter, and sends the request to the front of the waiting queue, with the freed blocks' contents intact until someone else reuses them. Editable source: <a href="/assets/img/llm-inference/request-lifecycle.excalidraw">request-lifecycle.excalidraw</a>. </div> <p>Two closing notes for completeness. Hybrid models (mixing full attention, sliding-window attention, and Mamba state layers) give each layer type its own KV spec and manage them in groups via a hybrid coordinator (<code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_coordinator.py</code>, <code class="language-plaintext highlighter-rouge">single_type_kv_cache_manager.py</code>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/hybrid_kv_cache_manager.md">docs/design/hybrid_kv_cache_manager.md</a>). And quantizing the KV cache to fp8 halves <code class="language-plaintext highlighter-rouge">page_size_bytes</code>, doubling the block count from the same pool; the per-token-head scales it needs are budgeted inside the page size (<code class="language-plaintext highlighter-rouge">vllm/v1/kv_cache_interface.py</code>). More on that in the quantization section.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2309.06180">Efficient Memory Management for Large Language Model Serving with PagedAttention - Kwon et al., SOSP 2023</a></li> <li><a href="https://vllm.ai/blog/2023-06-20-vllm">vLLM launch blog</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/prefix_caching.md">vLLM prefix caching design doc</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">metrics design doc</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/hybrid_kv_cache_manager.md">hybrid KV cache manager doc</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/v1/worker/gpu_worker.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/worker/utils.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/kv_cache_interface.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/block_pool.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_manager.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_utils.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/worker/block_table.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code>, <code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code></li> </ul> <hr/> <h4 id="prefix-caching"><strong>Prefix Caching</strong></h4> <h5 id="what-and-why"><strong>What and why</strong></h5> <p>Prefix caching is the observation that the KV cache is a pure function of the token sequence: same tokens after the same prefix produce the same K and V, always. So when a new request’s prompt shares a prefix with something the engine has already computed, recomputing that prefix’s KV is pure waste; reuse the blocks instead and start prefill at the first novel token. Correctness is exact, the same tokens yield the same KV, so it cannot change model outputs; vLLM’s design doc calls it “almost a free lunch” (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/prefix_caching.md">docs/design/prefix_caching.md</a>), and it is <strong>on by default</strong> (<code class="language-plaintext highlighter-rouge">enable_prefix_caching: bool = True</code>, <code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code>).</p> <p>The workloads it wins on are the workloads that dominate production: a system prompt shared across every request to a service, multi-turn chat where each turn re-sends the whole conversation so far, and few-shot prompts sharing their examples. One granularity caveat: only <strong>full</strong> blocks are cached, since a partial block’s KV would depend on tokens still being appended, so hits come in multiples of the block size. The design doc’s example: a 14-token shared prefix with block size 4 hits only 2 blocks, 8 tokens.</p> <h5 id="the-hash-chain"><strong>The hash chain</strong></h5> <p>The cache key is where the design earns its correctness. Each full block’s hash is</p> \[\text{hash}(\text{parent\_hash},\; \text{block\_tokens},\; \text{extra\_hashes})\] <ul> <li><strong><code class="language-plaintext highlighter-rouge">parent_hash</code></strong> chains every block to its entire prefix. This is the crucial bit: a block’s KV depends not just on its own 16 tokens but on everything before them, so the key must encode the whole history. Chaining the parent’s hash does that in constant space, and it means a block hash uniquely identifies “these tokens, after exactly this prefix.”</li> <li>The block’s <strong>exact tokens</strong> are included alongside the parent hash to reduce the impact of collisions.</li> <li><strong><code class="language-plaintext highlighter-rouge">extra_hashes</code></strong> carries anything else that changes the KV for identical token ids: the LoRA adapter id, multimodal input hashes, and <code class="language-plaintext highlighter-rouge">cache_salt</code>. The multimodal case is worth understanding: two different images tokenize to <em>identical</em> placeholder tokens, so a token-only hash would collide across images; the frontend’s hash of the actual image content is added to every block spanning it. And <code class="language-plaintext highlighter-rouge">cache_salt</code> is a security feature: it’s injected into the <em>first</em> block’s hash, so only requests carrying the same salt can share cache entries. This defends against a real attack, probing response timing to learn whether some other tenant’s prompt is already cached.</li> </ul> <p>The hash algorithm is configurable (<code class="language-plaintext highlighter-rouge">--prefix-caching-hash-algo</code>): <strong>sha256 is the default since v0.11</strong> (the earlier default was not collision-safe), <code class="language-plaintext highlighter-rouge">sha256_cbor</code> gives cross-version reproducibility, and <code class="language-plaintext highlighter-rouge">xxhash</code> is fast but non-cryptographic; the docs warn that in multi-tenant settings, constructible collisions can leak private information across requests, so the cheap hash is a real trade and not a free speed win.</p> <h5 id="mechanics-the-cache-that-costs-nothing"><strong>Mechanics: the cache that costs nothing</strong></h5> <p>Here’s the elegant part: given the block pool from the previous section, prefix caching needs almost no new machinery. <code class="language-plaintext highlighter-rouge">BlockPool</code> keeps one extra map from hash to block (<code class="language-plaintext highlighter-rouge">cached_block_hash_to_block</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/block_pool.py</code>). Lookup (<code class="language-plaintext highlighter-rouge">KVCacheManager.get_computed_blocks()</code>) hashes the incoming prompt block by block and walks the chain until the first miss, yielding the longest cached prefix. And the eviction policy <em>falls out of the allocator</em>:</p> <ul> <li>A freed block keeps its hash and contents in the free queue and remains hittable there.</li> <li>Eviction happens <strong>only at reallocation</strong>: popping a cached block off the free-queue head strips its hash from the map (<code class="language-plaintext highlighter-rouge">_maybe_evict_cached_block</code>). There is no evictor thread, no TTL, no reference-counting sweep; eviction is lazy and embedded in allocation.</li> <li>LRU ordering comes for free: the free queue is FIFO, freed blocks join at the tail, allocation pops at the head. And the reverse-order freeing from the previous section is the finishing touch: a request’s <em>deepest</em> blocks (encoding its longest, most request-specific prefix, the least likely to be shared) are freed first, landing nearest the eviction head, while shallow blocks (short shared prefixes, like system prompts) survive longest.</li> <li>A cache hit “touches” the block, ref_cnt up and off the free queue, so nothing referenced can be evicted.</li> </ul> <p>Two more behaviors complete the picture. Full blocks are registered in the hash map the moment they fill, not when the request finishes, so two same-prefix requests arriving in the <em>same batch</em> still share: the second one hits blocks the first one filled moments earlier. And because V1 block tables are append-only, a decoded block that happens to duplicate an already-cached block is not swapped out for the cached copy; the duplicate simply lives until its request frees it, a small memory cost bought for a simpler runtime (V1 dropped V0’s copy-on-write machinery on the same reasoning: implicit sharing plus recompute-on-preemption covers the cases that matter with near-zero overhead, per the history section of <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">docs/design/metrics.md</a>).</p> <h5 id="observability"><strong>Observability</strong></h5> <p>Hit rate is exported as two counters, <code class="language-plaintext highlighter-rouge">vllm:prefix_cache_queries</code> and <code class="language-plaintext highlighter-rouge">vllm:prefix_cache_hits</code> (in tokens), so any window’s hit rate is <code class="language-plaintext highlighter-rouge">rate(hits)/rate(queries)</code> in PromQL; the periodic log line uses the last 1k queries (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">docs/design/metrics.md</a>). Programmatic access goes through <code class="language-plaintext highlighter-rouge">LLM.get_metrics()</code> (<code class="language-plaintext highlighter-rouge">PrefixCacheStats</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/metrics/stats.py</code>). Disable with <code class="language-plaintext highlighter-rouge">--no-enable-prefix-caching</code>; and when benchmarking with repeated prompts, call <code class="language-plaintext highlighter-rouge">reset_prefix_cache()</code> first or the numbers measure the cache, not the model.</p> <p>One forward pointer: the hash chain names KV content by <em>what it is</em> rather than <em>where it lives</em>, which is exactly what makes KV shareable across machines. The disaggregation section builds on that.</p> <p><strong>References</strong></p> <ul> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/prefix_caching.md">vLLM prefix caching design doc</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">vLLM metrics design doc</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/v1/core/block_pool.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_manager.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/metrics/stats.py</code>, <code class="language-plaintext highlighter-rouge">vllm/config/cache.py</code></li> </ul> <hr/> <h4 id="scheduling-in-vllm-v1"><strong>Scheduling in vLLM V1</strong></h4> <h5 id="there-is-no-prefill-phase"><strong>There is no prefill phase</strong></h5> <p>The V1 scheduler (<code class="language-plaintext highlighter-rouge">Scheduler.schedule()</code> in <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code>, as of 2026-08-21) is built on one unifying abstraction, stated in the comment at the top of the function: “There’s no ‘decoding phase’ nor ‘prefill phase’ in the scheduler.” Each request simply carries two counters, <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> (how many of its tokens have been through the model) and <code class="language-plaintext highlighter-rouge">num_tokens_with_spec</code> (how many it has in total: prompt plus generated plus any speculative tokens), and at each step the scheduler assigns tokens to requests so that the first counter catches up to the second.</p> <p>It’s worth pausing on how much that one abstraction buys. A fresh prompt is just a request whose computed count is far behind its total: assign it a slice of the gap and you have prefill. Assign it less than the full gap because the budget ran out, and you have <em>chunked</em> prefill, no special case needed. A decoding request is one token behind: assign 1. A prefix-cache hit is a request that <em>starts</em> with <code class="language-plaintext highlighter-rouge">num_computed_tokens &gt; 0</code>, so the “prefill” it gets assigned is only the uncached tail. Speculative decoding adds draft tokens to the target count. Five features, one code path, one budget.</p> <p>The output of a scheduling step is a <code class="language-plaintext highlighter-rouge">SchedulerOutput</code>: for each scheduled request, how many tokens to run this step, plus the new KV block ids from <code class="language-plaintext highlighter-rouge">allocate_slots()</code>. The worker then executes <strong>one fused forward pass</strong> for the entire batch, prompt chunks and decodes mixed together.</p> <h5 id="budgets-and-limits"><strong>Budgets and limits</strong></h5> <table> <thead> <tr> <th>Knob</th> <th>Meaning</th> <th>Defaults (as of 2026-08-21)</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">max_num_batched_tokens</code></td> <td>token budget per step</td> <td>class default 2048 (<code class="language-plaintext highlighter-rouge">vllm/config/scheduler.py</code>); auto-tuned by usage context and GPU in <code class="language-plaintext highlighter-rouge">vllm/engine/arg_utils.py</code>: GPUs with 70+ GiB get 8192 (API server) or 16384 (<code class="language-plaintext highlighter-rouge">LLM</code> class), smaller GPUs 2048 / 8192</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">max_num_seqs</code></td> <td>max concurrently running requests</td> <td>class default 128; auto-tuned to 1024 (big GPUs) or 256</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">long_prefill_token_threshold</code></td> <td>cap on prompt tokens one request may take in one step</td> <td>0 = off</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">max_model_len</code></td> <td>positions cap per request</td> <td>model-dependent</td> </tr> </tbody> </table> <p>Chunking falls out of the budget arithmetic: a request gets \(\min(\text{remaining tokens},\; \text{leftover budget},\; \text{long-prefill threshold if set})\). The other constraint is KV memory: <code class="language-plaintext highlighter-rouge">allocate_slots()</code> can fail regardless of the token budget, and that failure is what triggers preemption.</p> <h5 id="one-scheduling-step-in-order"><strong>One scheduling step, in order</strong></h5> <ol> <li><strong>Running requests first.</strong> Loop over the running list while budget remains: compute each request’s token assignment (usually 1 for a decode, plus any speculative tokens; more if it’s still catching up on its prompt), then try to allocate KV.</li> <li><strong>If allocation fails, preempt.</strong> Under the default FCFS policy the victim is the <em>most recently added</em> running request (<code class="language-plaintext highlighter-rouge">self.running.pop()</code>); under the priority policy it’s the worst (priority, arrival time) pair, where a larger priority number means less important. Preemption is the full reset from the KV section: all blocks freed, computed count zeroed, victim prepended to the waiting queue to be recomputed later.</li> <li><strong>Waiting requests next, and only if nothing was preempted this step.</strong> Admit while budget remains and the running count is under <code class="language-plaintext highlighter-rouge">max_num_seqs</code>: prefix-cache lookup, then allocation. Requests that are blocked, waiting on remote KV transfer, on structured-output grammar compilation, or on LoRA slot limits (<code class="language-plaintext highlighter-rouge">max_loras</code> per batch), are skipped until unblocked.</li> <li>Queues are pluggable: <code class="language-plaintext highlighter-rouge">FCFSRequestQueue</code> is a deque; <code class="language-plaintext highlighter-rouge">PriorityRequestQueue</code> orders by (priority, arrival time) (<code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/request_queue.py</code>; <code class="language-plaintext highlighter-rouge">--scheduling-policy fcfs|priority</code>, with a per-request <code class="language-plaintext highlighter-rouge">priority</code> field where lower means more important).</li> </ol> <p>Three consequences of this ordering are the ones to internalize:</p> <ul> <li><strong>Decodes are effectively prioritized.</strong> Running requests are served before waiting ones, and the docs state that chunked-prefill scheduling batches all pending decodes before any prefill (<a href="https://github.com/vllm-project/vllm/blob/main/docs/configuration/optimization.md">docs/configuration/optimization.md</a>). Running-first protects the ITL of in-flight requests; admission, and therefore TTFT, gets the leftover budget. That’s a deliberate stance on the metrics trade-off from earlier.</li> <li><strong>The two ITL knobs.</strong> Raising <code class="language-plaintext highlighter-rouge">max_num_batched_tokens</code> buys throughput and TTFT at the cost of the ITL tail (bigger prefill slices per step); <code class="language-plaintext highlighter-rouge">long_prefill_token_threshold</code> caps any single request’s slice, protecting the batch at the cost of that request’s TTFT. If p99 ITL spikes correlate with long prompts, these are the two dials.</li> <li><strong>No admission after a preemption.</strong> The guard against admitting when something was just preempted has a simple justification: preemption means memory was insufficient for the <em>current</em> population, so admitting more work in the same step would immediately re-trigger preemption churn.</li> </ul> <h5 id="the-other-half-of-the-loop"><strong>The other half of the loop</strong></h5> <p>After the model runs, <code class="language-plaintext highlighter-rouge">Scheduler.update_from_output()</code> closes the cycle: append the sampled tokens, check token-level stop conditions (EOS, stop token ids, max-token and model-length limits; <code class="language-plaintext highlighter-rouge">check_stop</code> in <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/utils.py</code>), free finished requests’ blocks, and carry speculative-decoding bookkeeping (how many draft tokens were accepted) into the next step. Stop <em>strings</em> are the exception: they need detokenized text, so they’re detected in the frontend’s output processor (<code class="language-plaintext highlighter-rouge">vllm/v1/engine/output_processor.py</code>), which then aborts the request in the engine core; the runtime section explains why that split exists.</p> <p>One refinement on top: <strong>async scheduling</strong> (<code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/async_scheduler.py</code>) overlaps the scheduling of step \(N+1\) with the GPU’s execution of step \(N\). The trick is <code class="language-plaintext highlighter-rouge">num_output_placeholders</code>: schedule as if the in-flight step’s tokens have already arrived, and reconcile afterwards. This hides the CPU scheduling latency that would otherwise sit between every pair of forward passes.</p> <p>Seen from a distance, the V1 scheduler is a synthesis of the decade’s two big scheduling ideas: Orca’s iteration-level batching (<a href="https://www.usenix.org/conference/osdi22/presentation/yu">OSDI ‘22</a>) provides the fluid batch, Sarathi-Serve’s stall-free chunked prefill (<a href="https://arxiv.org/abs/2403.02310">arXiv:2403.02310</a>) provides the bounded prefill slices, and the token-budget-with-no-phases formulation is what lets both live in one loop.</p> <p><strong>References</strong></p> <ul> <li><a href="https://www.usenix.org/conference/osdi22/presentation/yu">Orca - Yu et al., OSDI ‘22</a></li> <li><a href="https://arxiv.org/abs/2403.02310">Sarathi-Serve - Agrawal et al.</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/configuration/optimization.md">vLLM optimization docs</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/request_queue.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/utils.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/async_scheduler.py</code>, <code class="language-plaintext highlighter-rouge">vllm/config/scheduler.py</code>, <code class="language-plaintext highlighter-rouge">vllm/engine/arg_utils.py</code></li> </ul> <hr/> <h4 id="speculative-decoding"><strong>Speculative Decoding</strong></h4> <h5 id="spending-flops-that-were-idle-anyway"><strong>Spending FLOPs that were idle anyway</strong></h5> <p>Recall the decode floor: a decode step reads all 16 GB of weights to produce one token per sequence, using about 16 µs of arithmetic against 4.8 ms of memory time. The compute units are idle 99% of the step. Speculative decoding is the technique that spends that idle compute: let a cheap <strong>draft</strong> mechanism propose \(k\) tokens, then have the target model <strong>verify all \(k\) in a single forward pass</strong>, which is possible because given the draft tokens, all \(k\) positions can be computed in parallel, exactly like prefill. A rejection rule then keeps only the longest correct prefix of the draft.</p> <p>The remarkable property, and the reason the technique is everywhere, is that <strong>the output distribution is exactly the target model’s</strong> (<a href="https://arxiv.org/abs/2211.17192">Leviathan et al., ICML 2023</a>); the paper’s claim is identical outputs, with no change to the distribution. This is a pure latency optimization, not an approximation, in contrast to lossy accelerations like quantization. The catch is scoping: vLLM’s docs recommend it for reducing inter-token latency in memory-bound, medium-to-low QPS regimes (<a href="https://github.com/vllm-project/vllm/blob/main/docs/features/speculative_decoding/README.md">docs/features/speculative_decoding/README.md</a>). At high QPS the batch is already large, decode’s arithmetic intensity approaches the ridge point, the idle FLOPs the technique feeds on disappear, and drafting becomes added load.</p> <h5 id="the-math"><strong>The math</strong></h5> <p>With acceptance rate \(\alpha\) (the probability a draft token survives verification) and \(\gamma\) draft tokens per step, the expected number of output tokens per target forward pass is (Leviathan, Eq. 1):</p> \[\mathbb{E}[\text{tokens per step}] = \frac{1 - \alpha^{\gamma+1}}{1 - \alpha}\] <p>At \(\alpha = 0.8\), \(\gamma = 4\): \((1 - 0.33)/0.2 \approx 3.4\) tokens per weight read instead of 1, a direct multiplier on the decode floor. Note the shape of the formula: gains saturate in \(\gamma\) (the \(\alpha^{\gamma}\) term dies off) while draft cost grows linearly in \(\gamma\), so there’s an optimal draft length that depends on \(\alpha\) and on how expensive drafting is. In practice acceptance also <em>decays with position</em>, because each draft token conditions on previous draft tokens and errors compound; vLLM tracks per-position acceptance counters for exactly this reason (<code class="language-plaintext highlighter-rouge">SpecDecodingStats</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/spec_decode/metrics.py</code>), and the decay is what caps useful \(\gamma\).</p> <h5 id="the-rejection-rule"><strong>The rejection rule</strong></h5> <p>The exactness guarantee lives in three lines. With \(q\) the draft distribution and \(p\) the target distribution at a given position (vLLM’s implementation, <code class="language-plaintext highlighter-rouge">vllm/v1/sample/rejection_sampler.py</code>, states in its docstring that it strictly follows the paper):</p> <ol> <li>Accept draft token \(x\) with probability \(\min(1,\; p(x)/q(x))\).</li> <li>On the first rejection, sample a <strong>recovered token</strong> from the residual distribution \(\mathrm{norm}(\max(0,\; p - q))\) and stop there.</li> <li>If all \(\gamma\) draft tokens are accepted, sample one <strong>bonus token</strong> from the target’s own distribution at the final position.</li> </ol> <p>Why this is exact: the accepted probability mass at \(x\) is \(q(x)\min(1, p(x)/q(x)) = \min(p(x), q(x))\), and the rejection path’s residual distribution restores precisely the mass \(\max(0, p(x) - q(x))\) that acceptance undercounts; summed, every token’s marginal probability is exactly \(p(x)\). The worst case is 1 token per step (the recovered token), so the scheme is never slower in tokens per step, though it can lose wall-clock time if drafting itself is expensive. One vLLM detail: the bonus token is sampled <em>outside</em> the rejection sampler so the full sampling configuration (top-p, top-k) applies to it.</p> <h5 id="where-drafts-come-from"><strong>Where drafts come from</strong></h5> <table> <thead> <tr> <th>Method (vLLM V1, <code class="language-plaintext highlighter-rouge">vllm/v1/spec_decode/</code>)</th> <th>Draft source</th> <th>Notes</th> </tr> </thead> <tbody> <tr> <td>EAGLE (<code class="language-plaintext highlighter-rouge">eagle.py</code>)</td> <td>a 1-layer head autoregressing on the target’s second-to-top-layer features plus shifted tokens</td> <td>paper reports 2.7-3.5x latency improvement on LLaMA2-Chat 70B (<a href="https://arxiv.org/abs/2401.15077">EAGLE</a>)</td> </tr> <tr> <td>MTP</td> <td>the target’s own multi-token-prediction head, if it trained one (DeepSeek-V3 does, <a href="https://arxiv.org/abs/2412.19437">tech report</a>)</td> <td>no separate draft model at all</td> </tr> <tr> <td>Draft model</td> <td>a smaller LM from the same tokenizer family</td> <td>the classic two-model setup</td> </tr> <tr> <td>N-gram (<code class="language-plaintext highlighter-rouge">ngram_proposer.py</code>)</td> <td>prompt lookup: find the most recent match of the current n-gram earlier in the context and copy the tokens that followed it</td> <td>no model, no training, zero GPU cost</td> </tr> <tr> <td>Suffix decoding (<code class="language-plaintext highlighter-rouge">suffix_decoding.py</code>)</td> <td>a suffix tree over previous outputs, dynamic depth</td> <td> </td> </tr> <tr> <td>Medusa, MLP speculator, PARD, custom</td> <td>see <a href="https://github.com/vllm-project/vllm/blob/main/docs/features/speculative_decoding/README.md">docs/features/speculative_decoding/</a></td> <td> </td> </tr> </tbody> </table> <p>Configured via <code class="language-plaintext highlighter-rouge">--speculative-config '{"method": "eagle", "model": ..., "num_speculative_tokens": k}'</code>. The n-gram proposer deserves a special word: it shines whenever the output copies the input, retrieval-augmented answers quoting their context, summarization, code editing, because the “draft model” is just string matching against text that’s already there. For workloads like that, it’s the first thing to try; a learned draft like EAGLE earns its keep on free-form generation where there’s nothing to copy.</p> <h5 id="engine-integration"><strong>Engine integration</strong></h5> <p>This is where the V1 scheduler’s phaseless design pays off again: speculative decoding required no new scheduling phase. A request’s target count <code class="language-plaintext highlighter-rouge">num_tokens_with_spec</code> simply includes the draft tokens, and <code class="language-plaintext highlighter-rouge">allocate_slots(..., num_lookahead_tokens)</code> pre-allocates KV blocks for draft tokens that may be rejected. Verification is one forward over context plus \(k\) draft tokens per request, batched together with everything else in the step; the rejection sampler then trims each sequence to its accepted prefix. Rejected tokens waste the KV slots and compute they consumed, their slots are simply reclaimed and overwritten, which is why acceptance-rate monitoring (<code class="language-plaintext highlighter-rouge">num_drafts</code>, <code class="language-plaintext highlighter-rouge">num_accepted_tokens</code>, and the per-position counters) is the health check that matters for a deployment.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2211.17192">Fast Inference from Transformers via Speculative Decoding - Leviathan et al., ICML 2023</a></li> <li><a href="https://arxiv.org/abs/2401.15077">EAGLE - Li et al.</a></li> <li><a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3 Technical Report</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/features/speculative_decoding/README.md">vLLM speculative decoding docs</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/v1/spec_decode/</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/sample/rejection_sampler.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/spec_decode/metrics.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code></li> </ul> <hr/> <h4 id="distributed-inference-tp-pp-dp-ep"><strong>Distributed Inference: TP, PP, DP, EP</strong></h4> <p>The numbers section ended on an observation: Llama 3 70B is 140 GB of bf16 weights, and an H100 has 80 GB. Some models simply do not fit on one GPU, and even when they do, the decode floor may be too high; 140 GB over 3.35 TB/s is a 42 ms step, about 24 tokens/s single-stream, if the model fit at all. Parallelism answers both problems, and the four flavors answer them differently.</p> <p>vLLM’s docs give a clean decision rule worth quoting in spirit (<a href="https://github.com/vllm-project/vllm/blob/main/docs/serving/parallelism_scaling.md">parallelism_scaling.md</a>): if the model fits on one GPU, use no parallelism; too big for one GPU, use tensor parallelism across the GPUs of one node; too big for one node, tensor parallelism within each node times pipeline parallelism across nodes; uneven GPU counts or no NVLink, prefer pipeline over tensor; and if throughput is the problem rather than fit, add whole replicas (data parallelism).</p> <h5 id="tensor-parallelism-split-every-matrix"><strong>Tensor parallelism: split every matrix</strong></h5> <p>Tensor parallelism (TP) splits each weight matrix across ranks, and vLLM implements Megatron-LM’s scheme (<a href="https://arxiv.org/abs/1909.08053">Shoeybi et al.</a>), whose cleverness is in <em>which way</em> each matrix is split. Pair a <strong>column-parallel</strong> layer (each rank holds a slice of the output dimension) with a <strong>row-parallel</strong> layer (each rank holds a slice of the input dimension): the column-parallel layer’s sharded output feeds the row-parallel layer’s sharded input directly, with no communication in between, and only the row-parallel layer’s output needs an all-reduce to reassemble the full hidden state.</p> <p>Both halves of a transformer layer have exactly this two-matmul shape. In vLLM’s Llama implementation (<code class="language-plaintext highlighter-rouge">vllm/model_executor/models/llama.py</code>, as of 2026-08-21): attention is <code class="language-plaintext highlighter-rouge">QKVParallelLinear</code> (column-parallel, which also splits the heads across ranks) into <code class="language-plaintext highlighter-rouge">o_proj</code> as <code class="language-plaintext highlighter-rouge">RowParallelLinear</code>; the MLP is <code class="language-plaintext highlighter-rouge">MergedColumnParallelLinear</code> (the fused gate/up projection) into <code class="language-plaintext highlighter-rouge">down_proj</code> as <code class="language-plaintext highlighter-rouge">RowParallelLinear</code>. <code class="language-plaintext highlighter-rouge">RowParallelLinear.forward</code> ends with <code class="language-plaintext highlighter-rouge">tensor_model_parallel_all_reduce</code> (<code class="language-plaintext highlighter-rouge">vllm/model_executor/layers/linear.py</code>), so a decoder layer costs <strong>exactly 2 all-reduces per forward pass</strong>, each synchronizing the \(d_{model}\) hidden state per token.</p> <p>Run the numbers for Llama 3 70B: 80 layers times 2 gives <strong>160 all-reduces per token position per step</strong>, each carrying a \(8192 \times 2\)-byte = 16 KiB vector per token. That is a torrent of tiny, latency-sensitive messages, the worst possible shape for a network, and it’s why TP effectively requires NVLink and why vLLM ships a family of custom all-reduce kernels rather than relying on NCCL alone (<code class="language-plaintext highlighter-rouge">vllm/distributed/device_communicators/</code>: <code class="language-plaintext highlighter-rouge">custom_all_reduce.py</code> for NVLink peer-to-peer, plus <code class="language-plaintext highlighter-rouge">quick_all_reduce.py</code>, <code class="language-plaintext highlighter-rouge">flashinfer_all_reduce.py</code>, <code class="language-plaintext highlighter-rouge">symm_mem.py</code>, <code class="language-plaintext highlighter-rouge">pynccl.py</code>).</p> <p>What TP buys is twofold, and the second part is the one people miss: it divides weight <strong>memory</strong> per rank, and it divides the per-step weight <em>read</em> per rank, which means it is the one parallelism that directly <strong>lowers the decode floor</strong>. The 70B model’s 42 ms floor becomes roughly 10.5 ms at TP4; the 8B model’s 16 GB read becomes 2 GB per rank at TP8. Pipeline parallelism, next, does neither.</p> <p>The KV cache shards too, with two traps. KV heads per GPU is \(\max(1,\; H_{kv} / \text{TP})\) (<code class="language-plaintext highlighter-rouge">ModelConfig.get_num_kv_heads</code>, <code class="language-plaintext highlighter-rouge">vllm/config/model.py</code>), so with Llama 3’s 8 KV heads, TP8 gives 1 head per rank, and <strong>TP16 replicates</strong>: beyond 8 ranks the per-rank KV footprint stops shrinking. And the code comment adds that MLA (covered in the kernels section) decodes as MQA with a single KV head regardless of TP. Nothing about TP is free, either: as TP grows, there are more and smaller collectives, the per-rank GEMMs get thinner and less efficient, and efficiency per GPU falls.</p> <h5 id="pipeline-parallelism-split-by-layers"><strong>Pipeline parallelism: split by layers</strong></h5> <p>Pipeline parallelism (PP, <code class="language-plaintext highlighter-rouge">--pipeline-parallel-size</code>) cuts the model into contiguous stages of layers, one stage per GPU group. Its communication profile is the mirror image of TP’s: one hidden-state tensor handed point-to-point per microbatch per stage boundary, tiny compared to TP’s per-layer all-reduces, which is why the docs recommend PP across nodes and for machines without fast interconnect, and why it tolerates uneven splits.</p> <p>The costs are also mirror-imaged. PP adds capacity and throughput but <strong>does not improve per-token latency at all</strong>: every token still traverses every layer serially, just on different GPUs. And keeping all stages busy requires enough concurrent work in flight; otherwise the pipeline has bubbles. The standard placement for a 2-node, 8-GPU-per-node cluster follows directly: TP8 inside each node (inside the NVLink domain), PP2 across nodes (cheap point-to-point over the network).</p> <h5 id="data-parallelism-more-copies"><strong>Data parallelism: more copies</strong></h5> <p>Data parallelism (DP, <code class="language-plaintext highlighter-rouge">--data-parallel-size</code>) runs full replicas of the model with independent request batches. It scales throughput, not model size, and for dense models the replicas are truly independent, DP is just load balancing.</p> <p>The interesting case is MoE plus MLA models like DeepSeek, where the favored layout is DP for the <em>attention</em> layers combined with TP or expert parallelism for the <em>expert</em> layers (<a href="https://github.com/vllm-project/vllm/blob/main/docs/serving/data_parallel_deployment.md">data_parallel_deployment.md</a>). Then DP ranks are no longer independent: the expert layers perform collectives every forward pass, so all ranks’ forward passes must stay aligned, and a rank with no requests of its own must still run empty “dummy” forward passes so its peers’ collectives don’t deadlock; a DP Coordinator process keeps them in lockstep.</p> <h5 id="expert-parallelism-split-the-experts"><strong>Expert parallelism: split the experts</strong></h5> <p>Expert parallelism (EP, <code class="language-plaintext highlighter-rouge">--enable-expert-parallel</code>) shards an MoE model’s experts across GPUs, with \(\text{EP} = \text{TP} \times \text{DP}\) (<a href="https://github.com/vllm-project/vllm/blob/main/docs/serving/expert_parallel_deployment.md">expert_parallel_deployment.md</a>). The router’s decisions turn into <strong>all-to-all</strong> communication: each token must travel to the GPUs holding its selected experts and back. The backend for that shuffle is selectable (<code class="language-plaintext highlighter-rouge">--all2all-backend</code>):</p> <table> <thead> <tr> <th>Backend</th> <th>Optimized for</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">allgather_reducescatter</code></td> <td>default; any EP+DP configuration</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">deepep_high_throughput</code></td> <td>multi-node <strong>prefill</strong> (grouped GEMM, contiguous layout)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">deepep_low_latency</code></td> <td>multi-node <strong>decode</strong> (CUDA-graph support, masked layout)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">flashinfer_nvlink_one_sided</code> / <code class="language-plaintext highlighter-rouge">two_sided</code></td> <td>multi-node NVLink domains</td> </tr> </tbody> </table> <p>Notice the prefill/decode split reappearing at the communication layer: the two phases want different all-to-all kernels for the same reasons they want different everything else, throughput-shaped bulk transfers for prefill, latency-shaped graph-capturable ones for decode. The two-regime structure from the start of this post runs the whole way down the stack.</p> <p>Operationally: the executor backend is Python multiprocessing on a single node and Ray for multi-node by default (<code class="language-plaintext highlighter-rouge">--distributed-executor-backend mp|ray</code>), and the startup lines worth knowing are “GPU KV cache size: N tokens” and “Maximum concurrency for M tokens per request: X.XXx” (<code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_utils.py</code>), both reported per replica.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/1909.08053">Megatron-LM - Shoeybi et al.</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/serving/parallelism_scaling.md">vLLM parallelism scaling docs</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/serving/data_parallel_deployment.md">data parallel deployment</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/serving/expert_parallel_deployment.md">expert parallel deployment</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/model_executor/models/llama.py</code>, <code class="language-plaintext highlighter-rouge">vllm/model_executor/layers/linear.py</code>, <code class="language-plaintext highlighter-rouge">vllm/distributed/device_communicators/</code>, <code class="language-plaintext highlighter-rouge">vllm/config/model.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/kv_cache_utils.py</code></li> </ul> <hr/> <h4 id="disaggregated-prefill-and-kv-transfer"><strong>Disaggregated Prefill and KV Transfer</strong></h4> <h5 id="why-split-the-phases-across-machines"><strong>Why split the phases across machines</strong></h5> <p>Everything so far bounded prefill’s interference with decode; disaggregation removes it. Run prefill and decode on <em>separate vLLM instances</em>, on separate GPUs, and ship the KV cache from one to the other. vLLM’s docs give exactly two reasons for doing this (<a href="https://github.com/vllm-project/vllm/blob/main/docs/features/disagg_prefill.md">disagg_prefill.md</a>, the feature is marked experimental):</p> <ol> <li><strong>Tune TTFT and ITL independently.</strong> Each instance gets its own parallelism and sizing: give the prefill fleet more TP to cut TTFT without touching the decode fleet’s ITL, or vice versa.</li> <li><strong>Control tail ITL by construction.</strong> Chunked prefill bounds the interference but leaves a chunk-size knob that the docs concede is hard to set correctly; disaggregation eliminates the interference class, since no decode step ever shares a GPU with a prefill.</li> </ol> <p>And one warning the docs put in bold that deserves repeating: disaggregated prefill <strong>does not improve throughput</strong>. You pay a KV transfer per request and you run two fleets; the win is SLO attainment, goodput, which is DistServe’s framing of the whole problem (<a href="https://arxiv.org/abs/2401.09670">DistServe</a>). The deeper rationale is the roofline again: prefill is compute-bound and decode is memory-bound, so the phases want different quantities of hardware and arguably different hardware generations entirely; Splitwise makes that argument explicitly, noting token generation doesn’t need the compute capability of the newest GPUs (<a href="https://arxiv.org/abs/2311.18677">Splitwise</a>). The production proof point is Mooncake, the serving system behind Kimi, a “KVCache-centric disaggregated architecture” with separate prefill and decode clusters and a cache pooled across CPU, DRAM, and SSD; the paper reports up to 525% throughput gains in simulation and 75% more requests served in production under SLOs, plus prediction-based early rejection under overload (<a href="https://arxiv.org/abs/2407.00079">Mooncake</a>).</p> <h5 id="the-transfer-problem"><strong>The transfer problem</strong></h5> <p>The catch is physics: the thing being shipped is large. One 8K-token request on Llama 3 70B carries 2.5 GiB of KV; moving that in 100 ms needs 25 GB/s of network. A 32K-token context is 10 GiB (\(32{,}768 \times 320\) KiB), and landing it in under 200 ms needs about 54 GB/s, roughly <strong>430 Gbit/s</strong>. Numbers like that are why the transports are RDMA-class (NIXL over UCX, GPUDirect Storage) and why the API is designed to overlap transfer with compute rather than serialize them.</p> <h5 id="how-vllm-implements-it"><strong>How vLLM implements it</strong></h5> <p>Two or more vLLM instances plus a <strong>connector</strong> that moves KV from the prefill instance to the decode instance; a <code class="language-plaintext highlighter-rouge">kv_role</code> of <code class="language-plaintext highlighter-rouge">kv_producer</code>, <code class="language-plaintext highlighter-rouge">kv_consumer</code>, or <code class="language-plaintext highlighter-rouge">kv_both</code>, with everything under <code class="language-plaintext highlighter-rouge">vllm/distributed/kv_transfer/</code>. The connector interface (<code class="language-plaintext highlighter-rouge">KVConnectorBase_V1</code>, <code class="language-plaintext highlighter-rouge">vllm/distributed/kv_transfer/kv_connector/v1/base.py</code>, as of 2026-08-21) splits cleanly along the engine’s own scheduler/worker line:</p> <ul> <li><strong>Scheduler-side</strong>: <code class="language-plaintext highlighter-rouge">get_num_new_matched_tokens()</code> reports how many of a request’s tokens the remote cache already holds, and it feeds the <em>same</em> <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> machinery as local prefix caching, so to the scheduler a remote hit looks exactly like a local one. <code class="language-plaintext highlighter-rouge">update_state_after_alloc()</code> reacts to allocation, and <code class="language-plaintext highlighter-rouge">request_finished()</code> decides whether blocks must outlive the request because an async transfer is still reading them; that deferred-free path is what keeps producer memory from leaking while a send is in flight.</li> <li><strong>Worker-side</strong>: <code class="language-plaintext highlighter-rouge">start_load_kv()</code>, <code class="language-plaintext highlighter-rouge">wait_for_layer_load(i)</code>, <code class="language-plaintext highlighter-rouge">save_kv_layer(i)</code>, <code class="language-plaintext highlighter-rouge">wait_for_save()</code>, <code class="language-plaintext highlighter-rouge">get_finished()</code>. The per-<strong>layer</strong> granularity is the point: the decode instance can start computing layer \(i\) as soon as layer \(i\)’s KV has landed, and the prefill instance can ship layer \(i\) while computing layer \(i+1\), so a multi-GiB transfer overlaps with the forward pass instead of stalling ahead of it.</li> </ul> <p>A request whose KV is still in flight waits in the queue in a blocked state (<code class="language-plaintext highlighter-rouge">WAITING_FOR_REMOTE_KVS</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code>) and is skipped by the scheduler until promotable, one of the skip conditions listed back in the scheduling section.</p> <p>The connector is pluggable, and the zoo (<a href="https://github.com/vllm-project/vllm/blob/main/docs/features/disagg_prefill.md">disagg_prefill.md</a>) tells you what people actually deploy:</p> <table> <thead> <tr> <th>Connector</th> <th>Transport / idea</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">NixlConnector</code></td> <td>NIXL library, fully async send/recv; backends include UCX and GDS</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">LMCacheConnectorV1</code></td> <td>the LMCache caching layer (NIXL underneath); can run a standalone <code class="language-plaintext highlighter-rouge">lmcache server</code> shared by instances</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MooncakeConnector</code></td> <td>Mooncake’s transfer engine</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MoRIIOConnector</code></td> <td>ROCm only</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">OffloadingConnector</code></td> <td>KV to CPU memory, with its own block size and <code class="language-plaintext highlighter-rouge">cpu_bytes_to_use</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">FlexKVConnectorV1</code></td> <td>distributed KV store, multi-level cache</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MultiConnector</code></td> <td>an ordered list of connectors, e.g. NIXL with a storage fallback</td> </tr> </tbody> </table> <p>Configured as <code class="language-plaintext highlighter-rouge">--kv-transfer-config '{"kv_connector": "NixlConnector", "kv_role": "kv_both", ...}'</code>.</p> <h5 id="offloading-is-the-sibling-not-the-same-thing"><strong>Offloading is the sibling, not the same thing</strong></h5> <p>KV <strong>offloading</strong> moves cold blocks down the memory hierarchy of the <em>same</em> serving stack, GPU to CPU RAM to filesystem, instead of evicting them; a prefix-cache miss in GPU memory can then hit in CPU memory and be copied back over PCIe, trading transfer bandwidth for prefill recompute (<code class="language-plaintext highlighter-rouge">OffloadingConnector</code> plus <code class="language-plaintext highlighter-rouge">vllm/v1/kv_offload/</code>; multi-tier configuration in <a href="https://github.com/vllm-project/vllm/blob/main/docs/features/kv_offloading_usage.md">kv_offloading_usage.md</a>). Disaggregation splits the <em>compute phases</em> across instances; offloading extends the <em>cache</em> of one instance. Mooncake’s pooled datacenter-wide cache is the same offloading idea at cluster scale.</p> <p>The reason all of this composes so cleanly is the prefix-cache hash chain: a block’s chained hash names “these tokens after exactly this prefix” independent of where the bytes physically live, so a local lookup, a CPU-tier lookup, and a remote-cluster lookup are all the same key computation. That’s what “KV-cache-centric” architectures are built on.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2311.18677">Splitwise - Patel et al.</a>, <a href="https://arxiv.org/abs/2401.09670">DistServe - Zhong et al.</a>, <a href="https://arxiv.org/abs/2407.00079">Mooncake - Qin et al.</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/features/disagg_prefill.md">vLLM disaggregated prefill docs</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/features/kv_offloading_usage.md">KV offloading docs</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/distributed/kv_transfer/kv_connector/v1/base.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/kv_offload/</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code></li> </ul> <hr/> <h4 id="attention-kernels"><strong>Attention Kernels</strong></h4> <p>The remaining three sections descend a level, from the engine to what it launches. They’re deliberately tighter than the sections above: each is a map of the territory with the load-bearing facts, not a full walkthrough.</p> <h5 id="why-attention-gets-custom-kernels"><strong>Why attention gets custom kernels</strong></h5> <p>Every other operation in a transformer’s forward pass is a well-behaved dense op: same shapes for every sequence in the batch, weights as operands, a cuBLAS call away. Attention is the exception, and for a structural reason: its operands are <em>per-request state</em>, the KV cache, with ragged lengths across the batch, and in vLLM those operands are physically scattered across non-contiguous blocks addressed through a block table. No BLAS library can express “multiply against a matrix whose rows live at these 200 scattered addresses, lengths varying per batch element.” Hence dedicated attention kernels, and per-backend “metadata builders” that pack the sequence lengths and block tables into whatever layout each kernel wants.</p> <p>The performance problem, independent of paging, is memory traffic: naive attention materializes the \(S = QK^\top\) score matrix to HBM, which is \(O(n^2)\) bytes of traffic per head. At long context that traffic, not the FLOPs, is the cost.</p> <h5 id="flashattention-briefly"><strong>FlashAttention, briefly</strong></h5> <p>FlashAttention (<a href="https://arxiv.org/abs/2205.14135">Dao et al., 2022</a>) is IO-aware <em>exact</em> attention: tile Q, K, V so the working set stays in on-chip SRAM, and compute the softmax <em>online</em>, tracking a running maximum \(m\) and running denominator \(l\) per row as tiles stream through. When a new tile raises the maximum, all previously accumulated exponentials are too large by a factor of \(\exp(m_{old} - m_{new})\), so the accumulator is rescaled by exactly that factor; this rescaling is what makes single-pass exact softmax possible, and the \(n \times n\) matrix is never written to HBM. In roofline terms, FlashAttention saves <strong>bytes, not FLOPs</strong>: the arithmetic is unchanged, the \(O(n^2)\) HBM traffic is eliminated, and the op’s arithmetic intensity rises accordingly.</p> <p>FlashAttention-2 (<a href="https://arxiv.org/abs/2307.08691">Dao, 2023</a>) keeps the math and fixes the mapping: fewer non-matmul FLOPs on the hot path, parallelization across the <em>sequence</em> dimension even within a single head, and work split across warps to cut shared-memory traffic, for roughly 2x over FA1 and 50-73% of peak FLOP/s on A100. The sequence-dimension parallelism is the part that matters for inference specifically: a decode or small-batch workload has only batch-times-heads independent work items, too few to fill an H100’s 132 SMs, and splitting along the sequence restores the missing parallelism.</p> <h5 id="the-pagedattention-kernel"><strong>The PagedAttention kernel</strong></h5> <p>The decode kernel that reads a paged cache is worth knowing at the block-diagram level (in-repo walkthrough: <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/paged_attention.md">docs/design/paged_attention.md</a>, sources in <code class="language-plaintext highlighter-rouge">csrc/attention/</code>). For one query token:</p> <ol> <li>Fetch the request’s block table and loop over the physical blocks it names.</li> <li>For each block, dot the query against its keys (the QK stage).</li> <li>Run the online-softmax bookkeeping: track the running max, then the exponential sum, normalizing as tiles arrive.</li> <li>Weight the values and accumulate (the LV stage), then write the output row.</li> </ol> <p>The contribution of the PagedAttention paper was co-designing this kernel with the block allocator: attention pays one level of indirection through the block table, and in exchange the memory system gets the OS-paging model from earlier. The indirection is cheap precisely because the kernel was built around it.</p> <h5 id="the-backend-zoo-and-two-integration-seams"><strong>The backend zoo, and two integration seams</strong></h5> <p>vLLM keeps a registry of attention backends (<code class="language-plaintext highlighter-rouge">vllm/v1/attention/backends/registry.py</code>, selection via <code class="language-plaintext highlighter-rouge">--attention-backend</code>; each backend’s feature support is validated by <code class="language-plaintext highlighter-rouge">validate_configuration()</code> and auto-documented in <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/attention_backends.md">docs/design/attention_backends.md</a>). The main CUDA ones: <code class="language-plaintext highlighter-rouge">flash_attn.py</code> (FlashAttention with paged KV), <code class="language-plaintext highlighter-rouge">flashinfer.py</code>, <code class="language-plaintext highlighter-rouge">triton_attn.py</code>, and <code class="language-plaintext highlighter-rouge">flex_attention.py</code> (PyTorch FlexAttention), plus MLA variants under <code class="language-plaintext highlighter-rouge">mla/</code>, Mamba and GDN backends for hybrid models, and ROCm-specific implementations.</p> <p>Two integration facts foreshadow the runtime section. For <strong>torch.compile</strong>, the entire attention operation hides behind one custom op, <code class="language-plaintext highlighter-rouge">torch.ops.vllm.unified_attention_with_output</code>, so Dynamo sees a single clean graph node instead of tracing ragged, data-dependent internals (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/torch_compile.md">docs/design/torch_compile.md</a>). For <strong>CUDA graphs</strong>, backend capabilities differ and drive the runtime mode: the design doc notes that only FlashAttention 3 supports unified full-graph capture, while others (FlashInfer, FlashMLA, Mamba) support full graphs only for pure decode batches (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/cuda_graphs.md">docs/design/cuda_graphs.md</a>). Attention, in short, is the op that breaks every whole-model optimization, and the engine is architected around walling it off.</p> <h5 id="mla-in-one-paragraph"><strong>MLA in one paragraph</strong></h5> <p>DeepSeek’s Multi-head Latent Attention stores a low-rank latent compression of K and V instead of full per-head KV; the per-head keys and values are re-projected from the latent at compute time. vLLM’s config comment states that MLA during decode becomes MQA with a single KV head (<code class="language-plaintext highlighter-rouge">ModelConfig.get_num_kv_heads</code>, <code class="language-plaintext highlighter-rouge">vllm/config/model.py</code>), so the cache holds one latent vector per token regardless of TP: a dramatic KV-per-token saving bought with extra projection compute, which is a good trade for a phase whose FLOPs were idle anyway. Dedicated backends live in <code class="language-plaintext highlighter-rouge">vllm/v1/attention/backends/mla/</code>.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2205.14135">FlashAttention - Dao et al.</a>, <a href="https://arxiv.org/abs/2307.08691">FlashAttention-2 - Dao</a></li> <li><a href="https://arxiv.org/abs/2309.06180">PagedAttention - Kwon et al.</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/paged_attention.md">vLLM paged attention kernel walkthrough</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/attention_backends.md">attention backends doc</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/cuda_graphs.md">CUDA graphs doc</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/torch_compile.md">torch.compile doc</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/v1/attention/backends/</code>, <code class="language-plaintext highlighter-rouge">csrc/attention/</code>, <code class="language-plaintext highlighter-rouge">vllm/config/model.py</code></li> </ul> <hr/> <h4 id="quantization-and-moe"><strong>Quantization and MoE</strong></h4> <h5 id="quantization-said-via-the-roofline"><strong>Quantization, said via the roofline</strong></h5> <p>Which quantization scheme helps depends entirely on which regime you’re in, and the roofline makes the decision mechanical.</p> <ul> <li><strong>Weight-only 4-bit (W4A16)</strong> cuts weight bytes 4x while computing in 16-bit. That’s a direct attack on memory-bound decode: the 16 GB weight read becomes 4 GB and the decode floor drops proportionally, while the extra dequantization arithmetic is free because the FLOPs were idle. But it does roughly nothing for compute-bound prefill or large-batch throughput, where bytes weren’t the bottleneck.</li> <li><strong>W8A8 (FP8 or INT8)</strong> quantizes activations too, so the GEMMs themselves run on the 8-bit tensor cores at double the 16-bit rate (<a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">Hopper in-depth</a>). That attacks the compute-bound regime as well, and it’s the scheme that helps prefill.</li> <li><strong>KV-cache quantization</strong> attacks the third byte stream, the one that grows with context and, unlike weights, does not amortize with batch.</li> </ul> <p>The methods behind the checkpoints (<a href="https://github.com/vllm-project/vllm/blob/main/docs/features/quantization/README.md">vLLM quantization docs</a>):</p> <table> <thead> <tr> <th>Method</th> <th>Idea</th> <th>Bits</th> </tr> </thead> <tbody> <tr> <td><a href="https://arxiv.org/abs/2210.17323">GPTQ</a> (Frantar et al., ICLR ‘23)</td> <td>one-shot post-training weight quantization using approximate second-order information</td> <td>3-4 bit weights</td> </tr> <tr> <td><a href="https://arxiv.org/abs/2306.00978">AWQ</a> (Lin et al., MLSys ‘24 best paper)</td> <td>activation-aware: protect the ~1% of weight channels with large activations by scaling, no backprop</td> <td>4-bit weights</td> </tr> <tr> <td>FP8 W8A8</td> <td>E4M3 weights and activations with per-tensor or per-channel scales; native tensor-core dtype on Hopper</td> <td>8</td> </tr> <tr> <td>INT8 W8A8, INT4 W4A16, W4A8</td> <td>via LLM Compressor</td> <td> </td> </tr> </tbody> </table> <p>AWQ’s insight generalizes: quantization error is not uniform, a small set of salient channels (identified by activation magnitude, not weight magnitude) dominates end quality, and protecting just those preserves accuracy at 4-bit. The rule-of-thumb from both papers’ results: weight-only 4-bit is near-lossless at 7B+ scale, while activation quantization is the hard part (outlier channels), which is why W8A8 needs calibration and scales while W4A16 barely does. On the kernel side, quantized weights need dequantization <em>fused inside</em> the GEMM, a dedicated kernel family: vLLM’s hardware table lists Marlin kernels for GPTQ/AWQ/FP8/FP4 on Ampere and newer, with sources under <code class="language-plaintext highlighter-rouge">csrc/quantization/</code>. vLLM ingests checkpoints from AutoAWQ, BitsAndBytes, GPTQModel, LLM Compressor, NVIDIA ModelOpt, TorchAO, Quark, and GGUF, plus online quantization.</p> <p>The KV-cache variant (<a href="https://github.com/vllm-project/vllm/blob/main/docs/features/quantization/quantized_kvcache.md">quantized_kvcache.md</a>): <code class="language-plaintext highlighter-rouge">kv_cache_dtype="fp8"</code> (E4M3 on CUDA and ROCm, E5M2 optional on CUDA), with per-tensor or per-head scales. One default to know before flipping it on: <strong>calibration is off by default and all scales are 1.0</strong> (<code class="language-plaintext highlighter-rouge">calculate_kv_scales=False</code>), which assumes the K/V values fit E4M3’s range as-is; real outliers will clip or lose precision silently. The alternatives are dynamic scale computation (<code class="language-plaintext highlighter-rouge">calculate_kv_scales=True</code>) or a checkpoint with calibrated scales. The capacity payoff is exactly the block-pool formula from earlier with \(b\) going from 2 to 1: half the page size, double the blocks, roughly double the maximum concurrency or context.</p> <h5 id="moe-essentials"><strong>MoE essentials</strong></h5> <p>A Mixture-of-Experts layer replaces the dense FFN with \(E\) expert FFNs plus a router that sends each token to its top-\(k\) experts. Compute per token stays roughly \(k\) experts’ worth while parameter count scales with \(E\); DeepSeek-V3 is 671B total parameters with 37B activated per token (<a href="https://arxiv.org/abs/2412.19437">tech report</a>). The catch for serving: <em>all</em> 671B must be resident, because any token may route to any expert. MoE thus moves the bottleneck from FLOPs to <strong>memory capacity and communication</strong>, which is why it’s the architecture that forces EP and wide NVLink domains.</p> <p>Two problems define the MoE serving stack. The <strong>kernel problem</strong>: each token multiplies a <em>different</em> weight matrix, so a plain GEMM is impossible; the fused MoE kernels (<code class="language-plaintext highlighter-rouge">vllm/model_executor/layers/fused_moe/</code>, Triton) sort and group tokens by expert, run grouped or batched GEMMs, apply the activation, and scatter results back weighted by router probabilities, all fused to avoid materializing the intermediate shuffles. The <strong>placement problem</strong>: routing is skewed, so some experts’ GPUs saturate while others idle, and the step takes the max over GPUs; <strong>EPLB</strong>, the expert parallelism load balancer (<code class="language-plaintext highlighter-rouge">vllm/distributed/eplb/</code>), re-places and replicates experts against the observed routing load to flatten that max. Together with the phase-specialized all-to-all backends from the distributed section, that’s the MoE serving picture.</p> <p><strong>References</strong></p> <ul> <li><a href="https://arxiv.org/abs/2210.17323">GPTQ - Frantar et al.</a>, <a href="https://arxiv.org/abs/2306.00978">AWQ - Lin et al.</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/features/quantization/README.md">vLLM quantization docs</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/features/quantization/quantized_kvcache.md">quantized KV cache</a></li> <li><a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3 Technical Report</a>, <a href="https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/">NVIDIA Hopper architecture in-depth</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">csrc/quantization/</code>, <code class="language-plaintext highlighter-rouge">vllm/model_executor/layers/fused_moe/</code>, <code class="language-plaintext highlighter-rouge">vllm/distributed/eplb/</code></li> </ul> <hr/> <h4 id="the-engine-at-runtime"><strong>The Engine at Runtime</strong></h4> <h5 id="two-processes"><strong>Two processes</strong></h5> <p>vLLM V1 splits the engine across two process roles (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/arch_overview.md">arch_overview.md</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/multiprocessing.md">multiprocessing.md</a>): the <strong>API server</strong> process handles HTTP, input processing (tokenization, multimodal media), and streaming results back; the <strong>engine core</strong> process runs the scheduler, manages the KV cache, coordinates the GPU workers, and runs a busy loop. They communicate over ZMQ sockets, and under data parallelism the API servers scale out to match, every API server connected to every engine core.</p> <p>The design principle behind the split is stated in the metrics doc and explains half the architecture: the engine core is the inner loop where performance is critical, so anything that <em>can</em> live in the frontend, detokenization, stop-string scanning, metrics assembly, <em>does</em>, where it overlaps with GPU execution instead of adding to it. Time spent on the CPU between forward passes is added ITL for every running request; time spent in the frontend is free. That’s why stop strings are detected in the frontend’s output processor (as noted in the scheduling section): they need detokenized text, and detokenization was banished from the hot loop.</p> <p>One request’s life, end to end: HTTP in, tokenize in the frontend, cross ZMQ, wait QUEUED in the engine core, get tokens and KV blocks assigned by the scheduler (SCHEDULED), run in the fused forward pass, sampled token crosses back per iteration in <code class="language-plaintext highlighter-rouge">EngineCoreOutputs</code>, frontend detokenizes, checks stop strings, streams the delta out. On the GPU side, the model runner (<code class="language-plaintext highlighter-rouge">vllm/v1/worker/gpu_model_runner.py</code>, as of 2026-08-21) keeps a <strong>persistent input batch</strong> (<code class="language-plaintext highlighter-rouge">gpu_input_batch.py</code>) that is updated incrementally as requests enter and leave rather than rebuilt per step, builds the per-backend attention metadata, launches the compiled-and-graphed model, and samples.</p> <h5 id="torchcompile"><strong>torch.compile</strong></h5> <p>vLLM compiles the model with a structure dictated by attention (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/torch_compile.md">torch_compile.md</a>). Dynamo captures the forward as a full graph, with attention appearing as the opaque <code class="language-plaintext highlighter-rouge">unified_attention_with_output</code> custom op from the kernels section. The graph is then <strong>split at the attention ops</strong> into piecewise submodules; for a Llama-style model that yields three <em>unique</em> subgraphs (before the first attention, the repeated middle between attentions, after the last), and the repeated middle compiles once and is reused. Compiled artifacts are cached on disk keyed by everything that was traced, model code, the vLLM files involved, and the relevant config, so any change to any of them is a cache miss and a recompile (<code class="language-plaintext highlighter-rouge">~/.cache/vllm/torch_compile_cache/...</code>). Batch and sequence dimensions are symbolic, with guard-dropping modes (<code class="language-plaintext highlighter-rouge">backed_size_oblivious</code>, <code class="language-plaintext highlighter-rouge">unbacked</code>) controlling the recompile-versus-guard-risk trade.</p> <h5 id="cuda-graphs"><strong>CUDA graphs</strong></h5> <p>Here’s where the kernel-launch overhead from the numbers discussion comes due. A decode step launches hundreds of small kernels for a few milliseconds of total GPU work; at microseconds of CPU launch overhead each, the launches themselves become a tax on exactly the step the decode floor already made expensive. CUDA graphs fix it by capturing the whole kernel sequence once and replaying it with a single launch.</p> <p>The complication, once again, is attention. vLLM’s answer (<a href="https://github.com/vllm-project/vllm/blob/main/docs/design/cuda_graphs.md">cuda_graphs.md</a>) is <strong>piecewise capture</strong>: the token-wise subgraphs <em>between</em> attentions (which is what the torch.compile split above produced) are captured as CUDA graphs, and attention runs eager in the gaps, since its ragged, paged, data-dependent access is, in the doc’s words, non-trivial to make cudagraph-compatible. A supporting detail with a purpose: the attention op takes its <em>output tensor as an input</em>, so all memory allocation stays inside the graphed pieces and pointers remain stable across replays.</p> <p>The runtime modes (<code class="language-plaintext highlighter-rouge">cudagraph_mode</code>): <code class="language-plaintext highlighter-rouge">NONE</code>, <code class="language-plaintext highlighter-rouge">PIECEWISE</code>, <code class="language-plaintext highlighter-rouge">FULL</code>, <code class="language-plaintext highlighter-rouge">FULL_DECODE_ONLY</code> (useful for the decode fleet of a disaggregated setup), and the default <code class="language-plaintext highlighter-rouge">FULL_AND_PIECEWISE</code>: full graphs for uniform decode batches, piecewise for everything else. A <code class="language-plaintext highlighter-rouge">CudagraphDispatcher</code> (<code class="language-plaintext highlighter-rouge">vllm/v1/cudagraph_dispatcher.py</code>) picks the mode per batch, preferring FULL over PIECEWISE over NONE, and pads each batch up to the nearest pre-captured size (<code class="language-plaintext highlighter-rouge">cudagraph_capture_sizes</code>), so the padding waste is bounded by the gap between consecutive capture sizes. Backend capability drives automatic downgrades (the FlashAttention-3-only full-capture caveat from the kernels section), and the doc is candid about the cost of the default: most performant, but the most memory and the longest capture time at startup.</p> <h5 id="sampling"><strong>Sampling</strong></h5> <p>The last stop in the loop (<code class="language-plaintext highlighter-rouge">Sampler.forward</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/sample/sampler.py</code>): apply logits processors and penalties, apply temperature, then greedy or random sampling with top-k/top-p, gather logprobs if requested, and merge speculative-decoding outputs. Greedy has a shortcut path, and temperature 0 maps to it. Structured output (JSON schemas, grammars) costs almost nothing at this point: the scheduler ships a grammar bitmask computed off the hot path (<code class="language-plaintext highlighter-rouge">Scheduler.get_grammar_bitmask</code>; requests wait in a blocked state during grammar compilation, one of the scheduling section’s skip conditions), and the sampler applies the mask to the logits before sampling, one masked-fill per step.</p> <p><strong>References</strong></p> <ul> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/arch_overview.md">vLLM architecture overview</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/multiprocessing.md">multiprocessing doc</a></li> <li><a href="https://github.com/vllm-project/vllm/blob/main/docs/design/cuda_graphs.md">vLLM CUDA graphs doc</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/torch_compile.md">torch.compile doc</a>, <a href="https://github.com/vllm-project/vllm/blob/main/docs/design/metrics.md">metrics design doc</a></li> <li>vLLM source, as of 2026-08-21: <code class="language-plaintext highlighter-rouge">vllm/v1/worker/gpu_model_runner.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/worker/gpu_input_batch.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/cudagraph_dispatcher.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/sample/sampler.py</code>, <code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code></li> </ul> <hr/> <h4 id="test-yourself"><strong>Test Yourself</strong></h4> <p>Everything above, as questions. Try answering each from memory before reading its answer; every answer traces back to a section of this post, and the sources are in those sections’ references. If a group feels shaky, that’s the section to reread.</p> <h5 id="the-numbers"><strong>The numbers</strong></h5> <p><strong>1. H100 SXM memory bandwidth and dense BF16 TFLOPS? Ridge point?</strong> 3.35 TB/s and ~1000 TFLOPS dense (1,979 “with sparsity”). Ridge \(\approx 10^{15}/3.35\times10^{12} \approx 300\) FLOP/byte.</p> <p><strong>2. KV bytes per token for Llama 3 70B in bf16? In fp8?</strong> \(2 \cdot 80 \cdot 8 \cdot 128 \cdot 2 = 320\) KiB/token in bf16; halve the bytes-per-element for fp8: 160 KiB.</p> <p><strong>3. Why is the marketing TFLOPS number double the usable one?</strong> It assumes 2:4 structured sparsity, which doubles the rate. Dense workloads, LLM inference included, get the dense number, half of it.</p> <p><strong>4. Minimum decode step time for a 70B bf16 model on one H100, and the implication?</strong> 140 GB / 3.35 TB/s \(\approx\) 42 ms, so ~24 tok/s single-stream, and the model doesn’t even fit in 80 GB. TP splits the weight read across ranks, cutting both memory per rank and the floor (TP4: ~10.5 ms).</p> <p><strong>5. At what batch size does decode stop being weight-bandwidth-bound on H100, roughly?</strong> Around the ridge point, ~300 sequences, since decode’s arithmetic intensity is roughly the batch size. KV reads scale with batch times context and don’t amortize, pushing the real crossover higher.</p> <h5 id="the-problem-and-metrics"><strong>The problem and metrics</strong></h5> <p><strong>1. Why is decode memory-bound if it does the same \(2N\) FLOPs per token as prefill?</strong> Same FLOPs, different amortization of bytes: prefill reads the weights once for \(P\) tokens (intensity \(\approx P\), above the ridge); decode reads them for 1 token per sequence (intensity \(\approx B\)). Below the ridge, bytes dominate and the FLOPs are irrelevant.</p> <p><strong>2. ITL vs TPOT: definitions, and which exposes a preemption?</strong> ITL is the gap between consecutive tokens, one sample per step; TPOT is \((\text{E2EL} - \text{TTFT})/(n-1)\), a per-request mean. A preemption is one huge ITL sample that TPOT smears into the average, so ITL p99 exposes it.</p> <p><strong>3. Why does a long prompt hurt other requests’ latency, and what bounds it?</strong> Its prefill tokens ride in the same forward pass as everyone’s decodes and stretch the step, which every co-batched request inherits as ITL. Chunked prefill bounds the per-step chunk via the token budget, trading that request’s TTFT.</p> <p><strong>4. A vendor claims 5000 tok/s versus another’s 3000. What do you ask?</strong> Batch size and the latency distribution; the SLOs (goodput), prompt/output length mix, arrival pattern, and client- versus engine-side measurement. Throughput without a latency constraint is purchasable with batch size and not comparable.</p> <p><strong>5. What does client-measured TTFT include that engine-measured TTFT does not?</strong> Network, HTTP, and client-side stream handling; engine TTFT starts at frontend arrival (tokenization). The delta isolates everything outside the engine.</p> <p><strong>6. Why does throughput rise roughly linearly with batch size, then flatten? What sets the knee?</strong> Each added sequence reuses the same weight read while memory-bound, so tokens/s scales almost linearly; the knee is where arithmetic intensity reaches the ridge point, bent earlier by growing KV traffic.</p> <p><strong>7. Static vs continuous batching: what specifically is wasted in static?</strong> Finished requests’ slots idle until the whole batch drains, and arrivals wait for the drain. Continuous batching re-forms the batch every step.</p> <p><strong>8. Is a preemption counted in vLLM’s decode-time histogram or reset, and why does that make sense?</strong> Counted in: the decode interval spans first to last token including the gap, and only the first SCHEDULED sets the timestamp. The user experienced the gap, so SLO-facing histograms should include it.</p> <h5 id="kv-cache-and-pagedattention"><strong>KV cache and PagedAttention</strong></h5> <p><strong>1. Name the three kinds of waste in contiguous per-request KV allocation and which ones paging eliminates.</strong> Over-reservation for an unknown output length, internal fragmentation in the tail, external fragmentation between regions. Paging eliminates reservation (allocate per block on demand) and external entirely; internal remains, bounded per sequence.</p> <p><strong>2. What bounds internal fragmentation per sequence, numerically?</strong> At most \(\text{block\_size} - 1 = 15\) wasted token slots, in the last partial block; under 4% overall per the launch blog.</p> <p><strong>3. Walk through admission with a 50% prefix-cache hit.</strong> <code class="language-plaintext highlighter-rouge">get_computed_blocks</code> hash-walks the prompt and returns the cached prefix blocks; <code class="language-plaintext highlighter-rouge">allocate_slots</code> touches them (ref count up, off the free queue), pops fresh blocks from the free-queue head for the rest (evicting their old identities), and registers full blocks in the hash map. <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> starts at the hit length, so prefill computes only the tail.</p> <p><strong>4. Why preallocate every <code class="language-plaintext highlighter-rouge">KVCacheBlock</code> and hand-roll an intrusive linked list?</strong> The scheduler’s hot loop allocates and frees at very high rates; preallocation avoids Python object churn, and intrusive links give O(1) removal from the middle of the free list when a cache hit touches a freed block.</p> <p><strong>5. How does vLLM size the block pool at startup, and what does <code class="language-plaintext highlighter-rouge">gpu_memory_utilization = 0.92</code> multiply?</strong> Total device memory times 0.92, minus measured weights, peak activation (an actual dummy forward), and non-torch overhead, divided by the per-block page size. It multiplies total memory, not free memory.</p> <p><strong>6. Who calls <code class="language-plaintext highlighter-rouge">allocate_slots</code>, and why does that placement matter?</strong> The scheduler, in the engine core. Memory is the scarce resource that admission and preemption hinge on, so allocation lives where those decisions are made; workers just receive block ids.</p> <p><strong>7. What happens to a preempted request’s state in V1, and how is that different from V0?</strong> All blocks freed, <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> reset to 0, prepended to waiting; recovery is recompute, possibly accelerated by prefix hits. V0 could swap blocks to CPU; V1 dropped swap because shared blocks plus recompute is simpler.</p> <p><strong>8. A block is “freed”. Is its content gone?</strong> No: content and hash survive in the free queue and remain hittable. The block dies only when a later allocation pops it and evicts its hash.</p> <h5 id="prefix-caching-1"><strong>Prefix caching</strong></h5> <p><strong>1. What three components go into a block hash, and why each?</strong> The parent hash (chains to the entire prefix), the block’s exact tokens (collision resistance), and extra hashes (LoRA id, multimodal hashes, <code class="language-plaintext highlighter-rouge">cache_salt</code>) for anything else that changes the KV.</p> <p><strong>2. Why cache full blocks only?</strong> A partial block’s KV depends on tokens still being appended, and the hash chain needs complete units. Consequence: hits come in multiples of the block size.</p> <p><strong>3. How does eviction work with no evictor thread or TTL?</strong> Freed-but-cached blocks queue FIFO; allocation pops the head, and popping a cached block strips its hash. Eviction is lazy, embedded in allocation, and LRU by construction.</p> <p><strong>4. Why are a request’s blocks freed tail-first?</strong> The deepest block encodes the longest, most request-specific prefix, least likely to be reused; freeing it first puts it nearest the eviction head.</p> <p><strong>5. Two requests with the same prompt arrive in the same batch. Does the second hit the cache?</strong> Yes, once the shared blocks fill: full blocks are hash-registered at fill time, not at request completion.</p> <p><strong>6. Why does an image need an extra hash when its placeholder tokens are already in the block?</strong> Different images tokenize to identical placeholders, so token-only hashes would collide across images; the frontend’s content hash disambiguates.</p> <p><strong>7. What attack does <code class="language-plaintext highlighter-rouge">cache_salt</code> mitigate, and how?</strong> Timing probes: an adversary measures latency to learn whether another tenant’s prompt is cached. Salting the first block’s hash partitions the cache by trust group.</p> <p><strong>8. Why can a cheap hash algorithm be a privacy problem in multi-tenant serving?</strong> Constructible collisions could let an attacker poison or read KV across requests; a non-cryptographic hash makes collisions easier, hence the docs’ warning.</p> <h5 id="scheduling"><strong>Scheduling</strong></h5> <p><strong>1. “There is no prefill phase in the V1 scheduler.” What replaced it?</strong> Every request just has <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> catching up to <code class="language-plaintext highlighter-rouge">num_tokens_with_spec</code>; assigning “how many tokens this step” subsumes prefill, chunking, decode, prefix hits, and speculative decoding in one code path under one token budget.</p> <p><strong>2. Who gets preempted under FCFS? Under priority? What happens to the victim?</strong> FCFS: the most recently added running request. Priority: the worst (priority, arrival time) pair. The victim loses all blocks, resets its computed count, and goes to the head of the waiting queue; its ITL spikes and its decode interval absorbs the gap.</p> <p><strong>3. Why schedule running requests before waiting ones?</strong> Running-first protects in-flight ITL; admission (prefills) gets the leftover budget, which is what keeps decode steps flat under chunked prefill. TTFT is protected only by leftover budget and queue policy.</p> <p><strong>4. p99 ITL spikes correlate with long prompts. Which two knobs, and their trade-offs?</strong> <code class="language-plaintext highlighter-rouge">max_num_batched_tokens</code> (bigger buys throughput and TTFT, worse ITL tail) and <code class="language-plaintext highlighter-rouge">long_prefill_token_threshold</code> (caps one request’s slice per step, worse TTFT for that request).</p> <p><strong>5. Why is admission halted in a step that preempted someone?</strong> Preemption means memory was insufficient for the current set; admitting more would immediately re-trigger preemption churn.</p> <p><strong>6. Where does prefix caching plug into scheduling?</strong> At admission: <code class="language-plaintext highlighter-rouge">get_computed_blocks</code> sets <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> to the cached-prefix length, so the scheduler assigns only the uncached tail. A full-prompt hit still schedules at least one token to produce output.</p> <p><strong>7. What does async scheduling overlap, and via what trick?</strong> Scheduling of step \(N+1\) with GPU execution of step \(N\), using <code class="language-plaintext highlighter-rouge">num_output_placeholders</code> to schedule as if in-flight tokens had already arrived.</p> <p><strong>8. What limits concurrency besides <code class="language-plaintext highlighter-rouge">max_num_seqs</code>?</strong> KV block availability (<code class="language-plaintext highlighter-rouge">allocate_slots</code> can fail far earlier), plus the multimodal encoder budget, LoRA slot limits, and grammar-compilation readiness for structured output.</p> <h5 id="speculative-decoding-1"><strong>Speculative decoding</strong></h5> <p><strong>1. State the acceptance rule and argue the output distribution is unchanged.</strong> Accept draft token \(x \sim q\) with probability \(\min(1, p(x)/q(x))\); on rejection, sample from \(\mathrm{norm}(\max(0, p - q))\). Accepted mass at \(x\) is \(\min(p(x), q(x))\), and the recovery distribution restores exactly the shortfall, so the marginal is exactly \(p\).</p> <p><strong>2. Expected tokens per step for \(\alpha = 0.7\), \(\gamma = 3\)?</strong> \((1 - 0.7^4)/(1 - 0.7) = 0.7599/0.3 \approx 2.53\).</p> <p><strong>3. Why does speculative decoding help precisely because decode is memory-bound?</strong> The weights are read anyway; verifying \(k\) extra tokens rides along on idle FLOPs. More tokens per weight read is a direct attack on the decode floor.</p> <p><strong>4. Why does per-position acceptance decay, and what does that imply for \(\gamma\)?</strong> Each draft position conditions on previous draft tokens, so errors compound. Past the point where \(\alpha^\gamma\) is small, extra draft length adds cost but almost no accepted tokens.</p> <p><strong>5. When would n-gram beat EAGLE despite having no model?</strong> When the output copies the context: RAG quoting, summarization, code edits. String lookup drafts those spans at zero GPU cost with no second model to host.</p> <p><strong>6. What happens to the KV blocks of rejected draft tokens?</strong> They were pre-allocated via <code class="language-plaintext highlighter-rouge">num_lookahead_tokens</code>; the slots are reclaimed and overwritten. The waste is the drafted-and-verified compute.</p> <p><strong>7. Why does the gain shrink at high QPS?</strong> Large batches push decode’s arithmetic intensity toward the ridge; the idle FLOPs disappear and drafting becomes added load, which is why the docs scope the feature to medium-to-low QPS.</p> <p><strong>8. What is the bonus token, and why is it sampled outside the rejection sampler?</strong> The extra token appended when all \(\gamma\) drafts are accepted, sampled from the target; it’s sampled outside so the full sampling configuration (top-p/top-k) applies.</p> <h5 id="distributed-inference"><strong>Distributed inference</strong></h5> <p><strong>1. Why pair a column-parallel with a row-parallel layer, and how many all-reduces per layer result?</strong> The column-parallel output stays sharded and feeds the row-parallel input with no communication; only the row-parallel output needs an all-reduce. Attention plus MLP gives 2 all-reduces per layer.</p> <p><strong>2. What per-token communication does TP8 decode on Llama 3 70B incur, and why NVLink?</strong> \(2 \times 80 = 160\) all-reduces per token, each on a 16 KiB hidden vector: small, frequent, latency-bound messages, exactly what NVLink and custom all-reduce kernels exist for.</p> <p><strong>3. TP vs PP: which reduces per-token latency, and why?</strong> TP: each rank reads \(1/\text{TP}\) of the weights per step, so the memory-bound floor drops. PP doesn’t: every token still crosses all layers serially; PP adds capacity and throughput only.</p> <p><strong>4. Where do TP and PP go in a 2-node, 8-GPU-per-node cluster?</strong> TP8 inside each node (NVLink domain), PP2 across nodes (one cheap point-to-point hidden-state transfer per microbatch per boundary).</p> <p><strong>5. Why do DP ranks of an MoE model need dummy forward passes?</strong> Expert layers run collectives every forward; a rank with no work must still participate or its peers deadlock, so idle ranks run empty passes, coordinated by the DP Coordinator.</p> <p><strong>6. Why different all-to-all backends for prefill and decode?</strong> Prefill wants throughput (grouped GEMM, contiguous layouts: <code class="language-plaintext highlighter-rouge">deepep_high_throughput</code>); decode wants latency and CUDA-graph compatibility (<code class="language-plaintext highlighter-rouge">deepep_low_latency</code>, masked layout).</p> <p><strong>7. What gets worse as TP grows?</strong> More and smaller collectives, thinner and less efficient per-rank GEMMs, and beyond <code class="language-plaintext highlighter-rouge">num_kv_heads</code> the KV cache stops sharding. Efficiency per GPU falls.</p> <p><strong>8. How does TP change each rank’s KV footprint?</strong> KV heads per rank is \(\max(1, H_{kv}/\text{TP})\): Llama 3’s 8 heads shard down to 1 at TP8, and TP16 replicates with no further saving. MLA decodes as MQA, 1 latent head regardless.</p> <h5 id="disaggregation-and-kv-transfer"><strong>Disaggregation and KV transfer</strong></h5> <p><strong>1. The two documented reasons for disaggregated prefill, and the one thing it does not improve?</strong> Independent TTFT/ITL tuning per fleet, and tail-ITL control by removing prefill interference. Explicitly not improved: throughput.</p> <p><strong>2. Chunked prefill and disaggregation both control tail ITL. When is disaggregation the better answer?</strong> Chunked prefill needs a well-chosen chunk size, which the docs concede is hard; disaggregation removes the interference class at the cost of a KV transfer and a second fleet. Strict ITL SLOs at scale favor it.</p> <p><strong>3. Sketch a request’s path through a prefill/decode split.</strong> The prefill instance computes the full prompt, saving KV per layer through the connector; the decode instance’s scheduler learns the remote cache holds the prefix via <code class="language-plaintext highlighter-rouge">get_num_new_matched_tokens()</code> (the same <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> slot as prefix caching), the request waits in <code class="language-plaintext highlighter-rouge">WAITING_FOR_REMOTE_KVS</code> until loaded, then decodes normally.</p> <p><strong>4. Why does the connector API have per-layer load and save hooks?</strong> So transfer overlaps compute layer by layer instead of serializing a multi-GiB blob before any work starts.</p> <p><strong>5. Estimate the bandwidth to move a 32K-token Llama 3 70B context in under 200 ms.</strong> \(32\text{K} \times 320\) KiB = 10 GiB \(\approx\) 10.7 GB; over 200 ms that’s ~54 GB/s \(\approx\) 430 Gbit/s: RDMA-NIC territory, hence NIXL, UCX, and GDS.</p> <p><strong>6. KV offloading versus disaggregation?</strong> Offloading moves cold KV down the same stack’s memory hierarchy (CPU RAM, disk) to extend cache capacity; disaggregation splits the compute phases across instances. Same connector API, different job.</p> <p><strong>7. What does Mooncake add beyond a prefill/decode split?</strong> A KV-cache-centric global scheduler over cache pooled across CPU, DRAM, and SSD cluster-wide, plus overload handling with prediction-based early rejection.</p> <p><strong>8. Why is the block hash the natural addressing scheme for remote KV?</strong> The chained hash names “these tokens after exactly this prefix” independent of where the bytes live, so local and remote lookup are the same key computation.</p> <h5 id="gpu-and-roofline"><strong>GPU and roofline</strong></h5> <p><strong>1. Define occupancy and the mechanism by which it hides latency.</strong> Active warps per SM over the maximum. The SM issues from any ready warp, so one warp’s memory stall is covered by another’s work.</p> <p><strong>2. Compute the H100 ridge point for dense BF16 and place batch-64 decode on the roofline.</strong> \(\approx 10^{15} / 3.35 \times 10^{12} \approx 300\) FLOP/byte. Batch-64 decode has intensity \(\approx 64\): memory-bound, attaining roughly \(64/300 \approx 21\%\) of peak.</p> <p><strong>3. Why does fusing a norm into a neighboring op help while fusing two GEMMs usually does not?</strong> A norm has \(O(1)\) intensity; fused, it rides on bytes already moving and removes a full read-write pass. GEMMs are each compute-bound with tiled reuse; fusing saves little traffic and breaks their optimal tilings.</p> <p><strong>4. TMA and thread block clusters, one sentence each.</strong> TMA: a per-SM DMA engine for asynchronous bulk global-to-shared transfers. Clusters: co-scheduled blocks across SMs that can access each other’s shared memory.</p> <p><strong>5. Why does FP8 shift both the roof and the kernel’s position?</strong> It doubles peak FLOP/s (roof up) and halves bytes per element (the same computation’s intensity up); both shifts push toward compute-bound.</p> <p><strong>6. A kernel sits at 40% of peak FLOP/s. What single question decides the optimization direction?</strong> Is its arithmetic intensity above or below the ridge point? Below: cut bytes (fusion, dtype, layout). Above: raise math throughput (tensor cores, occupancy, scheduling).</p> <h5 id="attention-kernels-1"><strong>Attention kernels</strong></h5> <p><strong>1. What does FlashAttention save, FLOPs or bytes?</strong> Bytes. The attention is exact, FLOPs unchanged; tiling keeps the \(n \times n\) scores in SRAM, eliminating \(O(n^2)\) HBM traffic and lifting the op’s arithmetic intensity.</p> <p><strong>2. Walk through online softmax: which two scalars, and why rescale?</strong> A running max \(m\) (numerical stability) and running denominator \(l\). A new tile can raise \(m\), making all previously accumulated exponentials too large by \(\exp(m_{old} - m_{new})\); rescaling by that factor keeps the single-pass accumulation exact.</p> <p><strong>3. What did FA2 change, and why does within-head parallelism matter for inference?</strong> Fewer non-matmul FLOPs, parallelism across sequence blocks within a head, warp-level partitioning to cut shared-memory traffic; ~2x FA1. Decode and small-batch have too few batch-times-heads work items to fill the SMs, so sequence-dimension parallelism is what keeps them busy.</p> <p><strong>4. How does the PagedAttention kernel find its KV?</strong> Through the request’s block table: logical position to physical block id to contents, looping over the table’s blocks doing QK, online softmax, then value accumulation.</p> <p><strong>5. Why does attention break CUDA-graph capture and torch.compile tracing, and how does vLLM wall it off?</strong> Ragged lengths, block tables, and paged gathers are data-dependent control that capture and tracing can’t specialize. torch.compile hides it behind the <code class="language-plaintext highlighter-rouge">unified_attention_with_output</code> custom op; CUDA graphs exclude it via piecewise capture (or require a backend like FlashAttention 3 for full capture).</p> <p><strong>6. Why does MLA decode like MQA, and what’s the KV win?</strong> It caches one shared latent projection instead of per-head KV (<code class="language-plaintext highlighter-rouge">get_num_kv_heads</code> returns 1), so KV per token shrinks to the latent size, paid for with extra projection compute.</p> <h5 id="quantization-and-moe-1"><strong>Quantization and MoE</strong></h5> <p><strong>1. W4A16 vs W8A8: which regime does each attack?</strong> W4A16 cuts weight bytes: memory-bound decode. Compute still runs 16-bit, so compute-bound prefill sees little; W8A8 runs the GEMMs on 8-bit tensor cores, doubling math throughput there.</p> <p><strong>2. What makes AWQ “activation-aware”, and why does 1% of channels matter?</strong> It scales weight channels by activation magnitude: the ~1% of channels with large activations dominate quantization error, and protecting them preserves accuracy at 4-bit with no backprop.</p> <p><strong>3. vLLM’s default fp8 KV scales are 1.0. Why is that risky?</strong> Unit scales assume values fit E4M3’s range; real outliers clip or lose precision silently. Alternatives: <code class="language-plaintext highlighter-rouge">calculate_kv_scales=True</code> or a checkpoint with calibrated scales.</p> <p><strong>4. What does fp8 KV do to max concurrency, via which formula?</strong> Bytes per element drops 2 to 1 in the KV-per-token formula, halving <code class="language-plaintext highlighter-rouge">page_size_bytes</code>, doubling <code class="language-plaintext highlighter-rouge">num_blocks</code>, and roughly doubling max concurrency.</p> <p><strong>5. Why does a 671B-parameter MoE with 37B active still need massive memory, and which parallelism answers it?</strong> Any token may route to any expert, so all parameters must be resident. Expert parallelism shards the experts, paying with all-to-all routing.</p> <p><strong>6. What does the fused MoE kernel fuse, and why is a plain GEMM impossible?</strong> Routing bookkeeping with the expert GEMMs: sort and group tokens by expert, run grouped GEMMs, scatter back weighted by router probabilities. A plain GEMM is impossible because each token multiplies a different weight matrix.</p> <p><strong>7. What is EPLB balancing, and why does routing skew hurt?</strong> Expert load across GPUs: skewed routing saturates some GPUs while others idle, and the step takes the max over GPUs. EPLB re-places or replicates experts to flatten it.</p> <h5 id="engine-runtime"><strong>Engine runtime</strong></h5> <p><strong>1. Why two processes, and what crosses the ZMQ boundary?</strong> To isolate the latency-critical scheduler-plus-GPU loop from Python-heavy HTTP and tokenization. In: tokenized requests. Out: <code class="language-plaintext highlighter-rouge">EngineCoreOutputs</code> (token ids, events, stats) per iteration.</p> <p><strong>2. Why does detokenization live in the frontend?</strong> Anything between forward passes is added ITL for every running request; frontend work overlaps with GPU execution instead.</p> <p><strong>3. What are piecewise CUDA graphs, and why is attention excluded?</strong> The token-wise subgraphs between attentions are captured and replayed; attention runs eager because its ragged, paged, data-dependent access doesn’t fit capture.</p> <p><strong>4. Why does the attention op take its output tensor as an input?</strong> So the graphed pieces own all memory allocation: eager attention writes into a graph-managed buffer, keeping pointers stable across replays.</p> <p><strong>5. What triggers a torch.compile cache miss?</strong> Any change to traced inputs: model source, inlined vLLM code, or relevant config; the cache directory is keyed on all of it.</p> <p><strong>6. What does FULL_AND_PIECEWISE do for a mixed batch versus a uniform decode batch?</strong> Mixed: dispatched to piecewise graphs, attention eager. Uniform decode: dispatched to the full graph captured for that size. The dispatcher picks per step, preferring FULL, then PIECEWISE, then NONE.</p> <p><strong>7. Why pad batches under CUDA graphs, and what bounds the waste?</strong> Graphs replay fixed shapes, so batches pad up to the nearest captured size; waste is bounded by the gap between consecutive capture sizes.</p> <p><strong>8. Where is structured output enforced, and what does it cost per step?</strong> The scheduler ships a grammar bitmask; the sampler masks invalid logits before sampling. Cost is one mask application per step, with grammar compilation off the hot path.</p> <hr/> <h4 id="wrapping-up"><strong>Wrapping up</strong></h4> <p>If one thing from this post survives, let it be the accounting habit: before reasoning about any part of the serving stack, ask what the bytes are doing. Prefill and decode split because of how they amortize the weight read. The decode floor is a division of two numbers you now know. Batching, speculative decoding, tensor parallelism, and weight quantization are the four distinct attacks on that floor: more tokens per read, more tokens per read again, the read split across GPUs, and a smaller read. The KV cache is the state that makes serving a systems problem; paging packs it, prefix caching deduplicates it, the hash chain names it, and offloading and disaggregation move it. And the scheduler is where all of it meets the metrics: every knob in it is a position on the TTFT-versus-ITL trade, not a way out of it.</p> <p>The other thing worth keeping is the pattern of the vLLM V1 design itself: one token-budget abstraction where there could have been five phases, eviction embedded in allocation where there could have been an evictor thread, recompute where there could have been swap machinery, measurement where there could have been estimates. Every one of those choices trades a little theoretical capability for a lot of simplicity in the hot loop, and the hot loop is where an inference engine lives or dies.</p> <p>If you find a mistake anywhere in here, please let me know and I’ll fix it.</p>]]></content><author><name></name></author><category term="GPU"/><category term="ML"/><summary type="html"><![CDATA[How LLM serving engines work, from prefill, decode, and the roofline to paged KV caches, scheduling, speculative decoding, parallelism, and the vLLM V1 internals that implement them]]></summary></entry><entry><title type="html">LayerNorm and RMSNorm - Forward Pass, Backward Pass, and Every Derivative</title><link href="https://monishver11.github.io/blog/2026/layernorm-rmsnorm/" rel="alternate" type="text/html" title="LayerNorm and RMSNorm - Forward Pass, Backward Pass, and Every Derivative"/><published>2026-08-23T18:00:00+00:00</published><updated>2026-08-23T18:00:00+00:00</updated><id>https://monishver11.github.io/blog/2026/layernorm-rmsnorm</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/layernorm-rmsnorm/"><![CDATA[<p>Hello all. This continues from the <a href="/blog/2026/mlp-forward-backward/">MLP forward and backward pass post</a>, and uses the <code class="language-plaintext highlighter-rouge">einsum</code> notation from the <a href="/blog/2026/einsum/">einsum post</a>. The conventions from the MLP post carry over unchanged, so I won’t restate them: a subscripted symbol is a scalar, a bare symbol is the whole array, vectors are columns, \(\odot\) is elementwise, and \(\partial L/\partial u\) always has the shape of \(u\). We derive in index form first and read off the packed form last.</p> <p>This post does LayerNorm and RMSNorm end to end. It’s a good next step after the MLP for one specific reason: normalization is the first operation where an output depends on <em>every</em> input through a shared statistic. The MLP had one of these, softmax, and it was the only place where the chain rule’s sum didn’t collapse. In a norm, the mean and the variance depend on all \(D\) components, so nothing collapses anywhere, and the full machinery gets a workout.</p> <p>The result we’re heading for is that LayerNorm’s input gradient has exactly three terms:</p> \[\frac{\partial L}{\partial x} = \frac{1}{s}\Big[\hat g \;-\; \underbrace{c_1}_{\text{shift}} \;-\; \underbrace{c_2\,\hat x}_{\text{scale}}\Big]\] <p>and that the two subtracted terms are there because LayerNorm is invariant to shifting and scaling its input. RMSNorm drops the mean subtraction, which costs it the shift invariance, and its backward pass is the same expression with that one term removed. One structural fact explains the cost difference in both directions, and it hands you two free correctness checks.</p> <p>New indices for this post: \(d\) and \(e\), both running \(1 \dots D\) over the feature dimension. \(d\) is the index we’re differentiating with respect to, \(e\) is the dummy being summed. Code is PyTorch.</p> <p>Let’s get started.</p> <hr/> <h4 id="what-normalization-does"><strong>What normalization does</strong></h4> <p>Take a single token’s feature vector \(x \in \mathbb{R}^{D}\). Normalizing it means: subtract its mean, divide by its standard deviation, then apply a learned scale and shift.</p> \[x \;\longrightarrow\; \hat x = \frac{x - \mu}{s} \;\longrightarrow\; y = \gamma \odot \hat x + \beta\] <p>The middle step forces the vector to have mean 0 and variance 1. That’s the stabilizing part: whatever the previous layer did, the next layer sees something with known first and second moments, so activations can’t drift or blow up as they pass through a deep stack.</p> <p>The third step looks like it undoes the second, and in a sense it does. If the network wants a mean of 3 and a standard deviation of 7, it can set \(\beta_d = 3\) and \(\gamma_d = 7\). The difference is that those are now <em>learned parameters</em> rather than whatever happened to come out of the layer below. You remove two degrees of freedom from the data and hand them back as weights. \(\gamma\) initializes to 1 and \(\beta\) to 0, so the layer starts as pure normalization and learns to deviate if that helps.</p> <p>The important structural detail is <strong>which axis gets reduced</strong>. The mean and variance here are over \(D\), the feature dimension, computed independently for each token. Nothing is shared across tokens or across the batch. That’s what makes it usable in a transformer: training and inference behave identically, a batch of 1 works exactly like a batch of 512, and sequence length is irrelevant.</p> <hr/> <h4 id="layernorm-forward"><strong>LayerNorm forward</strong></h4> <p>Index form first, then packed, as usual.</p> \[\mu = \frac{1}{D}\sum_e x_e \tag{1}\] \[v = \frac{1}{D}\sum_e (x_e - \mu)^2 \tag{2}\] \[s = \sqrt{v + \varepsilon}, \qquad \hat x_d = \frac{x_d - \mu}{s} \tag{3}\] \[y_d = \gamma_d \hat x_d + \beta_d \tag{4}\] \[y = \gamma \odot \hat x + \beta \qquad (D \times 1)\] <p>\(\mu\), \(v\) and \(s\) are <strong>scalars</strong>, one per token, not vectors. \(x\), \(\hat x\), \(\gamma\), \(\beta\), \(y\) are all \(D \times 1\). The \(\varepsilon\) is there so that \(s\) can’t be zero when a token’s features are all identical; typical values are \(10^{-5}\) or \(10^{-6}\). It’s inside the square root, not added after, and that placement matters for the derivation below.</p> <p>Note that \(v\) uses the biased variance, dividing by \(D\) rather than \(D-1\). Every framework does this for normalization layers, and it’s what makes \(\sum_d \hat x_d^2 = D\) exactly when \(\varepsilon = 0\), an identity we’ll use.</p> <hr/> <h4 id="the-derivative-we-need"><strong>The derivative we need</strong></h4> <p>Every gradient below flows through \(\hat x\), so the object we need is \(\partial \hat x_d / \partial x_e\): a full \(D \times D\) Jacobian, because \(\mu\) and \(v\) depend on all of \(x\) and therefore every output component depends on every input component. Same situation as the softmax Jacobian in the MLP post.</p> <p>Build it from the pieces. From (1), each \(x_e\) contributes \(1/D\) to the mean:</p> \[\frac{\partial \mu}{\partial x_e} = \frac{1}{D} \tag{5}\] <p>For the variance, differentiate (2) remembering that \(\mu\) itself depends on \(x_e\):</p> \[\frac{\partial v}{\partial x_e} = \frac{2}{D}\sum_f (x_f - \mu)\left(\delta_{fe} - \frac{1}{D}\right) = \frac{2}{D}\left[(x_e - \mu) - \frac{1}{D}\sum_f (x_f - \mu)\right]\] <p>The second sum is zero, since deviations from the mean always sum to zero, which is worth noticing because it’s the reason the variance derivative comes out as clean as it does:</p> \[\frac{\partial v}{\partial x_e} = \frac{2}{D}(x_e - \mu) \tag{6}\] <p>Then \(s = \sqrt{v + \varepsilon}\), so \(\partial s / \partial v = 1/(2s)\), and:</p> \[\frac{\partial s}{\partial x_e} = \frac{1}{2s}\cdot\frac{2}{D}(x_e - \mu) = \frac{x_e - \mu}{D\,s} = \frac{\hat x_e}{D} \tag{7}\] <p>That is a genuinely nice result: the derivative of the standard deviation with respect to an input is just that input’s own normalized value, over \(D\).</p> <p>Now assemble, using the quotient rule on \(\hat x_d = (x_d - \mu)/s\) and remembering both \(\mu\) and \(s\) depend on \(x_e\):</p> \[\frac{\partial \hat x_d}{\partial x_e} = \frac{\left(\delta_{de} - \frac{1}{D}\right)s - (x_d - \mu)\frac{\hat x_e}{D}}{s^2} = \frac{1}{s}\left[\delta_{de} - \frac{1}{D} - \frac{\hat x_d \hat x_e}{D}\right] \tag{8}\] <p>Three terms, and they’ll stay three terms all the way to the end. The \(\delta_{de}\) is the direct path (changing \(x_d\) changes \(\hat x_d\)), the \(1/D\) is the path through the mean, and the \(\hat x_d \hat x_e / D\) is the path through the standard deviation.</p> <hr/> <h4 id="layernorm-backward"><strong>LayerNorm backward</strong></h4> <p>Let \(g_d = \partial L / \partial y_d\) be the gradient arriving from downstream.</p> <h5 id="the-parameters"><strong>The parameters</strong></h5> <p>From (4), \(y_d\) depends on \(\gamma_d\) and \(\beta_d\) only, so no sum survives:</p> \[\frac{\partial L}{\partial \gamma_d} = g_d \hat x_d, \qquad \frac{\partial L}{\partial \beta_d} = g_d \tag{9}\] \[\frac{\partial L}{\partial \gamma} = g \odot \hat x, \qquad \frac{\partial L}{\partial \beta} = g \qquad (D \times 1) \;\checkmark\] <h5 id="through-the-affine-into-the-normalized-vector"><strong>Through the affine, into the normalized vector</strong></h5> \[\hat g_d := \frac{\partial L}{\partial \hat x_d} = g_d \gamma_d \qquad\Longrightarrow\qquad \hat g = g \odot \gamma \tag{10}\] <p>This is the gradient we actually propagate. Everything below is in terms of \(\hat g\), not \(g\).</p> <h5 id="the-input"><strong>The input</strong></h5> <p>Contract \(\hat g\) with the Jacobian (8), summing over \(d\):</p> \[\frac{\partial L}{\partial x_e} = \sum_d \hat g_d \frac{\partial \hat x_d}{\partial x_e} = \frac{1}{s}\sum_d \hat g_d \left[\delta_{de} - \frac{1}{D} - \frac{\hat x_d \hat x_e}{D}\right]\] <p>Distribute the sum over the three terms. The delta picks out \(\hat g_e\); the second term gives the mean of \(\hat g\); the third gives \(\hat x_e\) times the mean of \(\hat g \odot \hat x\):</p> \[\frac{\partial L}{\partial x_e} = \frac{1}{s}\left[\hat g_e - \frac{1}{D}\sum_d \hat g_d - \hat x_e \cdot \frac{1}{D}\sum_d \hat g_d \hat x_d\right]\] <p>Name the two scalars, since they’re the only things that need reducing:</p> \[c_1 = \frac{1}{D}\sum_d \hat g_d, \qquad c_2 = \frac{1}{D}\sum_d \hat g_d \hat x_d \tag{11}\] \[\boxed{\;\frac{\partial L}{\partial x} = \frac{1}{s}\Big[\hat g - c_1 - c_2\,\hat x\Big]\;} \qquad (D \times 1) \;\checkmark \tag{12}\] <p>(\(c_1\) is a scalar being subtracted from every component, so read it as \(c_1 \mathbf{1}\) if you prefer to keep the shapes explicit.)</p> <h5 id="what-the-three-terms-actually-are"><strong>What the three terms actually are</strong></h5> <p>This is worth spelling out, because it turns a formula you’d have to memorize into one you can reconstruct.</p> <p>Look at \(c_1\) and \(c_2\) as projections. Let \(\mathbf{1}\) be the all-ones vector. Then \(\mathbf{1}\cdot\mathbf{1} = D\), and (at \(\varepsilon = 0\)) \(\hat x \cdot \hat x = D\) as well. So:</p> \[c_1 \mathbf{1} = \frac{\hat g \cdot \mathbf{1}}{\mathbf{1}\cdot\mathbf{1}}\,\mathbf{1} = \text{proj}_{\mathbf{1}}(\hat g), \qquad c_2 \hat x = \frac{\hat g \cdot \hat x}{\hat x \cdot \hat x}\,\hat x = \text{proj}_{\hat x}(\hat g)\] <p>So equation (12) says: <strong>take the incoming gradient, remove its component along \(\mathbf{1}\), remove its component along \(\hat x\), and divide by \(s\).</strong> It’s one step of Gram-Schmidt against two specific directions.</p> <p>And because \(\sum_d \hat x_d = 0\), those two directions are orthogonal to each other, so the projections are independent and can be removed in either order without correction terms. (I checked this numerically: \(\mathbf{1}\cdot\hat x\) comes out at \(10^{-16}\), and (12) agrees with explicit Gram-Schmidt to \(2 \times 10^{-16}\).)</p> <p>Which raises the obvious question: why <em>those</em> two directions?</p> <hr/> <h4 id="two-free-correctness-checks"><strong>Two free correctness checks</strong></h4> <p>Because those are exactly the directions LayerNorm ignores.</p> <p><strong>Shift.</strong> Adding a constant to every component of \(x\) changes nothing downstream. \(\mu\) shifts by the same constant, \(x - \mu\) is unchanged, \(v\) is unchanged, so \(y\) is unchanged:</p> \[\mathrm{LN}(x + a\mathbf{1}) = \mathrm{LN}(x) \quad \text{for any } a\] <p>If moving along \(\mathbf{1}\) can’t change the output, it can’t change the loss, so the directional derivative along \(\mathbf{1}\) must be zero:</p> \[\sum_d \frac{\partial L}{\partial x_d} = 0 \tag{13}\] <p><strong>Scale.</strong> Multiplying \(x\) by a positive constant also changes nothing, <em>provided</em> \(\varepsilon = 0\): \(\mu\) scales by \(c\), \(s\) scales by \(c\), and the ratio is unchanged. So the directional derivative along \(\hat x\) is zero too:</p> \[\sum_d \hat x_d \frac{\partial L}{\partial x_d} = 0 \tag{14}\] <p>Both fall straight out of (12): substituting gives \(\sum_d \hat g_d - Dc_1 - c_2\sum_d \hat x_d\), and \(\sum_d \hat g_d = Dc_1\) and \(\sum_d \hat x_d = 0\) kill it. Similarly for (14).</p> <p><strong>Two invariances, two subtracted terms.</strong> That’s the whole structure of the backward pass. A normalization layer throws away information, and the gradient must be blind in exactly the directions the forward pass was blind in.</p> <h5 id="the-varepsilon-caveat-stated-precisely"><strong>The \(\varepsilon\) caveat, stated precisely</strong></h5> <p>These two identities are not equally exact, and it’s worth being honest about which is which.</p> <p><strong>(13) is exact for any \(\varepsilon\).</strong> Shift invariance doesn’t involve \(s\) at all, so \(\varepsilon\) never enters. Numerically, \(\sum_d \partial L/\partial x_d\) comes out as exactly \(0\) in float64.</p> <p><strong>(14) is exact only at \(\varepsilon = 0\).</strong> With \(\varepsilon &gt; 0\), LayerNorm isn’t quite scale invariant, because \(s = \sqrt{c^2 v + \varepsilon}\) isn’t \(c\sqrt{v}\). The identity picks up a residual, and you can write it down in closed form. Since \(\sum_d \hat x_d^2 = D\,v/(v+\varepsilon)\) rather than \(D\):</p> \[\sum_d \hat x_d \frac{\partial L}{\partial x_d} = \frac{c_2 D}{s}\cdot\frac{\varepsilon}{v + \varepsilon}\] <p>I checked this against the actual gradients over 150 random trials at \(\varepsilon = 10^{-5}\), \(10^{-3}\) and \(10^{-1}\), and the formula holds to better than \(10^{-15}\) in the worst case. In practice \(\varepsilon \ll v\), so the residual lands around \(10^{-7}\) and (14) is a perfectly good check with a loose tolerance. Just don’t assert it to machine precision and then wonder why it fails.</p> <p>Used as tests, these are excellent: they need no reference implementation, no finite differences, and no autograd. If a hand-written norm backward is wrong, (13) almost always catches it immediately.</p> <hr/> <h4 id="rmsnorm"><strong>RMSNorm</strong></h4> <p>RMSNorm removes the mean subtraction. That’s the entire change.</p> \[r = \sqrt{\frac{1}{D}\sum_e x_e^2 + \varepsilon}, \qquad \hat x_d = \frac{x_d}{r}, \qquad y_d = \gamma_d \hat x_d \tag{15}\] <p>No \(\mu\), and conventionally no \(\beta\) either. It divides by the root mean square instead of the standard deviation, which are the same thing only when the mean is zero.</p> <p>The derivation runs identically. With \(m = \frac{1}{D}\sum_e x_e^2\) we get \(\partial m/\partial x_e = 2x_e/D\), so:</p> \[\frac{\partial r}{\partial x_e} = \frac{1}{2r}\cdot\frac{2x_e}{D} = \frac{x_e}{D\,r} = \frac{\hat x_e}{D}\] <p>the same clean form as (7). And the Jacobian:</p> \[\frac{\partial \hat x_d}{\partial x_e} = \frac{\delta_{de}}{r} - \frac{x_d}{r^2}\cdot\frac{\hat x_e}{D} = \frac{1}{r}\left[\delta_{de} - \frac{\hat x_d \hat x_e}{D}\right] \tag{16}\] <p>Compare with (8). The \(1/D\) term is gone, because there’s no mean for \(x_e\) to flow through. Contracting with \(\hat g\):</p> \[\boxed{\;\frac{\partial L}{\partial x} = \frac{1}{r}\Big[\hat g - c_2\,\hat x\Big]\;} \qquad \frac{\partial L}{\partial \gamma} = g \odot \hat x \tag{17}\] <p><strong>LayerNorm’s result with the \(c_1\) term deleted.</strong> Nothing else changes, and \(c_2\) is defined exactly as before.</p> <p>The invariance story predicts this. RMSNorm is still scale invariant, so it still has the \(\hat x\) projection. It is <em>not</em> shift invariant, since adding a constant changes \(\sum x_e^2\) and there’s no mean to absorb it. So it loses the \(\mathbf{1}\) projection, and with it \(c_1\). Numerically: \(\mathrm{RMS}(x + 5\mathbf{1})\) differs from \(\mathrm{RMS}(x)\) by \(2.4\), and correspondingly \(\sum_d \partial L/\partial x_d = -0.123\), nowhere near zero, while \(\sum_d \hat x_d\,\partial L/\partial x_d\) is zero to machine precision at \(\varepsilon = 0\).</p> <hr/> <h4 id="side-by-side"><strong>Side by side</strong></h4> <table> <thead> <tr> <th> </th> <th>LayerNorm</th> <th>RMSNorm</th> </tr> </thead> <tbody> <tr> <td>Statistic</td> <td>mean and variance</td> <td>mean square</td> </tr> <tr> <td>Parameters</td> <td>\(\gamma\), \(\beta\)</td> <td>\(\gamma\)</td> </tr> <tr> <td>Shift invariant</td> <td>yes, exactly</td> <td><strong>no</strong></td> </tr> <tr> <td>Scale invariant</td> <td>yes (at \(\varepsilon = 0\))</td> <td>yes (at \(\varepsilon = 0\))</td> </tr> <tr> <td>Forward reductions</td> <td>2 (\(\sum x\), \(\sum x^2\))</td> <td>1 (\(\sum x^2\))</td> </tr> <tr> <td>Backward reductions</td> <td>2 (\(c_1\), \(c_2\))</td> <td>1 (\(c_2\))</td> </tr> <tr> <td>Backward terms</td> <td>3</td> <td>2</td> </tr> </tbody> </table> <p>Every row of the bottom half follows from the top half. That’s the argument for RMSNorm in a sentence: dropping the mean buys you one fewer reduction in each direction, one fewer parameter tensor, and one fewer term in the backward pass, and the empirical finding is that the shift invariance wasn’t doing much work anyway.</p> <hr/> <h4 id="residual-connections-and-the-fork-rule"><strong>Residual connections, and the fork rule</strong></h4> <p>Norms in a transformer never appear alone; they come attached to a residual. And residuals introduce the one core backprop rule the MLP post never needed.</p> <p>In the MLP, every intermediate value was consumed exactly once. In a residual block, \(x\) is used <strong>twice</strong>: once by the identity path and once by the sublayer.</p> \[y = x + F(x) \tag{18}\] <p>The rule for that is not new machinery; it’s the same many-routes chain rule from the MLP post, with the routes now being different <em>consumers</em> rather than different components. If \(u\) feeds into several places, perturbing \(u\) perturbs all of them, and the effects add:</p> \[\boxed{\;\frac{\partial L}{\partial u} = \sum_{\text{uses of } u} \frac{\partial L}{\partial u}\bigg|_{\text{that use}}\;}\] <p>So for (18):</p> \[\frac{\partial L}{\partial x} = \underbrace{\frac{\partial L}{\partial y}}_{\text{identity path}} + \underbrace{\left(\frac{\partial F}{\partial x}\right)^{\!\top}\frac{\partial L}{\partial y}}_{\text{sublayer path}}\] <p>In code this is <code class="language-plaintext highlighter-rouge">+=</code> rather than <code class="language-plaintext highlighter-rouge">=</code>, and forgetting it is a silent, plausible-looking bug: you get a gradient, it has the right shape, it’s just wrong. (I checked: for a random \(y = x + \tanh(Ax)\), taking only the identity path is off by \(1.4\) in max absolute error, while the sum matches autograd to \(2 \times 10^{-16}\).)</p> <h5 id="pre-norm-and-post-norm"><strong>Pre-norm and post-norm</strong></h5> <p>Where the norm sits relative to the fork is the whole pre-norm versus post-norm question.</p> \[\text{post-norm:} \quad y = \mathrm{LN}\big(x + F(x)\big) \qquad\qquad \text{pre-norm:} \quad y = x + F\big(\mathrm{LN}(x)\big)\] <p>In pre-norm, the identity path is a bare \(+\). The gradient reaching \(x\) contains \(\partial L/\partial y\) <em>unmodified</em>, plus whatever comes through the sublayer. Stack a hundred blocks and there’s still a clean path from the loss to every layer, with nothing attenuating it. That’s the “gradient highway”.</p> <p>In post-norm, the norm sits <em>on</em> the residual path, so every gradient must pass through the norm’s Jacobian once per layer. And we know exactly what that Jacobian does: by (12), it strips out the components along \(\mathbf{1}\) and \(\hat x\) and rescales by \(1/s\). Doing that a hundred times in a row is why deep post-norm transformers need learning-rate warmup to train stably, and why essentially everything modern is pre-norm.</p> <hr/> <h4 id="the-code"><strong>The code</strong></h4> <p>Both norms, forward and backward by hand, checked against autograd. The two reductions are written with <code class="language-plaintext highlighter-rouge">einsum</code> because they’re genuine contractions and the strings map straight onto (11); the elementwise work stays as ordinary operators.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>

<span class="n">torch</span><span class="p">.</span><span class="nf">set_default_dtype</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="n">float64</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">layernorm_forward</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">beta</span><span class="p">,</span> <span class="n">eps</span><span class="o">=</span><span class="mf">1e-5</span><span class="p">):</span>
    <span class="n">mu</span>   <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">mean</span><span class="p">()</span>                                  <span class="c1"># eq (1), scalar
</span>    <span class="n">v</span>    <span class="o">=</span> <span class="p">((</span><span class="n">x</span> <span class="o">-</span> <span class="n">mu</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>                    <span class="c1"># eq (2), scalar
</span>    <span class="n">s</span>    <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">v</span> <span class="o">+</span> <span class="n">eps</span><span class="p">)</span>                       <span class="c1"># eq (3)
</span>    <span class="n">xhat</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="n">mu</span><span class="p">)</span> <span class="o">/</span> <span class="n">s</span>
    <span class="k">return</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">xhat</span> <span class="o">+</span> <span class="n">beta</span><span class="p">,</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">s</span>              <span class="c1"># eq (4)
</span>

<span class="k">def</span> <span class="nf">layernorm_backward</span><span class="p">(</span><span class="n">g</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">s</span><span class="p">):</span>
    <span class="n">D</span>    <span class="o">=</span> <span class="n">xhat</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">ghat</span> <span class="o">=</span> <span class="n">g</span> <span class="o">*</span> <span class="n">gamma</span>                                 <span class="c1"># eq (10)
</span>    <span class="n">c1</span>   <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">d-&gt;</span><span class="sh">"</span><span class="p">,</span> <span class="n">ghat</span><span class="p">)</span> <span class="o">/</span> <span class="n">D</span>             <span class="c1"># eq (11)
</span>    <span class="n">c2</span>   <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">d,d-&gt;</span><span class="sh">"</span><span class="p">,</span> <span class="n">ghat</span><span class="p">,</span> <span class="n">xhat</span><span class="p">)</span> <span class="o">/</span> <span class="n">D</span>     <span class="c1"># eq (11)
</span>    <span class="n">dx</span>   <span class="o">=</span> <span class="p">(</span><span class="n">ghat</span> <span class="o">-</span> <span class="n">c1</span> <span class="o">-</span> <span class="n">c2</span> <span class="o">*</span> <span class="n">xhat</span><span class="p">)</span> <span class="o">/</span> <span class="n">s</span>               <span class="c1"># eq (12)
</span>    <span class="k">return</span> <span class="n">dx</span><span class="p">,</span> <span class="n">g</span> <span class="o">*</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">g</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>                   <span class="c1"># dx, dgamma, dbeta  eq (9)
</span>

<span class="k">def</span> <span class="nf">rmsnorm_forward</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">eps</span><span class="o">=</span><span class="mf">1e-5</span><span class="p">):</span>
    <span class="n">r</span>    <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">((</span><span class="n">x</span> <span class="o">**</span> <span class="mi">2</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span> <span class="o">+</span> <span class="n">eps</span><span class="p">)</span>         <span class="c1"># eq (15)
</span>    <span class="n">xhat</span> <span class="o">=</span> <span class="n">x</span> <span class="o">/</span> <span class="n">r</span>
    <span class="k">return</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">r</span>


<span class="k">def</span> <span class="nf">rmsnorm_backward</span><span class="p">(</span><span class="n">g</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">r</span><span class="p">):</span>
    <span class="n">D</span>    <span class="o">=</span> <span class="n">xhat</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">ghat</span> <span class="o">=</span> <span class="n">g</span> <span class="o">*</span> <span class="n">gamma</span>
    <span class="n">c2</span>   <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">d,d-&gt;</span><span class="sh">"</span><span class="p">,</span> <span class="n">ghat</span><span class="p">,</span> <span class="n">xhat</span><span class="p">)</span> <span class="o">/</span> <span class="n">D</span>     <span class="c1"># the only reduction
</span>    <span class="n">dx</span>   <span class="o">=</span> <span class="p">(</span><span class="n">ghat</span> <span class="o">-</span> <span class="n">c2</span> <span class="o">*</span> <span class="n">xhat</span><span class="p">)</span> <span class="o">/</span> <span class="n">r</span>                    <span class="c1"># eq (17), no c1
</span>    <span class="k">return</span> <span class="n">dx</span><span class="p">,</span> <span class="n">g</span> <span class="o">*</span> <span class="n">xhat</span>                              <span class="c1"># dx, dgamma
</span></code></pre></div></div> <p>Checking the forward against the framework and the backward against autograd:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">torch</span><span class="p">.</span><span class="nf">manual_seed</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="n">D</span><span class="p">,</span> <span class="n">eps</span> <span class="o">=</span> <span class="mi">9</span><span class="p">,</span> <span class="mf">1e-5</span>
<span class="n">x</span>     <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">D</span><span class="p">)</span> <span class="o">*</span> <span class="mi">3</span> <span class="o">+</span> <span class="mi">2</span>                       <span class="c1"># deliberately not zero-mean
</span><span class="n">gamma</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>
<span class="n">beta</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>

<span class="n">y</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="nf">layernorm_forward</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">beta</span><span class="p">,</span> <span class="n">eps</span><span class="p">)</span>
<span class="n">ref</span>     <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="n">functional</span><span class="p">.</span><span class="nf">layer_norm</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="p">(</span><span class="n">D</span><span class="p">,),</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">beta</span><span class="p">,</span> <span class="n">eps</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">forward vs F.layer_norm: </span><span class="si">{</span><span class="p">(</span><span class="n">y</span> <span class="o">-</span> <span class="n">ref</span><span class="p">).</span><span class="nf">abs</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="n">xg</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">clone</span><span class="p">().</span><span class="nf">requires_grad_</span><span class="p">()</span>
<span class="n">gg</span> <span class="o">=</span> <span class="n">gamma</span><span class="p">.</span><span class="nf">clone</span><span class="p">().</span><span class="nf">requires_grad_</span><span class="p">()</span>
<span class="n">bg</span> <span class="o">=</span> <span class="n">beta</span><span class="p">.</span><span class="nf">clone</span><span class="p">().</span><span class="nf">requires_grad_</span><span class="p">()</span>
<span class="n">w</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>                                  <span class="c1"># arbitrary downstream
</span><span class="n">yg</span><span class="p">,</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">s</span> <span class="o">=</span> <span class="nf">layernorm_forward</span><span class="p">(</span><span class="n">xg</span><span class="p">,</span> <span class="n">gg</span><span class="p">,</span> <span class="n">bg</span><span class="p">,</span> <span class="n">eps</span><span class="p">)</span>
<span class="p">(</span><span class="n">w</span> <span class="o">*</span> <span class="n">yg</span><span class="p">).</span><span class="nf">sum</span><span class="p">().</span><span class="nf">backward</span><span class="p">()</span>                            <span class="c1"># so g = w
</span>
<span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="n">dx</span><span class="p">,</span> <span class="n">dgamma</span><span class="p">,</span> <span class="n">dbeta</span> <span class="o">=</span> <span class="nf">layernorm_backward</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">xhat</span><span class="p">,</span> <span class="n">s</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">dx     </span><span class="si">{</span><span class="p">(</span><span class="n">dx</span>     <span class="o">-</span> <span class="n">xg</span><span class="p">.</span><span class="n">grad</span><span class="p">).</span><span class="nf">abs</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">dgamma </span><span class="si">{</span><span class="p">(</span><span class="n">dgamma</span> <span class="o">-</span> <span class="n">gg</span><span class="p">.</span><span class="n">grad</span><span class="p">).</span><span class="nf">abs</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">dbeta  </span><span class="si">{</span><span class="p">(</span><span class="n">dbeta</span>  <span class="o">-</span> <span class="n">bg</span><span class="p">.</span><span class="n">grad</span><span class="p">).</span><span class="nf">abs</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">check (13) sum(dx)        = </span><span class="si">{</span><span class="n">dx</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span><span class="si">:</span><span class="o">+</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">check (14) sum(xhat * dx) = </span><span class="si">{</span><span class="p">(</span><span class="n">xhat</span> <span class="o">*</span> <span class="n">dx</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span><span class="si">:</span><span class="o">+</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>forward vs F.layer_norm: 2.22e-16
dx     5.55e-17
dgamma 0.00e+00
dbeta  0.00e+00
check (13) sum(dx)        = +0.00e+00
check (14) sum(xhat * dx) = +7.47e-07
</code></pre></div></div> <p>Exactly as predicted: (13) is identically zero, and (14) sits at \(7 \times 10^{-7}\), which is the \(\varepsilon\) residual and not an error. RMSNorm checks out the same way against <code class="language-plaintext highlighter-rouge">F.rms_norm</code>.</p> <hr/> <h4 id="the-token-dimension-in-one-index"><strong>The token dimension, in one index</strong></h4> <p>Real input is \(N\) tokens, \(X\) of shape \((N, D)\). Every statistic is per token, so all the reductions gain an \(n\) that is <strong>kept</strong>, while the parameter gradients gain an \(n\) that is <strong>summed away</strong>, because \(\gamma\) and \(\beta\) are shared across every token. This is the fork rule again: one parameter used \(N\) times receives \(N\) contributions, and they add.</p> <p><code class="language-plaintext highlighter-rouge">einsum</code> makes the distinction visible, since it’s entirely a question of which letters survive the arrow:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Ghat</span> <span class="o">=</span> <span class="n">G</span> <span class="o">*</span> <span class="n">gamma</span>                                     <span class="c1"># (N, D)
</span>
<span class="n">c1</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">nd-&gt;n</span><span class="sh">"</span><span class="p">,</span>    <span class="n">Ghat</span><span class="p">)</span>       <span class="o">/</span> <span class="n">D</span>        <span class="c1"># (N,)  n kept, d summed
</span><span class="n">c2</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">nd,nd-&gt;n</span><span class="sh">"</span><span class="p">,</span> <span class="n">Ghat</span><span class="p">,</span> <span class="n">Xhat</span><span class="p">)</span> <span class="o">/</span> <span class="n">D</span>        <span class="c1"># (N,)  n kept, d summed
</span>
<span class="n">dX</span> <span class="o">=</span> <span class="p">(</span><span class="n">Ghat</span> <span class="o">-</span> <span class="n">c1</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">-</span> <span class="n">c2</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">Xhat</span><span class="p">)</span> <span class="o">/</span> <span class="n">s</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span>   <span class="c1"># (N, D)
</span>
<span class="n">dgamma</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">nd,nd-&gt;d</span><span class="sh">"</span><span class="p">,</span> <span class="n">G</span><span class="p">,</span> <span class="n">Xhat</span><span class="p">)</span>           <span class="c1"># (D,)  n summed, d kept
</span><span class="n">dbeta</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">nd-&gt;d</span><span class="sh">"</span><span class="p">,</span>    <span class="n">G</span><span class="p">)</span>                 <span class="c1"># (D,)  n summed, d kept
</span></code></pre></div></div> <p>Read the four strings and the entire batching story is right there. <code class="language-plaintext highlighter-rouge">-&gt;n</code> for per-token statistics, <code class="language-plaintext highlighter-rouge">-&gt;d</code> for shared parameters, and the letter that disappears is the one being summed over. Verified against autograd at \(N = 7\), \(D = 11\): <code class="language-plaintext highlighter-rouge">dX</code> matches to \(1.1\times10^{-16}\), <code class="language-plaintext highlighter-rouge">dgamma</code> to \(2.2\times10^{-16}\), and <code class="language-plaintext highlighter-rouge">dbeta</code> exactly.</p> <hr/> <h4 id="a-note-on-kernels"><strong>A note on kernels</strong></h4> <p>Worth a paragraph, since this is where norms differ most from the MLP.</p> <p>A norm does \(O(D)\) arithmetic and moves \(O(D)\) bytes, so its arithmetic intensity is a small constant, independent of \(D\). A matmul’s grows with the tile size. Norms are therefore <strong>memory bound</strong>, always, and the only thing that matters for making them fast is how many times you touch memory. Written naively as separate framework ops, a LayerNorm forward is around five kernel launches, each streaming the whole tensor in and out; fused into one kernel it’s a single read and a single write. That’s the whole game, and it’s why every serious implementation ships a fused norm.</p> <p>Two consequences fall out of the derivation:</p> <p><strong>One pass suffices in each direction.</strong> Forward needs \(\sum_d x_d\) and \(\sum_d x_d^2\), and both can be accumulated in the same sweep, recovering the variance as \(\mathbb{E}[x^2] - \mathbb{E}[x]^2\) afterwards (or with Welford’s algorithm if you’re worried about cancellation). Backward needs \(c_1\) and \(c_2\) from (11), which are two reductions over the <em>same</em> two vectors, so they also fuse into one sweep. Then a second pass applies (12) elementwise.</p> <p><strong>What to save from the forward pass is a real trade-off.</strong> The backward needs \(\hat x\), \(s\) and \(\gamma\). You can store \(\hat x\), which costs \(D\) values per token, or store just \(\mu\) and the reciprocal \(1/s\) (two scalars per token) and recompute \(\hat x = (x - \mu)/s\) from the input you already have to read. Kernels overwhelmingly do the latter: it turns \(D\) values of activation memory per token into 2, at the cost of arithmetic that was free anyway because the kernel is memory bound. Storing the reciprocal rather than \(s\) also replaces a per-element divide with a multiply.</p> <p>RMSNorm’s advantage in this framing is concrete: one accumulator instead of two in the forward, one instead of two in the backward, one scalar of saved state per token instead of two, and no \(\beta\) gradient to reduce.</p> <hr/> <h4 id="wrapping-up"><strong>Wrapping up</strong></h4> <p>The mechanical part is the same as always. Write the Jacobian of the normalization in index form, contract the incoming gradient against it, and the terms sort themselves out. Equation (8) is the only real work in the post; everything after it is bookkeeping.</p> <p>The part worth remembering is the structure. A normalization layer is invariant to certain changes in its input, and its backward pass removes exactly those directions from the gradient. LayerNorm ignores shifts and scales, so it subtracts two projections. RMSNorm ignores only scales, so it subtracts one. If you remember that, you can reconstruct both formulas without looking them up, and you get two assertions you can drop into a test that need no reference implementation at all.</p> <p>And the fork rule, which we’ll lean on constantly from here: a value used more than once accumulates the gradients from each use. Attention is next, where \(x\) feeds Q, K and V simultaneously and that rule stops being a footnote.</p> <p>If you find a mistake anywhere in here, please let me know and I’ll fix it.</p>]]></content><author><name></name></author><category term="ML"/><summary type="html"><![CDATA[Deriving the normalization backward pass from first principles, why it has exactly three terms, and why RMSNorm has two]]></summary></entry><entry><title type="html">einsum - From Index Notation to Code</title><link href="https://monishver11.github.io/blog/2026/einsum/" rel="alternate" type="text/html" title="einsum - From Index Notation to Code"/><published>2026-08-23T14:00:00+00:00</published><updated>2026-08-23T14:00:00+00:00</updated><id>https://monishver11.github.io/blog/2026/einsum</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/einsum/"><![CDATA[<p>Hello all. This is a short one, and it’s really a follow-on to the <a href="/blog/2026/mlp-forward-backward/">MLP forward and backward pass post</a>. In that post every gradient was derived twice: once in index form, with explicit sums over explicit indices, and once in packed matrix form with transposes and outer products. The index form was there because it’s unambiguous and there’s nothing to get wrong; the packed form was there because that’s what you type into numpy.</p> <p>It turns out you never needed the second step. The index-form equations <em>are</em> the code, in a notation called <code class="language-plaintext highlighter-rouge">einsum</code>. The translation is mechanical, takes about ten seconds per equation, and removes every decision about which argument to transpose.</p> <p>This post covers the rule, the translation, the vocabulary, and the one property that makes it worth switching to. Everything from here on in this series will use it, so it’s worth the twenty minutes.</p> <p>The index letters are the same as in the MLP post: \(i\) for the input dimension, \(j\) for hidden, \(k\) for output. Code is PyTorch, though <code class="language-plaintext highlighter-rouge">np.einsum</code> takes byte-identical strings if you prefer numpy.</p> <p>Let’s get started.</p> <hr/> <h4 id="the-rule"><strong>The rule</strong></h4> <p>An <code class="language-plaintext highlighter-rouge">einsum</code> call takes a subscript string and some tensors:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">ji,i-&gt;j</span><span class="sh">"</span><span class="p">,</span> <span class="n">W</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>
</code></pre></div></div> <p>The string has two halves separated by <code class="language-plaintext highlighter-rouge">-&gt;</code>.</p> <p><strong>Left of the arrow:</strong> one group of letters per input tensor, comma separated. Each letter names one axis, in order. So <code class="language-plaintext highlighter-rouge">ji</code> says “<code class="language-plaintext highlighter-rouge">W</code> has two axes; call the first one <code class="language-plaintext highlighter-rouge">j</code> and the second one <code class="language-plaintext highlighter-rouge">i</code>”, and <code class="language-plaintext highlighter-rouge">i</code> says “<code class="language-plaintext highlighter-rouge">x</code> has one axis, and it’s the same <code class="language-plaintext highlighter-rouge">i</code>”.</p> <p><strong>Right of the arrow:</strong> the letters of the output, in the order you want them.</p> <p>And then two rules, which are the whole notation:</p> <ol> <li>A letter that appears on the left but <strong>not</strong> on the right is <strong>summed over</strong>. These are the contracted indices.</li> <li>A letter that appears on the right is <strong>kept</strong>. These are the free indices.</li> </ol> <p>So <code class="language-plaintext highlighter-rouge">"ji,i-&gt;j"</code> reads: <code class="language-plaintext highlighter-rouge">i</code> appears on the left but not the right, so sum over it; <code class="language-plaintext highlighter-rouge">j</code> survives to the output. Which is:</p> \[\text{out}_j = \sum_i W_{ji} \, x_i\] <p>That is the definition of a matrix-vector product, and it is also, letter for letter, equation (1) from the MLP post.</p> <p>The thing worth noticing is what you <em>don’t</em> write. There is no \(\sum\) symbol anywhere in the string. You never say “sum over \(i\)”; you say “the output has a \(j\) and no \(i\)”, and the summation is inferred from that. <strong>You specify the shape of the result, and the contraction follows.</strong> That inversion is the entire mental shift, and everything else is bookkeeping.</p> <h5 id="the-translation-recipe"><strong>The translation recipe</strong></h5> <p>Given any equation in index form:</p> <ol> <li>Delete the \(\sum\) signs.</li> <li>For each tensor on the right-hand side, write its indices in order. Comma separate them.</li> <li>Write <code class="language-plaintext highlighter-rouge">-&gt;</code>.</li> <li>Write the indices of the left-hand side.</li> </ol> <p>That’s it. Four steps, no thinking required, and no point at which you have to reason about transposes or argument order.</p> <hr/> <h4 id="reading-the-string-off-the-math"><strong>Reading the string off the math</strong></h4> <p>Let’s do it with the actual equations from the MLP post rather than toy ones.</p> <p><strong>Hidden pre-activation</strong>, equation (1):</p> \[(h_a)_j = \sum_i W_{ji} \, x_i + b_j\] <p>The sum has two tensors, \(W\) with indices \(j, i\) and \(x\) with index \(i\). The left-hand side has index \(j\). So:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ha</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">ji,i-&gt;j</span><span class="sh">"</span><span class="p">,</span> <span class="n">W</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span> <span class="o">+</span> <span class="n">b</span>
</code></pre></div></div> <p>The bias is added outside, since it isn’t part of the contraction.</p> <p><strong>Output pre-activation</strong>, equation (3), identical in shape:</p> \[(y_a)_k = \sum_j V_{kj} \, h_j + c_k \qquad\longrightarrow\qquad \texttt{"kj,j-&gt;k"}\] <p><strong>Output weight gradient</strong>, equation (15):</p> \[\frac{\partial L}{\partial V_{kj}} = (y_k - t_k) \, h_j\] <p>There is no sum here at all. Both indices survive to the left-hand side, so both appear in the output:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dLdV</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">k,j-&gt;kj</span><span class="sh">"</span><span class="p">,</span> <span class="n">dLdya</span><span class="p">,</span> <span class="n">h</span><span class="p">)</span>
</code></pre></div></div> <p>No letter is contracted, so nothing is summed, and what you get is an outer product. Worth pausing on: <code class="language-plaintext highlighter-rouge">einsum</code> didn’t need a different function for this. In numpy the packed version needed <code class="language-plaintext highlighter-rouge">np.outer</code>, a different name from the <code class="language-plaintext highlighter-rouge">@</code> used two lines earlier, because <code class="language-plaintext highlighter-rouge">@</code> between two 1-D arrays would have given a dot product instead. Here it’s the same notation with different letters.</p> <p><strong>Hidden activation gradient</strong>, equation (17), the one with the transpose:</p> \[\frac{\partial L}{\partial h_j} = \sum_k (y_k - t_k) \, V_{kj}\] <p>Two tensors: \(V\) with indices \(k, j\), and the incoming gradient with index \(k\). Output index \(j\), so \(k\) is summed:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dLdh</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">kj,k-&gt;j</span><span class="sh">"</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">dLdya</span><span class="p">)</span>
</code></pre></div></div> <p>No <code class="language-plaintext highlighter-rouge">.T</code> anywhere. We’ll come back to this one, because it’s the whole argument.</p> <p>Operand order is yours to pick, incidentally, as long as the string matches it. <code class="language-plaintext highlighter-rouge">torch.einsum("k,kj-&gt;j", dLdya, V)</code> is the identical computation with the arguments swapped. The only thing that has to line up is each letter group with the tensor it describes.</p> <p><strong>Input weight gradient</strong>, equation (19):</p> \[\frac{\partial L}{\partial W_{ji}} = \frac{\partial L}{\partial (h_a)_j} \, x_i \qquad\longrightarrow\qquad \texttt{"j,i-&gt;ji"}\] <p>Five equations, five strings, and at no point did we think about shapes, argument order, or transposes. We read letters off the page.</p> <hr/> <h4 id="the-vocabulary"><strong>The vocabulary</strong></h4> <p>Almost everything you need is one of these. Every string below is verified.</p> <table> <thead> <tr> <th>Operation</th> <th>Index form</th> <th><code class="language-plaintext highlighter-rouge">einsum</code></th> </tr> </thead> <tbody> <tr> <td>matrix times vector</td> <td>\(\sum_i A_{ji} u_i\)</td> <td><code class="language-plaintext highlighter-rouge">"ji,i-&gt;j"</code></td> </tr> <tr> <td>matrix times matrix</td> <td>\(\sum_j A_{ij} B_{jk}\)</td> <td><code class="language-plaintext highlighter-rouge">"ij,jk-&gt;ik"</code></td> </tr> <tr> <td>outer product</td> <td>\(u_i v_j\)</td> <td><code class="language-plaintext highlighter-rouge">"i,j-&gt;ij"</code></td> </tr> <tr> <td>elementwise product</td> <td>\(u_i v_i\)</td> <td><code class="language-plaintext highlighter-rouge">"i,i-&gt;i"</code></td> </tr> <tr> <td>dot product</td> <td>\(\sum_i u_i v_i\)</td> <td><code class="language-plaintext highlighter-rouge">"i,i-&gt;"</code></td> </tr> <tr> <td>transpose</td> <td>\(A_{ij}\)</td> <td><code class="language-plaintext highlighter-rouge">"ij-&gt;ji"</code></td> </tr> <tr> <td>sum everything</td> <td>\(\sum_{ij} A_{ij}\)</td> <td><code class="language-plaintext highlighter-rouge">"ij-&gt;"</code></td> </tr> <tr> <td>sum over columns</td> <td>\(\sum_j A_{ij}\)</td> <td><code class="language-plaintext highlighter-rouge">"ij-&gt;i"</code></td> </tr> <tr> <td>trace</td> <td>\(\sum_i A_{ii}\)</td> <td><code class="language-plaintext highlighter-rouge">"ii-&gt;"</code></td> </tr> <tr> <td>diagonal</td> <td>\(A_{ii}\)</td> <td><code class="language-plaintext highlighter-rouge">"ii-&gt;i"</code></td> </tr> <tr> <td>batched matmul</td> <td>\(\sum_j A_{nij} B_{njk}\)</td> <td><code class="language-plaintext highlighter-rouge">"nij,njk-&gt;nik"</code></td> </tr> </tbody> </table> <p>Two pairs in that table are worth staring at, because between them they teach the whole notation.</p> <p><strong><code class="language-plaintext highlighter-rouge">"i,i-&gt;i"</code> against <code class="language-plaintext highlighter-rouge">"i,i-&gt;"</code>.</strong> Identical inputs. One letter of difference in the output. The first keeps \(i\), so nothing is summed and you get an elementwise product, shape <code class="language-plaintext highlighter-rouge">(5,)</code>. The second drops \(i\), so it’s summed away and you get a dot product, shape <code class="language-plaintext highlighter-rouge">()</code>, a scalar. Two of the most commonly confused operations in array code, and the notation makes the difference visible rather than something you infer from which function name was called.</p> <p><strong><code class="language-plaintext highlighter-rouge">"ij-&gt;ji"</code> against <code class="language-plaintext highlighter-rouge">"ii-&gt;i"</code>.</strong> The first is a transpose: one tensor, no multiplication, just relabeling axes. The second has a letter repeated <em>within a single operand</em>, which means “walk the diagonal”, giving <code class="language-plaintext highlighter-rouge">(4,)</code> from a <code class="language-plaintext highlighter-rouge">(4,4)</code> matrix. A repeated letter across two operands aligns them for multiplication; a repeated letter inside one operand takes its diagonal. Different meanings, and the second one surprises people.</p> <hr/> <h4 id="einsum-never-transposes-for-you"><strong>einsum never transposes for you</strong></h4> <p>This is the property that makes the notation worth adopting, and it’s best framed as the question I had when I first used it.</p> <blockquote> <p>When computing \(\dfrac{\partial L}{\partial h} = V^\top \dfrac{\partial L}{\partial y_a}\) with <code class="language-plaintext highlighter-rouge">einsum</code>, does it transpose \(V\) implicitly? Should I write <code class="language-plaintext highlighter-rouge">"jk"</code> instead of <code class="language-plaintext highlighter-rouge">"kj"</code> to represent the transpose?</p> </blockquote> <p><strong>No, and no.</strong> <code class="language-plaintext highlighter-rouge">einsum</code> never transposes implicitly, and it never needs to.</p> <p>Here is why the question dissolves. A transpose in a matrix product is not really an operation you want; it’s a <em>workaround</em>. Matrix multiplication only ever contracts the second index of the left operand against the first index of the right one. When the index you actually want to sum over is in the wrong position, you transpose to move it there. The transpose is you rearranging the data to fit the notation’s fixed contraction rule.</p> <p><code class="language-plaintext highlighter-rouge">einsum</code> has no fixed contraction rule. You name the index you want summed. So there is nothing to work around.</p> <p>Concretely: \(V\) has shape \(N_o \times N_h\), so its entries are \(V_{kj}\). The derivation gave</p> \[\frac{\partial L}{\partial h_j} = \sum_k (y_k - t_k) \, V_{kj}\] <p>The sum runs over \(k\), so \(k\) is the letter absent from the output:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dLdh</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">kj,k-&gt;j</span><span class="sh">"</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">dLdya</span><span class="p">)</span>
</code></pre></div></div> <p>Writing <code class="language-plaintext highlighter-rouge">"jk"</code> for \(V\) would be a factual error about the data, not a way of expressing a transpose. It would claim \(V\)’s first axis is the hidden dimension, and \(V\)’s first axis is the output dimension. The subscripts describe the tensor’s <strong>actual layout</strong>; the output spec describes <strong>what you want</strong>. Given both, the contraction is determined, and getting a transpose “right” stops being a decision you can make wrongly.</p> <p>There’s a safety argument here too, and it’s the practical reason I switched. With <code class="language-plaintext highlighter-rouge">@</code>, a wrong transpose is often still a <em>shape-valid</em> operation. If \(N_h\) and \(N_o\) happen to be equal, or your matrices are square, or you’re in the batch dimension of something, <code class="language-plaintext highlighter-rouge">A @ B</code> and <code class="language-plaintext highlighter-rouge">A.T @ B</code> both run, both produce correctly-shaped output, and one of them is silently wrong. Your loss goes down more slowly than it should and nothing ever raises. With <code class="language-plaintext highlighter-rouge">einsum</code> the letters are written down next to each other, so the same mistake is either a shape error or plainly visible on the page.</p> <hr/> <h4 id="the-mlp-converted"><strong>The MLP, converted</strong></h4> <p>Here is the whole thing side by side. Left column is the packed numpy from the MLP post, right column is the same computation as <code class="language-plaintext highlighter-rouge">einsum</code>.</p> <table> <thead> <tr> <th>Step</th> <th>Packed</th> <th><code class="language-plaintext highlighter-rouge">einsum</code></th> </tr> </thead> <tbody> <tr> <td>\(h_a\)</td> <td><code class="language-plaintext highlighter-rouge">W @ x</code></td> <td><code class="language-plaintext highlighter-rouge">"ji,i-&gt;j"</code></td> </tr> <tr> <td>\(y_a\)</td> <td><code class="language-plaintext highlighter-rouge">V @ h</code></td> <td><code class="language-plaintext highlighter-rouge">"kj,j-&gt;k"</code></td> </tr> <tr> <td>\(\partial L/\partial V\)</td> <td><code class="language-plaintext highlighter-rouge">np.outer(dLdya, h)</code></td> <td><code class="language-plaintext highlighter-rouge">"k,j-&gt;kj"</code></td> </tr> <tr> <td>\(\partial L/\partial h\)</td> <td><code class="language-plaintext highlighter-rouge">V.T @ dLdya</code></td> <td><code class="language-plaintext highlighter-rouge">"kj,k-&gt;j"</code></td> </tr> <tr> <td>\(\partial L/\partial W\)</td> <td><code class="language-plaintext highlighter-rouge">np.outer(dLdha, x)</code></td> <td><code class="language-plaintext highlighter-rouge">"j,i-&gt;ji"</code></td> </tr> </tbody> </table> <p>Look at the left column. Three different spellings, <code class="language-plaintext highlighter-rouge">@</code>, <code class="language-plaintext highlighter-rouge">np.outer</code>, and <code class="language-plaintext highlighter-rouge">.T @</code>, for what is conceptually one operation: multiply some things together and sum over some indices. Which spelling you need depends on the ranks of the operands and on which axis you’re contracting, and you have to work that out each time. The right column is one spelling, and the letters are copied off the equation.</p> <p>The full version, with the hand-derived gradients checked against autograd:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>

<span class="n">torch</span><span class="p">.</span><span class="nf">manual_seed</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="n">torch</span><span class="p">.</span><span class="nf">set_default_dtype</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="n">float64</span><span class="p">)</span>

<span class="n">Ni</span><span class="p">,</span> <span class="n">Nh</span><span class="p">,</span> <span class="n">No</span> <span class="o">=</span> <span class="mi">784</span><span class="p">,</span> <span class="mi">500</span><span class="p">,</span> <span class="mi">10</span>

<span class="n">W</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">Nh</span><span class="p">,</span> <span class="n">Ni</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.01</span>
<span class="n">b</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">Nh</span><span class="p">)</span>
<span class="n">V</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">No</span><span class="p">,</span> <span class="n">Nh</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.01</span>
<span class="n">c</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">No</span><span class="p">)</span>
<span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">c</span><span class="p">):</span>
    <span class="n">p</span><span class="p">.</span><span class="nf">requires_grad_</span><span class="p">(</span><span class="bp">True</span><span class="p">)</span>

<span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">Ni</span><span class="p">)</span>                      <span class="c1"># one training sample
</span><span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">No</span><span class="p">);</span> <span class="n">t</span><span class="p">[</span><span class="mi">3</span><span class="p">]</span> <span class="o">=</span> <span class="mf">1.0</span>          <span class="c1"># one-hot target, class 3
</span>
<span class="c1"># ---- forward ----
</span><span class="n">ha</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">ji,i-&gt;j</span><span class="sh">"</span><span class="p">,</span> <span class="n">W</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span> <span class="o">+</span> <span class="n">b</span>   <span class="c1"># (Nh,)
</span><span class="n">h</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">ha</span><span class="p">)</span>                   <span class="c1"># (Nh,)
</span><span class="n">ya</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">kj,j-&gt;k</span><span class="sh">"</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">h</span><span class="p">)</span> <span class="o">+</span> <span class="n">c</span>   <span class="c1"># (No,)
</span><span class="n">y</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">ya</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>            <span class="c1"># (No,)
</span><span class="n">L</span>  <span class="o">=</span> <span class="o">-</span><span class="p">(</span><span class="n">t</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">y</span><span class="p">)).</span><span class="nf">sum</span><span class="p">()</span>           <span class="c1"># scalar
</span>
<span class="n">L</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>                             <span class="c1"># autograd, for comparison only
</span>
<span class="c1"># ---- backward, by hand ----
</span><span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="n">dLdya</span> <span class="o">=</span> <span class="n">y</span> <span class="o">-</span> <span class="n">t</span>                                  <span class="c1"># (No,)
</span>    <span class="n">dLdV</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">k,j-&gt;kj</span><span class="sh">"</span><span class="p">,</span> <span class="n">dLdya</span><span class="p">,</span> <span class="n">h</span><span class="p">)</span>      <span class="c1"># (No, Nh)
</span>    <span class="n">dLdc</span>  <span class="o">=</span> <span class="n">dLdya</span>                                  <span class="c1"># (No,)
</span>    <span class="n">dLdh</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">kj,k-&gt;j</span><span class="sh">"</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">dLdya</span><span class="p">)</span>      <span class="c1"># (Nh,)
</span>    <span class="n">dLdha</span> <span class="o">=</span> <span class="n">dLdh</span> <span class="o">*</span> <span class="n">h</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">h</span><span class="p">)</span>                     <span class="c1"># (Nh,)
</span>    <span class="n">dLdW</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">j,i-&gt;ji</span><span class="sh">"</span><span class="p">,</span> <span class="n">dLdha</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>      <span class="c1"># (Nh, Ni)
</span>    <span class="n">dLdb</span>  <span class="o">=</span> <span class="n">dLdha</span>                                  <span class="c1"># (Nh,)
</span>
<span class="k">for</span> <span class="n">name</span><span class="p">,</span> <span class="n">ours</span><span class="p">,</span> <span class="n">ref</span> <span class="ow">in</span> <span class="p">[(</span><span class="sh">"</span><span class="s">W</span><span class="sh">"</span><span class="p">,</span> <span class="n">dLdW</span><span class="p">,</span> <span class="n">W</span><span class="p">.</span><span class="n">grad</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">b</span><span class="sh">"</span><span class="p">,</span> <span class="n">dLdb</span><span class="p">,</span> <span class="n">b</span><span class="p">.</span><span class="n">grad</span><span class="p">),</span>
                        <span class="p">(</span><span class="sh">"</span><span class="s">V</span><span class="sh">"</span><span class="p">,</span> <span class="n">dLdV</span><span class="p">,</span> <span class="n">V</span><span class="p">.</span><span class="n">grad</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">c</span><span class="sh">"</span><span class="p">,</span> <span class="n">dLdc</span><span class="p">,</span> <span class="n">c</span><span class="p">.</span><span class="n">grad</span><span class="p">)]:</span>
    <span class="k">assert</span> <span class="n">ours</span><span class="p">.</span><span class="n">shape</span> <span class="o">==</span> <span class="n">ref</span><span class="p">.</span><span class="n">shape</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">dL/d</span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s">: max|ours - autograd| = </span><span class="sh">"</span>
          <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="p">(</span><span class="n">ours</span> <span class="o">-</span> <span class="n">ref</span><span class="p">).</span><span class="nf">abs</span><span class="p">().</span><span class="nf">max</span><span class="p">().</span><span class="nf">item</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dL/dW: max|ours - autograd| = 3.47e-18
dL/db: max|ours - autograd| = 8.67e-19
dL/dV: max|ours - autograd| = 0.00e+00
dL/dc: max|ours - autograd| = 0.00e+00
</code></pre></div></div> <p>Exact to float64 roundoff, and two of them bit-identical. Checking against autograd rather than finite differences is the better move when you have it: no \(\varepsilon\) to tune and no truncation error, so a real disagreement shows up as a large number instead of hiding behind approximation noise.</p> <p>Note that <code class="language-plaintext highlighter-rouge">dLdha = dLdh * h * (1 - h)</code> stayed as a plain <code class="language-plaintext highlighter-rouge">*</code>. It could be written <code class="language-plaintext highlighter-rouge">"j,j,j-&gt;j"</code>, but there’s no reason to; <code class="language-plaintext highlighter-rouge">einsum</code> is for contractions, and an elementwise product isn’t one. Use it where indices are being summed, and use ordinary operators where they aren’t.</p> <hr/> <h4 id="gotchas"><strong>Gotchas</strong></h4> <p>A few things that will bite once.</p> <p><strong>Always write the arrow.</strong> <code class="language-plaintext highlighter-rouge">einsum</code> has an implicit mode where you omit <code class="language-plaintext highlighter-rouge">-&gt;</code>, as in <code class="language-plaintext highlighter-rouge">"ij,jk"</code>. It then sums any repeated index and returns the free indices <em>in alphabetical order</em>. This is fine until the alphabetical order isn’t the order you wanted, at which point you get a silently transposed result. Writing the output explicitly costs four characters and removes the failure mode entirely.</p> <p><strong>A repeated letter within one operand is a diagonal.</strong> <code class="language-plaintext highlighter-rouge">"ii-&gt;i"</code> extracts the diagonal; it does not square anything. If you meant an elementwise square of a matrix, that’s <code class="language-plaintext highlighter-rouge">A * A</code>, not <code class="language-plaintext highlighter-rouge">einsum</code>.</p> <p><strong>Use <code class="language-plaintext highlighter-rouge">...</code> for leading dimensions you don’t care about.</strong> <code class="language-plaintext highlighter-rouge">"...ij,...jk-&gt;...ik"</code> is a matmul over the last two axes with any number of batch dimensions in front. Handy when you want one function to work for both batched and unbatched input.</p> <p><strong>Contraction order matters for three or more operands, and the default may not be smart.</strong> With two operands, <code class="language-plaintext highlighter-rouge">torch.einsum</code> lowers to <code class="language-plaintext highlighter-rouge">matmul</code> or <code class="language-plaintext highlighter-rouge">bmm</code> and you get BLAS speed. With three or more, the order in which you contract them can change the FLOP count by orders of magnitude. <code class="language-plaintext highlighter-rouge">torch.einsum</code> uses <code class="language-plaintext highlighter-rouge">opt_einsum</code>’s path optimization when it’s available; <code class="language-plaintext highlighter-rouge">np.einsum</code> does <strong>not</strong> optimize unless you pass <code class="language-plaintext highlighter-rouge">optimize=True</code>, and the naive path can be dramatically slower. If you’re contracting three or more tensors in a hot loop, check what path you’re getting.</p> <p><strong>It isn’t a universal tool.</strong> <code class="language-plaintext highlighter-rouge">einsum</code> expresses products and sums over aligned indices. It won’t broadcast mismatched dimensions, apply nonlinearities, or do anything with strided or windowed access. Reach for it for contractions and use normal operations for the rest.</p> <hr/> <h4 id="why-this-matters-more-as-the-tensors-get-bigger"><strong>Why this matters more as the tensors get bigger</strong></h4> <p>For a two-layer MLP, honestly, <code class="language-plaintext highlighter-rouge">@</code> was fine. The case for <code class="language-plaintext highlighter-rouge">einsum</code> gets much stronger the moment you have more than two indices, which is immediately once you reach attention.</p> <p>Attention has four: batch \(b\), head \(h\), sequence position, and head dimension \(d\). And the sequence dimension appears <em>twice</em>, once for the query position and once for the key position, which are different axes that happen to have the same length. Call them \(s\) and \(t\).</p> \[S_{bhst} = \sum_d Q_{bhsd} \, K_{bhtd} \qquad\qquad O_{bhsd} = \sum_t P_{bhst} \, V_{bhtd}\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">S</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">bhsd,bhtd-&gt;bhst</span><span class="sh">"</span><span class="p">,</span> <span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span>
<span class="n">P</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">S</span> <span class="o">/</span> <span class="n">D</span><span class="o">**</span><span class="mf">0.5</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
<span class="n">O</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">bhst,bhtd-&gt;bhsd</span><span class="sh">"</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">V</span><span class="p">)</span>
</code></pre></div></div> <p>The packed equivalents are <code class="language-plaintext highlighter-rouge">Q @ K.transpose(-2, -1)</code> and <code class="language-plaintext highlighter-rouge">P @ V</code>, which do work, but you have to hold in your head that the transpose is over the last two axes specifically, that the batch and head dimensions are broadcasting along, and which of the two sequence axes you’re contracting. In the <code class="language-plaintext highlighter-rouge">einsum</code> version, the fact that we sum over \(d\) for the scores and over \(t\) for the output is stated in the string. And critically, packed matrix notation has no way to <em>name</em> the two sequence axes differently, so it can’t tell you which one is which; <code class="language-plaintext highlighter-rouge">einsum</code> gives them separate letters and the ambiguity disappears.</p> <p>That is the real argument. <code class="language-plaintext highlighter-rouge">einsum</code> is not a shortcut for matrix multiply. It’s the notation that keeps working when the number of indices exceeds what matrix notation can address, and it lets you carry the derivation straight into the code without a translation step where errors get introduced.</p> <hr/> <h4 id="wrapping-up"><strong>Wrapping up</strong></h4> <p>The summary is short. Write your equation in index form. Delete the sums. List each tensor’s indices, comma separated, then an arrow, then the indices you want out. Letters that don’t survive to the output get summed.</p> <p>Doing that removes the packed-matrix step from the pipeline entirely, and with it every transpose decision, every choice between <code class="language-plaintext highlighter-rouge">@</code> and <code class="language-plaintext highlighter-rouge">outer</code>, and the class of bugs where a wrong transpose is still shape-valid and therefore silent.</p> <p>The rest of this series uses <code class="language-plaintext highlighter-rouge">einsum</code> throughout, starting with LayerNorm and RMSNorm next.</p> <p>If you find a mistake anywhere in here, please let me know and I’ll fix it.</p>]]></content><author><name></name></author><category term="ML"/><summary type="html"><![CDATA[The index-form equations you already wrote are einsum strings; the translation is mechanical, and it removes every transpose decision]]></summary></entry><entry><title type="html">A Simple MLP - Forward Pass, Backward Pass, and Every Derivative</title><link href="https://monishver11.github.io/blog/2026/mlp-forward-backward/" rel="alternate" type="text/html" title="A Simple MLP - Forward Pass, Backward Pass, and Every Derivative"/><published>2026-08-20T14:00:00+00:00</published><updated>2026-08-20T14:00:00+00:00</updated><id>https://monishver11.github.io/blog/2026/mlp-forward-backward</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/mlp-forward-backward/"><![CDATA[<p>Hello all. This post works through a two-layer MLP end to end: the forward pass, every derivative we need, the backward pass, and finally the working code. Nothing here is new, but I wanted one page where the math and the code line up exactly, with no hand-waving in between and with the shapes checked at every step.</p> <p>The reason I wrote this: I kept running into notes (including my own) where a symbol is a vector in one line and a scalar two lines later, where the same dot means matrix multiply in one equation and elementwise multiply in the next, and where a chain rule is written as a product of two things that cannot actually be multiplied. Each of those is small on its own, and together they are exactly what makes backprop feel like something you re-derive from scratch every time instead of something you know. So the first section here is spent entirely on notation, and the rest of the post never deviates from it.</p> <p>The plan:</p> <ul> <li>The network, and the notation and shape conventions we’ll hold to</li> <li>Forward pass</li> <li>The derivatives we need: Kronecker delta, softmax Jacobian, cross-entropy, softmax with cross-entropy together, sigmoid, and the linear layer</li> <li>Backward pass, one gradient at a time, with shapes checked</li> <li>The whole graph on one page, as a reference diagram</li> <li>The full runnable code, and a gradient check to prove the derivations are right</li> <li>Batching, which turns out to be one extra index</li> </ul> <p>Let’s get started.</p> <hr/> <h4 id="the-network"><strong>The network</strong></h4> <p>We’ll use a two-layer MLP with a sigmoid hidden layer and a softmax output, trained with cross-entropy loss on a single training sample:</p> \[h = \sigma(W x + b), \qquad y = \mathrm{softmax}(V h + c)\] <p>Splitting out the pre-activations, because we need them by name in the backward pass:</p> \[\begin{aligned} h_a &amp;= W x + b \\ h &amp;= \sigma(h_a) \\ y_a &amp;= V h + c \\ y &amp;= \mathrm{softmax}(y_a) \\ L &amp;= -\sum_k t_k \log y_k \end{aligned}\] <p>That’s the whole model. Everything below is bookkeeping on top of these five lines.</p> <h5 id="notation"><strong>Notation</strong></h5> <p>Three dimensions, and one index letter for each:</p> <table> <thead> <tr> <th>Dimension</th> <th>Size</th> <th>Index</th> <th>Range</th> </tr> </thead> <tbody> <tr> <td>Input</td> <td>\(N_i\)</td> <td>\(i\)</td> <td>\(1 \dots N_i\)</td> </tr> <tr> <td>Hidden</td> <td>\(N_h\)</td> <td>\(j\)</td> <td>\(1 \dots N_h\)</td> </tr> <tr> <td>Output</td> <td>\(N_o\)</td> <td>\(k\)</td> <td>\(1 \dots N_o\)</td> </tr> </tbody> </table> <p>A note on why \(j\) and not \(h\) for the hidden index, since \(h\) is the more natural letter: \(h\) is already taken by the hidden activation vector. If we also used it as an index we’d be writing \(h_h\), and the letter would mean the hidden dimension in one position and the hidden activation in another. That collision is one of the things this post is trying to remove, so the index letters are \(i\), \(j\), \(k\) and they mean nothing else anywhere in the post.</p> <p>Every quantity in the model, with its type and shape:</p> <table> <thead> <tr> <th>Symbol</th> <th>Type</th> <th>Shape</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td>\(x\)</td> <td>vector</td> <td>\(N_i \times 1\)</td> <td>input, one training sample</td> </tr> <tr> <td>\(W\)</td> <td>matrix</td> <td>\(N_h \times N_i\)</td> <td>first layer weights</td> </tr> <tr> <td>\(b\)</td> <td>vector</td> <td>\(N_h \times 1\)</td> <td>first layer bias</td> </tr> <tr> <td>\(h_a\)</td> <td>vector</td> <td>\(N_h \times 1\)</td> <td>hidden pre-activation</td> </tr> <tr> <td>\(h\)</td> <td>vector</td> <td>\(N_h \times 1\)</td> <td>hidden activation</td> </tr> <tr> <td>\(V\)</td> <td>matrix</td> <td>\(N_o \times N_h\)</td> <td>second layer weights</td> </tr> <tr> <td>\(c\)</td> <td>vector</td> <td>\(N_o \times 1\)</td> <td>second layer bias</td> </tr> <tr> <td>\(y_a\)</td> <td>vector</td> <td>\(N_o \times 1\)</td> <td>output pre-activation, the logits</td> </tr> <tr> <td>\(y\)</td> <td>vector</td> <td>\(N_o \times 1\)</td> <td>predicted class probabilities</td> </tr> <tr> <td>\(t\)</td> <td>vector</td> <td>\(N_o \times 1\)</td> <td>one-hot target</td> </tr> <tr> <td>\(L\)</td> <td><strong>scalar</strong></td> <td>\(1\)</td> <td>cross-entropy loss</td> </tr> </tbody> </table> <h5 id="four-conventions"><strong>Four conventions</strong></h5> <p>These four rules are the whole point of this section. They hold for the rest of the post without exception.</p> <p><strong>1. A subscripted symbol is a scalar; a bare symbol is the whole array.</strong></p> <p>\(x_i\) is a single number, the \(i\)-th component of \(x\). \(x\) is the full \(N_i \times 1\) vector. \(W_{ji}\) is one entry of \(W\); \(W\) is the full matrix. For the pre-activations, whose names already carry a subscript, we write the component with parentheses: \((h_a)_j\) is a scalar, \(h_a\) is the vector. This is why the loss is written \(L = -\sum_k t_k \log y_k\) and never \(-t \log y\); the first is a scalar built by summing \(N_o\) scalars, the second is a vector, and they are not the same object.</p> <p><strong>2. Every vector is a column vector.</strong></p> <p>Shapes are always written as (rows \(\times\) columns). So \(N_i \times 1\) means \(N_i\) rows and a single column: tall and thin, a <strong>column</strong> vector. A row vector would be \(1 \times N_i\), a single row with \(N_i\) columns, short and wide. Worth being explicit about this because the two look similar written down and behave completely differently under multiplication.</p> <p>So \(x\) is \(N_i \times 1\), and \(x^\top\) is the \(1 \times N_i\) row version of it. This convention is what makes \(W x\) shape-check as \((N_h \times N_i)(N_i \times 1) = (N_h \times 1)\), and it’s what makes \(g h^\top\) an outer product of shape \((N_o \times 1)(1 \times N_h) = (N_o \times N_h)\). In code the arrays are 1-D, <code class="language-plaintext highlighter-rouge">(Ni,)</code> rather than <code class="language-plaintext highlighter-rouge">(Ni, 1)</code>, and the transposes largely disappear, because numpy decides orientation from context rather than from a stored shape. We’ll map the math onto the code explicitly when we get there, so the gap never has to be guessed at.</p> <p><strong>3. Three distinct multiplication symbols, never overloaded.</strong></p> <table> <thead> <tr> <th>Symbol</th> <th>Operation</th> <th>Example</th> </tr> </thead> <tbody> <tr> <td>juxtaposition</td> <td>matrix multiply</td> <td>\(W x\), \(V^\top g\), \(g h^\top\)</td> </tr> <tr> <td>\(\odot\)</td> <td>elementwise (Hadamard) product</td> <td>\(u \odot v\), same shape in, same shape out</td> </tr> <tr> <td>\(\oslash\)</td> <td>elementwise division</td> <td>\(t \oslash y\)</td> </tr> </tbody> </table> <p>A plain \(\cdot\) never appears as a multiplication in this post. It’s the easiest symbol in the world to overload; one dot can end up meaning matrix multiply on one line, elementwise multiply on the next, and “multiply by the scalar 1” on the one after that, and once that happens you can no longer tell from the page whether a gradient needs a transpose.</p> <p><strong>4. A gradient has the same shape as the thing it’s taken with respect to.</strong></p> <p>\(L\) is a scalar, so for any quantity \(u\) in the network, \(\dfrac{\partial L}{\partial u}\) has exactly the shape of \(u\):</p> \[\frac{\partial L}{\partial W} \in \mathbb{R}^{N_h \times N_i}, \qquad \frac{\partial L}{\partial h} \in \mathbb{R}^{N_h \times 1}, \qquad \frac{\partial L}{\partial c} \in \mathbb{R}^{N_o \times 1}\] <p>This has to be true for gradient descent to work at all; you subtract \(\eta \, \partial L / \partial W\) from \(W\), so the two must be the same shape. It’s also the single most useful check while deriving: if a step produces something of the wrong shape, the step is wrong. In the backward pass below, every line ends with this check.</p> <h5 id="the-chain-rule-on-scalars"><strong>The chain rule, on scalars</strong></h5> <p>Every gradient in this post comes from one rule, so it’s worth deriving it once rather than pattern-matching it later.</p> <p>The single-variable chain rule is the familiar one: if \(L\) depends on \(u\), and \(u\) depends on \(\theta\), then</p> \[\frac{\partial L}{\partial \theta} = \frac{\partial L}{\partial u} \frac{\partial u}{\partial \theta}\] <p>The situation we actually have is different in one way: \(\theta\) doesn’t reach \(L\) through a single intermediate value, it reaches it through <em>many</em> at once. A weight \(V_{kj}\) feeds into the vector \(y_a\), which has \(N_o\) components, and the loss sees all of them. So we need the version of the rule that handles many intermediates.</p> <p>Say \(L\) depends on scalars \(u_1, u_2, \dots, u_m\), and each \(u_p\) depends on \(\theta\). Nudge \(\theta\) by a small amount \(\varepsilon\). Every intermediate responds:</p> \[\Delta u_p \approx \frac{\partial u_p}{\partial \theta} \, \varepsilon\] <p>and \(L\) feels <em>all</em> of those shifts simultaneously. To first order, the change in \(L\) is the sum of what each shifted intermediate contributes:</p> \[\Delta L \approx \sum_p \frac{\partial L}{\partial u_p} \, \Delta u_p = \sum_p \frac{\partial L}{\partial u_p} \frac{\partial u_p}{\partial \theta} \, \varepsilon\] <p>Divide through by \(\varepsilon\) and let it go to zero:</p> \[\boxed{\;\frac{\partial L}{\partial \theta} = \sum_p \frac{\partial L}{\partial u_p} \frac{\partial u_p}{\partial \theta}\;}\] <p>That sum is the whole idea. \(\theta\) has \(m\) separate routes to reach \(L\), one through each \(u_p\), and they all fire at the same time; the total effect is the sum of the individual effects, not any single one of them. Everything in the backward pass is this rule applied over and over, with \(\theta\) being whatever we’re differentiating with respect to and \(u\) being whatever sits immediately in front of it in the graph.</p> <p>Two practical notes on using it.</p> <p><strong>Write the full sum, then let the algebra prune it.</strong> For the output weights, \(\theta = V_{kj}\) and the intermediates are the \(N_o\) components of \(y_a\):</p> \[\frac{\partial L}{\partial V_{kj}} = \sum_{k'} \frac{\partial L}{\partial (y_a)_{k'}} \frac{\partial (y_a)_{k'}}{\partial V_{kj}}\] <p>In this particular case most of those routes are dead: \(V_{kj}\) only ever appears in the expression for \((y_a)_k\), so the factor \(\partial (y_a)_{k'} / \partial V_{kj}\) is zero unless \(k' = k\), and a Kronecker delta will collapse the sum down to a single term. But we write the full sum first and let that fall out, rather than deciding in advance which routes matter. Sometimes they all matter: for \(\partial L / \partial (y_a)_l\) the intermediates are the softmax outputs \(y_k\), and because softmax couples every output to every input, all \(N_o\) routes are live and none of them drop.</p> <p><strong>The summed index must be a fresh letter.</strong> Above, the target index is \(k\) and the summed index is \(k'\). They range over the same dimension but they are not the same thing; \(k\) is fixed by what we’re differentiating, \(k'\) is the dummy variable being summed away. Reusing \(k\) for both is the fastest way to produce nonsense.</p> <h5 id="one-form-ill-avoid"><strong>One form I’ll avoid</strong></h5> <p>It’s tempting to write the chain rule for a weight matrix directly in packed form:</p> \[\frac{\partial L}{\partial V} = \frac{\partial L}{\partial y_a} \frac{\partial y_a}{\partial V}\] <p>It looks like the scalar rule with the sum hidden inside a matrix product, but it isn’t right, and it’s worth saying why. \(\partial y_a / \partial V\) is the derivative of an \(N_o\)-vector with respect to an \(N_o \times N_h\) matrix, so it’s a rank-3 object of shape \(N_o \times N_o \times N_h\). There’s no matrix product between an \(N_o \times 1\) vector and a rank-3 tensor, so the expression doesn’t typecheck; you can only get the right answer out of it by already knowing the right answer and inserting a transpose to make the shapes line up.</p> <p>So we do it the other way around: derive in index form using the scalar rule above, where every factor is a plain number and there’s nothing to get wrong, then read off the packed matrix form at the end and confirm it with the shape check from convention 4. This costs one extra line per gradient and removes all of the guesswork. The index form is also the version that survives contact with harder cases: once a quantity has more than two indices, packed matrix notation runs out of room and index notation just keeps working.</p> <hr/> <h4 id="forward-pass"><strong>Forward pass</strong></h4> <p>Each line below is given in index form first, then in packed form with the shape check.</p> <p><strong>Hidden pre-activation</strong></p> \[(h_a)_j = \sum_{i} W_{ji} \, x_i + b_j \tag{1}\] \[h_a = W x + b \qquad (N_h \times N_i)(N_i \times 1) + (N_h \times 1) = (N_h \times 1)\] <p><strong>Hidden activation</strong></p> <p>Sigmoid, applied elementwise:</p> \[\sigma(z) = \frac{1}{1 + e^{-z}}, \qquad h_j = \sigma\big((h_a)_j\big) \tag{2}\] \[h = \sigma(h_a) \qquad (N_h \times 1)\] <p><strong>Output pre-activation</strong></p> \[(y_a)_k = \sum_{j} V_{kj} \, h_j + c_k \tag{3}\] \[y_a = V h + c \qquad (N_o \times N_h)(N_h \times 1) + (N_o \times 1) = (N_o \times 1)\] <p><strong>Output activation</strong></p> <p>Softmax, which unlike sigmoid is <em>not</em> elementwise; every output component depends on every input component through the shared denominator:</p> \[y_k = \frac{e^{(y_a)_k}}{\sum_{k'} e^{(y_a)_{k'}}} \tag{4}\] \[y = \mathrm{softmax}(y_a) \qquad (N_o \times 1)\] <p><strong>Loss</strong></p> \[L = -\sum_k t_k \log y_k \tag{5}\] <p>\(L\) is a <strong>scalar</strong>. Since \(t\) is one-hot, with its single 1 at index \(k^\ast\), the sum collapses to \(L = -\log y_{k^\ast}\); every other term is multiplied by zero. Writing it as a sum is still the right thing to do, because the derivative is cleaner that way and because it generalizes to soft targets, but it’s worth knowing that only one term ever survives.</p> <hr/> <h4 id="the-derivatives-we-need"><strong>The derivatives we need</strong></h4> <p>Before the backward pass, we derive the five pieces it’s built from. Each of these is standalone, so you can skip to the backward pass and come back when a step references one.</p> <h5 id="kronecker-delta"><strong>Kronecker delta</strong></h5> <p>This shows up in every derivation below, so it goes first. The Kronecker delta is:</p> \[\delta_{pq} = \begin{cases} 1, &amp; p = q \\ 0, &amp; p \ne q \end{cases}\] <p>It’s an index selector. Inside a sum it picks out one term and kills the rest:</p> \[\sum_p a_p \, \delta_{pq} = a_q\] <p>It appears in derivatives whenever a variable affects only its own component. For example, in \(\frac{\partial}{\partial z_q} e^{z_p}\), the exponential \(e^{z_p}\) depends on \(z_q\) only when \(p = q\), so:</p> \[\frac{\partial e^{z_p}}{\partial z_q} = e^{z_p} \delta_{pq}\] <p>Whenever you see a \(\delta\) appear, that’s what it’s saying: this term only contributes when the indices match.</p> <h5 id="softmax-jacobian"><strong>Softmax Jacobian</strong></h5> <p>Softmax maps an \(N_o\)-vector to an \(N_o\)-vector, so its derivative is a full \(N_o \times N_o\) Jacobian (the table of every output’s partial derivative with respect to every input; entry \((k, l)\) answers “if I nudge input \(l\), how much does output \(k\) move?”), not a single number per component. We want \(\dfrac{\partial y_k}{\partial (y_a)_l}\).</p> <p>Start from the definition, writing the denominator as \(S\) to keep things readable:</p> \[y_k = \frac{e^{(y_a)_k}}{S}, \qquad S = \sum_{k'} e^{(y_a)_{k'}}\] <p>Differentiate with respect to \((y_a)_l\) using the quotient rule:</p> \[\frac{\partial y_k}{\partial (y_a)_l} = \frac{S \, \dfrac{\partial}{\partial (y_a)_l} e^{(y_a)_k} \;-\; e^{(y_a)_k} \, \dfrac{\partial S}{\partial (y_a)_l}}{S^2}\] <p>The two derivatives we need:</p> \[\frac{\partial}{\partial (y_a)_l} e^{(y_a)_k} = e^{(y_a)_k} \delta_{kl}, \qquad \frac{\partial S}{\partial (y_a)_l} = e^{(y_a)_l}\] <p>The second one has no delta because \(S\) sums over all components, so it depends on every \((y_a)_l\). Substituting:</p> \[\frac{\partial y_k}{\partial (y_a)_l} = \frac{S \, e^{(y_a)_k} \delta_{kl} - e^{(y_a)_k} e^{(y_a)_l}}{S^2} = \frac{e^{(y_a)_k}}{S} \left( \delta_{kl} - \frac{e^{(y_a)_l}}{S} \right)\] <p>Both fractions are just softmax outputs again, \(y_k\) and \(y_l\), which gives the final form:</p> \[\boxed{\;\frac{\partial y_k}{\partial (y_a)_l} = y_k \left( \delta_{kl} - y_l \right)\;} \tag{6}\] <p>Packed as a matrix, this is:</p> \[J = \mathrm{diag}(y) - y \, y^\top \qquad (N_o \times N_o)\] <p>The \(\mathrm{diag}(y)\) term is the \(\delta_{kl}\) part, the outer product \(y y^\top\) is the \(-y_k y_l\) part. The off-diagonal entries are nonzero, which is the formal statement that softmax is not elementwise: pushing up one logit pushes down every other probability.</p> <h5 id="cross-entropy-with-respect-to-the-probabilities"><strong>Cross-entropy, with respect to the probabilities</strong></h5> \[L = -\sum_{k'} t_{k'} \log y_{k'}\] <p>Differentiating with respect to a single \(y_k\), only the \(k' = k\) term survives:</p> \[\boxed{\;\frac{\partial L}{\partial y_k} = -\frac{t_k}{y_k}\;} \tag{7}\] <p>Packed, this is \(-\,t \oslash y\), an <strong>elementwise</strong> division of two \(N_o \times 1\) vectors giving an \(N_o \times 1\) vector. It is not a vector divided by a vector in any other sense; there’s no such operation. This is one of the places where the packed form is genuinely less clear than the index form, so it’s worth keeping (7) in mind rather than the packed version.</p> <h5 id="softmax-and-cross-entropy-together"><strong>Softmax and cross-entropy together</strong></h5> <p>Neither (6) nor (7) is very pleasant on its own, but composed they collapse to something remarkably clean. This is the single most useful result in the post.</p> <p>We want \(\dfrac{\partial L}{\partial (y_a)_l}\). The loss depends on \((y_a)_l\) only through the probabilities \(y\), and it depends on <em>all</em> of them, so the chain rule sums over \(k\):</p> \[\frac{\partial L}{\partial (y_a)_l} = \sum_k \frac{\partial L}{\partial y_k} \frac{\partial y_k}{\partial (y_a)_l}\] <p>Substitute (7) and (6):</p> \[= \sum_k \left( -\frac{t_k}{y_k} \right) y_k \left( \delta_{kl} - y_l \right)\] <p>The \(y_k\) cancels, which is where all the simplification comes from:</p> \[= -\sum_k t_k \left( \delta_{kl} - y_l \right) = -\sum_k t_k \delta_{kl} + y_l \sum_k t_k\] <p>\(y_l\) comes out of the second sum because it doesn’t depend on \(k\). Now apply the two facts we have: the delta selects \(t_l\), and the one-hot target sums to 1.</p> \[\sum_k t_k \delta_{kl} = t_l, \qquad \sum_k t_k = 1\] <p>Giving:</p> \[\boxed{\;\frac{\partial L}{\partial (y_a)_l} = y_l - t_l\;} \tag{8}\] \[\frac{\partial L}{\partial y_a} = y - t \qquad (N_o \times 1)\] <p>Predicted minus target. The Jacobian, the reciprocals, the deltas all cancel out. Worth noting <em>why</em> this happens: it’s not a coincidence of softmax and cross-entropy specifically, it’s what you get whenever the output activation is the canonical link for the loss (canonical link: the activation and the loss are a matched pair drawn from the same probability model, so the activation’s Jacobian is precisely the thing the loss’s derivative was built to cancel against). Softmax with cross-entropy is the multinomial pair. Sigmoid with binary cross-entropy is the Bernoulli pair, and gives the same \(y - t\). A linear output with squared error \(L = \tfrac{1}{2}\sum_k (y_k - t_k)^2\) is the Gaussian pair, and also gives \(y - t\).</p> <p>This is also why frameworks fuse the two into a single <code class="language-plaintext highlighter-rouge">softmax_cross_entropy</code> op. Computing them separately means building the \(N_o \times N_o\) Jacobian and contracting it, when the answer is a single subtraction; the fused version is faster and numerically better behaved, since the \(1/y_k\) in (7) blows up for confidently wrong predictions while \(y_l - t_l\) is bounded in \([-1, 1]\).</p> <h5 id="sigmoid-derivative"><strong>Sigmoid derivative</strong></h5> \[\sigma(z) = \frac{1}{1 + e^{-z}}\] <p>Differentiating:</p> \[\frac{d\sigma}{dz} = \frac{d}{dz}\left(1 + e^{-z}\right)^{-1} = \frac{e^{-z}}{(1 + e^{-z})^2}\] <p>To rewrite this in terms of \(\sigma\) itself, note that:</p> \[1 - \sigma(z) = 1 - \frac{1}{1+e^{-z}} = \frac{e^{-z}}{1 + e^{-z}}\] <p>So the numerator and one factor of the denominator together are exactly \(1 - \sigma(z)\), and what’s left is \(\sigma(z)\):</p> \[\boxed{\;\frac{d\sigma}{dz} = \sigma(z)\big(1 - \sigma(z)\big)\;} \tag{9}\] <p>This is the form worth remembering, because it means the backward pass needs no new computation: we already have \(h = \sigma(h_a)\) saved from the forward pass, so the derivative is just \(h \odot (1 - h)\), with no exponentials evaluated a second time.</p> <p>As a Jacobian, sigmoid is applied independently per component, so \(h_j\) depends only on \((h_a)_j\):</p> \[\frac{\partial h_j}{\partial (h_a)_{j'}} = h_j (1 - h_j) \, \delta_{j j'} \tag{10}\] <p>The Jacobian is diagonal. That’s the precise reason sigmoid backward is an elementwise <code class="language-plaintext highlighter-rouge">*</code> in code while softmax backward is not: contracting with a diagonal matrix is the same as an elementwise product, so the sum over \(j'\) collapses to a single term.</p> <h5 id="the-linear-layer"><strong>The linear layer</strong></h5> <p>Both layers of the network are affine maps, so deriving this once covers \(W, b\) and \(V, c\) both. Take a generic layer:</p> \[u = A z + d, \qquad u_p = \sum_q A_{pq} z_q + d_p\] <p>with \(A \in \mathbb{R}^{m \times n}\), \(z \in \mathbb{R}^{n \times 1}\), and \(d, u \in \mathbb{R}^{m \times 1}\). Suppose the gradient arriving from downstream is:</p> \[g_p = \frac{\partial L}{\partial u_p}, \qquad g \in \mathbb{R}^{m \times 1}\] <p>The three local derivatives, straight from the component form:</p> \[\frac{\partial u_p}{\partial A_{rs}} = \delta_{pr} z_s, \qquad \frac{\partial u_p}{\partial z_q} = A_{pq}, \qquad \frac{\partial u_p}{\partial d_r} = \delta_{pr}\] <p>The first one deserves a second look: \(u_p\) is a sum over \(q\) of \(A_{pq} z_q\), so the entry \(A_{rs}\) appears in that sum only if \(p = r\) (right output component), and when it does its coefficient is \(z_s\). That’s exactly \(\delta_{pr} z_s\).</p> <p>Now chain each one, summing over the downstream index \(p\) (downstream = the side closer to the loss; \(p\) indexes this layer’s output \(u\), which is the direction the gradient \(g\) is arriving from, so \(p\) is the index we sum away).</p> <p><strong>Weights.</strong></p> \[\frac{\partial L}{\partial A_{rs}} = \sum_p g_p \, \delta_{pr} z_s = g_r z_s \qquad\Longrightarrow\qquad \frac{\partial L}{\partial A} = g \, z^\top \tag{11}\] \[(m \times 1)(1 \times n) = (m \times n) \;\checkmark\] <p>An <strong>outer product</strong>. Every weight gradient in a fully connected network is an outer product of the incoming gradient with the saved input; that’s the whole rule.</p> <p><strong>Input.</strong></p> \[\frac{\partial L}{\partial z_q} = \sum_p g_p A_{pq} \qquad\Longrightarrow\qquad \frac{\partial L}{\partial z} = A^\top g \tag{12}\] \[(n \times m)(m \times 1) = (n \times 1) \;\checkmark\] <p>The transpose isn’t a trick, it’s forced: the sum in the index form runs over \(p\), which is \(A\)’s <em>first</em> index, and the only way to contract a matrix over its first index with a matmul is to transpose it first. This is worth internalizing, because it’s the step people most often get backwards. The index form tells you unambiguously which transpose you need: look at which index is being summed, and if it’s the matrix’s first index, you transpose.</p> <p><strong>Bias.</strong></p> \[\frac{\partial L}{\partial d_r} = \sum_p g_p \, \delta_{pr} = g_r \qquad\Longrightarrow\qquad \frac{\partial L}{\partial d} = g \tag{13}\] \[(m \times 1) \;\checkmark\] <p>The bias gradient is the incoming gradient, unchanged. Not “multiplied by 1”; there’s no multiplication happening, the local derivative is a delta and the delta selects one term.</p> <p>Those are all the pieces. (8) handles the output layer’s activation and loss, (9) and (10) handle the hidden activation, and (11), (12), (13) handle both linear layers. The backward pass is now just applying them in order.</p> <hr/> <h4 id="backward-pass"><strong>Backward pass</strong></h4> <p>We walk the graph in reverse, from \(L\) back to the parameters. At each node we take the gradient that has already arrived from downstream, apply one local derivative from the previous section, and pass the result upstream.</p> <p>Backprop starts by seeding the output:</p> \[\frac{\partial L}{\partial L} = 1\] <p>which is trivially true and is the base case the whole recursion hangs off. From there, every step below is: index form, packed form, shape check.</p> <h5 id="1-output-pre-activation-y_a"><strong>1. Output pre-activation</strong> \(y_a\)</h5> <p>This is (8), already derived:</p> \[\frac{\partial L}{\partial (y_a)_k} = y_k - t_k \qquad\Longrightarrow\qquad \frac{\partial L}{\partial y_a} = y - t \qquad (N_o \times 1) \;\checkmark \tag{14}\] <p>Notice what we skipped: we never computed \(\partial L / \partial y\) on its own. We could have, it’s \(-t \oslash y\) from (7), and then contracted it with the softmax Jacobian. Going straight to \(y_a\) instead is fewer operations and avoids the \(1/y_k\) blowup, which is exactly the fusion argument from earlier. From here on, \(y - t\) is the gradient we propagate.</p> <h5 id="2-output-weights-v"><strong>2. Output weights</strong> \(V\)</h5> <p>The output layer is a linear layer, so this is (11) with \(A = V\), \(z = h\), and \(g = \partial L / \partial y_a = y - t\). Since it’s the first one, here it is in full rather than by citation.</p> <p>Start from the general rule, summing over every component of \(y_a\):</p> \[\frac{\partial L}{\partial V_{kj}} = \sum_{k'} \frac{\partial L}{\partial (y_a)_{k'}} \frac{\partial (y_a)_{k'}}{\partial V_{kj}}\] <p>For the local factor, go back to the forward equation (3), \((y_a)_{k'} = \sum_{j'} V_{k'j'} h_{j'} + c_{k'}\). The specific entry \(V_{kj}\) appears in that sum only when \(k' = k\) and \(j' = j\), and its coefficient there is \(h_j\):</p> \[\frac{\partial (y_a)_{k'}}{\partial V_{kj}} = \delta_{k'k} \, h_j\] <p>This is the pruning we talked about. Substituting, the delta collapses the sum to its single surviving term:</p> \[\frac{\partial L}{\partial V_{kj}} = \sum_{k'} (y_{k'} - t_{k'}) \, \delta_{k'k} \, h_j = (y_k - t_k) \, h_j\] \[\frac{\partial L}{\partial V} = (y - t) \, h^\top \qquad (N_o \times 1)(1 \times N_h) = (N_o \times N_h) \;\checkmark \tag{15}\] <p>An outer product of the incoming gradient with the layer’s saved input, exactly as (11) said it would be.</p> <h5 id="3-output-bias-c"><strong>3. Output bias</strong> \(c\)</h5> <p>By (13), with the same \(g\):</p> \[\frac{\partial L}{\partial c_k} = y_k - t_k \qquad\Longrightarrow\qquad \frac{\partial L}{\partial c} = y - t \qquad (N_o \times 1) \;\checkmark \tag{16}\] <p>The same vector as (14). Bias gradients are always just the incoming gradient passed through untouched.</p> <h5 id="4-hidden-activation-h"><strong>4. Hidden activation</strong> \(h\)</h5> <p>By (12). This is the step where all the routes stay live: \(h_j\) feeds into <em>every</em> output component through the matrix multiply, so every class contributes to its gradient and nothing drops out of the sum.</p> \[\frac{\partial L}{\partial h_j} = \sum_k \frac{\partial L}{\partial (y_a)_k} \frac{\partial (y_a)_k}{\partial h_j} = \sum_k (y_k - t_k) \, V_{kj}\] \[\frac{\partial L}{\partial h} = V^\top (y - t) \qquad (N_h \times N_o)(N_o \times 1) = (N_h \times 1) \;\checkmark \tag{17}\] <p>The transpose is worth pausing on, because it’s the step most often gotten backwards. The sum runs over \(k\), which is \(V\)’s <strong>first</strong> index. A plain matrix product \(V g\) would contract over \(V\)’s second index instead, giving the wrong contraction and, conveniently, the wrong shape. Transposing \(V\) swaps which index gets contracted, so \(V^\top g\) sums over \(k\) as required. The shape check catches it either way: \(V g\) is \((N_o \times N_h)(N_o \times 1)\), which doesn’t even multiply.</p> <h5 id="5-hidden-pre-activation-h_a"><strong>5. Hidden pre-activation</strong> \(h_a\)</h5> <p>Sigmoid, so we use its Jacobian (10):</p> \[\frac{\partial L}{\partial (h_a)_j} = \sum_{j'} \frac{\partial L}{\partial h_{j'}} \frac{\partial h_{j'}}{\partial (h_a)_j} = \sum_{j'} \frac{\partial L}{\partial h_{j'}} \, h_{j'} (1 - h_{j'}) \, \delta_{j'j} = \frac{\partial L}{\partial h_j} \, h_j (1 - h_j)\] \[\frac{\partial L}{\partial h_a} = \frac{\partial L}{\partial h} \odot h \odot (1 - h) \qquad (N_h \times 1) \;\checkmark \tag{18}\] <p>Here the delta is doing something structurally different from the one in step 2. There, the delta pruned a sum that came from a matrix multiply. Here it’s the <em>entire</em> Jacobian: sigmoid is elementwise, so its Jacobian is diagonal, and contracting with a diagonal matrix is the same thing as an elementwise product. That is the formal reason this line is a <code class="language-plaintext highlighter-rouge">*</code> in code while step 4 was a matmul.</p> <p>Also note that \(h_a\) itself never appears on the right-hand side. We wrote the derivative as \(h \odot (1-h)\) using (9), so the forward pass only needs to have saved \(h\); no exponentials are recomputed.</p> <h5 id="6-input-weights-w"><strong>6. Input weights</strong> \(W\)</h5> <p>Same as step 2, one layer down. Apply (11) with \(A = W\), \(z = x\), and \(g = \partial L / \partial h_a\):</p> \[\frac{\partial L}{\partial W_{ji}} = \frac{\partial L}{\partial (h_a)_j} \, x_i\] \[\frac{\partial L}{\partial W} = \frac{\partial L}{\partial h_a} \, x^\top \qquad (N_h \times 1)(1 \times N_i) = (N_h \times N_i) \;\checkmark \tag{19}\] <h5 id="7-input-bias-b"><strong>7. Input bias</strong> \(b\)</h5> <p>By (13):</p> \[\frac{\partial L}{\partial b_j} = \frac{\partial L}{\partial (h_a)_j} \qquad\Longrightarrow\qquad \frac{\partial L}{\partial b} = \frac{\partial L}{\partial h_a} \qquad (N_h \times 1) \;\checkmark \tag{20}\] <h5 id="and-the-one-we-dont-need"><strong>And the one we don’t need</strong></h5> <p>For completeness, the gradient with respect to the input itself, by (12):</p> \[\frac{\partial L}{\partial x} = W^\top \frac{\partial L}{\partial h_a} \qquad (N_i \times 1) \tag{21}\] <p>We don’t need it here, because \(x\) is data and there’s nothing below it to update. But this is precisely the quantity a layer hands to the layer beneath it, and it’s why backprop composes: every layer takes \(\partial L / \partial(\text{its output})\) in, and returns \(\partial L / \partial(\text{its input})\) out, plus its own parameter gradients on the side. Step 4 was exactly this for the output layer. Stack a hundred of these and you have a deep network; the per-layer work never changes.</p> <h5 id="all-of-it-in-one-place"><strong>All of it in one place</strong></h5> <table> <thead> <tr> <th>Gradient</th> <th>Index form</th> <th>Packed form</th> <th>Shape</th> </tr> </thead> <tbody> <tr> <td>\(\partial L / \partial y_a\)</td> <td>\(y_k - t_k\)</td> <td>\(y - t\)</td> <td>\(N_o \times 1\)</td> </tr> <tr> <td>\(\partial L / \partial V\)</td> <td>\((y_k - t_k) h_j\)</td> <td>\((y-t) \, h^\top\)</td> <td>\(N_o \times N_h\)</td> </tr> <tr> <td>\(\partial L / \partial c\)</td> <td>\(y_k - t_k\)</td> <td>\(y - t\)</td> <td>\(N_o \times 1\)</td> </tr> <tr> <td>\(\partial L / \partial h\)</td> <td>\(\sum_k (y_k - t_k) V_{kj}\)</td> <td>\(V^\top (y-t)\)</td> <td>\(N_h \times 1\)</td> </tr> <tr> <td>\(\partial L / \partial h_a\)</td> <td>\(\dfrac{\partial L}{\partial h_j} h_j (1 - h_j)\)</td> <td>\(\dfrac{\partial L}{\partial h} \odot h \odot (1-h)\)</td> <td>\(N_h \times 1\)</td> </tr> <tr> <td>\(\partial L / \partial W\)</td> <td>\(\dfrac{\partial L}{\partial (h_a)_j} x_i\)</td> <td>\(\dfrac{\partial L}{\partial h_a} \, x^\top\)</td> <td>\(N_h \times N_i\)</td> </tr> <tr> <td>\(\partial L / \partial b\)</td> <td>\(\dfrac{\partial L}{\partial (h_a)_j}\)</td> <td>\(\dfrac{\partial L}{\partial h_a}\)</td> <td>\(N_h \times 1\)</td> </tr> </tbody> </table> <p>Four of the seven are parameter gradients, \(W, b, V, c\), and those are what the optimizer consumes. The other three, \(\partial L/\partial y_a\), \(\partial L/\partial h\), \(\partial L/\partial h_a\), are intermediates that exist only to carry the gradient further back.</p> <p>Every parameter gradient has the same shape as its parameter, which is what lets the update step be a plain elementwise subtraction:</p> \[W \leftarrow W - \eta \frac{\partial L}{\partial W}, \qquad b \leftarrow b - \eta \frac{\partial L}{\partial b}, \qquad V \leftarrow V - \eta \frac{\partial L}{\partial V}, \qquad c \leftarrow c - \eta \frac{\partial L}{\partial c}\] <h5 id="what-the-forward-pass-has-to-keep"><strong>What the forward pass has to keep</strong></h5> <p>Reading the table right to left, the backward pass touches \(x\), \(h\), \(y\), \(t\), and \(V\). The parameters are around anyway, so the activations that must survive the forward pass are:</p> \[x \; (N_i), \qquad h \; (N_h), \qquad y \; (N_o)\] <p>\(h_a\) and \(y_a\) can both be dropped the moment their activations are computed. \(h_a\) is not needed because (9) let us write the sigmoid derivative as \(h \odot (1-h)\), and \(y_a\) is not needed because (8) collapsed the softmax-and-loss derivative to \(y - t\). Both are small wins here, but this is the same accounting that decides activation memory in a real training run, and it’s the reason the fused and rewritten-in-terms-of-the-output forms are worth knowing rather than just deriving the obvious thing.</p> <hr/> <h4 id="the-whole-thing-on-one-page"><strong>The whole thing on one page</strong></h4> <p>Everything above, as a single reference: the model and shapes, the computation graph, the forward pass with its shape chains, the backward pass, and a line of code for each step.</p> <div class="row justify-content-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/mlp-fw-bwd.svg" sizes="95vw"/> <img src="/assets/img/mlp-fw-bwd.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Two-layer MLP forward pass, backward pass, shapes and einsum" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Open the image in a new tab to zoom. Editable source: <a href="/assets/img/mlp-fw-bwd.excalidraw">mlp-fw-bwd.excalidraw</a>, which you can drop into <a href="https://excalidraw.com">excalidraw.com</a>. </div> <p>Reading it: the graph across the top is the forward direction, left to right, with rectangles for values, blue circles for the two arithmetic ops, and green for the nonlinearities and the loss. The red arrow underneath is the backward direction, with the three intermediate gradients marked at the points they’re computed. The two columns below pair each equation with its shape and its one line of code.</p> <hr/> <h4 id="the-code"><strong>The code</strong></h4> <p>Everything above, as a script that runs. The only thing worth flagging before the listing is how the column-vector convention from the math survives contact with numpy.</p> <p>In the math, \(x\) is \(N_i \times 1\) and row vectors need an explicit \(x^\top\). In numpy the arrays are 1-D, shape <code class="language-plaintext highlighter-rouge">(Ni,)</code>, with no stored orientation at all; numpy infers what you meant from the operation. So the transposes in the packed forms don’t map one-to-one onto <code class="language-plaintext highlighter-rouge">.T</code> in the code:</p> <table> <thead> <tr> <th>Math</th> <th>numpy</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td>\(W x\)</td> <td><code class="language-plaintext highlighter-rouge">W @ x</code></td> <td>matmul against a 1-D array contracts its last axis</td> </tr> <tr> <td>\(V^\top (y-t)\)</td> <td><code class="language-plaintext highlighter-rouge">V.T @ dLdya</code></td> <td>a real transpose, because we contract \(V\)’s first index</td> </tr> <tr> <td>\((y-t) \, h^\top\)</td> <td><code class="language-plaintext highlighter-rouge">np.outer(dLdya, h)</code></td> <td><code class="language-plaintext highlighter-rouge">@</code> between two 1-D arrays gives a scalar, not an outer product</td> </tr> <tr> <td>\(\dfrac{\partial L}{\partial h} \odot h \odot (1-h)\)</td> <td><code class="language-plaintext highlighter-rouge">dLdh * h * (1 - h)</code></td> <td><code class="language-plaintext highlighter-rouge">*</code> is elementwise</td> </tr> </tbody> </table> <p>The third row is the one that bites. <code class="language-plaintext highlighter-rouge">dLdya @ h</code> would give you a dot product, silently, with no shape error to warn you. Outer products have to be asked for by name.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="n">Ni</span><span class="p">,</span> <span class="n">Nh</span><span class="p">,</span> <span class="n">No</span> <span class="o">=</span> <span class="mi">784</span><span class="p">,</span> <span class="mi">500</span><span class="p">,</span> <span class="mi">10</span>                  <span class="c1"># input, hidden, output
</span>
<span class="n">W</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mf">0.01</span><span class="p">,</span> <span class="p">(</span><span class="n">Nh</span><span class="p">,</span> <span class="n">Ni</span><span class="p">));</span>  <span class="n">b</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">Nh</span><span class="p">)</span>
<span class="n">V</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mf">0.01</span><span class="p">,</span> <span class="p">(</span><span class="n">No</span><span class="p">,</span> <span class="n">Nh</span><span class="p">));</span>  <span class="n">c</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">No</span><span class="p">)</span>

<span class="n">x</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">Ni</span><span class="p">)</span>                   <span class="c1"># one training sample
</span><span class="n">t</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">No</span><span class="p">);</span> <span class="n">t</span><span class="p">[</span><span class="mi">3</span><span class="p">]</span> <span class="o">=</span> <span class="mf">1.0</span>               <span class="c1"># one-hot target, class 3
</span>

<span class="k">def</span> <span class="nf">sigmoid</span><span class="p">(</span><span class="n">z</span><span class="p">):</span>
    <span class="k">return</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">+</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">z</span><span class="p">))</span>


<span class="k">def</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z</span><span class="p">):</span>
    <span class="n">z</span> <span class="o">=</span> <span class="n">z</span> <span class="o">-</span> <span class="n">z</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span>                        <span class="c1"># subtract the max before exponentiating
</span>    <span class="n">e</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">e</span> <span class="o">/</span> <span class="n">e</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>


<span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">c</span><span class="p">):</span>
    <span class="n">ha</span> <span class="o">=</span> <span class="n">W</span> <span class="o">@</span> <span class="n">x</span> <span class="o">+</span> <span class="n">b</span>                         <span class="c1"># (Nh,)   eq (1)
</span>    <span class="n">h</span>  <span class="o">=</span> <span class="nf">sigmoid</span><span class="p">(</span><span class="n">ha</span><span class="p">)</span>                       <span class="c1"># (Nh,)   eq (2)
</span>    <span class="n">ya</span> <span class="o">=</span> <span class="n">V</span> <span class="o">@</span> <span class="n">h</span> <span class="o">+</span> <span class="n">c</span>                         <span class="c1"># (No,)   eq (3)
</span>    <span class="n">y</span>  <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">ya</span><span class="p">)</span>                       <span class="c1"># (No,)   eq (4)
</span>    <span class="n">L</span>  <span class="o">=</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">t</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">y</span> <span class="o">+</span> <span class="mf">1e-12</span><span class="p">))</span>    <span class="c1"># scalar  eq (5)
</span>    <span class="k">return</span> <span class="n">h</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">L</span>


<span class="n">h</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="nf">forward</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">c</span><span class="p">)</span>

<span class="c1"># --- backward ---
</span><span class="n">dLdya</span> <span class="o">=</span> <span class="n">y</span> <span class="o">-</span> <span class="n">t</span>                              <span class="c1"># (No,)      eq (14)
</span><span class="n">dLdV</span>  <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">outer</span><span class="p">(</span><span class="n">dLdya</span><span class="p">,</span> <span class="n">h</span><span class="p">)</span>                 <span class="c1"># (No, Nh)   eq (15)
</span><span class="n">dLdc</span>  <span class="o">=</span> <span class="n">dLdya</span>                              <span class="c1"># (No,)      eq (16)
</span><span class="n">dLdh</span>  <span class="o">=</span> <span class="n">V</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">dLdya</span>                        <span class="c1"># (Nh,)      eq (17)
</span><span class="n">dLdha</span> <span class="o">=</span> <span class="n">dLdh</span> <span class="o">*</span> <span class="n">h</span> <span class="o">*</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">h</span><span class="p">)</span>               <span class="c1"># (Nh,)      eq (18)
</span><span class="n">dLdW</span>  <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">outer</span><span class="p">(</span><span class="n">dLdha</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>                 <span class="c1"># (Nh, Ni)   eq (19)
</span><span class="n">dLdb</span>  <span class="o">=</span> <span class="n">dLdha</span>                              <span class="c1"># (Nh,)      eq (20)
</span>
<span class="c1"># --- update ---
</span><span class="n">eta</span> <span class="o">=</span> <span class="mf">0.01</span>
<span class="n">W</span> <span class="o">-=</span> <span class="n">eta</span> <span class="o">*</span> <span class="n">dLdW</span><span class="p">;</span>  <span class="n">b</span> <span class="o">-=</span> <span class="n">eta</span> <span class="o">*</span> <span class="n">dLdb</span>
<span class="n">V</span> <span class="o">-=</span> <span class="n">eta</span> <span class="o">*</span> <span class="n">dLdV</span><span class="p">;</span>  <span class="n">c</span> <span class="o">-=</span> <span class="n">eta</span> <span class="o">*</span> <span class="n">dLdc</span>
</code></pre></div></div> <p>Two details in there that aren’t in the math. <code class="language-plaintext highlighter-rouge">softmax</code> subtracts the max before exponentiating; this changes nothing mathematically, since the constant cancels between numerator and denominator, but it keeps \(e^{z}\) from overflowing when the logits are large. And the loss adds <code class="language-plaintext highlighter-rouge">1e-12</code> inside the log, so a probability that has underflowed to exactly zero gives a large finite number instead of <code class="language-plaintext highlighter-rouge">-inf</code>. Neither affects the gradients, because we never differentiate through them; \(y - t\) was derived before either was introduced.</p> <h5 id="checking-it"><strong>Checking it</strong></h5> <p>Derivations look right far more often than they are right, so it’s worth making the machine check. The gradient of \(L\) with respect to one parameter entry is a plain scalar derivative, so we can approximate it with a central difference and compare:</p> \[\frac{\partial L}{\partial \theta} \approx \frac{L(\theta + \varepsilon) - L(\theta - \varepsilon)}{2\varepsilon}\] <p>Central difference rather than \((L(\theta+\varepsilon) - L(\theta))/\varepsilon\) because its error shrinks like \(\varepsilon^2\) instead of \(\varepsilon\), which buys several digits for free.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">grad_check</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">dP</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">25</span><span class="p">,</span> <span class="n">eps</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Central finite difference on n random entries. Run before the update.</span><span class="sh">"""</span>
    <span class="n">worst</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>
        <span class="n">idx</span> <span class="o">=</span> <span class="nf">tuple</span><span class="p">(</span><span class="n">rng</span><span class="p">.</span><span class="nf">integers</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">s</span><span class="p">)</span> <span class="k">for</span> <span class="n">s</span> <span class="ow">in</span> <span class="n">P</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>
        <span class="n">orig</span> <span class="o">=</span> <span class="n">P</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span>
        <span class="n">P</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">orig</span> <span class="o">+</span> <span class="n">eps</span><span class="p">;</span>  <span class="n">Lp</span> <span class="o">=</span> <span class="nf">forward</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">c</span><span class="p">)[</span><span class="mi">2</span><span class="p">]</span>
        <span class="n">P</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">orig</span> <span class="o">-</span> <span class="n">eps</span><span class="p">;</span>  <span class="n">Lm</span> <span class="o">=</span> <span class="nf">forward</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">c</span><span class="p">)[</span><span class="mi">2</span><span class="p">]</span>
        <span class="n">P</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">orig</span>
        <span class="n">worst</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="n">worst</span><span class="p">,</span> <span class="nf">abs</span><span class="p">((</span><span class="n">Lp</span> <span class="o">-</span> <span class="n">Lm</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">eps</span><span class="p">)</span> <span class="o">-</span> <span class="n">dP</span><span class="p">[</span><span class="n">idx</span><span class="p">]))</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">dL/d</span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s">: max |analytic - numeric| = </span><span class="si">{</span><span class="n">worst</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>


<span class="k">for</span> <span class="n">name</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">dP</span> <span class="ow">in</span> <span class="p">[(</span><span class="sh">"</span><span class="s">W</span><span class="sh">"</span><span class="p">,</span> <span class="n">W</span><span class="p">,</span> <span class="n">dLdW</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">b</span><span class="sh">"</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">dLdb</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">V</span><span class="sh">"</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">dLdV</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">c</span><span class="sh">"</span><span class="p">,</span> <span class="n">c</span><span class="p">,</span> <span class="n">dLdc</span><span class="p">)]:</span>
    <span class="k">assert</span> <span class="n">P</span><span class="p">.</span><span class="n">shape</span> <span class="o">==</span> <span class="n">dP</span><span class="p">.</span><span class="n">shape</span><span class="p">,</span> <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">P</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="s"> vs </span><span class="si">{</span><span class="n">dP</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="sh">"</span>
    <span class="nf">grad_check</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">dP</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dL/dW: max |analytic - numeric| = 2.44e-10
dL/db: max |analytic - numeric| = 1.75e-10
dL/dV: max |analytic - numeric| = 2.72e-10
dL/dc: max |analytic - numeric| = 2.57e-10
</code></pre></div></div> <p>Agreement to ten digits, which is about the floor for float64 at this \(\varepsilon\); the remaining error is roundoff in the two loss evaluations, not a flaw in the derivation. The <code class="language-plaintext highlighter-rouge">assert</code> on shapes is worth keeping too. It’s the convention-4 check turned into code, and it catches a transposed outer product instantly.</p> <p>If you ever get a mismatch, the size of it tells you where to look. Off by a clean factor (2, or the batch size) means a missing or doubled sum. Off only for one parameter means one line is wrong. Off everywhere means the forward pass and the loss disagree about something.</p> <hr/> <h4 id="batching-in-one-index"><strong>Batching, in one index</strong></h4> <p>Everything so far was a single sample. Real training uses a batch, and the whole change is one extra index.</p> <p>Let \(n = 1 \dots N\) index the samples, and stack them as <em>rows</em>, so \(X\) is \(N \times N_i\) and row \(n\) is one input. The loss becomes the mean over the batch:</p> \[L = -\frac{1}{N} \sum_n \sum_k T_{nk} \log Y_{nk}\] <p>Now look at what happens to the index \(n\) in each gradient, because there are exactly two cases:</p> <p><strong>Activations carry \(n\) through.</strong> Each sample has its own hidden vector, its own probabilities, its own intermediate gradients. Nothing is shared, so \(n\) just comes along:</p> \[\frac{\partial L}{\partial (H_a)_{nj}} = \frac{\partial L}{\partial H_{nj}} \, H_{nj}(1 - H_{nj})\] <p><strong>Parameter gradients sum \(n\) away.</strong> The same \(W\) is used by every sample, so every sample contributes a term, and the total derivative is the sum of those contributions. This is the many-routes chain rule again, with the routes now being the samples:</p> \[\frac{\partial L}{\partial W_{ji}} = \sum_n \frac{\partial L}{\partial (H_a)_{nj}} \, X_{ni}\] <p>That sum over \(n\) is exactly what a matrix product does, which is why batching costs no extra code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">softmax_rows</span><span class="p">(</span><span class="n">Z</span><span class="p">):</span>                 <span class="c1"># softmax along the last axis, per sample
</span>    <span class="n">Z</span> <span class="o">=</span> <span class="n">Z</span> <span class="o">-</span> <span class="n">Z</span><span class="p">.</span><span class="nf">max</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdims</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="n">E</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">E</span> <span class="o">/</span> <span class="n">E</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdims</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">c</span><span class="p">):</span>
    <span class="n">Ha</span> <span class="o">=</span> <span class="n">X</span> <span class="o">@</span> <span class="n">W</span><span class="p">.</span><span class="n">T</span> <span class="o">+</span> <span class="n">b</span>                 <span class="c1"># (N, Nh)
</span>    <span class="n">H</span>  <span class="o">=</span> <span class="nf">sigmoid</span><span class="p">(</span><span class="n">Ha</span><span class="p">)</span>                 <span class="c1"># (N, Nh)
</span>    <span class="n">Ya</span> <span class="o">=</span> <span class="n">H</span> <span class="o">@</span> <span class="n">V</span><span class="p">.</span><span class="n">T</span> <span class="o">+</span> <span class="n">c</span>                 <span class="c1"># (N, No)
</span>    <span class="n">Y</span>  <span class="o">=</span> <span class="nf">softmax_rows</span><span class="p">(</span><span class="n">Ya</span><span class="p">)</span>            <span class="c1"># (N, No)
</span>    <span class="k">return</span> <span class="n">H</span><span class="p">,</span> <span class="n">Y</span><span class="p">,</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">T</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">Y</span> <span class="o">+</span> <span class="mf">1e-12</span><span class="p">))</span> <span class="o">/</span> <span class="n">N</span>


<span class="n">dLdYa</span> <span class="o">=</span> <span class="p">(</span><span class="n">Y</span> <span class="o">-</span> <span class="n">T</span><span class="p">)</span> <span class="o">/</span> <span class="n">N</span>                  <span class="c1"># (N, No)    the 1/N from the mean
</span><span class="n">dLdV</span>  <span class="o">=</span> <span class="n">dLdYa</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">H</span>                  <span class="c1"># (No, Nh)   n summed away
</span><span class="n">dLdc</span>  <span class="o">=</span> <span class="n">dLdYa</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>            <span class="c1"># (No,)      n summed away
</span><span class="n">dLdH</span>  <span class="o">=</span> <span class="n">dLdYa</span> <span class="o">@</span> <span class="n">V</span>                    <span class="c1"># (N, Nh)    n carried through
</span><span class="n">dLdHa</span> <span class="o">=</span> <span class="n">dLdH</span> <span class="o">*</span> <span class="n">H</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">H</span><span class="p">)</span>           <span class="c1"># (N, Nh)    n carried through
</span><span class="n">dLdW</span>  <span class="o">=</span> <span class="n">dLdHa</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">X</span>                  <span class="c1"># (Nh, Ni)   n summed away
</span><span class="n">dLdb</span>  <span class="o">=</span> <span class="n">dLdHa</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>            <span class="c1"># (Nh,)      n summed away
</span></code></pre></div></div> <p>Three things changed and nothing else. The outer products became matrix products, since <code class="language-plaintext highlighter-rouge">dLdYa.T @ H</code> sums over \(n\) where <code class="language-plaintext highlighter-rouge">np.outer</code> had nothing to sum. The bias gradients became explicit <code class="language-plaintext highlighter-rouge">.sum(axis=0)</code>, because a bias is added to every row and therefore receives a contribution from every row. And the \(1/N\) from the mean rides along from the start, folded into <code class="language-plaintext highlighter-rouge">dLdYa</code> once rather than applied to each parameter gradient separately.</p> <p>Note also that the parameter gradients keep exactly the shapes they had for a single sample; <code class="language-plaintext highlighter-rouge">dLdW</code> is still \(N_h \times N_i\) whichever batch size you use. That has to be true, since it’s still being subtracted from <code class="language-plaintext highlighter-rouge">W</code>. The batch dimension appears in the intermediates and is gone by the time you reach the parameters. Same gradient check applies, and it passes to the same ten digits.</p> <hr/> <h4 id="wrapping-up"><strong>Wrapping up</strong></h4> <p>The thing I wanted out of writing this was the habit rather than the results. Derive on scalars where the chain rule is unambiguous, write the full sum and let the Kronecker deltas prune it, read off the packed form last, and check the shape at every line. Do that and the gradients fall out the same way every time, whether it’s a two-layer MLP or something with a lot more indices.</p> <p>The results worth keeping in your head are small. Softmax with cross-entropy collapses to \(y - t\). A weight gradient is an outer product of the incoming gradient with the layer’s saved input. An input gradient is the transposed weight matrix times the incoming gradient, and the transpose is there because you’re contracting the first index. A bias gradient is the incoming gradient, unchanged. Elementwise activations have diagonal Jacobians, which is why they show up as <code class="language-plaintext highlighter-rouge">*</code> and not as matmuls. Those five facts cover every line of the backward pass above.</p> <p>If you find a mistake anywhere in here, please let me know and I’ll fix it.</p>]]></content><author><name></name></author><category term="ML"/><summary type="html"><![CDATA[Deriving every gradient in a two-layer MLP from first principles, checking shapes at each step, and turning it into working code]]></summary></entry><entry><title type="html">CuTe DSL fundamentals and primitives [FA3]</title><link href="https://monishver11.github.io/blog/2026/fa3-cute/" rel="alternate" type="text/html" title="CuTe DSL fundamentals and primitives [FA3]"/><published>2026-06-07T18:52:00+00:00</published><updated>2026-06-07T18:52:00+00:00</updated><id>https://monishver11.github.io/blog/2026/fa3-cute</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/fa3-cute/"><![CDATA[<h4 id="what-cute-is-cutlass-templates-and-an-embedded-dsl"><strong>What CuTe is: CUTLASS, templates, and an embedded DSL</strong></h4> <p>CuTe stands for “CUDA Tensor” and is the layout/tensor algebra at the heart of <strong>CUTLASS 3.x</strong> (the version we’re using). CUTLASS is NVIDIA’s open-source library of high-performance GEMM and convolution primitives. Before getting to what CuTe gives us, it helps to understand what CUTLASS is built on, and why.</p> <p><strong>CUTLASS is a C++ template library.</strong> Templates here are doing one specific job: they let tile shapes, layouts, dtypes, and instruction variants be <strong>compile-time parameters</strong>, so the compiler can specialize a single generic kernel across many concrete shapes without runtime cost. A handwritten GEMM kernel hardcodes its tile sizes; a templated one accepts them as types and lets the compiler emit one specialized SASS per instantiation. The same logic, recompiled for \(bM = 128, bN = 128, bK = 64\) on FP16 versus \(bM = 64, bN = 256, bK = 32\) on FP8, produces two completely different machine-code kernels, each as efficient as a hand-tuned version, with no runtime branching on shape. That’s the win templates buy: zero-cost generic programming.</p> <p>The cost: CUTLASS C++ code is hard to read. Layouts, strides, swizzles, and instruction variants all live in template parameters, often deeply nested. A simple “load this tile from SMEM and multiply it” can become a several-line template instantiation. This works fine when you’re writing kernels professionally, but it’s a steep wall for everyone else.</p> <p><strong>That’s where the DSL comes in.</strong> A <strong>DSL</strong> (Domain-Specific Language) is a small language designed for one specific domain, with primitives that match the domain’s vocabulary directly. SQL is a DSL for relational queries; regex is a DSL for string patterns. The advantage over a general-purpose language is that DSL programs are short and read like the domain they describe, instead of being buried under host-language plumbing.</p> <p>A DSL can be <strong>standalone</strong> (its own parser, compiler, runtime) or <strong>embedded</strong> in a host language, where the DSL’s “syntax” is just a particular use of the host language’s existing constructs. Embedded DSLs are common because they ride on the host’s tooling (editor support, packaging, debugging) for free.</p> <p><strong>CuTe Python DSL is an embedded DSL inside Python.</strong> The DSL lives as a Python library: when you write <code class="language-plaintext highlighter-rouge">cute.make_layout(shape=(4, 3), stride=(1, 4))</code> or decorate a function with <code class="language-plaintext highlighter-rouge">@cute.jit</code>, you’re not in a separate language. You’re using Python objects whose meaning is defined by CuTe. The DSL provides a single, uniform vocabulary for talking about how data is laid out in any memory space (GMEM, SMEM, registers) and how it gets partitioned across threads and warps for Tensor-Core instructions. Raw CUDA forces you to track all of this with bare pointers and hand-written indexing; CuTe makes the <strong>Layout</strong> a first-class object that the compiler can reason about.</p> <p>How does Python code end up running on a GPU? Two compilation models are worth distinguishing first.</p> <p><strong>AOT (Ahead-Of-Time) compilation</strong> is what happens with a normal C++ or CUDA C++ program: you run a compiler once, before the program starts, and it produces a finished binary. The compiler sees the source code, optimizes it, emits machine code, and that’s it. When the program runs later, there’s no compilation step in sight, you’re just executing pre-built instructions. Standard CUDA kernels work this way: <code class="language-plaintext highlighter-rouge">nvcc</code> compiles your <code class="language-plaintext highlighter-rouge">.cu</code> files into a binary that contains PTX and SASS, ready to launch.</p> <p><strong>JIT (Just-In-Time) compilation</strong> is what happens with languages like Python, Java, or JavaScript: the program starts running first, and a compiler kicks in <em>during execution</em> to turn parts of the program into machine code on demand. The compilation step is real, it does the same kind of work as AOT (parsing, optimizing, emitting machine code), but it runs inside the live process rather than offline.</p> <p>CuTe Python uses <strong>JIT compilation</strong> for the GPU side. The workflow looks like this: you write a function and decorate it with <code class="language-plaintext highlighter-rouge">@cute.jit</code>. At that point, nothing has compiled yet, it’s just a Python function holding source code. When you call <code class="language-plaintext highlighter-rouge">cute.compile(my_kernel, ...)</code>, the Python process introspects the function, builds an internal representation (IR), and lowers it through CuTe’s internal compilation pipeline down to PTX and finally SASS, the GPU’s machine code. After this compile step finishes, the kernel is a regular compiled GPU kernel that you launch and run on the device. So the GPU never executes Python. It executes the same kind of compiled binary you’d get from <code class="language-plaintext highlighter-rouge">nvcc</code>, the difference is <em>when</em> the compilation happens (during your Python program’s run, not before it).</p> <p>Why JIT and not AOT? Because the kernel’s specialization depends on values the user supplies at run time: which dtype, what tile sizes, what head dimension, what swizzle. An AOT model would force the kernel author to either pre-compile every combination (combinatorially huge) or pay runtime branching costs to handle them generically. JIT sidesteps both: the user supplies the concrete configuration, CuTe compiles exactly the specialized kernel needed for that configuration, and from then on every launch with the same configuration reuses the compiled kernel. You get the specialization benefits of CUTLASS C++ templates without having to enumerate every instantiation up front.</p> <p>We’ll use <strong>“JIT-time”</strong> and <strong>“compile-time”</strong> interchangeably from here on. They refer to the same moment in the lifecycle: the window during the Python program’s execution when CuTe is lowering your <code class="language-plaintext highlighter-rouge">@cute.jit</code> function to PTX/SASS, <em>before</em> any kernel launches on the GPU. Calling it “compile-time” emphasizes “the compiler has full visibility and can optimize”; calling it “JIT-time” emphasizes “this is happening inside the running program, not in a separate offline step.” Both names point to the same thing.</p> <p>This brings us to a constraint that will show up everywhere in CuTe code: many values must be <strong>JIT-time constants</strong>, meaning the compiler must be able to evaluate them at JIT time, before any kernel launches. Concretely, a JIT-time constant is a value whose magnitude is known by the time <code class="language-plaintext highlighter-rouge">cute.compile</code> runs, so it can be folded directly into the emitted PTX as an immediate, used to size loops the compiler will unroll, and used to validate that shapes are compatible. A <em>runtime</em> value, in contrast, is one that only exists once the kernel is actually executing on the GPU: an entry of a tensor, a thread index, the result of a load. The compiler doesn’t know what these values are when it’s emitting code, only that “some 32-bit float will be here when the kernel runs.”</p> <p>The reason this distinction matters: CuTe’s whole performance story depends on layouts being JIT-time objects. Tile sizes, strides, swizzle patterns, and partitioning must all be known to the compiler so it can fold offset arithmetic into constants, unroll inner loops, and emit specialized instructions. If a layout’s shape were a runtime value, none of this would be possible, the compiler would have to emit generic indexing code and lose the specialization advantage. So layouts (and the values used to build them, like tile sizes, stride numbers, and stage counts) are always JIT-time. Tensor <em>data</em> (the actual numbers being computed on) is always runtime.</p> <p>CuTe gives you two explicit markers for JIT-time values:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">cutlass.const_expr(x)</code></strong> declares that <code class="language-plaintext highlighter-rouge">x</code> is a JIT-time constant. You’ll see this used in conditionals (e.g., <code class="language-plaintext highlighter-rouge">if cutlass.const_expr(stride &lt; M):</code>) to tell the compiler that the branch can be decided at JIT time and the dead path discarded entirely.</li> <li><strong><code class="language-plaintext highlighter-rouge">cutlass.range_constexpr(N)</code></strong> is the JIT-time equivalent of Python’s <code class="language-plaintext highlighter-rouge">range(N)</code>. It signals that <code class="language-plaintext highlighter-rouge">N</code> is JIT-time-known and the loop should be fully unrolled at compile time. You’ll see this used for inner loops over fixed shapes (e.g., <code class="language-plaintext highlighter-rouge">for j in cutlass.range_constexpr(n_cols):</code>), where the compiler emits a flat sequence of instructions instead of a runtime loop.</li> </ul> <p>The practical consequence: if you accidentally pass a runtime value where a JIT-time constant is expected, CuTe fails. The failure usually happens inside the IR lowering pass, and the Python traceback doesn’t always point cleanly at the offending line. We’ll see specific instances of this as we write kernels.</p> <p><strong>A minimal example.</strong> A taste of what a CuTe layout looks like in the DSL:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># A 4-by-3 layout. The shape says "4 along the first axis, 3 along the second."
# The strides say "step 1 along the first axis, step 4 along the second."
# Together this is a function: coord (i, j) -&gt; integer offset i*1 + j*4.
</span><span class="n">layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="n">shape</span><span class="o">=</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">3</span><span class="p">),</span> <span class="n">stride</span><span class="o">=</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">))</span>

<span class="c1"># Indexing the layout function at coord (2, 1) gives offset 2*1 + 1*4 = 6.
</span><span class="n">offset</span> <span class="o">=</span> <span class="nf">layout</span><span class="p">((</span><span class="mi">2</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>   <span class="c1"># -&gt; 6
</span></code></pre></div></div> <p>That’s a layout: a shape, a stride, and the function from coordinate to offset they define. No data is involved yet, the layout doesn’t know about any pointer. It just describes <em>how to walk an indexed grid</em>. The next subsection unpacks layouts properly, and we’ll see why this separation between “shape + stride” and “actual data” is what makes the whole abstraction work.</p> <h4 id="layouts"><strong>Layouts</strong></h4> <p>A layout is the most important object in CuTe. Almost everything else is built on top of it. So before introducing tensors, partitioning, or any of the higher-level machinery, we need to understand what a layout actually is and how it works.</p> <h5 id="shape-and-stride-a-function-from-coordinate-to-offset"><strong>Shape and stride: a function from coordinate to offset</strong></h5> <p>A <strong>CuTe layout</strong> is a pair: a <strong>shape</strong> and a <strong>stride</strong>. Together they define a pure function that takes a coordinate (a tuple of integers) and returns an integer offset. That’s it. A layout by itself is just this function. It does not point to memory, hold any data, or know about pointers. It only describes <em>how to walk an indexed grid</em>.</p> <p>Concretely, given a layout with shape \((s_0, s_1, \ldots, s_{n-1})\) and stride \((d_0, d_1, \ldots, d_{n-1})\), the layout function evaluates a coordinate \((i_0, i_1, \ldots, i_{n-1})\) as:</p> \[\text{layout}(i_0, i_1, \ldots, i_{n-1}) = i_0 \cdot d_0 + i_1 \cdot d_1 + \cdots + i_{n-1} \cdot d_{n-1}\] <p>The shape tells you the <em>bounds</em> of each axis: how many values that axis can take. The stride tells you the <em>step</em> along each axis: how far you move in the linear offset when you increment one coordinate by 1.</p> <p>Here is a 4-by-3 layout from the previous subsection, drawn out:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="n">shape</span><span class="o">=</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">3</span><span class="p">),</span> <span class="n">stride</span><span class="o">=</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">))</span>
<span class="c1"># coord (i, j) maps to offset i*1 + j*4
</span>
<span class="c1"># Visualizing the offsets at each (i, j):
#
#        j=0   j=1   j=2
# i=0:    0     4     8
# i=1:    1     5     9
# i=2:    2     6    10
# i=3:    3     7    11
#
# coord (2, 1) -&gt; offset 2*1 + 1*4 = 6
</span></code></pre></div></div> <p>The first axis has 4 entries and stride 1, so moving along it walks through consecutive offsets 0, 1, 2, 3. The second axis has 3 entries and stride 4, so moving along it jumps by 4 each time. The layout function is just doing the dot product.</p> <p>This is exactly the definition you’d use for a regular row-major or column-major 2D array, just expressed as shape and stride explicitly. If you want a 4x3 column-major matrix (rows contiguous), the layout is <code class="language-plaintext highlighter-rouge">shape=(4, 3), stride=(1, 4)</code>. If you want it row-major (columns contiguous), it’s <code class="language-plaintext highlighter-rouge">shape=(4, 3), stride=(3, 1)</code>. The shape stays the same, the stride changes, and CuTe handles both as the same kind of object.</p> <h5 id="layouts-describe-walks-not-storage"><strong>Layouts describe walks, not storage</strong></h5> <p>The crucial property: a layout describes a walk, not a piece of memory. Two completely different memory regions can share the same layout (the same indexing pattern), and the same memory region can be described by multiple layouts (different indexing patterns over the same bytes). We’ll see both of these in the next subsection on tensors and aliasing.</p> <p>This separation is what lets CuTe do its job. Once a layout is in the compiler’s hands, it can analyze the indexing pattern, compose it with other layouts (more on this below), specialize tile arithmetic, and validate that two layouts are compatible, all without touching any data. The data shows up later, when we wrap a layout with a pointer to create a tensor.</p> <h5 id="layouts-are-jit-time-objects"><strong>Layouts are JIT-time objects</strong></h5> <p>Layouts are almost always <strong>JIT-time</strong> values, not runtime values. When <code class="language-plaintext highlighter-rouge">cute.compile</code> runs, it knows every layout’s shape and stride concretely. That’s what lets the compiler fold offset arithmetic into immediates, unroll loops over fixed shapes, and statically check that the SMEM you’re partitioning has the structure the Tensor Core expects. This is the same constraint we discussed in the JIT subsection: the layout itself is metadata the compiler reasons about, while the data the layout points to is the runtime tensor.</p> <p>If you tried to construct a layout from a runtime value (say, an entry of a tensor), CuTe would fail. The shape and stride must be JIT-time-known. This is why you’ll see layout construction always using either literal integers or <code class="language-plaintext highlighter-rouge">cutlass.const_expr</code>-wrapped expressions.</p> <h5 id="hierarchical-nested-shapes"><strong>Hierarchical (nested) shapes</strong></h5> <p>So far, layouts have looked like simple flat shape and stride tuples. The move that makes CuTe powerful is allowing the shape (and stride) to be <strong>nested</strong>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="n">shape</span><span class="o">=</span><span class="p">((</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">),</span> <span class="mi">3</span><span class="p">),</span> <span class="n">stride</span><span class="o">=</span><span class="p">((</span><span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">),</span> <span class="mi">8</span><span class="p">))</span>
<span class="c1"># coord ((i0, i1), j) -&gt; offset i0*1 + i1*4 + j*8
</span></code></pre></div></div> <p>The outer rank is 2 (there are two top-level modes: the nested pair <code class="language-plaintext highlighter-rouge">(2, 2)</code> and the scalar <code class="language-plaintext highlighter-rouge">3</code>). The first top-level mode is itself a tuple, with its own shape <code class="language-plaintext highlighter-rouge">(2, 2)</code> and stride <code class="language-plaintext highlighter-rouge">(1, 4)</code>. A coordinate along this nested mode is itself a pair: <code class="language-plaintext highlighter-rouge">(i0, i1)</code>. The layout function then sums all stride contributions, from both the inner and outer modes.</p> <p>We need to be careful about a piece of CuTe vocabulary here. A <strong>mode</strong> is one top-level axis of a layout, and a mode can be either <strong>flat</strong> (a single integer shape and stride) or <strong>nested</strong> (a tuple of sub-shapes, each with its own stride). The terminology is: a flat mode has one shape and one stride. A nested mode has a <em>tuple</em> of sub-shapes, each with a corresponding sub-stride, and the offset contributed by that mode is the sum over all sub-contributions.</p> <p>This is the key bit: a nested mode is <em>one logical axis</em> of the layout, but its internals fan out into multiple stride contributions. A single logical coordinate along that axis decomposes into a tuple, and the offset for that mode comes from summing strides across the tuple.</p> <p><strong>Why does this matter? Because real hardware geometries are hierarchical.</strong> Before working an example, it helps to separate three things that are easy to conflate:</p> <ul> <li>A <strong>flat coordinate</strong> \(m\): a single logical position along the axis, counted the natural way (\(0, 1, 2, \ldots\)).</li> <li>A <strong>nested coordinate</strong> \((m_0, m_1, \ldots)\): the <em>same</em> logical position, re-expressed as a tuple. Flat and nested are two encodings of one position, not two different positions.</li> <li>The <strong>offset</strong>: the memory result. Not a coordinate at all, it’s the output of the layout function.</li> </ul> <p>Two different sets of multipliers move between these, and conflating them is the usual source of confusion. The <strong>shape</strong> converts flat to nested (pure index bookkeeping, no memory). The <strong>stride</strong> converts nested to offset (this is the layout function, the step where memory enters).</p> <p>Concretely, consider an axis of size 64 described as a nested mode with shape \((8, 2, 4)\) and strides \((1, 16, 32)\). A flat coordinate \(m \in [0, 64)\) decomposes into a nested coordinate \((m_0, m_1, m_2)\) where \(m_0 \in [0, 8)\), \(m_1 \in [0, 2)\), \(m_2 \in [0, 4)\).</p> <p><strong>Flat to nested uses the shape as place-value bases.</strong> Similar to place-value in decimal, but with the shape sizes as the bases instead of 10: \(m_0\) is the innermost coordinate, \(m_1\) steps once \(m_0\) has covered all 8 of its values, and \(m_2\) steps once \(m_1\) has covered both of its values. So:</p> \[m = m_0 + 8\, m_1 + (8 \cdot 2)\, m_2 = m_0 + 8\, m_1 + 16\, m_2\] <p>The multipliers \(1, 8, 16\) are the <em>running products of the inner shapes</em> (1, then 8, then \(8 \cdot 2 = 16\)). Nothing here touches memory: this is purely how a flat index unpacks into a tuple.</p> <p><strong>Nested to offset uses the strides.</strong> This is a separate calculation, and it’s the one that maps into memory:</p> \[\text{offset}(m) = m_0 \cdot 1 + m_1 \cdot 16 + m_2 \cdot 32\] <p>The same tuple \((m_0, m_1, m_2)\) plays two roles: the shape multipliers <em>produced</em> it from \(m\), and the stride multipliers <em>consume</em> it to get the offset. So one flat coordinate \(m\) pulls contributions from three different strides.</p> <p>To anchor this with numbers: \(m = 6\) unpacks to \((6, 0, 0)\) via the shape, then maps to offset \(6 \cdot 1 + 0 + 0 = 6\). Its flat neighbor \(m = 8\) unpacks to \((0, 1, 0)\), then maps to offset \(0 + 1 \cdot 16 + 0 = 16\). Adjacent in flat space, but the offset jumps from 6 to 16 because crossing the \(m_0\) boundary switches which stride is doing the work. This is <em>exactly</em> how WGMMA describes its M axis: the hierarchy is the hardware geometry. The question “is the M dimension a single contiguous stride?” no longer has a yes-or-no answer. It has a hierarchical answer, where some bits of \(m\) walk one stride and other bits walk a different stride.</p> <p>If this feels abstract right now, that’s fine. The point to internalize is that <strong>nested modes let one logical axis encode multiple stride patterns at once</strong>, and that’s what makes layouts powerful enough to describe Tensor-Core fragments, swizzled SMEM, and thread/value partitioning, all with the same primitive. We’ll see this used concretely when we get to TV layouts and WGMMA fragments later in this section.</p> <h5 id="operations-on-layouts"><strong>Operations on layouts</strong></h5> <p>CuTe provides a small set of layout operations that you’ll see throughout FA3 kernel code. The full list is larger; these are the ones we actually use.</p> <p><strong><code class="language-plaintext highlighter-rouge">cute.size(layout)</code></strong> returns the total number of elements the layout addresses. For a flat layout it’s just the product of the shape; for a hierarchical layout it’s the product of all leaf shape values.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="n">shape</span><span class="o">=</span><span class="p">((</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">),</span> <span class="mi">3</span><span class="p">),</span> <span class="n">stride</span><span class="o">=</span><span class="p">((</span><span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">),</span> <span class="mi">8</span><span class="p">))</span>
<span class="n">cute</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="n">layout</span><span class="p">)</span>            <span class="c1"># -&gt; 2 * 2 * 3 = 12
</span><span class="n">cute</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="n">layout</span><span class="p">,</span> <span class="n">mode</span><span class="o">=</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>  <span class="c1"># -&gt; 2 * 2 = 4   (size of mode 0, including its nested structure)
</span><span class="n">cute</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="n">layout</span><span class="p">,</span> <span class="n">mode</span><span class="o">=</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>  <span class="c1"># -&gt; 3
</span></code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">mode=</code> argument is important. It takes a list (because modes can themselves be nested) and returns the size of that mode, handling hierarchy correctly.</p> <p><strong><code class="language-plaintext highlighter-rouge">cute.rank(layout)</code></strong> returns the number of top-level modes. For the layout above, <code class="language-plaintext highlighter-rouge">cute.rank(layout) == 2</code>. Rank counts top-level modes only; it doesn’t peer inside nested modes.</p> <p><strong><code class="language-plaintext highlighter-rouge">cute.slice_(layout, coord)</code></strong> takes a coordinate where some entries are integers (which get <em>fixed</em>) and some are <code class="language-plaintext highlighter-rouge">None</code> (which stay <em>free</em>), and returns a new layout over only the free axes. This is how you take a slice through a higher-dimensional layout to look at a sub-region. The fixed-coordinate values contribute their stride contributions to a constant offset baked into the new layout; the free axes keep their original shape and stride.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 3D layout, shape (4, 3, 2), strides (1, 4, 12).
</span><span class="n">layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="n">shape</span><span class="o">=</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">2</span><span class="p">),</span> <span class="n">stride</span><span class="o">=</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">12</span><span class="p">))</span>

<span class="c1"># Fix the third coord at 0, leave the first two free.
</span><span class="n">sliced</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">slice_</span><span class="p">(</span><span class="n">layout</span><span class="p">,</span> <span class="p">(</span><span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span>

<span class="c1"># `sliced` is now a 2D layout with shape (4, 3) and stride (1, 4).
# It represents: "the layout you get if you walk the first two axes of
# the original 3D grid, with the third axis pinned to index 0."
# Indexing sliced[(i, j)] is equivalent to indexing the original at (i, j, 0).
</span></code></pre></div></div> <p>Slicing doesn’t reduce the layout to a different <em>function</em>. It just fixes some inputs of that function and exposes a lower-rank view over the rest. In a kernel, you use this constantly: a per-CTA tile is a slice of the full GMEM tensor’s layout (with the CTA’s tile-coords fixed), a per-stage SMEM view is a slice with the pipeline-stage index fixed, and so on.</p> <p><strong><code class="language-plaintext highlighter-rouge">cute.append(a, b)</code></strong> extends a layout by adding <code class="language-plaintext highlighter-rouge">b</code> as a new outer mode. It’s the inverse direction of slicing: you build up a higher-rank layout by appending modes. We’ll see this used in the TV-layout subsection when we surgically reassemble layouts from extracted pieces.</p> <p><strong><code class="language-plaintext highlighter-rouge">cute.flat_divide(tensor, tile_shape)</code></strong> is the workhorse for partitioning a tensor into tiles. Given a tensor and a tile shape, it returns a new tensor whose layout has the <em>tile interior</em> at the front and the <em>tile coordinates</em> at the back.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Suppose mQ has logical shape (seqlen, head_dim) = (1024, 128).
# Tile it into (Br, d) = (64, 128) tiles.
</span><span class="n">tiled</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">flat_divide</span><span class="p">(</span><span class="n">mQ</span><span class="p">,</span> <span class="p">(</span><span class="n">Br</span><span class="p">,</span> <span class="n">d</span><span class="p">))</span>
<span class="c1"># tiled has shape (Br, d, n_M_tiles)
#                  ^^^^^^^^   ^^^^^^^^^
#                  one tile   how many tiles
# Here n_M_tiles = 1024 / 64 = 16.
</span></code></pre></div></div> <p>You then index <code class="language-plaintext highlighter-rouge">tiled[(None, None, m)]</code> to get the \(m\)-th tile, which still has the same Br-by-d shape. This is how every kernel in this blog gets its per-CTA tile out of the full GMEM tensor.</p> <p>These four primitives (<code class="language-plaintext highlighter-rouge">size</code>, <code class="language-plaintext highlighter-rouge">rank</code>, <code class="language-plaintext highlighter-rouge">slice_</code>, <code class="language-plaintext highlighter-rouge">append</code>, plus <code class="language-plaintext highlighter-rouge">flat_divide</code>) are most of what you need to navigate layouts in CuTe code. Specific kernels will use a few more as they come up.</p> <p><strong>Layouts never touch data.</strong> This is worth stating explicitly before we move on. Every operation in this subsection (<code class="language-plaintext highlighter-rouge">size</code>, <code class="language-plaintext highlighter-rouge">rank</code>, <code class="language-plaintext highlighter-rouge">slice_</code>, <code class="language-plaintext highlighter-rouge">append</code>, <code class="language-plaintext highlighter-rouge">flat_divide</code>) operates <em>only on the layout</em>: it rearranges, slices, or extends the coordinate-to-offset function. None of these operations read from memory, copy bytes, or modify the underlying data buffer. They’re pure compile-time transformations of an indexing pattern.</p> <p>The actual data shows up only when we wrap a layout with a pointer to create a tensor (the next subsection). At that point, indexing the tensor at coordinate \(c\) does two things: (1) compute the offset via the layout function \(\text{layout}(c)\), and (2) read or write the byte at that offset. Step 1 is a pure arithmetic operation the compiler can heavily optimize. Step 2 is the only step that ever touches memory.</p> <p>So when a kernel calls <code class="language-plaintext highlighter-rouge">cute.flat_divide(mQ, (Br, d))</code>, no Q data moves. The result is a <em>re-layout</em> of <code class="language-plaintext highlighter-rouge">mQ</code>: same underlying bytes, new coordinate function for walking them. Same when a kernel slices a layout to pick out one stage of a circular buffer, or appends a mode to extend a fragment shape. Layouts are the planning step; the work itself happens later, when data flows through <code class="language-plaintext highlighter-rouge">cute.copy</code> (memory motion) or <code class="language-plaintext highlighter-rouge">cute.gemm</code> (Tensor-Core compute).</p> <p>With that in hand, we can introduce the object that pairs a layout with actual data: the <strong>tensor</strong>.</p> <h4 id="tensors"><strong>Tensors</strong></h4> <p>A layout describes a walk over an indexed grid, but no data. A <strong>tensor</strong> is the object that pairs a layout with actual data, by attaching a pointer. Once you have both, you can index, read, and write.</p> <h5 id="a-tensor-is-an-iterator-layout-pair"><strong>A tensor is an (iterator, layout) pair</strong></h5> <p>In CuTe, a tensor is:</p> \[\text{tensor} = (\text{iterator},\; \text{layout})\] <p>The <strong>iterator</strong> is a pointer-like handle that holds a base address along with type information (the dtype of the elements it points to). It is <em>not</em> a Python iterator in the <code class="language-plaintext highlighter-rouge">for x in it</code> sense. Conceptually, treat it as a typed C++ pointer: it knows the address it points to and the size of each element, but nothing about shape or strides. Those are entirely the layout’s job.</p> <p>Indexing a tensor at coordinate \(c\) is then a two-step operation:</p> \[\text{tensor}[c] \;=\; \ast(\text{iterator} + \text{layout}(c))\] <p>Compute the offset using the layout function, add it to the iterator, dereference. The layout decides <em>where</em> in memory; the iterator decides <em>what’s there</em>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Suppose `some_pointer` is a typed pointer to a buffer of FP32 values in some memory space.
</span><span class="n">tensor</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tensor</span><span class="p">(</span><span class="n">some_pointer</span><span class="p">,</span> <span class="n">my_layout</span><span class="p">)</span>

<span class="c1"># Indexing the tensor at coord (2, 1):
</span><span class="n">val</span> <span class="o">=</span> <span class="n">tensor</span><span class="p">[(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">1</span><span class="p">)]</span>   <span class="c1"># equivalent to *(some_pointer + my_layout((2, 1)))
</span></code></pre></div></div> <p>The iterator stays the same across operations; only the layout changes if you re-view the tensor differently. This is the key separation that makes CuTe’s machinery work: data is one thing, indexing is another.</p> <h5 id="aliasing-two-views-into-the-same-data"><strong>Aliasing: two views into the same data</strong></h5> <p>Because a tensor is just an iterator and a layout, a direct consequence is that <strong>two tensors can share an iterator but use different layouts</strong>. The two tensors point at the same physical bytes (registers, SMEM, GMEM, whichever) but interpret those bytes through different coordinate functions. No data is copied, no memory motion happens. You just have two different ways to walk the same storage.</p> <p>This is called <strong>aliasing</strong>, and it’s used constantly in kernel code. The most important reason: hardware-imposed layouts and human-friendly layouts often disagree, and aliasing lets you write code under the human-friendly view while the hardware still sees the layout it expects.</p> <p>A concrete example from FA3 (we’ll see this again in K4):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># `acc_pv` is a register tensor allocated by CuTe for the PV WGMMA accumulator.
# Its layout is whatever hierarchical structure the WGMMA atom expects, which is
# not a clean 2D rectangle from a programmer's point of view.
</span><span class="n">acc_pv</span> <span class="o">=</span> <span class="n">pv_thr_mma</span><span class="p">.</span><span class="nf">make_fragment_C</span><span class="p">(</span><span class="n">pv_acc_shape</span><span class="p">)</span>

<span class="c1"># We want a friendlier 2D view of the same physical registers, where mode-0 is
# "M row in this thread's view of the tile" and mode-1 is "N column."
# Same iterator (same registers), different layout.
</span><span class="n">acc_pv_mn</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tensor</span><span class="p">(</span>
    <span class="n">acc_pv</span><span class="p">.</span><span class="n">iterator</span><span class="p">,</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">layout_acc_mn</span><span class="p">(</span><span class="n">pv_tiled_mma</span><span class="p">,</span> <span class="n">acc_pv</span><span class="p">.</span><span class="n">layout</span><span class="p">),</span>
<span class="p">)</span>
</code></pre></div></div> <p>Both <code class="language-plaintext highlighter-rouge">acc_pv</code> and <code class="language-plaintext highlighter-rouge">acc_pv_mn</code> refer to <em>the exact same registers</em>. Writing <code class="language-plaintext highlighter-rouge">acc_pv_mn[i, j] = x</code> is sugar over a register store, indexed in a way humans can reason about. The store lands in whichever physical register the alias’s layout maps <code class="language-plaintext highlighter-rouge">(i, j)</code> to, which is the same physical register the hardware-native <code class="language-plaintext highlighter-rouge">acc_pv</code> knows about.</p> <p>Aliasing is a no-op at runtime. The compiler sees two layout objects over one iterator and generates indexing code for whichever view is being used at each point. No bytes move. No registers are reallocated. The two tensors are <em>the same data, indexed two ways</em>.</p> <h5 id="identity-tensors-coordinate-reporting-views"><strong>Identity tensors: coordinate-reporting views</strong></h5> <p>CuTe also lets you build a tensor whose <em>values</em> are coordinates rather than data. Indexing it at coord \(c\) returns \(c\) itself, as a tuple. This is called an <strong>identity tensor</strong>, and it has no underlying storage. It exists only as a probe to ask the question: “for thread T’s slice of some partitioned tensor, what tile-coordinates do thread T’s elements correspond to?”</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># An identity tensor over the shape (Br, d).
# cO[(m, n)] returns the tuple (m, n) itself, not a data value.
</span><span class="n">cO</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_identity_tensor</span><span class="p">((</span><span class="n">self</span><span class="p">.</span><span class="n">Br</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">d</span><span class="p">))</span>
</code></pre></div></div> <p>Identity tensors come into their own when partitioned the same way a real tensor is partitioned. If you apply the same partitioning that distributes an output tile across threads to an identity tensor over that same tile, indexing the partitioned identity at a per-thread slot tells you which \((M, N)\) position in the original tile that slot corresponds to. We’ll see this concretely in the partitioning subsection and again when we write the output scatter in a kernel.</p> <p>The takeaway here: identity tensors are coordinate probes. They piggyback on the same layout/partitioning machinery the real tensors use, so they always agree with the real tensors on which thread owns what. They’re a debugging and indexing tool, not a data structure.</p> <h5 id="tensor-data-is-runtime-layout-is-jit-time"><strong>Tensor data is runtime; layout is JIT-time</strong></h5> <p>We touched on this in the JIT subsection but it’s worth restating here in the tensor context. When you write <code class="language-plaintext highlighter-rouge">tensor[c]</code>, two things happen at different times:</p> <ul> <li><strong>The offset computation</strong> \(\text{layout}(c)\) is purely arithmetic on JIT-time-known shape and stride values. The compiler can fold offsets into immediates, unroll inner loops, and emit specialized addressing.</li> <li><strong>The dereference</strong> is a runtime memory operation. Whatever value is at that byte address right now is what comes back.</li> </ul> <p>So the layout is part of the kernel’s compiled structure (baked in at JIT time), and the actual data flows through at runtime. This is what lets CuTe specialize aggressively: the indexing code is a sequence of cheap integer additions on compile-time constants, and the loads and stores hit the right registers or memory locations directly.</p> <p>With layouts (coordinate functions) and tensors (layouts plus pointers) in place, the next question is: when a kernel runs with hundreds of threads, how does each thread know which subset of a tensor to operate on? That’s what partitioning answers.</p> <h4 id="partitioning-and-the-tiledmma-object"><strong>Partitioning and the TiledMMA object</strong></h4> <p>A kernel runs with many threads (one warpgroup = 128 threads on Hopper, often more), and they cooperate on a tile. The question this subsection answers: given a tile in SMEM, in registers, or in GMEM, <em>which slice of it does each thread own</em>, and how does CuTe express that?</p> <p>The short answer: there’s an object called <strong>TiledMMA</strong> that knows everything about how a warpgroup-level Tensor-Core instruction distributes work across threads. It carries the partitioning recipes for the three operands \(A\), \(B\), \(C\), and we use it to produce per-thread tensors that line up with what the hardware expects. Most of the deep mechanics (the exact fragment layouts, which thread holds which value of a WGMMA output) live with K4. Here we cover the abstractions and enough of the API to read kernel code.</p> <h5 id="the-tiledmma-object"><strong>The TiledMMA object</strong></h5> <p>A <strong>TiledMMA</strong> is a CuTe object that bundles three things:</p> <ul> <li>An <strong>MMA atom</strong>: the Tensor-Core instruction we want to issue. For our kernels this is WGMMA on Hopper, with a specific shape and dtype combination (e.g., a \(64 \times 64 \times 16\) BF16 atom).</li> <li>An <strong>atom layout</strong>: how many copies of the atom we tile across the CTA’s work region (e.g., two atoms along the M direction, one along N and K).</li> <li>The <strong>partitioning recipes</strong> that say how each atom’s \(A\), \(B\), and \(C\) tiles are distributed across the warpgroup’s 128 threads.</li> </ul> <p>In CuTe Python we build it with a helper. From FA3:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">qk_tiled_mma</span> <span class="o">=</span> <span class="n">sm90_utils</span><span class="p">.</span><span class="nf">make_trivial_tiled_mma</span><span class="p">(</span>
    <span class="n">INPUT_DTYPE</span><span class="p">,</span> <span class="n">INPUT_DTYPE</span><span class="p">,</span>                         <span class="c1"># dtypes of A and B
</span>    <span class="n">warpgroup</span><span class="p">.</span><span class="n">OperandMajorMode</span><span class="p">.</span><span class="n">K</span><span class="p">,</span>                     <span class="c1"># A is K-major
</span>    <span class="n">warpgroup</span><span class="p">.</span><span class="n">OperandMajorMode</span><span class="p">.</span><span class="n">K</span><span class="p">,</span>                     <span class="c1"># B is K-major
</span>    <span class="n">ACC_DTYPE</span><span class="p">,</span>                                        <span class="c1"># accumulator dtype
</span>    <span class="n">self</span><span class="p">.</span><span class="n">atom_layout_mnk</span><span class="p">,</span>                             <span class="c1"># tiling across atoms
</span>    <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">Br</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">Bc</span><span class="p">),</span>                               <span class="c1"># the CTA's work tile shape (M, N)
</span>    <span class="n">warpgroup</span><span class="p">.</span><span class="n">OperandSource</span><span class="p">.</span><span class="n">SMEM</span><span class="p">,</span>                     <span class="c1"># where A comes from
</span><span class="p">)</span>
</code></pre></div></div> <p>This is one TiledMMA, configured for the QK matmul (\(S = Q K^\top\)). FA3’s PV matmul (\(O = P V\)) gets its own TiledMMA with different settings (e.g., \(A\) sourced from registers, since the freshly-computed \(P\) from the previous matmul lives in registers). The key point for now: <strong>a TiledMMA is a per-matmul object</strong>, configured once on the host (well, JIT-time), and reused for every iteration of the inner loop.</p> <p>What about the operand-source choice (“\(A\) from SMEM”), the major modes, and the atom layout? These are decisions about how the matmul talks to the hardware: where the data lives, in what byte order, and how many parallel atom-issues we want. They genuinely matter for performance, but they’re decisions you make when designing the kernel (K3 introduces them with TMA, K4 with WGMMA). For now, treat the TiledMMA as an opaque object that knows everything about partitioning.</p> <h5 id="getting-a-per-thread-slice-get_slice"><strong>Getting a per-thread slice: <code class="language-plaintext highlighter-rouge">get_slice</code></strong></h5> <p>A TiledMMA describes the <em>warpgroup-level</em> partitioning. To get the slice for a specific thread, you ask the TiledMMA for it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">thr_mma</span> <span class="o">=</span> <span class="n">tiled_mma</span><span class="p">.</span><span class="nf">get_slice</span><span class="p">(</span><span class="n">tidx</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">thr_mma</code> is a thread-local view of <code class="language-plaintext highlighter-rouge">tiled_mma</code> that knows which thread you are (<code class="language-plaintext highlighter-rouge">tidx</code> is the thread index inside the warpgroup, 0 to 127). Every subsequent <code class="language-plaintext highlighter-rouge">partition_*</code> and <code class="language-plaintext highlighter-rouge">make_fragment_*</code> call on <code class="language-plaintext highlighter-rouge">thr_mma</code> will produce a tensor that is <em>this thread’s piece</em> of the warpgroup-level tile.</p> <p>Importantly, calling <code class="language-plaintext highlighter-rouge">get_slice</code> is cheap. The TiledMMA itself holds JIT-time partitioning metadata; <code class="language-plaintext highlighter-rouge">get_slice</code> just baking the thread index in.</p> <h5 id="partition_a-partition_b-partition_c"><strong><code class="language-plaintext highlighter-rouge">partition_A</code>, <code class="language-plaintext highlighter-rouge">partition_B</code>, <code class="language-plaintext highlighter-rouge">partition_C</code></strong></h5> <p>The three partitioning calls are:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tCsQ</span> <span class="o">=</span> <span class="n">thr_mma</span><span class="p">.</span><span class="nf">partition_A</span><span class="p">(</span><span class="n">sQ_full</span><span class="p">)</span>   <span class="c1"># this thread's slice of A, in SMEM
</span><span class="n">tCsK</span> <span class="o">=</span> <span class="n">thr_mma</span><span class="p">.</span><span class="nf">partition_B</span><span class="p">(</span><span class="n">sK_full</span><span class="p">)</span>   <span class="c1"># this thread's slice of B, in SMEM
</span><span class="n">tCgC</span> <span class="o">=</span> <span class="n">thr_mma</span><span class="p">.</span><span class="nf">partition_C</span><span class="p">(</span><span class="n">gC_tile</span><span class="p">)</span>   <span class="c1"># this thread's slice of C, in GMEM
</span></code></pre></div></div> <p>Each call takes a source tile (anywhere in memory) and returns a tensor whose <strong>layout is this thread’s slice</strong> under the corresponding operand’s partitioning recipe. Crucially, <strong>the returned tensor still points at the original storage</strong>. Partitioning doesn’t copy any bytes; it builds a re-layout that walks only this thread’s portion of the tile.</p> <p>So <code class="language-plaintext highlighter-rouge">tCsQ</code> has the same iterator as <code class="language-plaintext highlighter-rouge">sQ_full</code> (it points to the same SMEM location), but its layout is the per-thread \(A\) partitioning that the WGMMA atom expects, sliced down to thread <code class="language-plaintext highlighter-rouge">tidx</code>. The compiler knows exactly which bytes this thread will touch.</p> <p>Two subtleties worth flagging up front:</p> <ol> <li><strong>For WGMMA’s SS variant</strong> (where both \(A\) and \(B\) come from SMEM), <code class="language-plaintext highlighter-rouge">partition_A</code> and <code class="language-plaintext highlighter-rouge">partition_B</code> are slightly degenerate: every thread of the warpgroup gets the <em>same</em> layout view, because the Tensor-Core hardware reads SMEM directly via a descriptor, not via per-thread loads. We’ll see why in K4. The name <code class="language-plaintext highlighter-rouge">partition_A</code> is a holdover from the more general API where per-thread loads do exist; in SS mode it’s really a “reshape into the WGMMA-expected layout” rather than a true thread-by-thread slicing. For our purposes the API call is the same regardless.</li> <li><strong>For \(C\) (the accumulator)</strong>, the partitioning is genuinely per-thread, because the accumulator lives in registers and each thread owns a specific subset of the output tile’s values. <code class="language-plaintext highlighter-rouge">partition_C(gC_tile)</code> returns this thread’s slice of where its register-held values <em>should land</em> in GMEM.</li> </ol> <h5 id="make_fragment_a-make_fragment_b-make_fragment_c"><strong><code class="language-plaintext highlighter-rouge">make_fragment_A</code>, <code class="language-plaintext highlighter-rouge">make_fragment_B</code>, <code class="language-plaintext highlighter-rouge">make_fragment_C</code></strong></h5> <p>A <strong>fragment</strong> is the WGMMA-encoded view of an operand, ready to feed to the Tensor Core. Two variants:</p> <ul> <li>For SMEM-sourced operands, <code class="language-plaintext highlighter-rouge">make_fragment_A(tCsA)</code> wraps the partitioned SMEM tensor in the <em>exact</em> layout encoding WGMMA’s SMEM-descriptor instruction expects. Same iterator (still pointing into SMEM), new layout. No register allocation happens.</li> <li>For register-sourced operands, <code class="language-plaintext highlighter-rouge">make_fragment_C(shape)</code> actually allocates a per-thread register tensor of the right size to hold this thread’s portion of the accumulator. This is a real allocation; the tensor’s iterator points at freshly reserved registers.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># SMEM-sourced A (QK matmul): reinterpret tCsQ in the WGMMA-A layout.
</span><span class="n">tSrQ</span> <span class="o">=</span> <span class="n">qk_tiled_mma</span><span class="p">.</span><span class="nf">make_fragment_A</span><span class="p">(</span><span class="n">tCsQ</span><span class="p">)</span>

<span class="c1"># Register-allocated C: this is where the warpgroup will accumulate.
</span><span class="n">qk_acc_shape</span> <span class="o">=</span> <span class="n">qk_thr_mma</span><span class="p">.</span><span class="nf">partition_shape_C</span><span class="p">((</span><span class="n">self</span><span class="p">.</span><span class="n">Br</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">Bc</span><span class="p">))</span>
<span class="n">acc_qk</span> <span class="o">=</span> <span class="n">qk_thr_mma</span><span class="p">.</span><span class="nf">make_fragment_C</span><span class="p">(</span><span class="n">qk_acc_shape</span><span class="p">)</span>
</code></pre></div></div> <p>The first call doesn’t move data; the second one allocates registers. Both produce tensors that <code class="language-plaintext highlighter-rouge">cute.gemm</code> knows how to feed directly to a WGMMA instruction.</p> <p>A note on what’s actually in registers per thread: a single \(64 \times 64\) WGMMA atom produces \(4096\) output values, computed cooperatively by all 128 threads of the warpgroup. So each thread holds \(4096 / 128 = 32\) of those values in its registers. These 32 values are <em>not</em> a contiguous sub-rectangle of the tile; they’re scattered across it in a pattern fixed by the WGMMA hardware. This scatter pattern is the heart of TV layouts, which we get to next. K4 covers the exact pattern.</p> <h5 id="a-note-on-smem-descriptors"><strong>A note on SMEM descriptors</strong></h5> <p>We’ve said twice now that the Tensor Core “reads SMEM directly via a descriptor.” It’s worth to anchor what this means, since the term shows up in any WGMMA discussion you’ll encounter.</p> <p>A <strong>SMEM descriptor</strong> is a 64-bit value the Tensor Core uses to find an operand tile in SMEM. It packs the tile’s base address, the strides needed to walk between sub-blocks of the tile, the swizzle pattern applied to the SMEM layout, and an alignment offset, everything the hardware needs to walk the entire operand tile on its own. The crucial property: with the descriptor in hand, the Tensor Core does not need per-thread loads to assemble the operand. It reads SMEM directly through hardware-managed addressing, using the descriptor as the address recipe.</p> <p>This is why <code class="language-plaintext highlighter-rouge">make_fragment_A</code> and <code class="language-plaintext highlighter-rouge">make_fragment_B</code> perform no data motion when the operand is sourced from SMEM: there’s nothing to move. The “fragment” is a layout reinterpretation that lets CuTe construct the right descriptor when <code class="language-plaintext highlighter-rouge">cute.gemm</code> issues the WGMMA instruction. Every thread of the warpgroup ends up holding the same 64-bit descriptor value in a register, because the descriptor describes warpgroup-level data and all 128 threads consume it identically. By contrast, register-sourced operands (used in FA3’s PV matmul, where the freshly-computed P from QK’s softmax lives in registers) need to actually have their data in the right physical registers per thread, because there’s no descriptor mechanism for register operands. K4 covers the descriptor’s internal fields and how the descriptor mechanism enables WGMMA’s asynchrony.</p> <h5 id="tv-layouts-thread-and-value-partitioning"><strong>TV layouts: thread-and-value partitioning</strong></h5> <p>The partitioning recipes inside a TiledMMA are themselves CuTe layouts, with a specific structure. Two top-level modes:</p> \[(T, V) \;\longrightarrow\; \text{position in the tile}\] <p>The first mode is the <strong>thread index</strong> \(T\) (which thread in the warpgroup, \(T \in [0, 128)\)). The second mode is the <strong>value index</strong> \(V\) (which one of this thread’s owned values, \(V \in [0, 32)\) for the \(64 \times 64\) case). The layout function tells you, for any \((T, V)\) pair, which position in the underlying tile that value corresponds to.</p> <p>Every TiledMMA exposes three TV layouts, one per operand:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tiled_mma</span><span class="p">.</span><span class="n">tv_layout_A</span>
<span class="n">tiled_mma</span><span class="p">.</span><span class="n">tv_layout_B</span>
<span class="n">tiled_mma</span><span class="p">.</span><span class="n">tv_layout_C</span>
</code></pre></div></div> <p>The crucial property: both the \(T\) and \(V\) modes can be <strong>hierarchical</strong>, and <em>both contribute to both axes of the tile</em>. Sub-bits of the thread index walk along \(M\); other sub-bits walk along \(N\); same for the value index. So a single thread’s 32 values are scattered across the \(64 \times 64\) tile in a hardware-defined pattern, not arranged as a clean sub-rectangle. This is the structural reason for everything that’s awkward about per-thread reasoning in WGMMA code, and it’s what makes hierarchical layouts essential rather than decorative.</p> <p>The reason this matters for kernel code: when softmax wants <code class="language-plaintext highlighter-rouge">rowmax</code> and <code class="language-plaintext highlighter-rouge">rowsum</code>, it’s reducing over a row of the score tile. A given row’s values are split across multiple lanes’ registers, so the reduction needs warp-level coordination (warp shuffles) to combine the per-lane contributions. The pattern of which lanes share a row is encoded in <code class="language-plaintext highlighter-rouge">tv_layout_C</code>’s strides. K4 unpacks this carefully; here we just establish that TV layouts are the object that holds it all.</p> <h5 id="the-txry--txsy-naming-convention"><strong>The <code class="language-plaintext highlighter-rouge">tXrY</code> / <code class="language-plaintext highlighter-rouge">tXsY</code> naming convention</strong></h5> <p>Reading CuTe kernel code requires understanding a naming convention that’s used consistently throughout. Every partitioned tensor has a four-character name of the form:</p> \[t\langle X \rangle\langle \text{mem} \rangle\langle \text{Operand} \rangle\] <ul> <li><strong><code class="language-plaintext highlighter-rouge">t</code></strong> (“thread-partitioned”) is always there. It tells you this tensor came from a partitioning call.</li> <li><strong><code class="language-plaintext highlighter-rouge">X</code></strong> is <em>which matmul</em> this tensor is for. In FA3, <code class="language-plaintext highlighter-rouge">S</code> means the QK matmul (because the output is \(S\), the score matrix), and <code class="language-plaintext highlighter-rouge">O</code> means the PV matmul (because the output is \(O\)).</li> <li><strong>mem</strong> is <em>where the data lives</em>: <code class="language-plaintext highlighter-rouge">s</code> for SMEM, <code class="language-plaintext highlighter-rouge">r</code> for registers, <code class="language-plaintext highlighter-rouge">g</code> for GMEM, <code class="language-plaintext highlighter-rouge">c</code> for coordinates (the identity-tensor case from the previous subsection).</li> <li><strong>Operand</strong> is <em>which operand role</em>: <code class="language-plaintext highlighter-rouge">Q</code>, <code class="language-plaintext highlighter-rouge">K</code>, <code class="language-plaintext highlighter-rouge">V</code>, <code class="language-plaintext highlighter-rouge">P</code>, <code class="language-plaintext highlighter-rouge">S</code>, <code class="language-plaintext highlighter-rouge">O</code>.</li> </ul> <p>A few concrete decodings from FA3 kernel code:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">tSsQ</code></strong> = QK matmul, thread-partitioned, SMEM-resident, Q-operand. This is the per-thread slice of \(Q\) in SMEM, the input to the QK matmul.</li> <li><strong><code class="language-plaintext highlighter-rouge">tSrK</code></strong> = QK matmul, thread-partitioned, register-fragment view, K-operand. This is the WGMMA-A-encoded view of \(K\) that gets fed to <code class="language-plaintext highlighter-rouge">cute.gemm</code>. For SMEM-sourced K, this is a layout reinterpretation, not a register allocation.</li> <li><strong><code class="language-plaintext highlighter-rouge">tOrP</code></strong> = PV matmul, thread-partitioned, register-resident, P-operand. The output of the QK softmax, reinterpreted as the \(A\) operand of PV.</li> <li><strong><code class="language-plaintext highlighter-rouge">tOcO</code></strong> = PV matmul, thread-partitioned, coordinate tensor, O-destination. Used as a coordinate probe for the output scatter (the identity-tensor pattern from the previous subsection).</li> </ul> <p>The convention is just a convention, not enforced by CuTe. But every kernel in this blog follows it strictly, and most kernels in CUTLASS-land do too. Once you internalize it, you can decode unfamiliar kernel code at a glance: see <code class="language-plaintext highlighter-rouge">tOsV</code>, know it’s “PV matmul’s \(V\) tile in SMEM, partitioned per thread,” and move on.</p> <p><strong>Putting it all together.</strong> To make this section concrete, here’s the full chain of objects you build up for one matmul in a CuTe kernel, end to end. You start on the host side (at JIT time) by constructing a <strong>TiledMMA</strong> for that matmul, which bundles three things: the <strong>MMA atom</strong> (the specific WGMMA instruction, with its fixed dtype and shape, e.g. \(64 \times 64 \times 16\) BF16), the <strong>atom layout</strong> (how many atoms tile across the CTA’s work region, e.g. two atoms along M), and the <strong>TV layouts</strong> (<code class="language-plaintext highlighter-rouge">tv_layout_A</code>, <code class="language-plaintext highlighter-rouge">tv_layout_B</code>, <code class="language-plaintext highlighter-rouge">tv_layout_C</code>) that encode how the warpgroup’s 128 threads split each operand’s tile. The TiledMMA itself doesn’t touch any data; it’s a pure JIT-time description of <em>how the matmul will be carried out by the hardware</em>.</p> <p>Once you’re inside the kernel (on the device, at runtime, though all the layout machinery is still JIT-time), each thread asks the TiledMMA for its own slice: <code class="language-plaintext highlighter-rouge">thr_mma = tiled_mma.get_slice(tidx)</code>. This thread-local view of the TiledMMA is what every subsequent <code class="language-plaintext highlighter-rouge">partition_*</code> and <code class="language-plaintext highlighter-rouge">make_fragment_*</code> call goes through. It is cheap, it just bakes the thread index into the TiledMMA’s metadata, no data motion.</p> <p>Then you partition the operand tiles. The full operand tiles live somewhere (in SMEM, after a TMA load, or in registers, after a previous matmul). You hand each one to its matching partition call: <code class="language-plaintext highlighter-rouge">tCsA = thr_mma.partition_A(sA_full)</code> for the A operand, <code class="language-plaintext highlighter-rouge">tCsB = thr_mma.partition_B(sB_full)</code> for B, <code class="language-plaintext highlighter-rouge">tCgC = thr_mma.partition_C(gC_tile)</code> for C. Each call returns a <strong>partitioned tensor</strong>: same iterator as the source (no bytes moved), but a new layout that is <em>this thread’s slice</em> under the corresponding TV layout. The compiler now knows exactly which bytes this thread will touch.</p> <p>The partitioned tensors are not yet in the form WGMMA wants. The Tensor Core expects operand tiles wrapped in a specific encoded layout, and the accumulator wrapped in a register-allocated fragment. So you wrap them with <code class="language-plaintext highlighter-rouge">make_fragment_*</code>: <code class="language-plaintext highlighter-rouge">tCrA = tiled_mma.make_fragment_A(tCsA)</code> reinterprets the SMEM-resident A in the WGMMA-A encoding (no data motion, since the Tensor Core will read SMEM directly via a descriptor; the “fragment” here is just a layout-level reshape). <code class="language-plaintext highlighter-rouge">tCrB = tiled_mma.make_fragment_B(tCsB)</code> does the same for B. <code class="language-plaintext highlighter-rouge">acc = thr_mma.make_fragment_C(shape)</code> is different: it genuinely allocates per-thread registers to hold this thread’s share of the accumulator (32 values per thread for a \(64 \times 64\) atom, scattered across the tile in the hardware-defined Z-pattern). This is the only call in the chain that performs a real allocation.</p> <p>At this point you have three per-thread tensors (<code class="language-plaintext highlighter-rouge">tCrA</code>, <code class="language-plaintext highlighter-rouge">tCrB</code>, <code class="language-plaintext highlighter-rouge">acc</code>) ready to feed to <code class="language-plaintext highlighter-rouge">cute.gemm</code>. Every byte these tensors point at is in the right place and the right layout for WGMMA. The matmul itself, the actual <code class="language-plaintext highlighter-rouge">cute.gemm</code> dispatch that issues WGMMA instructions, is what the next subsection covers.</p> <p>A useful summary, then, of what each step does and where the data lives:</p> <ul> <li><strong>TiledMMA</strong> — JIT-time, no data. The complete description of how the matmul maps to hardware.</li> <li><strong><code class="language-plaintext highlighter-rouge">get_slice(tidx)</code></strong> — JIT-time, no data. A per-thread view of the TiledMMA.</li> <li><strong><code class="language-plaintext highlighter-rouge">partition_*</code></strong> — runtime tensors, but no data motion. Re-layouts of the source tiles, one per operand, sliced to this thread.</li> <li><strong><code class="language-plaintext highlighter-rouge">make_fragment_A</code> / <code class="language-plaintext highlighter-rouge">make_fragment_B</code></strong> — runtime tensors, but no data motion. Layout reinterpretations that match the WGMMA encoding. For SMEM-sourced operands these are pure layout reshapes; the Tensor Core will read SMEM directly via a descriptor (more on this in K4).</li> <li><strong><code class="language-plaintext highlighter-rouge">make_fragment_C</code></strong> — runtime tensor, <em>real allocation</em>. Per-thread registers reserved to hold the accumulator.</li> </ul> <p>Everything before <code class="language-plaintext highlighter-rouge">cute.gemm</code> is preparation: arranging layouts and reserving registers. The actual matmul, and the rest of the kernel’s data motion (TMA loads, SMEM-to-register movement), is what consumes the tensors we’ve built. We turn to <code class="language-plaintext highlighter-rouge">cute.gemm</code> next.</p> <h4 id="cutegemm-dispatching-the-matmul"><strong><code class="language-plaintext highlighter-rouge">cute.gemm</code>: dispatching the matmul</strong></h4> <p>We now have everything <code class="language-plaintext highlighter-rouge">cute.gemm</code> consumes: a TiledMMA that knows how the matmul maps to hardware, and three per-thread tensors (<code class="language-plaintext highlighter-rouge">tCrA</code>, <code class="language-plaintext highlighter-rouge">tCrB</code>, <code class="language-plaintext highlighter-rouge">acc</code>) holding the partitioned operands in the right encoding. The call is short:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cute</span><span class="p">.</span><span class="nf">gemm</span><span class="p">(</span><span class="n">tiled_mma</span><span class="p">,</span> <span class="n">acc</span><span class="p">,</span> <span class="n">tCrA</span><span class="p">,</span> <span class="n">tCrB</span><span class="p">,</span> <span class="n">acc</span><span class="p">)</span>
</code></pre></div></div> <p>That’s the entire dispatch. Behind it, CuTe figures out which WGMMA instructions to issue, in what order, with what accumulator state. This subsection unpacks what the call actually does, at the abstraction level. The hardware-side details (the inline PTX, the instruction pipeline, the synchronization) live with K4.</p> <h5 id="what-cutegemm-consumes"><strong>What <code class="language-plaintext highlighter-rouge">cute.gemm</code> consumes</strong></h5> <p>The arguments line up directly with the matmul we want:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">tiled_mma</code></strong> — the TiledMMA built earlier. It carries the MMA atom, the atom layout, and the TV layouts. Everything CuTe needs to emit the right instruction is in here.</li> <li><strong><code class="language-plaintext highlighter-rouge">acc</code></strong> — the destination accumulator (this thread’s register fragment), passed both as the <em>write</em> destination and the <em>read</em> source for the accumulated value. Same tensor in both positions because WGMMA computes <code class="language-plaintext highlighter-rouge">C = A @ B + C</code> (or <code class="language-plaintext highlighter-rouge">C = A @ B</code> when accumulation is off, controlled separately).</li> <li><strong><code class="language-plaintext highlighter-rouge">tCrA</code>, <code class="language-plaintext highlighter-rouge">tCrB</code></strong> — the per-thread A and B fragments, as produced by <code class="language-plaintext highlighter-rouge">make_fragment_*</code>. For SMEM-sourced operands these are descriptor-backed layout reinterpretations; for register-sourced operands they hold the actual data.</li> </ul> <p>The dispatch is per-thread, but the <em>effect</em> is warpgroup-level: when all 128 threads call <code class="language-plaintext highlighter-rouge">cute.gemm</code> together, the Tensor Core sees a coordinated set of operand descriptors and accumulator registers, and it issues the actual WGMMA instructions for the warpgroup.</p> <h5 id="the-two-level-dispatch"><strong>The two-level dispatch</strong></h5> <p>Internally, <code class="language-plaintext highlighter-rouge">cute.gemm</code> has a two-level structure that’s worth understanding because it makes the shapes of <code class="language-plaintext highlighter-rouge">tCrA</code>, <code class="language-plaintext highlighter-rouge">tCrB</code>, and <code class="language-plaintext highlighter-rouge">acc</code> make sense.</p> <p>The partitioned tensors are not single-atom-sized. The TiledMMA’s MMA atom is a specific shape (e.g., \(64 \times 64 \times 16\)), but the CTA’s work tile usually requires multiple atom-issues to cover. So each partitioned tensor has a layout shape with one mode for <em>one atom’s worth of data</em> (called the V mode, for “value”) and additional modes counting <em>how many atoms fit along each axis</em> (MMA_M, MMA_N, MMA_K).</p> <p>The conventional shape for the A fragment is:</p> \[\text{tCrA shape} = (V,\; \text{MMA\_M},\; \text{MMA\_K})\] <p>For B:</p> \[\text{tCrB shape} = (V,\; \text{MMA\_N},\; \text{MMA\_K})\] <p>For C (the accumulator):</p> \[\text{acc shape} = (V,\; \text{MMA\_M},\; \text{MMA\_N})\] <p>For a concrete example: with a \(64 \times 64 \times 16\) MMA atom and a CTA work tile of \(B_r \times d = 64 \times 64\) for the QK matmul, MMA_M = 1 (one atom along M), MMA_N = 1 (one along N), MMA_K = 4 (four atoms along K, since the K dimension is \(d = 64\) and each atom covers \(K = 16\)). The V mode is one atom’s per-thread data, encoded as whatever layout the WGMMA atom expects (a single descriptor for A/B; 32 register slots for C).</p> <p>Given that structure, <code class="language-plaintext highlighter-rouge">cute.gemm</code> works in two levels:</p> <ul> <li><strong>The outer level walks the atoms.</strong> It loops over MMA_M, MMA_N, and MMA_K, visiting each \((m, n, k)\) position in turn. The dimensions are JIT-time constants, so this loop is fully unrolled at compile time, the SASS that runs on the GPU is a flat sequence of WGMMA issues, not an actual loop.</li> <li><strong>The inner level issues one WGMMA per atom.</strong> At each \((m, n, k)\) position, <code class="language-plaintext highlighter-rouge">cute.gemm</code> slices out the V-mode of each input tensor (one atom’s worth of data, for A this is a single descriptor; for the accumulator this is the 32 register slots this thread owns for the atom). With one atom’s data isolated, it calls into the MMA atom, which issues exactly one WGMMA instruction. The PTX side of this issue, the inline assembly, the operand registers, the immediate flags, is what K4 covers.</li> </ul> <p>So one <code class="language-plaintext highlighter-rouge">cute.gemm</code> call expands into MMA_M × MMA_N × MMA_K WGMMA issues. In our QK example that’s \(1 \times 1 \times 4 = 4\) WGMMA instructions per call. The K-reduction loop (4 atoms along K) accumulates into the same <code class="language-plaintext highlighter-rouge">acc</code> slot, summing partial products as K is swept. The M and N loops produce different output regions; for QK with MMA_M = MMA_N = 1, there’s only one output region per call.</p> <p>This is also why the V mode exists as a distinct first mode: it’s the unit <code class="language-plaintext highlighter-rouge">cute.gemm</code>’s inner dispatch operates on. After stripping the outer (m, n, k) indices, V is one atom’s worth of data, which is exactly what <code class="language-plaintext highlighter-rouge">mma_unpack</code> and the MMA atom’s <code class="language-plaintext highlighter-rouge">fma</code> method consume.</p> <h5 id="the-accumulate-flag"><strong>The <code class="language-plaintext highlighter-rouge">ACCUMULATE</code> flag</strong></h5> <p>WGMMA can either accumulate into the existing C state (\(C = A B + C\)) or write fresh (\(C = A B\)). This is controlled by an <code class="language-plaintext highlighter-rouge">ACCUMULATE</code> field on the TiledMMA, set per-issue:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tiled_mma</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">warpgroup</span><span class="p">.</span><span class="n">Field</span><span class="p">.</span><span class="n">ACCUMULATE</span><span class="p">,</span> <span class="n">accumulate_flag</span><span class="p">)</span>
<span class="n">cute</span><span class="p">.</span><span class="nf">gemm</span><span class="p">(</span><span class="n">tiled_mma</span><span class="p">,</span> <span class="n">acc</span><span class="p">,</span> <span class="n">tCrA_k</span><span class="p">,</span> <span class="n">tCrB_k</span><span class="p">,</span> <span class="n">acc</span><span class="p">)</span>
</code></pre></div></div> <p>For the K-reduction inside one matmul, you want the first K-step to <em>write</em> (so <code class="language-plaintext highlighter-rouge">accumulate_flag = False</code>, since <code class="language-plaintext highlighter-rouge">acc</code> may contain garbage at the start), and every subsequent K-step to <em>add</em> (<code class="language-plaintext highlighter-rouge">accumulate_flag = True</code>). FA3’s kernel code expresses this with <code class="language-plaintext highlighter-rouge">ACCUMULATE = (k_block_idx != 0)</code>.</p> <p>For matmuls whose accumulator is a running quantity across multiple kernel iterations (like PV in FlashAttention, where the output is accumulated across all KV chunks), <code class="language-plaintext highlighter-rouge">ACCUMULATE</code> is always <code class="language-plaintext highlighter-rouge">True</code>, and the accumulator is zero-initialized once at the start.</p> <h5 id="what-cutegemm-does-not-do"><strong>What <code class="language-plaintext highlighter-rouge">cute.gemm</code> does <em>not</em> do</strong></h5> <p>A few things are worth flagging because they’re easy to assume <code class="language-plaintext highlighter-rouge">cute.gemm</code> handles automatically:</p> <ul> <li><strong>It does not synchronize.</strong> WGMMA is asynchronous: <code class="language-plaintext highlighter-rouge">cute.gemm</code> issues the instructions and returns. The actual computation continues on the Tensor Core in parallel with the issuing warpgroup. You need explicit synchronization (a <code class="language-plaintext highlighter-rouge">fence</code> before the issue, a <code class="language-plaintext highlighter-rouge">commit_group</code> and <code class="language-plaintext highlighter-rouge">wait_group(N)</code> to wait for results) bracketing the call. K4 covers this in detail; for now, register that the synchronization is the <em>caller’s</em> responsibility.</li> <li><strong>It does not load data into SMEM or registers.</strong> The partitioned tensors must already point at the right data when <code class="language-plaintext highlighter-rouge">cute.gemm</code> is called. The data motion (TMA loads for SMEM, the QK→PV register handoff for the register-sourced case) is separate code that runs before the dispatch.</li> <li><strong>It does not allocate the accumulator.</strong> That happened back in <code class="language-plaintext highlighter-rouge">make_fragment_C</code>. <code class="language-plaintext highlighter-rouge">cute.gemm</code> reads and writes the existing registers.</li> <li><strong>It does not unroll itself at runtime.</strong> The outer (MMA_M, MMA_N, MMA_K) loop is fully unrolled at JIT time, because the dimensions are JIT-time constants. The kernel that hits the SASS is a flat sequence of WGMMA instructions, not a loop.</li> </ul> <h5 id="the-full-structure-around-a-cutegemm-call"><strong>The full structure around a <code class="language-plaintext highlighter-rouge">cute.gemm</code> call</strong></h5> <p>Putting this together with the synchronization primitives (covered in detail in K4), a complete WGMMA dispatch looks roughly like:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">warpgroup</span><span class="p">.</span><span class="nf">fence</span><span class="p">()</span>                                    <span class="c1"># register operands ready
</span><span class="k">for</span> <span class="n">k_block_idx</span> <span class="ow">in</span> <span class="n">cutlass</span><span class="p">.</span><span class="nf">range_constexpr</span><span class="p">(</span><span class="n">num_k_blocks</span><span class="p">):</span>
    <span class="n">tiled_mma</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">warpgroup</span><span class="p">.</span><span class="n">Field</span><span class="p">.</span><span class="n">ACCUMULATE</span><span class="p">,</span> <span class="n">k_block_idx</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">cute</span><span class="p">.</span><span class="nf">gemm</span><span class="p">(</span>
        <span class="n">tiled_mma</span><span class="p">,</span>
        <span class="n">acc</span><span class="p">,</span>
        <span class="n">tCrA</span><span class="p">[(</span><span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="n">k_block_idx</span><span class="p">)],</span>
        <span class="n">tCrB</span><span class="p">[(</span><span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="n">k_block_idx</span><span class="p">)],</span>
        <span class="n">acc</span><span class="p">,</span>
    <span class="p">)</span>
<span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">warpgroup</span><span class="p">.</span><span class="nf">commit_group</span><span class="p">()</span>                              <span class="c1"># close the batch
</span><span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">warpgroup</span><span class="p">.</span><span class="nf">wait_group</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>                               <span class="c1"># wait for it
</span></code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">range_constexpr</code> loop and the per-iteration <code class="language-plaintext highlighter-rouge">ACCUMULATE</code> toggle handle the K-reduction. The slicing <code class="language-plaintext highlighter-rouge">tCrA[(None, None, k_block_idx)]</code> selects the k-th k_block from <code class="language-plaintext highlighter-rouge">tCrA</code>’s shape <code class="language-plaintext highlighter-rouge">(V, MMA_M, MMA_K)</code>, picking out one atom along K. The fence-commit-wait triplet brackets the asynchronous WGMMA pipeline.</p> <p>We’re handwaving over the synchronization here. The detailed semantics, why the fence is needed, what <code class="language-plaintext highlighter-rouge">commit_group</code> and <code class="language-plaintext highlighter-rouge">wait_group</code> actually do, why Hopper’s same-accumulator-shape exception lets multiple WGMMAs pipeline without internal fences, all live with K4. Here the point is just that <code class="language-plaintext highlighter-rouge">cute.gemm</code> is the dispatch primitive that produces the actual WGMMA instructions, and that synchronization is a separate concern wrapping it.</p> <p>With <code class="language-plaintext highlighter-rouge">cute.gemm</code> covered, the only major piece of CuTe machinery we haven’t introduced is the data-motion side: how data gets into SMEM from GMEM in the first place. That’s what <code class="language-plaintext highlighter-rouge">cute.copy</code> and TMA atoms handle, and they’re the subject of the next subsection.</p> <h4 id="cutecopy-and-tma-atoms-dispatching-data-motion"><strong><code class="language-plaintext highlighter-rouge">cute.copy</code> and TMA atoms: dispatching data motion</strong></h4> <p>We’ve covered how a kernel does its math (<code class="language-plaintext highlighter-rouge">cute.gemm</code>). Now we need the other side: how the data actually gets to where the math needs it. SMEM doesn’t fill itself; the kernel has to issue loads from GMEM. On Hopper, the right way to do this is <strong>TMA</strong> (Tensor Memory Accelerator), the dedicated hardware unit for bulk asynchronous tile copies. CuTe wraps TMA with the same atom-and-dispatch pattern we just saw for WGMMA. This subsection covers that pattern at the abstraction level, the deep TMA mechanics (descriptors, mbarriers, pipelines) live with K3.</p> <h5 id="what-tma-is-briefly"><strong>What TMA is, briefly</strong></h5> <p>TMA is the SM90 hardware unit that performs bulk asynchronous copies of multidimensional tiles between GMEM and SMEM. Three properties make it different from the pre-Hopper <code class="language-plaintext highlighter-rouge">cp.async</code> model that you may have seen in earlier-generation kernels:</p> <ul> <li><strong>One thread issues the whole copy.</strong> A single thread executes the TMA instruction; the hardware does the rest.</li> <li><strong>The tile shape is multidimensional and known to hardware.</strong> Before the kernel runs, you build a <em>tensor descriptor</em> that captures the source tensor’s shape, strides, and dtype. The hardware uses this descriptor to walk the source tile correctly without per-thread address computation.</li> <li><strong>The copy is asynchronous.</strong> TMA issues return immediately; the thread that issued continues executing while the copy proceeds in the background. Completion is tracked by a barrier.</li> </ul> <p>For our purposes here, we just need to know that TMA exists, that we use it to load Q, K, and V tiles from GMEM into SMEM, and that CuTe gives us a primitive to issue these copies. K3 covers TMA’s mechanics (descriptors, mbarriers, pipelines) in depth.</p> <h5 id="tma-atoms"><strong>TMA atoms</strong></h5> <p>Just as a WGMMA matmul is wrapped by an <em>MMA atom</em>, a TMA copy is wrapped by a <strong>TMA atom</strong>. The TMA atom bundles three things:</p> <ul> <li>The <strong>TMA operation type</strong> (e.g., a single-CTA GMEM-to-SMEM bulk tile copy).</li> <li>A <strong>tensor descriptor</strong> for the GMEM source (its shape, strides, dtype), built once at JIT time.</li> <li>The <strong>SMEM destination layout</strong> (which tile structure in SMEM the copy lands in, with its swizzle pattern).</li> </ul> <p>In CuTe Python you build it with a helper:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tma_atom_q</span><span class="p">,</span> <span class="n">tma_tensor_q</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">cpasync</span><span class="p">.</span><span class="nf">make_tiled_tma_atom</span><span class="p">(</span>
    <span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">cpasync</span><span class="p">.</span><span class="nc">CopyBulkTensorTileG2SOp</span><span class="p">(),</span>    <span class="c1"># the op: G2S (GMEM→SMEM) bulk tile
</span>    <span class="n">mQ</span><span class="p">,</span>                                              <span class="c1"># GMEM source tensor (any layout)
</span>    <span class="n">sQ_layout</span><span class="p">,</span>                                       <span class="c1"># SMEM destination layout
</span>    <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">Br</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">d</span><span class="p">),</span>                               <span class="c1"># tile shape to copy per issue
</span>    <span class="n">num_multicast</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>                                 <span class="c1"># no broadcast across CTAs
</span><span class="p">)</span>
</code></pre></div></div> <p>This returns two things: the <strong>TMA atom</strong> itself (<code class="language-plaintext highlighter-rouge">tma_atom_q</code>, the handle holding the descriptor and metadata, used in subsequent dispatch calls) and a <strong>TMA-compatible re-wrap</strong> of the source tensor (<code class="language-plaintext highlighter-rouge">tma_tensor_q</code>). From here on, use <code class="language-plaintext highlighter-rouge">tma_tensor_q</code>, not <code class="language-plaintext highlighter-rouge">mQ</code>, when partitioning for TMA. <code class="language-plaintext highlighter-rouge">cute.copy</code> from raw <code class="language-plaintext highlighter-rouge">mQ</code> will not work.</p> <p>Building a TMA atom is a JIT-time operation. The descriptor’s shape, strides, and dtype are baked in. At runtime, each TMA issue is just “load the tile at <em>this</em> coordinate from GMEM into <em>this</em> SMEM slot, using the metadata in the atom.”</p> <p>You also typically prefetch the descriptor into the SM’s caches before the first issue, to avoid descriptor-fetch latency on the first load:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">cpasync</span><span class="p">.</span><span class="nf">prefetch_descriptor</span><span class="p">(</span><span class="n">tma_atom_q</span><span class="p">)</span>
</code></pre></div></div> <p>FA3 does this for all three operands (Q, K, V) on warp 0 before the main loop starts.</p> <h5 id="partitioning-for-tma-tma_partition"><strong>Partitioning for TMA: <code class="language-plaintext highlighter-rouge">tma_partition</code></strong></h5> <p>Once the TMA atom is built, partition the source and destination tensors for the issue. The function is analogous to the MMA <code class="language-plaintext highlighter-rouge">partition_*</code> calls, but specific to TMA:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tQsQ</span><span class="p">,</span> <span class="n">tQgQ</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">cpasync</span><span class="p">.</span><span class="nf">tma_partition</span><span class="p">(</span>
    <span class="n">tma_atom_q</span><span class="p">,</span>
    <span class="mi">0</span><span class="p">,</span>                            <span class="c1"># multicast slot index (0 = no multicast)
</span>    <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>          <span class="c1"># CTA layout (trivial 1-element = single CTA)
</span>    <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">sQ_full</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">2</span><span class="p">),</span>    <span class="c1"># SMEM dest, tile interior grouped
</span>    <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">gQ_tiles</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">2</span><span class="p">),</span>   <span class="c1"># GMEM source, tile interior grouped
</span><span class="p">)</span>
</code></pre></div></div> <p>A few moving pieces here:</p> <ul> <li>The <code class="language-plaintext highlighter-rouge">0</code> and trivial 1-element layout say “no multicast, this single CTA only.” Multicast lets one TMA broadcast the same tile to multiple CTAs, which is useful for shared inputs but not what we use in our basic FA3 kernel.</li> <li><code class="language-plaintext highlighter-rouge">cute.group_modes(tensor, 0, 2)</code> collapses the first two modes of a tensor into one. TMA treats “the tile” as a single opaque blob (the hardware knows how to walk it via the descriptor), so we collapse the tile-interior modes into one mode. The outer modes remain to identify <em>which tile</em> in the larger source.</li> </ul> <p>A quick aside on pipelining, because the destination shape mentions stages. Loading a tile from GMEM is slow relative to consuming it with WGMMA, so a serial “load one tile, compute on it, load the next” pattern leaves the Tensor Core idle most of the time. The standard fix is to keep multiple loads in flight at once: allocate several SMEM slots, called <em>stages</em>, and arrange them as a circular buffer. While the consumer (WGMMA) is computing on stage \(i\), the producer (TMA) is already filling stages \(i+1\), \(i+2\), and so on. The SMEM destination layout therefore has an extra outermost mode counting the stages, and a tile loaded at iteration \(j\) lands in slot \(j \bmod \text{n\_stages}\). K3 covers the producer-consumer coordination and the mbarrier mechanics that make this work; here we just need to know that the stage dimension exists in the SMEM destination layout.</p> <p>The return values follow the same naming convention we saw with MMA partitioning:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">tQsQ</code></strong> — TMA-partitioned SMEM destination. Shape <code class="language-plaintext highlighter-rouge">(tma_unit, n_stages)</code> where <code class="language-plaintext highlighter-rouge">n_stages</code> is the pipeline-stage count if the SMEM has a circular buffer.</li> <li><strong><code class="language-plaintext highlighter-rouge">tQgQ</code></strong> — TMA-partitioned GMEM source. Shape <code class="language-plaintext highlighter-rouge">(tma_unit, tile_coord)</code> where the outer mode identifies which tile to load.</li> </ul> <p>The naming convention extends from MMA partitioning: the first letter <code class="language-plaintext highlighter-rouge">t</code> for thread-partitioned, the second identifies <em>which operation</em>’s partitioner produced it (here <code class="language-plaintext highlighter-rouge">Q</code> because we’re partitioning for the Q tensor’s TMA copy), and the third indicates memory space (<code class="language-plaintext highlighter-rouge">s</code> for SMEM, <code class="language-plaintext highlighter-rouge">g</code> for GMEM). So <code class="language-plaintext highlighter-rouge">tQsQ</code> reads as “TMA-partition for Q, in SMEM” and <code class="language-plaintext highlighter-rouge">tQgQ</code> as “TMA-partition for Q, in GMEM.”</p> <h5 id="issuing-a-copy-cutecopy"><strong>Issuing a copy: <code class="language-plaintext highlighter-rouge">cute.copy</code></strong></h5> <p>With the atom built and the source and destination partitioned, you issue the copy with <code class="language-plaintext highlighter-rouge">cute.copy</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cute</span><span class="p">.</span><span class="nf">copy</span><span class="p">(</span>
    <span class="n">tma_atom_q</span><span class="p">,</span>                                       <span class="c1"># the atom
</span>    <span class="n">tQgQ</span><span class="p">[(</span><span class="bp">None</span><span class="p">,</span> <span class="n">bidx_m</span><span class="p">)],</span>                             <span class="c1"># source: tile at GMEM coord bidx_m
</span>    <span class="n">tQsQ</span><span class="p">[(</span><span class="bp">None</span><span class="p">,</span> <span class="n">q_producer_state</span><span class="p">.</span><span class="n">index</span><span class="p">)],</span>             <span class="c1"># dest:   SMEM at stage q_producer_state.index
</span>    <span class="n">tma_bar_ptr</span><span class="o">=</span><span class="n">q_pipeline</span><span class="p">.</span><span class="nf">producer_get_barrier</span><span class="p">(</span><span class="n">q_producer_state</span><span class="p">),</span>  <span class="c1"># mbarrier for completion
</span><span class="p">)</span>
</code></pre></div></div> <p>This is analogous in spirit to <code class="language-plaintext highlighter-rouge">cute.gemm</code>. The atom carries the operation metadata; the partitioned tensors say which tile and which destination; <code class="language-plaintext highlighter-rouge">cute.copy</code> produces the actual TMA instruction.</p> <p>A few things to register:</p> <ul> <li><strong>Source indexing.</strong> <code class="language-plaintext highlighter-rouge">tQgQ[(None, bidx_m)]</code> slices the GMEM-source partition at the tile coordinate. <code class="language-plaintext highlighter-rouge">bidx_m</code> is the M-tile index for this CTA. The <code class="language-plaintext highlighter-rouge">None</code> leaves the TMA-unit mode unsliced (TMA always processes the whole tile-unit).</li> <li><strong>Destination indexing.</strong> <code class="language-plaintext highlighter-rouge">tQsQ[(None, q_producer_state.index)]</code> slices the SMEM destination at a specific pipeline stage. We’re writing into stage <code class="language-plaintext highlighter-rouge">state.index</code> of the SMEM circular buffer.</li> <li><strong>Completion barrier.</strong> <code class="language-plaintext highlighter-rouge">tma_bar_ptr</code> is an mbarrier (memory barrier) that the TMA hardware will arrive on when the copy completes. The consumer (the WGMMA that wants this data) waits on this barrier before reading SMEM. The whole pipeline machinery, the producer-consumer dance, the circular buffer, the barrier arrivals, lives with K3.</li> <li><strong>Asynchrony.</strong> <code class="language-plaintext highlighter-rouge">cute.copy</code> is asynchronous, like <code class="language-plaintext highlighter-rouge">cute.gemm</code>. The instruction issues, the function returns, and the actual copy proceeds on the TMA hardware in the background.</li> </ul> <p>You typically issue copies on a single “producer” warp (e.g., warp 0) while the rest of the warpgroup acts as consumers. The producer issues the next batch of TMAs; the consumers wait for prior batches to land and then run WGMMA on them. This producer-consumer pattern, the pipeline-stage management, and the mbarrier mechanics are all covered in K3.</p> <h5 id="what-cutecopy-and-tma-atoms-do-not-do-here"><strong>What <code class="language-plaintext highlighter-rouge">cute.copy</code> and TMA atoms do <em>not</em> do here</strong></h5> <p>Mirroring the <code class="language-plaintext highlighter-rouge">cute.gemm</code> story, a few things are easy to assume but are <em>not</em> the dispatch primitive’s job:</p> <ul> <li><strong>They do not synchronize</strong> with consumers. The mbarrier passed in tracks completion, but the consumer’s <code class="language-plaintext highlighter-rouge">wait</code> on that barrier is separate code.</li> <li><strong>They do not allocate SMEM.</strong> That’s done once when the kernel allocates its <code class="language-plaintext highlighter-rouge">SharedStorage</code>. <code class="language-plaintext highlighter-rouge">cute.copy</code> reads/writes the existing SMEM.</li> <li><strong>They do not manage pipeline state.</strong> The producer/consumer state advancement, the producer_acquire / consumer_release dance, is wrapped by a pipeline object (covered in K3).</li> <li><strong>They do not handle multi-tile sweeps.</strong> One <code class="language-plaintext highlighter-rouge">cute.copy</code> issues one TMA: one tile, one destination. The kernel loops over tiles explicitly, issuing one TMA per loop iteration.</li> </ul> <h5 id="a-note-on-swizzle-and-smem-layout"><strong>A note on swizzle and SMEM layout</strong></h5> <p>We’ve mentioned that the SMEM destination layout has a “swizzle pattern.” Briefly: SMEM has 32 banks, and naive layouts cause bank conflicts when many threads access SMEM simultaneously. Swizzling permutes addresses (via XOR-based bit manipulation) so accesses scatter across banks. The TMA descriptor and the SMEM layout must agree on the swizzle pattern: TMA writes bytes in the swizzled order, and downstream reads (by WGMMA) expect them in that order.</p> <p>CuTe provides pre-built SMEM layouts that satisfy the swizzle requirements automatically. In FA3 these come from <code class="language-plaintext highlighter-rouge">sm90_utils.make_smem_layout_b(...)</code> and similar helpers. The pattern matters when the SMEM is consumed by WGMMA, because WGMMA’s expected layout has a specific swizzle. This is K3 (TMA side) and K4 (WGMMA side) territory; here we just register that swizzle exists and that the helpers produce the right layouts automatically.</p> <h5 id="the-full-cute-vocabulary-recap"><strong>The full CuTe vocabulary, recap</strong></h5> <p>We’ve now built up everything CuTe needs to express a kernel: layouts, tensors, partitioning, and the two dispatch primitives <code class="language-plaintext highlighter-rouge">cute.gemm</code> and <code class="language-plaintext highlighter-rouge">cute.copy</code>. To close this section, here’s the full vocabulary in one place, with what each piece does:</p> <ul> <li><strong>Layouts</strong> are coordinate-to-offset functions, JIT-time constants, the planning step.</li> <li><strong>Tensors</strong> are <code class="language-plaintext highlighter-rouge">(iterator, layout)</code> pairs that attach a pointer to a layout; indexing is where computation meets memory.</li> <li><strong>TiledMMA</strong> describes how a matmul maps to the hardware (MMA atom + atom layout + TV layouts).</li> <li><strong><code class="language-plaintext highlighter-rouge">partition_A/B/C</code></strong> produce per-thread re-layouts of operand tiles, no data motion.</li> <li><strong><code class="language-plaintext highlighter-rouge">make_fragment_A/B</code></strong> wrap partitioned tensors in the WGMMA-encoded form (no allocation for SMEM sources; the Tensor Core reads SMEM via a descriptor).</li> <li><strong><code class="language-plaintext highlighter-rouge">make_fragment_C</code></strong> actually allocates per-thread registers for the accumulator.</li> <li><strong><code class="language-plaintext highlighter-rouge">cute.gemm(tiled_mma, acc, A, B, acc)</code></strong> dispatches the matmul, expanding into one WGMMA per atom.</li> <li><strong>TMA atom</strong> wraps a single TMA copy operation with its descriptor and metadata.</li> <li><strong><code class="language-plaintext highlighter-rouge">tma_partition</code></strong> prepares source and destination tensors for TMA dispatch.</li> <li><strong><code class="language-plaintext highlighter-rouge">cute.copy</code></strong> issues the actual TMA copy (asynchronous, completion signaled on an mbarrier).</li> <li><strong>Identity tensors</strong> are coordinate-reporting views, used as probes to figure out where partitioned values live in the original tile.</li> <li><strong><code class="language-plaintext highlighter-rouge">cute.flat_divide</code></strong> tiles a tensor into chunks for per-block work.</li> <li><strong>TV layouts</strong> (<code class="language-plaintext highlighter-rouge">tv_layout_A/B/C</code>) encode how threads and per-thread values map to positions in the tile, scattered in hardware-defined patterns.</li> </ul> <p>That’s the full vocabulary. Every kernel from K1 through K8 uses this same set of primitives. What changes from kernel to kernel is which operands come from SMEM versus registers, how aggressively the loads are pipelined, whether the kernel splits warps into specialized roles, and what precisions it uses, but the building blocks are the same. We now have everything we need to start writing kernels.</p>]]></content><author><name></name></author><category term="GPU-NYU"/><category term="GPU"/><summary type="html"><![CDATA[Covers CuTe DSL fundamentals and primitives and as part of FA3 Worklog.]]></summary></entry><entry><title type="html">Graduated from NYU with MSCS</title><link href="https://monishver11.github.io/blog/2026/nyu-grad/" rel="alternate" type="text/html" title="Graduated from NYU with MSCS"/><published>2026-05-22T18:48:00+00:00</published><updated>2026-05-22T18:48:00+00:00</updated><id>https://monishver11.github.io/blog/2026/nyu-grad</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/nyu-grad/"><![CDATA[<p>Master of Science in Computer Science from the Courant Institute of Mathematical Sciences, New York University.</p> <p><strong>Some highlights and takeaways from the last 1.5+ years of my life in the US;</strong></p> <ul> <li>I can cook and make a meal.</li> <li>Attended NVIDIA GTC, plus pitched myself for opportunities in the domain I want to work in.</li> <li>Started working on open-source projects and merged code changes (vLLM, Nvidia/cuda-python).</li> <li>CUDA kernel fusion(silu_mul + fp8 block quantisation) works to vLLM, used in Deepseek-R1, Non-MOE Qwen and llama3-fp8-block models.</li> <li>Attended an in-person live Jimmy Fallon show at Rockefeller Centre in New York City.</li> <li>Recitation Leader(TA) for Calculus I: Held weekly teaching sessions for 60+ undergrad students. Got a good stipend and was able to manage at least 5 months of my whole expenses.</li> <li>I can understand what my family has to sacrifice for this and the betterment as a whole.</li> <li>Wrote 51 ML foundational blogs with absolute math rigour.</li> <li>My perspectives have expanded, and now optimising experiences as a factor.</li> <li>Made some of the best friends for life.</li> <li>Trained for intelligence(both mentally and emotionally) through a lot of breakdowns and breakthroughs.</li> <li>Can read, understand, teach and build in-depth technical content without anyone’s help/guidance.</li> <li>Built a lot of confidence in myself and conviction in what I want.</li> <li>Explored many courses &amp; directions to find my genuine interest and found one where I want to concentrate.</li> <li>Not willing to settle for generic roles.</li> <li>Increased my appetite for risk and uncertainties. My views on it changed to see more on the positives, yet be discriminative enough to dig deep through to choose the optimal one.</li> <li>I’m fortunate enough.</li> <li>Importance of support systems.</li> <li>Explored NYC with friends over Christmas and New Year’s Eve.</li> <li>Holding multiple oppositional thoughts doesn’t seem to tire me as it used to be.</li> <li>Met and connected with the best people in the industry, I want to specialise in.</li> </ul> <p><strong>Learnings I’m taking for life:</strong></p> <ul> <li>Actions alone matter.</li> <li>Health is important. I’m yet to make a lot of progress in this.</li> <li>Learning, unlearn and transform everyday. Keep moving.</li> <li>Everything is a muscle that can be trained for.</li> <li>Later never comes.</li> </ul> <p>I wish to expand on each of these points, but not at this moment. Each of these experiences, along with many others not mentioned, has shaped me in ways I never imagined, and for the better. Thanks to all. Currently, I am focused on pursuing the career path I’ve set for myself, and that is my only goal at the moment. So, moving towards this next stop in life and keep going further.</p>]]></content><author><name></name></author><category term="Life"/><summary type="html"><![CDATA[Reflections on 2024-2026 Master's journey.]]></summary></entry><entry><title type="html">FlashAttention 3 - A Worklog[WIP]</title><link href="https://monishver11.github.io/blog/2026/fa3-worklog/" rel="alternate" type="text/html" title="FlashAttention 3 - A Worklog[WIP]"/><published>2026-05-15T19:59:00+00:00</published><updated>2026-05-15T19:59:00+00:00</updated><id>https://monishver11.github.io/blog/2026/fa3-worklog</id><content type="html" xml:base="https://monishver11.github.io/blog/2026/fa3-worklog/"><![CDATA[<p>Hello all. This blog post is about how to implement Flash Attention 3, specifically with CuteDSL, targeting H100 GPUs. The main goal of this work is to create a worklog on incrementally building and adding features to each kernel, to clearly and cleanly understand how we can get to Model FLOPs Utilization(MFU).</p> <p>Much of this work is inspired by some of the best people whose content I really enjoyed in the GPU kernel optimization and ML performance engineering domain — specifically Simon, Aleksa Gordic, Kapil Sharma, and Hamza for their similar worklogs, which made me want to create one like this in the first place. Since this is going to be my take on such a worklog, I wanted to do something not available on the internet, or at least not in the form of a worklog.</p> <ul> <li><a href="https://siboehm.com/articles/22/CUDA-MMM">How to Optimize a CUDA Matmul Kernel for cuBLAS-like Performance: a Worklog - Simon Boehm</a></li> <li><a href="https://www.aleksagordic.com/blog/matmul">Inside NVIDIA GPUs: Anatomy of high performance matmul kernels - Aleksa Gordić</a></li> <li><a href="https://www.kapilsharma.dev/posts/learn-cutlass-the-hard-way/">Learn CUTLASS the hard way! - Kapil Sharma</a></li> <li><a href="https://hamzaelshafie.bearblog.dev/worklog-optimising-gemm-on-nvidia-h100-for-cublas-like-performance-wip/">Worklog: Optimising GEMM on NVIDIA H100 for cuBLAS-like Performance (WIP) - Hamza</a></li> </ul> <p>At present, most ML/AI architectures depend on two basic primitives: GEMM and Attention. I’m taking Attention(specifically Flash Attention) and exploring its kernel design and how to push it toward MFU. Below is the rough plan.</p> <p><strong>Flash Attention 3(FA3) Worklog:</strong></p> <ul> <li>Attention math</li> <li>Online softmax</li> <li>GPU memory and compute hierarchy</li> <li>Tiling</li> <li>FA2</li> <li>CuTe DSL fundamentals and primitives</li> <li>K1 — naive attention</li> <li>K2 — tiled softmax</li> <li>K3 — using TMA</li> <li>K4 — using WGMMA</li> <li>K5 — warp specialization</li> <li>K6 — ping-pong scheduling</li> <li>K7 — FP8</li> <li>K8 — FP8 + incoherent processing</li> <li>Flash decode</li> </ul> <p>The plan is to follow the above as one section each. Throughout this worklog, I’ll provide good intuition along with the details on how things work or what we need to make them work. Since each of these can be its own topic, I’ll also add separate notes for many of them, which you can refer to if you want to go in-depth.</p> <p>This is a work in progress, marked with the “WIP” tag. Once it’s complete, I’ll remove that.</p> <p>I sincerely thank all the many people who planted this idea in me. I hope this will be helpful for anyone who wants to understand FlashAttention 3 (FA3) from first principles. All the code will be made available here: <a href="https://github.com/Monishver11/FlashAttention3.git">FlashAttention3</a></p> <p>I’ve made sure there aren’t any conceptual mistakes. If you find any, please let me know and I’m happy to correct them and make this useful for everyone. If you’re in this domain, feel free to ping me; I’m happy to collaborate on projects around GPU kernels and ML perf engineering.</p> <p>I’ll add references along each section, instead of tagging them at the end. I’m assuming basic ML/AI and GPU knowledge to get the best out of this blog. All the analysis will be on H100 SXM GPUs, since FA3 was targeted at the Hopper architecture and aims to squeeze out its full utilization through Hopper-specific features like TMA, WGMMA, asynchronous operations, and FP8 support. One point worth mentioning here: there isn’t a huge algorithmic difference between FA2 and FA3, but a lot of engineering work went into FA3 to take advantage of Hopper. FA2 reached around 70% of theoretical max FLOPS on Ampere (A100) but didn’t exploit Hopper-specific features, while FA3 reaches around 75% on H100 in FP16, and with FP8 it gets close to 1.2 PFLOPS with 2.6x smaller numerical error than baseline FP8 attention. More on this in the upcoming sections.</p> <p>Also, you’ll come across a lot of acronyms throughout this blog. I expand each term on its first use, so if you hit one you’re unsure about, a quick Ctrl+F will take you back to where it was introduced.</p> <p>That’s it on the intro. Let’s get started.</p> <p>Reference of FlashAttention-3 Paper - <a href="https://arxiv.org/pdf/2407.08608">Link</a></p> <hr/> <h4 id="attention-math"><strong>Attention Math</strong></h4> <p>Attention is the core of transformers, and transformers are the core of large language models (LLMs). Transformers consist of two primitives — attention, followed by feed-forward networks (FFNs) — surrounded by norms and residual connections. Below is an image that captures this.</p> <div class="row justify-content-center"> <div class="col-sm-8 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/fa3/tf-block-480.webp 480w,/assets/img/fa3/tf-block-800.webp 800w,/assets/img/fa3/tf-block-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/fa3/tf-block.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="tf-block" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Attention gives you the context of what is being referred to and meant in a sentence, and the FFN predicts the next token based on this context. Both are critical to each other and together form the backbone of present-day LLMs.</p> <p>Now, if we look at attention in more detail, it’s composed of three operations:</p> \[S = Q K^\top \tag{1}\] \[P = \text{softmax}(S) \quad \text{(along the last dimension)} \tag{2}\] \[O = P V \tag{3}\] <p>Refer to <a href="https://jalammar.github.io/illustrated-transformer/">Jay Alammar’s transformers</a> blog if you’re not sure of the attention mechanism and what happens under the hood.</p> <p>Here, (1) and (3) are matrix multiplications. In GPU terminology, we call them GEMM (General Matrix Multiply), and (2) is a softmax along the last dimension. This matters because softmax was a major bottleneck before, and FlashAttention made algorithmic changes to how it’s computed, while staying numerically exact.</p> <p><strong>Note:</strong> There is a scale factor of \(1/\sqrt{d}\) applied to \(S\), which we’re ignoring here for simplicity. The scale keeps the dot products from growing too large as \(d\) increases, which would otherwise push the softmax into saturated, low-gradient regions. In code, we’ll definitely include it. There is also a causal mask that needs to be applied, to make sure each query attends only to keys prior to it and not after. This is needed for next-token prediction.</p> <p>The reason for optimizing attention is that materializing the intermediate \(S\) and \(P\) matrices is expensive. Why? Think in terms of dimensions and how much memory we’d need to get this computation done.</p> <p>Let’s do a simple calculation. Let \(Q\), \(K\), and \(V\) be of shape \((b, s, d)\) → (batch, sequence length, model dim). Then \(S = QK^\top\) has shape \((b, s_1, s_2)\). Now apply softmax. What happens when the sequence length is 1 million and batch size is 1? Assuming FP32 (4 bytes per element):</p> \[1 \times 10^6 \times 10^6 \times 4 \text{ bytes} = 4 \times 10^{12} \text{ bytes} = 4 \text{ TB}\] <p>And that’s just to materialize one \(S\) matrix. We still need to do the softmax and another matmul on top of that. Attention is also computed many times, since LLMs have hundreds of transformer blocks. Most GPUs don’t have 4 TB of memory, and even if they did, it still wouldn’t be feasible — we need space for intermediate results and bookkeeping too.</p> <p>This is the major bottleneck FlashAttention addresses. At its core, it does two things: <strong>online softmax</strong> and <strong>tiling</strong>. Together, these ensure we never materialize the full \(s \times s\) matrix in GPU memory, while producing the exact same numerical result as if we had. This is what made it possible to train models with longer sequence lengths, which gave us more context and helped LLMs become much more useful.</p> <p>There are a lot of details in and around this, but we’ll focus on FlashAttention 3 going forward, with all necessary information and intuition along the way for each optimization and topic. With a sense of what attention does and why we need to optimize it, we’ll next talk about online softmax.</p> <p><strong>A side note:</strong></p> <p>If you want to understand how LLMs work more clearly, try implementing one. Check out the <a href="https://cs336.stanford.edu/">Stanford CS336 course</a> and follow their assignments — they’re self-contained and really push you to have a clear understanding of all the internals of LLMs. I did it as part of my coursework at NYU, and I’m glad to have taken that course. Highly recommend it.</p> <p>Also, take a look at this: <a href="https://monishver11.github.io/blog/2026/transformer-block-accounting/">Transformer block accounting</a> and the below image — think through it to understand the memory and FLOPs counts for a transformer block. This gives you a picture of what it takes to run one transformer block, so you can appreciate all the engineering that’s gone into making it fast and efficient.</p> <div class="row justify-content-center"> <div class="col-sm-8 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/fa3/transformer-operations-480.webp 480w,/assets/img/fa3/transformer-operations-800.webp 800w,/assets/img/fa3/transformer-operations-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/fa3/transformer-operations.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="tf-block" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Credits: [How To Scale Your Model - A Systems View of LLMs on TPUs](https://jax-ml.github.io/scaling-book/) </div> <hr/> <h4 id="online-softmax"><strong>Online Softmax</strong></h4> <p>First, let’s revisit softmax and where it’s used.</p> <p>Given a list of numbers \(\{x_1, x_2, \ldots, x_N\}\), for each element we compute \(e^{x_i} / \sum_j e^{x_j}\) and that produces a new list. Why do we do this? To make sure the new list sums to 1. Softmax is applied as the final step so that the probabilities over all choices add up to one. In the case of attention, we apply softmax to \(QK^\top\). These are the scores given to each key for a given query, and all the scores for a given query must add up to 1. We then use these scores to weight the value matrix through the \(PV\) matmul.</p> <p>Now, one more important aspect: along which dimension of \(S = QK^\top\) do we apply softmax? The shape of \(S\) is \((b, s_1, s_2)\), where \(s_1\) is the query sequence length and \(s_2\) is the key sequence length. The last dimension is the key’s sequence length, and softmax is applied along this one, so that for each query we get the importance of every key and can extract the context.</p> <p>With the softmax concept in place, we’ll proceed to online softmax. Here, instead of processing the full list at once, we process it in chunks and rescale the partial results as and when needed. At the end, we get the exact same result as if we had processed the whole list at once. To process in chunks and rescale, we need to keep a few temporary variables for bookkeeping. The reason we want this: our bottleneck is that we can’t hold the full list of numbers in memory, and chunk-by-chunk processing avoids that, at the cost of a little bookkeeping. That’s the gist of online softmax. Now let’s look at the actual math.</p> <h5 id="the-math"><strong>The math</strong></h5> <p>Before getting to online softmax, we need to address an overflow issue with exponentials. The largest representable value in float16 (fp16) is 65504. For large \(x\), \(e^x\) overflows this range. To mitigate this, we use a trick known as <em>safe softmax</em>: subtract the max before exponentiating.</p> \[\frac{e^{x_i}}{\sum_{j=1}^{N} e^{x_j}} = \frac{e^{x_i - m}}{\sum_{j=1}^{N} e^{x_j - m}}, \quad \text{where } m = \max_{j=1}^{N} x_j \tag{4}\] <p>Subtracting \(m\) guarantees \(x_i - m \le 0\), so every exponent is non-positive, which is the safe region for the exponential.</p> <p><strong>Note:</strong> this can make some terms very small. If \(x_i\) is far below the max, \(e^{x_i - m}\) can underflow to 0 in fp16. Unlike overflow, this is harmless: a term that underflows was already negligibly small, so it contributes essentially nothing to the sum or to the final probability. Overflow produces <code class="language-plaintext highlighter-rouge">inf</code> and corrupts the whole result, whereas underflow just drops a value that rounds to zero anyway.</p> <p><strong>3-pass safe softmax</strong></p> <p>Notation: \(m_i = \max_{j=1}^{i} x_j\) with \(m_0 = -\infty\); \(d_i = \sum_{j=1}^{i} e^{x_j - m_N}\) with \(d_0 = 0\) (\(d_N\) is the safe-softmax denominator); \(a_i\) is the final softmax value.</p> \[m_i = \max(m_{i-1},\, x_i) \tag{5}\] \[d_i = d_{i-1} + e^{x_i - m_N} \tag{6}\] \[a_i = \frac{e^{x_i - m_N}}{d_N} \tag{7}\] \[\begin{aligned} &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad m_i = \max(m_{i-1},\, x_i) \\[6pt] &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad d_i = d_{i-1} + e^{x_i - m_N} \\[6pt] &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad a_i = \dfrac{e^{x_i - m_N}}{d_N} \end{aligned}\] <p>This algorithm iterates over \([1, N]\) three times. In the context of self-attention, the \(x_i\) are the pre-softmax logits computed by \(QK^\top\). When \(N\) is large, all \(N\) logits don’t fit in memory at once, so we need to break the computation into chunks rather than processing the full row in one shot. That is exactly what online softmax does (the GPU memory hierarchy that makes this necessary is covered in the next section).</p> <p><strong>From safe softmax to online softmax</strong></p> <p>If we fuse equations (5), (6), and (7) into a single loop, we reduce the number of global memory passes from 3 to 1. Unfortunately, we cannot fuse (5) and (6) directly, because (6) depends on \(m_N\), which isn’t known until the first loop finishes.</p> <p>The fix: define a surrogate sequence</p> \[d_i' := \sum_{j=1}^{i} e^{x_j - m_i} \tag{8}\] <p>as a stand-in for the original \(d_i := \sum_{j=1}^{i} e^{x_j - m_N}\). This removes the dependency on \(m_N\), and the \(N\)-th terms of both sequences are identical: \(d_N = d_N'\). So we can safely replace \(d_N\) in equation (7) with \(d_N'\).</p> <p>We can also find a recurrence between \(d_i'\) and \(d_{i-1}'\):</p> \[d_i' = \sum_{j=1}^{i} e^{x_j - m_i} = \left( \sum_{j=1}^{i-1} e^{x_j - m_i} \right) + e^{x_i - m_i} = \left( \sum_{j=1}^{i-1} e^{x_j - m_{i-1}} \right) e^{m_{i-1} - m_i} + e^{x_i - m_i} = d_{i-1}'\, e^{m_{i-1} - m_i} + e^{x_i - m_i} \tag{9}\] <p>This recurrence only depends on \(m_i\) and \(m_{i-1}\), so we can compute \(m_i\) and \(d_i'\) together in the same loop.</p> <p><strong>2-pass online softmax</strong></p> \[\begin{aligned} &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad m_i = \max(m_{i-1},\, x_i) \\ &amp;\quad d_i' = d_{i-1}' \cdot e^{m_{i-1} - m_i} + e^{x_i - m_i} \\[6pt] &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad a_i = \dfrac{e^{x_i - m_N}}{d_N'} \end{aligned}\] <p>It still takes two passes. And we can reduce it to a single pass to minimize global I/O. We’ll see that in the FA2 section, after we’ve covered the GPU hierarchy and tiling.</p> <p><strong>References</strong></p> <ul> <li><a href="https://courses.cs.washington.edu/courses/cse599m/23sp/notes/flashattn.pdf">From Online Softmax to FlashAttention - by Zihao Ye</a> (notation used in this section is from here)</li> </ul> <hr/> <h4 id="gpu-memory-and-compute-hierarchy"><strong>GPU Memory and Compute Hierarchy</strong></h4> <p>We left online softmax at two passes. The piece still missing is <em>where</em> those chunks live and <em>how</em> we move them between memory levels. To understand that, and to understand why we process attention in chunks at all, we first need a mental model of how a GPU stores and computes data. That is what this section builds.</p> <p>We’ll build the memory hierarchy first, then the compute side. Tiling, which combines the two, follows in the next section.</p> <h5 id="the-memory-hierarchy"><strong>The memory hierarchy</strong></h5> <p>Why a hierarchy? Memory on a GPU is layered based on functionality, bandwidth, size, and access pattern. It is very similar to a CPU’s hierarchy of HDD, RAM, cache, and registers. On a GPU, the levels are: global memory (GMEM, also called HBM, high-bandwidth memory, or off-chip memory), L2 cache, shared memory (SMEM, on-chip memory) and L1 cache, and registers. This ordering goes from largest to smallest in size, lowest to highest in bandwidth, and highest to lowest in latency.</p> <p>Some rough numbers to keep in mind, for the H100 SXM specifically:</p> <ul> <li><strong>GMEM / HBM3</strong>: ~80 GB capacity, ~3.35 TB/s bandwidth, hundreds of cycles of latency.</li> <li><strong>L2 cache</strong>: ~50 MB, shared across the whole GPU.</li> <li><strong>SMEM / L1</strong>: up to 228 KB per SM, configurable as a split between shared memory and L1 cache in code. Bandwidth is roughly an order of magnitude higher than HBM, with latency in the tens of cycles.</li> <li><strong>Registers</strong>: 65536 32-bit registers per SM (often written as 64K), the fastest storage, allocated per thread.</li> </ul> <p>The point of these numbers is the <em>gap</em>: each step down the hierarchy is dramatically faster but dramatically smaller. That tension is what every kernel optimization has to navigate.</p> <p>There is a clear tradeoff in designing kernels around these hardware characteristics. This is one of the reasons I love this domain: you can clearly see what you gain and what you give up. One example is occupancy. We want to maximize the number of warps resident per SM, but to do so we need enough resources (registers, shared memory) available on that SM, and resource usage depends on how you framed the algorithm and how you use memory and registers. Balancing this is what it takes to achieve high utilization.</p> <p>I highly recommend Aleksa Gordic’s writeup on NVIDIA GPU anatomy if you want more depth before proceeding.</p> <h5 id="the-compute-hierarchy"><strong>The compute hierarchy</strong></h5> <p>We’ve summarized the memory hierarchy. Let’s do a similar one for compute.</p> <p>Kernels are launched as a <em>grid</em>, which contains multiple <em>blocks</em>, each of which is composed of multiple <em>warps</em>, and each warp is 32 <em>threads</em>. The number of threads per warp is fixed by hardware; warps are not directly exposed as a software unit you tune. Blocks and grids are the software-accessible, tunable units. Multiple blocks can be resident within an SM, depending on the block’s resource requirements and the SM’s available resources. All threads within a block can share that SM’s shared memory to communicate and exchange data.</p> <p>That covers the <em>structure</em> of parallel compute. The compute units themselves, inside each SM, are: CUDA cores, Tensor cores, and Special Function Units (SFUs). Each is built for specific use cases: CUDA cores for general arithmetic, Tensor cores for matrix multiply and fused multiply-add (FMA), and SFUs for transcendental functions like exponentials.</p> <div class="row justify-content-center"> <div class="col-sm-8 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/fa3/h100sm-480.webp 480w,/assets/img/fa3/h100sm-800.webp 800w,/assets/img/fa3/h100sm-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/fa3/h100sm.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="tf-block" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Because these are separate hardware units, they can run in parallel. I mention this because it is one of the optimizations we’ll apply to make attention faster: use the Tensor cores for the matmuls while the CUDA cores or SFUs simultaneously handle the softmax. This is just to plant the idea; we’ll return to it in the warp specialization and ping-pong sections.</p> <p>You can reference the NVIDIA H100 GPU Whitepaper for more details.</p> <h5 id="simt-the-execution-model"><strong>SIMT: the execution model</strong></h5> <p>One more foundational idea, and arguably <em>the</em> foundation of GPU programming: SIMD/SIMT, Single Instruction Multiple Data / Single Instruction Multiple Threads. We issue a single instruction and apply it across many data elements at once. This is the core reason GPUs accelerate deep learning so well.</p> <p>Matrix multiplication is a natural fit for this model. Each element of the output matrix is an independent dot product: element \((0,0)\) of the result is the dot product of row 0 of \(A\) with column 0 of \(B\), element \((0,1)\) is row 0 of \(A\) with column 1 of \(B\), and so on. In a basic model where one thread computes one output element, every thread runs the <em>same</em> instruction (a dot product) but operates on <em>different</em> data. That is SIMT.</p> <p>This is also the bridge to the next section. The block/warp/thread structure above is <em>how</em> work is grouped; tiling is <em>how we shape the data</em> so that grouping maps cleanly onto the memory hierarchy.</p> <hr/> <h4 id="tiling"><strong>Tiling</strong></h4> <p>Before defining tiling, let’s see why we need it, and why it is the thing that lets us combine the memory and compute hierarchies. There are two reasons.</p> <p>First, we can’t fit all the data in fast memory at once, so we break it into chunks and process them piece by piece. (This is exactly the constraint that forced online softmax into chunked processing.)</p> <p>Second, we want to avoid repeated transfers from GMEM to SMEM. Moving data is where much of the time goes. HBM bandwidth, while large in absolute terms, is far lower than the rate at which the compute units can consume data, so without care the compute units sit idle waiting for data to arrive. These are <em>memory stalls</em>, and we want to minimize them. Since SMEM is small, we can only hold a small portion of the data at a time, which again pushes us toward breaking the data into small pieces.</p> <p>Why “tiles”? Because we chunk in 2D, along both rows and columns, so each piece is a tile. The Ampere and Hopper Tensor cores consume matrix fragments directly, so tiling has become the norm, and modern libraries like Triton and CuTe expose <em>tiles</em> rather than scalar elements as their basic primitive.</p> <h5 id="how-tiling-works"><strong>How tiling works</strong></h5> <p>Here is the standard framing, on a generic GEMM \(C = A \times B\) where \(A\) is \(M \times K\), \(B\) is \(K \times N\), and \(C\) is \(M \times N\).</p> <p>Instead of one thread computing one full output element (one full dot product over the entire \(K\) dimension), we organize the work as:</p> <ul> <li>An <strong>outer loop over output tiles</strong>: the \(M \times N\) output is divided into tiles of size \(T_M \times T_N\). Each block of threads is responsible for one output tile.</li> <li>An <strong>inner loop over the \(K\) dimension</strong>: the shared dimension \(K\) is also split into chunks of size \(T_K\). For each step, the block loads one \(T_M \times T_K\) tile of \(A\) and one \(T_K \times T_N\) tile of \(B\) from GMEM into SMEM, multiplies them, and <strong>accumulates</strong> the partial result into registers.</li> </ul> <p>After the inner loop finishes walking the full \(K\) dimension, the accumulator in registers holds the complete output tile, which is then written back to GMEM once.</p> <p>In pseudocode:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># C = A @ B
# A: (M, K), B: (K, N), C: (M, N)
# tile sizes: T_M, T_N, T_K

for each output tile (i, j) in the (M/T_M) x (N/T_N) grid:      # outer loop, parallel across blocks
    acc = zeros(T_M, T_N)                                       # lives in registers

    for k_step in range(0, K, T_K):                             # inner loop over K
        A_tile = load A[i*T_M : (i+1)*T_M, k_step : k_step+T_K]  # GMEM -&gt; SMEM
        B_tile = load B[k_step : k_step+T_K, j*T_N : (j+1)*T_N]  # GMEM -&gt; SMEM
        acc += A_tile @ B_tile                                  # compute on SMEM data

    C[i*T_M : (i+1)*T_M, j*T_N : (j+1)*T_N] = acc               # SMEM/registers -&gt; GMEM
</code></pre></div></div> <p>The inner \(K\)-loop is pure accumulation: each \(K\)-step loads a <em>fresh</em> pair of tiles and adds its partial product into <code class="language-plaintext highlighter-rouge">acc</code>. The loop walks the shared dimension in \(K/T_K\) steps, and only after the last step does <code class="language-plaintext highlighter-rouge">acc</code> hold a complete output tile.</p> <p>Compare this with the naive version, where each output element is computed independently and every access goes to GMEM:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># naive: one thread, one output element
for i in range(M):
    for j in range(N):
        acc = 0
        for k in range(K):
            acc += A[i, k] * B[k, j]    # every read hits GMEM
        C[i, j] = acc
</code></pre></div></div> <p>To tie this back to the compute hierarchy from the previous section, here is how the tiling concepts map onto the GPU’s parallel execution units:</p> <table> <thead> <tr> <th>Tiling concept</th> <th>Hardware unit</th> <th>Role</th> </tr> </thead> <tbody> <tr> <td>The full output grid of tiles</td> <td>grid</td> <td>The whole launch. One grid covers the entire \(M \times N\) output.</td> </tr> <tr> <td>One output tile \((i, j)\)</td> <td>thread block</td> <td>One block owns one output tile and runs the inner \(K\)-loop for it. Blocks are independent, so all output tiles can be computed in parallel.</td> </tr> <tr> <td>A sub-tile within the block’s tile</td> <td>warp (32 threads)</td> <td>The block’s tile is further divided among its warps, with each warp cooperatively computing a portion.</td> </tr> <tr> <td>One output element (or small group)</td> <td>thread</td> <td>The finest unit of work, executing the same instruction as its warp-mates on different data (SIMT).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">A_tile</code>, <code class="language-plaintext highlighter-rouge">B_tile</code></td> <td>shared memory (SMEM)</td> <td>Loaded once from GMEM per \(K\)-step, then read many times by all threads in the block. This shared reuse is why the load is amortized.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">acc</code> (the accumulator)</td> <td>registers</td> <td>Per-thread, holds the partial result across the entire inner loop. Written to GMEM only once, at the end.</td> </tr> </tbody> </table> <p>The key thing this mapping makes clear: the <em>grid/block</em> split is what gives us parallelism across output tiles, while the <em>SMEM/register</em> split is what gives us data reuse within a tile. Tiling works because it exploits both at once. (The warp-level detail, how a warp’s threads cooperate with the Tensor cores on a fragment, is deferred to the WGMMA section.)</p> <p>We’ve described <em>what</em> the loops do and <em>how</em> the work maps to hardware. The next subsection makes the payoff concrete: it counts the memory traffic and shows exactly why this structure is faster.</p> <h5 id="why-tiling-reduces-memory-traffic"><strong>Why tiling reduces memory traffic</strong></h5> <p>The intuition that tiling helps becomes concrete once we count. We’ll count GMEM traffic in <em>elements</em> rather than bytes; the bytes-per-element factor is the same for both versions, so it cancels in the final ratio. For simplicity, assume square tiles of size \(T \times T\) (so \(T_M = T_N = T_K = T\)).</p> <p><strong>Naive version.</strong> Each output element of \(C\) is one dot product over the full \(K\) dimension. Computing it reads \(K\) elements of \(A\) and \(K\) elements of \(B\) from GMEM, so \(2K\) reads per output element. There are \(M \cdot N\) output elements, so total GMEM traffic is:</p> \[Q_{\text{naive}} = 2 \cdot M \cdot N \cdot K \quad \text{elements}\] <p>The useful compute is one multiply and one add per term, over \(K\) terms, for each of the \(M \cdot N\) outputs:</p> \[W = 2 \cdot M \cdot N \cdot K \quad \text{FLOPs}\] <p>The <em>arithmetic intensity</em> is the ratio of compute to memory traffic:</p> \[I_{\text{naive}} = \frac{W}{Q_{\text{naive}}} = \frac{2 \cdot M \cdot N \cdot K}{2 \cdot M \cdot N \cdot K} = 1 \quad \text{FLOP per element}\] <p>One FLOP per element moved. The kernel is entirely memory-bound: the compute units do a single operation for every value dragged across the slow GMEM boundary.</p> <p><strong>Tiled version.</strong> Now the output is divided into \((M/T) \times (N/T)\) tiles. For each output tile, the inner loop walks the \(K\) dimension in \(K/T\) steps. At each step it loads one \(T \times T\) tile of \(A\) and one \(T \times T\) tile of \(B\), which is \(2T^2\) elements. So the GMEM traffic for a single output tile is:</p> \[\frac{K}{T} \cdot 2T^2 = 2 \cdot T \cdot K \quad \text{elements}\] <p>There are \(\dfrac{M \cdot N}{T^2}\) output tiles in total, so:</p> \[Q_{\text{tiled}} = \frac{M \cdot N}{T^2} \cdot 2 \cdot T \cdot K = \frac{2 \cdot M \cdot N \cdot K}{T} \quad \text{elements}\] <p>The compute \(W\) is unchanged, \(2 \cdot M \cdot N \cdot K\) FLOPs, since tiling reorganizes the work but does not remove any of it. The arithmetic intensity becomes:</p> \[I_{\text{tiled}} = \frac{W}{Q_{\text{tiled}}} = \frac{2 \cdot M \cdot N \cdot K}{\,2 \cdot M \cdot N \cdot K \,/\, T\,} = T \quad \text{FLOPs per element}\] <p><strong>The ratio.</strong> Comparing the two:</p> \[\frac{I_{\text{tiled}}}{I_{\text{naive}}} = \frac{T}{1} = T\] <p>Tiling improves arithmetic intensity by a factor of \(T\), the tile size. Equivalently, it cuts GMEM traffic by a factor of \(T\) for the same amount of compute.</p> <p><strong>Where the factor of \(T\) comes from.</strong> It is worth seeing <em>why</em> the ratio is exactly \(T\), because the reuse happens at a finer level than “load tile A, load tile B.” The line <code class="language-plaintext highlighter-rouge">acc += A_tile @ B_tile</code> looks like a single operation, but multiplying two \(T \times T\) tiles is itself a \(T \times T \times T\) triple loop: every row of <code class="language-plaintext highlighter-rouge">A_tile</code> must be dotted with every column of <code class="language-plaintext highlighter-rouge">B_tile</code>. So a single row of <code class="language-plaintext highlighter-rouge">A_tile</code>, once it sits in SMEM, is read \(T\) times, once for each output column, rather than being fetched from GMEM \(T\) separate times. Likewise each column of <code class="language-plaintext highlighter-rouge">B_tile</code> is reused across all \(T\) rows. In numbers: the tile-tile multiply does \(2T^3\) FLOPs of compute on only \(2T^2\) loaded elements, so each loaded element is touched \(T\) times. That is the factor of \(T\). Note that this reuse lives <em>inside</em> the tile-tile multiply, not across \(K\)-steps: each \(K\)-step still loads a fresh pair of tiles, but each loaded tile, once in SMEM, earns \(T\) uses before being discarded.</p> <p><strong>What the factor of \(T\) buys you.</strong> To see why this matters, compare arithmetic intensity against real hardware. For H100 SXM in BF16, the Tensor cores deliver roughly 990 TFLOP/s of compute, while HBM3 delivers roughly 3.35 TB/s of bandwidth. To stay consistent with our element-based traffic count, we convert that bandwidth into elements per second: at 2 bytes per BF16 element, 3.35 TB/s is about \(1.675 \times 10^{12}\) elements/s. Dividing compute by bandwidth gives the hardware’s break-even arithmetic intensity:</p> \[I_{\text{ridge}} = \frac{990 \times 10^{12} \text{ FLOP/s}}{1.675 \times 10^{12} \text{ elem/s}} \approx 590 \text{ FLOPs per element}\] <p>This is the <em>roofline model</em>: a kernel’s attainable performance is capped by whichever constraint binds first, memory bandwidth or peak compute. The crossover is the <em>ridge point</em>: any kernel below it is memory-bound, any kernel above it is compute-bound. A naive GEMM sits at \(I = 1\), nearly 600× below the threshold. The Tensor cores can compute almost 600 times faster than HBM can feed them, so they spend the overwhelming majority of their time idle, waiting on data. Tiling raises \(I\) from 1 toward \(T\), moving the kernel up the roofline, toward the regime where compute, not memory, is the limit.</p> <p>This is the whole game. Tiling does not reduce <em>compute</em>; it reduces how many times each value crosses the slow GMEM boundary, moving the kernel from memory-bound (\(I = 1\)) toward compute-bound (\(I = T\)), which is the regime where the Tensor cores can actually be kept busy.</p> <p>We’ll cover how this same tiling idea applies to attention, where the second matmul (\(P \times V\)) depends on the softmax output of the first, in the FA2 section. The attention case has an extra wrinkle that plain GEMM does not, and online softmax is what resolves it.</p> <p>For a deeper, code-level walkthrough of GEMM tiling, see Simon’s GEMM worklog.</p> <p><strong>References</strong></p> <ul> <li><a href="https://siboehm.com/articles/22/CUDA-MMM">How to Optimize a CUDA Matmul Kernel for cuBLAS-like Performance: a Worklog - Simon Boehm</a></li> <li><a href="https://www.aleksagordic.com/blog/matmul">Inside NVIDIA GPUs: Anatomy of high performance matmul kernels - Aleksa Gordić</a></li> <li><a href="https://resources.nvidia.com/en-us-hopper-architecture/nvidia-h100-tensor-c">NVIDIA H100 GPU Whitepaper</a></li> </ul> <hr/> <h4 id="flashattention-2-fa2"><strong>FlashAttention-2 (FA2)</strong></h4> <p>Now that we have all the basic building blocks we need to construct FlashAttention, let’s do it.</p> <p>First and foremost: FA1 and FA2 are very similar. They differ only in loop ordering and in what is kept in shared memory. We’ll look at FA2 directly. The algorithm at its core is also the same for FA3, so getting it clear here lets us relate to and build on it for the rest of the blog.</p> <p>Before we get into the GPU-specific details, let’s do the math-heavy part first. The UWashington paper by Zihao Ye does a very good job of this, so we’ll use it as our reference here.</p> <p>A quick note on notation before the derivation begins. We adopt the paper’s bracket-index style for this section, because shapes will matter at every step and bracket notation makes them self-documenting:</p> <ul> <li>\(Q[k, :]\): the \(k\)-th row of the \(Q\) matrix, shape \((1, d)\).</li> <li>\(K^\top[:, i]\): the \(i\)-th column of \(K^\top\), shape \((d, 1)\).</li> <li>\(V[i, :]\): the \(i\)-th row of the \(V\) matrix, shape \((1, d)\).</li> <li>\(O[k, :]\): the \(k\)-th row of the output matrix \(O\), shape \((1, d)\).</li> </ul> <p>The running scalars and row vectors carry over from the online-softmax section: \(m_i\) is the running max, \(d_i'\) is the running (surrogate) softmax denominator (both scalars), and we now add \(o_i'\), the running (surrogate) output accumulator (a row vector of shape \((1, d)\)). The computation of every output row is independent, so we explain it for a single row.</p> <p>Also, as in the online-softmax section, we drop the \(1/\sqrt{d}\) softmax scale and the causal mask here for simplicity. Both are present in real code: the scale keeps the dot products from saturating the softmax as \(d\) grows, and the causal mask ensures each query attends only to keys at or before its position. We omit them only to keep the recurrence readable.</p> <h5 id="from-multi-pass-to-single-pass-attention"><strong>From multi-pass to single-pass attention</strong></h5> <p>In the online-softmax section we asked whether softmax could be done in one pass and the answer was no. But in self-attention our final target is not the attention-score matrix \(A\). It is the output matrix \(O = A V\). So the better question is: can we find a one-pass recurrence for \(O\) instead?</p> <p>With the notation in hand from the section intro, we add three more symbols specific to this derivation:</p> <ul> <li>\(x_i\): a scalar, the \(i\)-th attention score. Comes from a row times a column: \(x_i = Q[k, :]\, K^\top[:, i]\).</li> <li>\(a_i\): a scalar, the \(i\)-th softmax weight.</li> <li>\(o_i\): a row vector of shape \((1, d)\), the partial aggregation \(\sum_{j=1}^{i} a_j\, V[j, :]\). This is the running output state.</li> </ul> <p>We can now write the per-row attention computation as a recurrence. We reuse the 2-pass online softmax from before, and add a second loop that accumulates the output row.</p> <p><strong>Multi-pass self-attention.</strong></p> \[\begin{aligned} &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad x_i \leftarrow Q[k, :]\, K^\top[:, i] \\ &amp;\quad m_i \leftarrow \max(m_{i-1},\, x_i) \\ &amp;\quad d_i' \leftarrow d_{i-1}'\, e^{m_{i-1} - m_i} + e^{x_i - m_i} \\[6pt] &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad a_i \leftarrow \dfrac{e^{x_i - m_N}}{d_N'} &amp;(10)\\ &amp;\quad o_i \leftarrow o_{i-1} + a_i\, V[i, :] &amp;(11)\\[6pt] &amp;O[k, :] \leftarrow o_N \end{aligned}\] <p>A few things to read off this:</p> <ul> <li>In equation (10), \(a_i\) is scalar: \(x_i, m_N, d_N'\) are all scalars.</li> <li>In equation (11), \(a_i\, V[i, :]\) is a scalar times a row vector, producing a row vector of shape \((1, d)\). Adding it to \(o_{i-1}\) (also \((1, d)\)) gives \(o_i\) of shape \((1, d)\). So \(o_i\) accumulates a sum of \(d\)-dimensional row vectors, one per loop iteration.</li> <li>After the loop, \(o_N = \sum_{j=1}^{N} a_j\, V[j, :]\), which is the \(k\)-th row of \(O\).</li> </ul> <p>This is multi-pass: the second loop cannot start until the first finishes, because \(a_i\) in equation (10) depends on \(m_N\) and \(d_N'\), which are only known after the first loop completes.</p> <h5 id="fusing-into-a-single-pass"><strong>Fusing into a single pass</strong></h5> <p>To fuse the two loops we have to remove the dependency on \(m_N\) and \(d_N'\). Start by substituting the definition of \(a_i\) from equation (10) into equation (11):</p> \[o_i := \sum_{j=1}^{i} \frac{e^{x_j - m_N}}{d_N'}\, V[j, :] \quad (12)\] <p>Same dependency problem: \(m_N\) and \(d_N'\) aren’t available inside the loop. We play the same surrogate trick we used for \(d_i'\) in the online-softmax section: define a surrogate sequence \(o_i'\) that uses the <em>running</em> values \(m_i\) and \(d_i'\) instead of the final \(m_N\) and \(d_N'\):</p> \[o_i' := \sum_{j=1}^{i} \frac{e^{x_j - m_i}}{d_i'}\, V[j, :]\] <p>The \(N\)-th terms of the two sequences agree. Setting \(i = N\) in the surrogate definition, the running \(m_i\) and \(d_i'\) become the final \(m_N\) and \(d_N'\):</p> \[o_N' = \sum_{j=1}^{N} \frac{e^{x_j - m_N}}{d_N'}\, V[j, :] = o_N\] <p>The right-hand side is exactly the definition of \(o_N\) from equation (12). So computing the surrogate \(o_N'\) gives us the true output. The reason this works is the same as for \(d_i'\): only the final term of the sequence is needed, and at the final term the running statistics have caught up to the global ones. Revisit the online-softmax section for the fuller argument.</p> <p>Next we find a recurrence between \(o_i'\) and \(o_{i-1}'\). The goal is to write \(o_i'\) in terms of \(o_{i-1}'\), a constant, and the data of step \(i\). We walk it step by step.</p> <p>Start from the definition of \(o_i'\) and split off the last term (\(j = i\)):</p> \[o_i' = \sum_{j=1}^{i} \frac{e^{x_j - m_i}}{d_i'}\, V[j, :] = \left( \sum_{j=1}^{i-1} \frac{e^{x_j - m_i}}{d_i'}\, V[j, :] \right) + \frac{e^{x_i - m_i}}{d_i'}\, V[i, :]\] <p>Now we need to massage the first sum so it matches the definition of \(o_{i-1}'\). By definition, \(o_{i-1}' = \sum_{j=1}^{i-1} \frac{e^{x_j - m_{i-1}}}{d_{i-1}'} V[j, :]\). The first sum above has \(m_i\) and \(d_i'\) where we’d like \(m_{i-1}\) and \(d_{i-1}'\). We change them one at a time.</p> <p>First, rewrite the exponent. Using \(e^{x_j - m_i} = e^{x_j - m_{i-1} + m_{i-1} - m_i} = e^{x_j - m_{i-1}}\, e^{m_{i-1} - m_i}\):</p> \[\sum_{j=1}^{i-1} \frac{e^{x_j - m_i}}{d_i'}\, V[j, :] = \sum_{j=1}^{i-1} \frac{e^{x_j - m_{i-1}}\, e^{m_{i-1} - m_i}}{d_i'}\, V[j, :]\] <p>The factor \(e^{m_{i-1} - m_i}\) doesn’t depend on \(j\), so it can come out of the sum:</p> \[= e^{m_{i-1} - m_i} \sum_{j=1}^{i-1} \frac{e^{x_j - m_{i-1}}}{d_i'}\, V[j, :]\] <p>Now we still have \(d_i'\) where we’d like \(d_{i-1}'\). Multiply and divide by \(d_{i-1}'\):</p> \[= e^{m_{i-1} - m_i}\, \frac{d_{i-1}'}{d_i'} \sum_{j=1}^{i-1} \frac{e^{x_j - m_{i-1}}}{d_{i-1}'}\, V[j, :]\] <p>The sum is now exactly \(o_{i-1}'\). Substituting:</p> \[\sum_{j=1}^{i-1} \frac{e^{x_j - m_i}}{d_i'}\, V[j, :] = o_{i-1}'\, \frac{d_{i-1}'}{d_i'}\, e^{m_{i-1} - m_i}\] <p>Plug back into the split form of \(o_i'\):</p> \[o_i' = o_{i-1}'\, \frac{d_{i-1}'}{d_i'}\, e^{m_{i-1} - m_i} + \frac{e^{x_i - m_i}}{d_i'}\, V[i, :] \quad (13)\] <p>The recurrence (13) depends only on \(d_i', d_{i-1}', m_i, m_{i-1}, x_i,\) and \(V[i, :]\). Every one of these is available within a single loop iteration, so we can fuse the entire attention computation into one pass:</p> <p><strong>FlashAttention (single-pass).</strong></p> \[\begin{aligned} &amp;\textbf{for } i = 1 \ldots N: \\ &amp;\quad x_i \leftarrow Q[k, :]\, K^\top[:, i] \\ &amp;\quad m_i \leftarrow \max(m_{i-1},\, x_i) \\ &amp;\quad d_i' \leftarrow d_{i-1}'\, e^{m_{i-1} - m_i} + e^{x_i - m_i} \\ &amp;\quad o_i' \leftarrow o_{i-1}'\, \dfrac{d_{i-1}'}{d_i'}\, e^{m_{i-1} - m_i} + \dfrac{e^{x_i - m_i}}{d_i'}\, V[i, :] \\[6pt] &amp;O[k, :] \leftarrow o_N' \end{aligned}\] <p>A note on the shapes inside the loop: every operation on the right side of the \(o_i'\) update is a scalar coefficient multiplying either \(o_{i-1}'\) (a \((1, d)\) row) or \(V[i, :]\) (a \((1, d)\) row). The result is a \((1, d)\) row, matching \(o_i'\)’s shape. The expensive part of one iteration is the row addition; the rest is scalar arithmetic.</p> <p>The states \(x_i, m_i, d_i',\) and \(o_i'\) all have small footprints that fit comfortably in GPU shared memory: three scalars and one \((1, d)\) row per query. This is the property that makes FlashAttention work. We never materialize the full \(N \times N\) score matrix, only a handful of running states per row.</p> <h5 id="tiling-the-algorithm"><strong>Tiling the algorithm</strong></h5> <p>The single-pass algorithm processes the entire sequence as a loop over individual key/value indices. Tiling changes <em>how many rows we process per loop step</em>, in two independent ways. We do them one at a time. Step 1 follows the paper exactly: tile the key/value sequence dimension. Step 2 extends the same idea to query rows, which lifts us into the form the real kernel uses (Algorithm 1).</p> <p><strong>Step 1: tiling the key/value rows.</strong></p> <p>The paper does this step first because all operations in the single-pass algorithm are associative: nothing forces us to process one \(K^\top\) column and one \(V\) row at a time. We can process a block of them in one loop step, as long as the recurrence’s max-tracking and rescaling still work.</p> <p>We do not touch the query row. \(Q[k, :]\) stays whole. What we tile is the key/value <em>sequence dimension</em>: instead of walking it one row at a time, we walk it \(b\) rows at a time, where \(b\) is the tile size.</p> <p>New notations for this step (matching the paper):</p> <ul> <li>\(b\): the block size of the tile.</li> <li>\(\#\text{tiles} = N / b\): number of tiles in one row of the score matrix.</li> <li>\(x_i\): now a row vector of shape \((1, b)\), holding the dot products of \(Q[k, :]\) against the \(i\)-th tile of \(K^\top\). So \(x_i = Q[k, :]\, K^\top[:, (i-1)b : ib]\), covering columns \((i-1)b + 1\) through \(ib\).</li> <li>\(x_i[j]\): the \(j\)-th entry of \(x_i\) (scalar). It’s the score of \(Q[k, :]\) against the \(j\)-th key row inside tile \(i\).</li> <li>\(m_i^{(\text{local})}\): scalar, the local maximum within \(x_i\).</li> <li>\(m_i, d_i', o_i'\): same running states as before (two scalars and one row vector of shape \((1, d)\)). They now update per tile instead of per row.</li> </ul> <p>With those, the tiled algorithm is:</p> \[\begin{aligned} &amp;\textbf{for } i = 1 \ldots \#\text{tiles}: \\ &amp;\quad x_i \leftarrow Q[k, :]\, K^\top[:, (i-1)b : ib] \\ &amp;\quad m_i^{(\text{local})} \leftarrow \max_{j = 1 \ldots b}\, x_i[j] \\ &amp;\quad m_i \leftarrow \max\!\left(m_{i-1},\, m_i^{(\text{local})}\right) \\ &amp;\quad d_i' \leftarrow d_{i-1}'\, e^{m_{i-1} - m_i} + \sum_{j=1}^{b} e^{x_i[j] - m_i} \\ &amp;\quad o_i' \leftarrow o_{i-1}'\, \dfrac{d_{i-1}'}{d_i'}\, e^{m_{i-1} - m_i} + \sum_{j=1}^{b} \dfrac{e^{x_i[j] - m_i}}{d_i'}\, V[j + (i-1)b, :] \\[6pt] &amp;O[k, :] \leftarrow o_{N/b}' \end{aligned}\] <p>Reading it: \(m_i^{(\text{local})}\) is the max within the current tile only, and the regular \(m_i\) folds that local max into the running global max. The \(d_i'\) update is the same recurrence as before, except the new contribution is a <em>sum</em> over the \(b\) entries of \(x_i\) instead of a single \(e^{x_i - m_i}\). The \(o_i'\) update similarly: the per-step contribution is now a sum of \(b\) scaled value rows. Nothing in the algorithm structure changed; we just process \(b\) entries per loop step instead of one.</p> <p>This is the form the paper stops at. The states \(x_i, m_i, d_i', o_i'\) all still have small footprints that fit comfortably in GPU shared memory (the largest is \(o_i'\), one row vector of \(d\) elements). One loop step now consumes a whole \((b, d)\) tile of \(K^\top\) and \(V\), which is what lets each tile be loaded into SMEM once and fully consumed there.</p> <p><strong>Step 2: tiling the query rows.</strong></p> <p>The paper’s algorithm stops at K/V tiling, but the real kernel (Algorithm 1 below) also tiles query rows. So we take one more step.</p> <p>So far we have processed one query row \(Q[k, :]\). But every output row is computed independently: row \(k\) of \(O\) never depends on row \(k'\). That independence means we don’t need a new recurrence to handle many query rows. We just run the Step 1 recurrence for a <em>block</em> of query rows at once, and every per-row quantity gains a row dimension. The paper’s \(b\) becomes \(B_c\) (the K/V tile size, “c” for the column dimension of the score matrix), and we introduce \(B_r\) (the Q tile size, “r” for the row dimension).</p> <p>Concretely, lift each shape:</p> <ul> <li>The query block becomes \(Q_{[\,\cdot\,]}\) of shape \((B_r, d)\) instead of one row.</li> <li>The score \(x_i\) becomes a <strong>matrix</strong> of shape \((B_r, B_c)\), where row \(r\) contains the scores of query row \(r\) against all \(B_c\) key rows of tile \(i\).</li> <li>The running statistics \(m_i\) and \(d_i'\) become <strong>vectors</strong> of shape \((B_r,)\), one value per query row.</li> <li>The running output \(o_i'\) becomes a <strong>block</strong> of shape \((B_r, d)\).</li> <li>\(V_{[(i-1)B_c : iB_c]}\) is the \(i\)-th K/V tile of shape \((B_c, d)\), same as Step 1.</li> </ul> <p>The scalar reductions inside the loop become per-row reductions: the max over a tile becomes a <code class="language-plaintext highlighter-rouge">rowmax</code> (reduce the \(B_c\) axis, one max per query row), and the sum becomes a <code class="language-plaintext highlighter-rouge">rowsum</code>. The recurrence is otherwise identical:</p> \[\begin{aligned} &amp;\textbf{for } i = 1 \ldots N/B_c: \\ &amp;\quad x_i \leftarrow Q_{[\,\cdot\,]}\, K^\top[:, (i-1)B_c : iB_c] \\ &amp;\quad m_i^{(\text{local})} \leftarrow \text{rowmax}(x_i) \\ &amp;\quad m_i \leftarrow \max\!\left(m_{i-1},\, m_i^{(\text{local})}\right) \quad \text{(elementwise)} \\ &amp;\quad d_i' \leftarrow d_{i-1}'\, e^{m_{i-1} - m_i} + \text{rowsum}\!\left(e^{x_i - m_i}\right) \\ &amp;\quad o_i' \leftarrow \text{diag}\!\left(\tfrac{d_{i-1}'}{d_i'}\, e^{m_{i-1} - m_i}\right) o_{i-1}' + \text{diag}\!\left(\tfrac{1}{d_i'}\right) e^{x_i - m_i}\, V[(i-1)B_c : iB_c,\ :] \\[6pt] &amp;O_{[\,\cdot\,]} \leftarrow o_{N/B_c}' \end{aligned}\] <p>A note on the shapes inside the loop: \(m_i\) is a \((B_r,)\) vector, so in \(x_i - m_i\) it broadcasts across the \(B_c\) columns of the \((B_r, B_c)\) score matrix. In the \(o_i'\) update, the per-row scalars \(\frac{d_{i-1}'}{d_i'} e^{m_{i-1} - m_i}\) and \(\frac{1}{d_i'}\) multiply each query row of the output block. \(\text{diag}(\cdot)\) is the cleanest notation for this: \(\text{diag}(v)\) turns a length-\(B_r\) vector \(v\) into a \(B_r \times B_r\) diagonal matrix, so that \(\text{diag}(v)\, M\) scales row \(r\) of matrix \(M\) by entry \(v[r]\). There is no new algebra here, just the Step 1 recurrence with a row index attached.</p> <p>With both tilings in place, one loop step now processes a full \((B_r, B_c)\) score tile: a block of query rows against a block of key/value rows, with all running state held as small \((B_r,)\) vectors and one \((B_r, d)\) output block. This is the form that maps directly onto the GPU, which is the next subsection.</p> <h5 id="mapping-to-hardware"><strong>Mapping to hardware</strong></h5> <p>The general mechanics of tiling, the grid/block/warp split, and SMEM-versus-register reuse were covered in the tiling section. Here we cover what is specific to attention, and one important point up front: the hardware mapping does <strong>not</strong> line up one-to-one with the clean mental model from the tiled algorithm above. The algorithm is a sequential recurrence over rows and tiles; the hardware is a mesh of warps and Tensor cores that only goes fast when work is shaped to fit them. Bridging that gap is exactly what the kernel sections (K1–K8) are about. Here we set up the correct picture so the algorithm walkthrough that follows is straightforward.</p> <p><strong>The outer loop: blocks over query tiles.</strong></p> <p>This part <em>does</em> match the mental model. The work is parallelized over query blocks: each thread block takes one \(B_r \times d\) block of query rows and computes the corresponding \(B_r \times d\) block of output rows. Output rows are independent (the property we used to tile Q), so blocks are fully independent and the GPU schedules them across SMs with no inter-block communication. This is the outer loop, line 3 of Algorithm 1.</p> <p><strong>The inner loop: where the mental model and the hardware diverge.</strong></p> <p>In the tiled algorithm, the inner loop reads as a simple sequential sweep: for each key/value tile, compute the score tile, update \(m\), \(d'\), \(o'\), move on. It is tempting to map this as “one thread (or one warp) owns one query row and runs the recurrence for it.” That model is intuitive but wrong, and it is worth seeing why, because the real mapping is what every later optimization builds on.</p> <p>The inner loop’s heavy work is two matmuls:</p> <ul> <li>\(S = Q_i K_j^\top\), producing a \(B_r \times B_c\) score tile (the first matmul),</li> <li>\(\tilde{P} V_j\), producing the \(B_r \times d\) output contribution (the second matmul).</li> </ul> <p>A matmul tile on a modern GPU is <strong>not</strong> computed by independent per-row threads. It is computed <em>cooperatively</em> by warps through the Tensor cores. A Tensor core consumes a <em>fragment</em>: a small matrix shard whose elements are spread across all 32 lanes of a warp. A single thread does not hold a full row of the result, it holds a few scattered elements of the fragment. So the \(B_r\) query rows of the block are partitioned across the block’s warps as a <em>byproduct of the matmul fragment layout</em>, not by an explicit “row \(r\) → thread \(r\)” assignment.</p> <p>This has a direct consequence for the softmax. The <code class="language-plaintext highlighter-rouge">rowmax</code> and <code class="language-plaintext highlighter-rouge">rowsum</code> in the recurrence are reductions <em>along a row</em>, over the \(B_c\) score values for a given query row. But after the first matmul, those \(B_c\) values for one row are spread across multiple lanes of a warp. Computing the max or sum for that row therefore requires <strong>warp-level coordination</strong>, lanes exchanging partial results through warp-shuffle instructions, not a single thread looping over the row. So the inner loop is genuinely cooperative: warps work together on the matmul tiles, and again on the cross-lane reductions for the softmax statistics, and again on the rescaling of the running output.</p> <p>So the corrected picture of one inner-loop step is: the block’s warps cooperatively compute the \(S\) tile on the Tensor cores, cooperatively reduce it row-wise (warp shuffles) to get \(m^{(\text{local})}\) and the denominator update, cooperatively rescale the running output, and cooperatively compute the \(\tilde{P} V\) matmul on the Tensor cores. The sequential recurrence is still exactly what executes, it is just executed by a team of warps per step rather than by one thread per row.</p> <p>The precise fragment layout, how many warps cooperate, and how the two matmuls are scheduled relative to the softmax, depend on the GPU generation. On Hopper this is the warpgroup-level WGMMA instruction, and getting it right (overlapping the matmuls with the softmax, keeping the Tensor cores fed) is the substance of the later kernel sections. We will cover WGMMA and that scheduling in the K4 section; here it is enough to hold the model that the inner loop is warp-cooperative, not per-thread.</p> <p>The sweep direction itself is simple: a block walks the \(K^\top\) tiles left to right and the \(V\) tiles top to bottom, updating the running state \(m\), \(d'\), \(O\) after each tile.</p> <p><strong>Why the footprint is what makes this work.</strong></p> <p>The SRAM footprint of a block depends only on the block sizes (\(B_r\), \(B_c\)) and the head dimension \(d\), never on the sequence length \(L\). At any moment a block holds only its current Q tile, one K tile, one V tile, and the small \((B_r,)\) running-state vectors, all sized by the block parameters and \(d\). The \(N \times N\) score matrix is never materialized. Since shared memory is small (228 KB/SM on H100) and \(L\) can be very large (16k or more), this \(L\)-independence is exactly what lets FlashAttention scale to long context without running out of memory.</p> <p>This connects back to the occupancy tradeoff from the memory and compute hierarchy section: because the footprint is fixed by the block parameters and \(d\), the block size becomes a tuning knob. Larger blocks mean more reuse per tile but more SRAM and registers per block, which lowers how many blocks can be resident per SM. That balance is exactly the kind of tradeoff we’ll revisit when we profile real kernels.</p> <p>With this mapping in mind, blocks over query tiles, warp-cooperative inner loop over key/value tiles, two Tensor-core matmuls bridged by a warp-reduced softmax, the full algorithm reads directly off the hardware.</p> <h5 id="the-full-algorithm-flashattention-2-forward-pass"><strong>The full algorithm: FlashAttention-2 forward pass</strong></h5> <p>We have now built the algorithm twice: as a per-row recurrence, and as the \(B_r \times B_c\) tiled form that maps onto the hardware. Algorithm 1 below is that same algorithm exactly as the FA2 paper states it. It is kept in the paper’s own notation so it can serve as a faithful reference; the mapping back to the blog’s notation follows.</p> <blockquote> <p><strong>Algorithm 1 — FlashAttention-2 forward pass</strong></p> <p><strong>Require:</strong> Matrices \(Q, K, V \in \mathbb{R}^{N \times d}\) in HBM, block sizes \(B_c\), \(B_r\).</p> <ol> <li>Divide \(Q\) into \(T_r = \lceil N / B_r \rceil\) blocks \(Q_1, \ldots, Q_{T_r}\) of size \(B_r \times d\) each, and divide \(K, V\) into \(T_c = \lceil N / B_c \rceil\) blocks \(K_1, \ldots, K_{T_c}\) and \(V_1, \ldots, V_{T_c}\), of size \(B_c \times d\) each.</li> <li>Divide the output \(O \in \mathbb{R}^{N \times d}\) into \(T_r\) blocks \(O_1, \ldots, O_{T_r}\) of size \(B_r \times d\) each, and divide the logsumexp \(L\) into \(T_r\) blocks \(L_1, \ldots, L_{T_r}\) of size \(B_r\) each.</li> <li><strong>for</strong> \(1 \le i \le T_r\) <strong>do</strong></li> <li>    Load \(Q_i\) from HBM to on-chip SRAM.</li> <li>    On chip, initialize \(O_i^{(0)} = (0)_{B_r \times d}\), \(\ell_i^{(0)} = (0)_{B_r}\), \(m_i^{(0)} = (-\infty)_{B_r}\).</li> <li>    <strong>for</strong> \(1 \le j \le T_c\) <strong>do</strong></li> <li>        Load \(K_j, V_j\) from HBM to on-chip SRAM.</li> <li>        On chip, compute \(S_i^{(j)} = Q_i K_j^\top \in \mathbb{R}^{B_r \times B_c}\).</li> <li>        On chip, compute \(m_i^{(j)} = \max\!\left(m_i^{(j-1)},\ \text{rowmax}(S_i^{(j)})\right) \in \mathbb{R}^{B_r}\), \(\tilde{P}_i^{(j)} = \exp\!\left(S_i^{(j)} - m_i^{(j)}\right) \in \mathbb{R}^{B_r \times B_c}\) (pointwise), \(\ell_i^{(j)} = e^{\,m_i^{(j-1)} - m_i^{(j)}} \ell_i^{(j-1)} + \text{rowsum}(\tilde{P}_i^{(j)}) \in \mathbb{R}^{B_r}\).</li> <li>       On chip, compute \(O_i^{(j)} = \text{diag}\!\left(e^{\,m_i^{(j-1)} - m_i^{(j)}}\right) O_i^{(j-1)} + \tilde{P}_i^{(j)} V_j\).</li> <li>    <strong>end for</strong></li> <li>    On chip, compute \(O_i = \text{diag}\!\left(\ell_i^{(T_c)}\right)^{-1} O_i^{(T_c)}\).</li> <li>    On chip, compute \(L_i = m_i^{(T_c)} + \log\!\left(\ell_i^{(T_c)}\right)\).</li> <li>    Write \(O_i\) to HBM as the \(i\)-th block of \(O\).</li> <li>    Write \(L_i\) to HBM as the \(i\)-th block of \(L\).</li> <li><strong>end for</strong></li> <li><strong>Return</strong> the output \(O\) and the logsumexp \(L\).</li> </ol> </blockquote> <p><strong>Reading the algorithm with what we’ve built.</strong> Every line maps onto something we’ve already derived. The paper’s notation lines up with the blog’s as follows: \(S_i^{(j)}\) is our score tile \(x_i\), \(\ell\) is our denominator \(d_i'\), \(m_i^{(j)}\) is our running max \(m_i\), \(\tilde{P}\) is the unnormalized \(e^{x_i - m_i}\) term, and \(O_i^{(j)}\) is our running output \(o_i'\). The superscript \((j)\) is the inner-loop step.</p> <ul> <li><strong>Lines 1–2</strong> are the tiling setup: \(Q\) is cut into \(T_r\) row blocks of size \(B_r\), and \(K, V\) into \(T_c\) row blocks of size \(B_c\), the two tile sizes from the Q-tiling and K/V-tiling steps above.</li> <li><strong>Line 3</strong> iterates over query blocks; <strong>line 6</strong> iterates over key/value tiles. The hardware actors behind these loops, one independent thread block per query block, a warp-cooperative sweep inside, were covered in the mapping subsection.</li> <li><strong>Lines 4, 7</strong> are the GMEM → SRAM loads: one Q block once per outer iteration, one K and V block per inner step.</li> <li><strong>Line 8</strong> is the first matmul, \(S = QK^\top\), producing a \(B_r \times B_c\) score tile.</li> <li><strong>Line 9</strong> is the online softmax, batched over \(B_r\) rows. The <code class="language-plaintext highlighter-rouge">rowmax</code> and <code class="language-plaintext highlighter-rouge">rowsum</code> here are exactly the warp-reduced steps from the hardware mapping above: a row’s \(B_c\) score values are spread across warp lanes, so these reductions run as warp shuffles. \(\ell\) is the running denominator \(d_i'\), and its update is the same surrogate recurrence we derived.</li> <li><strong>Line 10</strong> is the output recurrence. \(O_i^{(j)}\) is the running output \(o_i'\). The \(\text{diag}(e^{m^{(j-1)} - m^{(j)}})\) factor rescales the previous partial output when the max changes, and \(\tilde{P}_i^{(j)} V_j\) is the second matmul, \(P \times V\), adding the current tile’s contribution.</li> </ul> <p><strong>The one real difference: rescaling is deferred.</strong> Look closely at line 10 versus our earlier \(o_i'\) recurrence. Our recurrence divided by \(d_i'\) at <em>every</em> step. Algorithm 1 does <strong>not</strong> divide inside the inner loop. \(O_i^{(j)}\) is kept <em>unnormalized</em> throughout the sweep, rescaled only by the max-correction factor \(e^{m^{(j-1)} - m^{(j)}}\). The division by the denominator happens exactly once, on <strong>line 12</strong>, after the inner loop finishes: \(O_i = \text{diag}(\ell_i^{(T_c)})^{-1} O_i^{(T_c)}\).</p> <p>This is the key FA1 → FA2 change. FA1 rescaled the output by the denominator on every inner step; FA2 moves that division out of the inner loop and applies it a single time at the end. The inner loop then contains only matmuls and the cheap max-correction, with the expensive per-step normalization removed. Fewer non-matmul operations on the hot path means the Tensor cores stay busier, and combined with the loop reordering (outer loop over query blocks, which parallelizes cleanly across thread blocks), this is most of where FA2’s speedup over FA1 comes from.</p> <p><strong>Line 13, the logsumexp.</strong> \(L_i = m_i^{(T_c)} + \log(\ell_i^{(T_c)})\) is only needed for the backward pass, where it lets the gradient recompute the softmax without storing the full probability matrix. Since this blog covers only the forward pass, we include \(L\) for completeness, to keep Algorithm 1 faithful to the paper, but we won’t use it further.</p> <p><strong>Note:</strong> in this section we only cover the forward pass, since inference is the main thing we want to cover. Training additionally needs the backward pass to propagate gradients and update parameters; for that, please refer to the paper.</p> <p><strong>References</strong></p> <ul> <li><a href="https://tridao.me/publications/flash2/flash2.pdf">FlashAttention-2 paper, Tri Dao</a></li> </ul> <hr/> <h4 id="cute-dsl-fundamentals-and-primitives"><strong>CuTe DSL fundamentals and primitives</strong></h4> <p>We’ve built the FlashAttention algorithm and tiled it for the GPU. From here on, every kernel we write (K1 through K8) will be written in <strong>CuTe Python DSL</strong>, the surface we use to express layouts, tensors, and Tensor-Core operations on Hopper. This section is the foundation: enough CuTe to read and understand the kernels that follow, built from first principles. Deep, kernel-specific mechanics (TMA hardware behavior, WGMMA fragment internals, swizzle and core-matrix details) are deferred to the kernel sections where they actually matter. Here we cover the abstractions.</p> <p>We’ll go: what CuTe is and why it exists, then layouts, then tensors, then partitioning, then <code class="language-plaintext highlighter-rouge">cute.gemm</code> and <code class="language-plaintext highlighter-rouge">cute.copy</code> at a high level. <strong>Note</strong>: I initially added this part here, but it was very long, so I had to move it to a separate blog post to keep the content structured while still explaining most of it. So, pls refer to this <a href="/blog/2026/fa3-cute/">CuTe DSL</a> for the full in-depth ideas and understanding.</p> <hr/> <h4 id="experimental-setup"><strong>Experimental setup</strong></h4> <p>Before getting into K1, two practical notes. All measurements in this worklog run on the same hardware and software stack (H100 SXM, fixed CuTe DSL and PyTorch versions, etc.), and all kernels are launched through a single shared harness with the same benchmark and correctness-check conventions. Rather than recapping that in the blog, the details live in the project README: hardware and software versions, the repo layout, the workload sweep (sequence lengths, head dims, causal flag), the tensor layout convention, and how to run any kernel from the command line.</p> <p>The repo is at: <a href="https://github.com/Monishver11/FlashAttention3.git">GitHub</a>.</p> <p>We’ll introduce just enough of the harness inline when K1 lands (one example command, a one-line note on what <code class="language-plaintext highlighter-rouge">CHECK=1</code> does, and what TFLOPS gets reported). From K2 onward, the same conventions apply unchanged. If anything in the setup is unclear while reading a kernel section, the README is the reference.</p> <p>With that, on to K1.</p> <hr/> <p>The kernels are being worked on, and the content for K1-K8 will be available soon, one by one.</p>]]></content><author><name></name></author><category term="GPU-NYU"/><category term="GPU"/><summary type="html"><![CDATA[Implementing FlashAttention 3 from scratch]]></summary></entry></feed>